name: CD

on:
  push:
    branches: [ main, develop ]
  workflow_dispatch:
    inputs:
      environment:
        description: 'Deployment environment'
        required: true
        type: choice
        options:
          - staging
          - production

permissions:
  contents: read
  deployments: write

# Concurrency control for deployments
concurrency:
  group: ${{ github.workflow }}-${{ github.event.inputs.environment || 'push' }}
  cancel-in-progress: false

jobs:
  deploy-staging:
    name: Deploy to Staging
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/develop' || github.event.inputs.environment == 'staging'
    environment:
      name: staging
      url: https://staging.armored-archer.example.com
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # Deployment observability: Create deployment status
      - name: Create deployment (staging)
        uses: chrnorm/deviation-action@20240229
        id: deployment
        with:
          environment: staging
          description: 'Staging environment deployment'
          # No token needed - uses GITHUB_TOKEN by default

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: backend/package-lock.json

      - name: Install dependencies
        working-directory: ./backend
        run: npm ci

      - name: Build TypeScript
        working-directory: ./backend
        run: npm run build

      - name: Run tests
        working-directory: ./backend
        run: npm test

      - name: Build and start Docker containers
        working-directory: ./backend
        run: |
          docker-compose up -d --build

      - name: Wait for services to be healthy
        working-directory: ./backend
        run: |
          echo "Waiting for PostgreSQL to be healthy..."
          until docker-compose exec -T postgres pg_isready -U postgres -d nakama; do
            echo "PostgreSQL is not ready yet..."
            sleep 2
          done
          echo "PostgreSQL is healthy!"

          echo "Waiting for Nakama to be healthy..."
          until docker-compose exec -T nakama /nakama/nakama healthcheck; do
            echo "Nakama is not ready yet..."
            sleep 2
          done
          echo "Nakama is healthy!"

      - name: Verify services are running
        working-directory: ./backend
        run: |
          docker-compose ps

      - name: Health check verification
        working-directory: ./backend
        run: |
          echo "Checking PostgreSQL health..."
          docker-compose exec -T postgres pg_isready -U postgres -d nakama

          echo "Checking Nakama health..."
          docker-compose exec -T nakama /nakama/nakama healthcheck

      # Deployment observability: Update deployment status to success
      - name: Update deployment status (success)
        if: success()
        uses: chrnorm/deviation-action@20240229
        with:
          deployment_id: ${{ steps.deployment.outputs.deployment_id }}
          state: 'success'
          environment: staging

      # Deployment observability: Update deployment status to failure
      - name: Update deployment status (failure)
        if: failure()
        uses: chrnorm/deviation-action@20240229
        with:
          deployment_id: ${{ steps.deployment.outputs.deployment_id }}
          state: 'failure'
          environment: staging

      # Deployment observability: Send deployment notification
      - name: Send deployment notification (staging)
        if: always()
        uses: slackapi/slack-github-action@6c661ce58804a1a20f6dc5fbee7f0381b469e001 # v1.25.0
        with:
          payload: |
            {
              "text": "Deployment to *Staging* ${{ job.status }}",
              "blocks": [
                {
                  "type": "header",
                  "text": {
                    "type": "plain_text",
                    "text": "Deployment ${{ job.status == 'success' && 'Succeeded' || 'Failed' }} : Staging"
                  }
                },
                {
                  "type": "section",
                  "fields": [
                    {
                      "type": "mrkdwn",
                      "text": "*Environment:*\nStaging"
                    },
                    {
                      "type": "mrkdwn",
                      "text": "*Status:*\n${{ job.status }}"
                    },
                    {
                      "type": "mrkdwn",
                      "text": "*Commit:*\n${{ github.sha }}"
                    },
                    {
                      "type": "mrkdwn",
                      "text": "*Workflow:*\n${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
                    }
                  ]
                },
                {
                  "type": "actions",
                  "elements": [
                    {
                      "type": "button",
                      "text": {
                        "type": "plain_text",
                        "text": "View Deployment"
                      },
                      "url": "${{ steps.deployment.outputs.deployment_url }}"
                    }
                  ]
                }
              ]
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
          SLACK_WEBHOOK_TYPE: INCOMING_WEBHOOK

  deploy-production:
    name: Deploy to Production
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' || github.event.inputs.environment == 'production'
    environment:
      name: production
      url: https://armored-archer.example.com
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # Deployment observability: Create deployment status
      - name: Create deployment (production)
        uses: chrnorm/deviation-action@20240229
        id: deployment
        with:
          environment: production
          description: 'Production environment deployment'

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: backend/package-lock.json

      - name: Install dependencies
        working-directory: ./backend
        run: npm ci

      - name: Build TypeScript
        working-directory: ./backend
        run: npm run build

      - name: Run tests
        working-directory: ./backend
        run: npm test

      - name: Build and start Docker containers
        working-directory: ./backend
        run: |
          docker-compose up -d --build

      - name: Wait for services to be healthy
        working-directory: ./backend
        run: |
          echo "Waiting for PostgreSQL to be healthy..."
          until docker-compose exec -T postgres pg_isready -U postgres -d nakama; do
            echo "PostgreSQL is not ready yet..."
            sleep 2
          done
          echo "PostgreSQL is healthy!"

          echo "Waiting for Nakama to be healthy..."
          until docker-compose exec -T nakama /nakama/nakama healthcheck; do
            echo "Nakama is not ready yet..."
            sleep 2
          done
          echo "Nakama is healthy!"

      - name: Verify services are running
        working-directory: ./backend
        run: |
          docker-compose ps

      - name: Health check verification
        working-directory: ./backend
        run: |
          echo "Checking PostgreSQL health..."
          docker-compose exec -T postgres pg_isready -U postgres -d nakama

          echo "Checking Nakama health..."
          docker-compose exec -T nakama /nakama/nakama healthcheck

      # Deployment observability: Update deployment status to success
      - name: Update deployment status (success)
        if: success()
        uses: chrnorm/deviation-action@20240229
        with:
          deployment_id: ${{ steps.deployment.outputs.deployment_id }}
          state: 'success'
          environment: production

      # Deployment observability: Update deployment status to failure
      - name: Update deployment status (failure)
        if: failure()
        uses: chrnorm/deviation-action@20240229
        with:
          deployment_id: ${{ steps.deployment.outputs.deployment_id }}
          state: 'failure'
          environment: production

      # Deployment observability: Send deployment notification
      - name: Send deployment notification (production)
        if: always()
        uses: slackapi/slack-github-action@6c661ce58804a1a20f6dc5fbee7f0381b469e001 # v1.25.0
        with:
          payload: |
            {
              "text": "Deployment to *Production* ${{ job.status }}",
              "blocks": [
                {
                  "type": "header",
                  "text": {
                    "type": "plain_text",
                    "text": "Deployment ${{ job.status == 'success' && 'Succeeded' || 'Failed' }} : Production"
                  }
                },
                {
                  "type": "section",
                  "fields": [
                    {
                      "type": "mrkdwn",
                      "text": "*Environment:*\nProduction"
                    },
                    {
                      "type": "mrkdwn",
                      "text": "*Status:*\n${{ job.status }}"
                    },
                    {
                      "type": "mrkdwn",
                      "text": "*Commit:*\n${{ github.sha }}"
                    },
                    {
                      "type": "mrkdwn",
                      "text": "*Workflow:*\n${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
                    }
                  ]
                },
                {
                  "type": "actions",
                  "elements": [
                    {
                      "type": "button",
                      "text": {
                        "type": "plain_text",
                        "text": "View Deployment"
                      },
                      "url": "${{ steps.deployment.outputs.deployment_url }}"
                    }
                  ]
                }
              ]
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
          SLACK_WEBHOOK_TYPE: INCOMING_WEBHOOK
