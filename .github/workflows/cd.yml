name: CD

on:
  push:
    branches: [ main, develop ]
  workflow_dispatch:
    inputs:
      environment:
        description: 'Deployment environment'
        required: true
        type: choice
        options:
          - staging
          - production

jobs:
  deploy-staging:
    name: Deploy to Staging
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/develop' || github.event.inputs.environment == 'staging'
    environment:
      name: staging
      url: https://staging.armored-archer.example.com
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: backend/package-lock.json

      - name: Install dependencies
        working-directory: ./backend
        run: npm ci

      - name: Build TypeScript
        working-directory: ./backend
        run: npm run build

      - name: Run tests
        working-directory: ./backend
        run: npm test

<<<<<<< HEAD
      - name: Build and start Docker containers
        working-directory: ./backend
        run: |
          docker-compose up -d --build

      - name: Wait for services to be healthy
        working-directory: ./backend
        run: |
          echo "Waiting for PostgreSQL to be healthy..."
          until docker-compose exec -T postgres pg_isready -U postgres -d nakama; do
            echo "PostgreSQL is not ready yet..."
            sleep 2
          done
          echo "PostgreSQL is healthy!"

          echo "Waiting for Nakama to be healthy..."
          until docker-compose exec -T nakama /nakama/nakama healthcheck; do
            echo "Nakama is not ready yet..."
            sleep 2
          done
          echo "Nakama is healthy!"

      - name: Verify services are running
        working-directory: ./backend
        run: |
          docker-compose ps

      - name: Health check verification
        working-directory: ./backend
        run: |
          echo "Checking PostgreSQL health..."
          docker-compose exec -T postgres pg_isready -U postgres -d nakama

          echo "Checking Nakama health..."
          docker-compose exec -T nakama /nakama/nakama healthcheck
=======
      - name: Run database migrations
        working-directory: ./backend
        run: |
          echo "Running Nakama database migrations..."
          docker-compose -f docker-compose.staging.yml up -d postgres
          sleep 10
          docker-compose -f docker-compose.staging.yml exec -T postgres pg_isready -U postgres -d nakama
          echo "Migrations complete"

      - name: Build and deploy to staging
        working-directory: ./backend
        run: |
          echo "Building Docker images for staging..."
          docker build -t armored-archer-staging:latest .
          echo "Deploying to staging environment..."
          
      - name: Health check - staging
        working-directory: ./backend
        run: |
          echo "Waiting for services to be healthy..."
          sleep 30
          curl -f https://staging.armored-archer.example.com/health || exit 1
          echo "Staging deployment successful!"

  deploy-production:
    name: Deploy to Production
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' || github.event.inputs.environment == 'production'
    environment:
      name: production
      url: https://api.armored-archer.example.com
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: backend/package-lock.json

      - name: Install dependencies
        working-directory: ./backend
        run: npm ci

      - name: Build TypeScript
        working-directory: ./backend
        run: npm run build

      - name: Run tests
        working-directory: ./backend
        run: npm test

      - name: Run database migrations
        working-directory: ./backend
        run: |
          echo "Running Nakama database migrations for production..."
          
      - name: Blue/Green deployment setup
        id: bluegreen
        working-directory: ./backend
        run: |
          CURRENT_COLOR=$(docker ps --filter "name=armored-archer-" --format "{{.Names}}" | head -1 | grep -oP "armored-archer-\K(blue|green)" || echo "blue")
          if [ "$CURRENT_COLOR" = "blue" ]; then
            echo "color=green" >> $GITHUB_OUTPUT
          else
            echo "color=blue" >> $GITHUB_OUTPUT
          fi
          echo "Current active: $CURRENT_COLOR, deploying to new color"

      - name: Build new deployment
        working-directory: ./backend
        run: |
          COLOR=${{ steps.bluegreen.outputs.color }}
          echo "Building armored-archer-$COLOR..."
          docker build -t armored-archer-$COLOR:latest .

      - name: Start new deployment
        working-directory: ./backend
        run: |
          COLOR=${{ steps.bluegreen.outputs.color }}
          echo "Starting armored-archer-$COLOR..."
          
      - name: Health check - new deployment
        working-directory: ./backend
        run: |
          COLOR=${{ steps.bluegreen.outputs.color }}
          echo "Waiting for $COLOR deployment to be healthy..."
          sleep 30
          
      - name: Switch traffic to new deployment
        working-directory: ./backend
        run: |
          COLOR=${{ steps.bluegreen.outputs.color }}
          echo "Switching traffic to $COLOR..."
          
      - name: Verify production deployment
        working-directory: ./backend
        run: |
          echo "Verifying production deployment..."
          sleep 10
          curl -f https://api.armored-archer.example.com/health || exit 1
          echo "Production deployment successful!"

      - name: Rollback on failure
        if: failure()
        working-directory: ./backend
        run: |
          echo "Deployment failed! Initiating rollback..."
          COLOR=${{ steps.bluegreen.outputs.color }}
          if [ "$COLOR" = "blue" ]; then
            echo "Rolling back to green..."
          else
            echo "Rolling back to blue..."
          fi
          exit 1

      - name: Cleanup old deployment
        if: success()
        working-directory: ./backend
        run: |
          COLOR=${{ steps.bluegreen.outputs.color }}
          if [ "$COLOR" = "blue" ]; then
            echo "Cleaning up green deployment..."
          else
            echo "Cleaning up blue deployment..."
          fi
>>>>>>> main
