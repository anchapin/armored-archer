# Eforge Configuration for Armored Archer
# Generated by Claude Code

build:
  # Commands to run after merging PRD-generated code
  # Order matters - each command runs in sequence and must succeed
  postMergeCommands:
    - cd backend && go fmt ./...
    - cd backend && go test ./...
    - cd backend && npm run typecheck
    - make duplicate-code-check
