---
phase: 08-fix-broken-packages-establish-quality-gates
plan: 06
type: execute
wave: 6
depends_on: []
files_modified:
  - backend/tests/rpg/rpg_test.go
  - backend/tests/season/season_test.go
autonomous: true
requirements:
  - INF-01
user_setup: []
must_haves:
  truths:
    - "All test files compile without syntax errors"
    - "RPG test package builds successfully"
    - "Season test package builds successfully"
  artifacts:
    - path: "backend/tests/rpg/rpg_test.go"
      provides: "RPG package tests"
      contains: "func Test"
    - path: "backend/tests/season/season_test.go"
      provides: "Season package tests"
      contains: "func Test"
  key_links:
    - from: "tests/rpg/rpg_test.go"
      to: "go test -c"
      via: "String literal syntax"
      pattern: "fmt\\.Sprintf\\(\\\""
    - from: "tests/season/season_test.go"
      to: "go test -c"
      via: "String literal syntax"
      pattern: "fmt\\.Sprintf\\(\\\""
---

<objective>
Fix string literal syntax errors in RPG and season test files to enable compilation and coverage measurement.

Purpose: The test files have unterminated string literals due to double opening quotes (`""XP` instead of `"XP`). These syntax errors prevent compilation and coverage measurement for these packages.

Output: Both test packages compile successfully without syntax errors.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-fix-broken-packages-establish-quality-gates/08-VERIFICATION.md
@.planning/phases/08-fix-broken-packages-establish-quality-gates/08-02-SUMMARY.md

# Compilation errors identified in VERIFICATION.md
1. tests/rpg/rpg_test.go:60 - missing ',' in argument list (string literal syntax error)
2. tests/season/season_test.go:91 - string literal not terminated (unterminated string)

# Root cause from 08-02-SUMMARY deviation #1
- "Fixed missing closing quotes in rpg_test.go, season_test.go, store_test.go"
- These fixes were made but not committed, so errors persist in git state

# Current error patterns
- Line 60 in rpg_test.go: fmt.Sprintf(""XP for level %d", tt.level) - has double opening quote
- Lines 91-92 in season_test.go: fmt.Sprintf(""%d", tt.minCoins) - has double opening quote
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix string literal syntax in rpg_test.go</name>
  <files>backend/tests/rpg/rpg_test.go</files>
  <read_first>
    backend/tests/rpg/rpg_test.go (lines 58-62 to see the fmt.Sprintf call)
  </read_first>
  <action>
    Fix the string literal syntax error on line 60:

    1. Read the current content of line 60: `fmt.Sprintf(""XP for level %d", tt.level))`
    2. Replace the double opening quote `""XP` with single quote `"XP`
    3. The corrected line should be: `fmt.Sprintf("XP for level %d", tt.level))`

    This is a simple syntax fix - the double opening quote causes the Go parser to interpret it incorrectly, resulting in "missing ',' in argument list" error.
  </action>
  <verify>
    <automated>cd backend && go test -c ./tests/rpg 2>&1 | grep -i "missing.*argument\|syntax\|literal" || echo "No syntax errors in rpg_test.go"</automated>
  </verify>
  <done>
    rpg_test.go line 60 has correct string literal syntax with single opening quote, go test -c compiles successfully for tests/rpg package.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix string literal syntax in season_test.go</name>
  <files>backend/tests/season/season_test.go</files>
  <read_first>
    backend/tests/season/season_test.go (lines 88-94 to see both fmt.Sprintf calls)
  </read_first>
  <action>
    Fix the string literal syntax errors on lines 91-92:

    1. Read the current content of lines 91-92
    2. Line 91: `testhelpers.AssertTrue(t, rewards.Coins >= tt.minCoins, "Coins should be at least "+fmt.Sprintf(""%d", tt.minCoins))`
       - Replace `"%d"` with `"%d"` (remove the extra opening quote)
       - Corrected: `"Coins should be at least "+fmt.Sprintf("%d", tt.minCoins)`
    3. Line 92: `testhelpers.AssertTrue(t, rewards.Gems >= tt.minGems, "Gems should be at least "+fmt.Sprintf(""%d", tt.minGems))`
       - Replace `"%d"` with `"%d"` (remove the extra opening quote)
       - Corrected: `"Gems should be at least "+fmt.Sprintf("%d", tt.minGems)`

    Both lines have the same pattern error - double opening quotes in the format string literal.
  </action>
  <verify>
    <automated>cd backend && go test -c ./tests/season 2>&1 | grep -i "unterminated\|syntax\|literal" || echo "No syntax errors in season_test.go"</automated>
  </verify>
  <done>
    season_test.go lines 91-92 have correct string literal syntax with single opening quotes, go test -c compiles successfully for tests/season package.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify all test packages compile</name>
  <files></files>
  <read_first>
    (no specific files - verify compilation across test packages)
  </read_first>
  <action>
    Verify that all test packages now compile successfully:

    1. Run `go test -c ./tests/rpg ./tests/season ./tests/store` to compile test binaries
    2. Verify no syntax errors, missing argument errors, or unterminated string errors
    3. Run `go build ./...` to verify overall backend compilation
    4. Confirm exit code 0 with no compilation errors

    This validates that INF-01 is closer to completion - test file syntax errors resolved.
  </action>
  <verify>
    <automated>cd backend && go test -c ./tests/rpg ./tests/season ./tests/store 2>&1 | grep -E "(syntax|literal|argument)" || echo "All test packages compile successfully"</automated>
  </verify>
  <done>
    All test packages (rpg, season, store) compile without syntax errors, go build ./... succeeds with zero compilation-related error messages.
  </done>
</task>

</tasks>

<verification>
After task completion:
- Run `go test -c ./tests/rpg` to verify rpg test package compiles
- Run `go test -c ./tests/season` to verify season test package compiles
- Run `go build ./...` to verify overall compilation
- Check that no syntax errors remain in test files
</verification>

<success_criteria>
1. rpg_test.go line 60 has correct string literal: `fmt.Sprintf("XP for level %d", tt.level)`
2. season_test.go lines 91-92 have correct string literals: `fmt.Sprintf("%d", tt.minCoins)`
3. go test -c compiles tests/rpg, tests/season without errors
4. go build ./... exits with code 0 for test packages
</success_criteria>

<output>
After completion, create `.planning/phases/08-fix-broken-packages-establish-quality-gates/08-06-SUMMARY.md`
</output>
