---
phase: 08-fix-broken-packages-establish-quality-gates
plan: 11
type: execute
wave: 7
depends_on: []
files_modified:
  - backend/tests/matchmaking/matchmaking_test.go
autonomous: true
requirements:
  - INF-01
  - INF-02
user_setup: []
must_haves:
  truths:
    - "All 27 Go backend packages compile successfully without errors"
    - "Matchmaking test package compiles without undefined function errors"
    - "Baseline coverage measurement includes matchmaking test package"
  artifacts:
    - path: "backend/tests/matchmaking/matchmaking_test.go"
      provides: "Matchmaking test package"
      contains: "func TestPlayerRanking"
    - path: "coverage/coverage.out"
      provides: "Coverage report including matchmaking package"
      contains: "tests/matchmaking"
  key_links:
    - from: "tests/matchmaking/matchmaking_test.go"
      to: "internal/matchmaking/matchmaking.go"
      via: "Import of matchmaking package and use of PlayerRanking struct"
      pattern: "matchmaking\\.PlayerRanking"
---

<objective>
Fix undefined function errors in tests/matchmaking/matchmaking_test.go to enable compilation and complete baseline coverage measurement.

Purpose: Lines 222 and 252 of matchmaking_test.go reference matchmaking.NewPlayerRanking and NewMatchRecord which don't exist. The matchmaking package exports PlayerRanking struct but has no constructor function. MatchRecord type doesn't exist - tests should use matchmaking.PvPMatch for match records.

Output: Matchmaking test package compiles successfully, enabling complete coverage measurement for all 27 Go packages.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-fix-broken-packages-establish-quality-gates/08-VERIFICATION.md

# Root cause from VERIFICATION.md gap analysis
- Line 222: `ranking := matchmaking.NewPlayerRanking("user123", "Player1", 1000, 5)` - matchmaking.NewPlayerRanking doesn't exist
- Line 252: `record := matchmaking.NewMatchRecord("user123", "match123", ...)` - matchmaking.NewMatchRecord doesn't exist

# matchmaking.go exports (from internal/matchmaking/matchmaking.go)
- PlayerRanking struct (lines 367-377) with fields: UserID, Username, Elo, Rank, Wins, Losses, SeasonID, LastActive
- No NewPlayerRanking constructor function exists
- No MatchRecord type exists - tests should use PvPMatch for match-related tests

# Match types available in matchmaking package
- PvPMatch struct (lines 37-54) with fields: MatchID, CreatorID, OpponentID, CreatorRank, OpponentRank, MatchType, IsPunchUp, Status, CreatedAt, UpdatedAt, Winner, ExpiresAt, LastTurnTs
- NewPvPMatch constructor function (lines 95-112) exists

# Fix required
1. Line 222: Replace `matchmaking.NewPlayerRanking(...)` with direct struct initialization of `matchmaking.PlayerRanking{...}`
2. Line 252-253: Replace `matchmaking.NewMatchRecord(...)` and related MatchRecord references with `matchmaking.PvPMatch` usage
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix TestPlayerRanking undefined function error</name>
  <files>backend/tests/matchmaking/matchmaking_test.go</files>
  <read_first>
    backend/tests/matchmaking/matchmaking_test.go (lines 221-233 to see TestPlayerRanking function and matchmaking.NewPlayerRanking reference)
    backend/internal/matchmaking/matchmaking.go (lines 367-402 to see PlayerRanking struct definition and available methods)
  </read_first>
  <action>
    Fix the NewPlayerRanking undefined error in TestPlayerRanking function:

    1. Read line 222 which currently has `ranking := matchmaking.NewPlayerRanking("user123", "Player1", 1000, 5)`
    2. Replace with direct struct initialization of PlayerRanking:
       ```go
       ranking := &matchmaking.PlayerRanking{
           UserID:     "user123",
           Username:   "Player1",
           Elo:        1000,
           Rank:       5,
           Wins:       0,
           Losses:    0,
           SeasonID:   "",
           LastActive: time.Now().UnixMilli(),
       }
       ```

    3. Verify line 223-227 still work with the new struct (they call ranking.UpdateWinLoss, ranking.GetWinRate, ranking.Wins, ranking.Losses)

    The PlayerRanking struct is available in the matchmaking package but has no constructor function. Direct initialization is required.
  </action>
  <verify>
    <automated>cd backend && go test -c ./tests/matchmaking 2>&1 | grep -i "NewPlayerRanking.*undefined" || echo "Matchmaking test compiles successfully"</automated>
  </verify>
  <done>
    Line 222 uses direct PlayerRanking struct initialization instead of matchmaking.NewPlayerRanking, go test -c ./tests/matchmaking compiles without NewPlayerRanking undefined error.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix MatchRecord undefined function error</name>
  <files>backend/tests/matchmaking/matchmaking_test.go</files>
  <read_first>
    backend/tests/matchmaking/matchmaking_test.go (lines 251-291 to see TestMatchRecord function and MatchRecord references)
    backend/internal/matchmaking/matchmaking.go (lines 37-54, 95-112 to see PvPMatch struct and NewPvPMatch constructor)
  </read_first>
  <action>
    Fix the NewMatchRecord undefined error in TestMatchRecord function:

    1. Read lines 251-262 which use MatchRecord type
    2. Replace references to matchmaking.NewMatchRecord with matchmaking.NewPvPMatch
    3. Replace MatchRecord type references with PvPMatch type

    Current code uses non-existent MatchRecord - should use PvPMatch instead:
    - Change function name from TestMatchRecord to TestPvPMatch (or keep name and use PvPMatch internally)
    - Replace `matchmaking.MatchResultWin` constant with `matchmaking.MatchResultWin`
    - Replace `matchmaking.MatchTypeRanked` with `matchmaking.MatchTypeRanked`
    - Change field access to match PvPMatch structure (e.g., match.Result -> match.Status, etc.)

    The PvPMatch struct has all needed fields: MatchID, UserID (via CreatorID/OpponentID), MatchType, Status, Winner. The test needs to be updated to match PvPMatch semantics.
  </action>
  <verify>
    <automated>cd backend && go test -c ./tests/matchmaking 2>&1 | grep -i "MatchRecord.*undefined" || echo "Matchmaking test compiles successfully"</automated>
  </verify>
  <done>
    TestMatchRecord function updated to use PvPMatch type instead of non-existent MatchRecord, all MatchRecord references replaced with PvPMatch equivalents, go test -c ./tests/matchmaking compiles without MatchRecord undefined error.
  </done>
</task>

</tasks>

<verification>
After task completion:
- Run `cd backend && go test -c ./tests/matchmaking` to verify matchmaking test package compiles
- Run `cd backend && go build ./...` to verify all packages compile
- Verify no "undefined NewPlayerRanking" or "undefined MatchRecord" errors remain
</verification>

<success_criteria>
1. matchmaking_test.go line 222 uses direct PlayerRanking struct initialization
2. matchmaking_test.go TestMatchRecord function uses PvPMatch type
3. go test -c ./tests/matchmaking compiles without undefined function errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-fix-broken-packages-establish-quality-gates/08-11-SUMMARY.md`
</output>
