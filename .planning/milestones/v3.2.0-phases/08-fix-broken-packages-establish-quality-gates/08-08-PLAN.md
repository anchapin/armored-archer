---
phase: 08-fix-broken-packages-establish-quality-gates
plan: 08
type: execute
wave: 7
depends_on:
  - 08-06
  - 08-07
files_modified:
  - backend/scripts/generate-coverage-report.sh
autonomous: true
requirements:
  - INF-02
user_setup: []
must_haves:
  truths:
    - "Coverage script fails on compilation errors instead of continuing"
    - "Complete baseline coverage measurement includes all packages"
    - "coverage.out file contains data for all 27 packages"
  artifacts:
    - path: "backend/scripts/generate-coverage-report.sh"
      provides: "Coverage generation with error enforcement"
      contains: "set -e"
    - path: "backend/coverage/coverage.out"
      provides: "Coverage profile for all packages"
      contains: "mode: atomic"
  key_links:
    - from: "scripts/generate-coverage-report.sh"
      to: "go test ./..."
      via: "Error propagation"
      pattern: "^go test.*coverprofile.*\\./\\.\\.\\."
---

<objective>
Update coverage generation script to fail on compilation errors instead of continuing, ensuring complete and accurate baseline coverage measurement.

Purpose: The coverage script uses `|| true` to continue past failures, which results in incomplete coverage measurement when packages have compilation errors. After fixing compilation errors in 08-06 and 08-07, remove `|| true` to enforce that coverage measurement only succeeds when all packages compile.

Output: Coverage script fails immediately on compilation errors, ensuring complete baseline measurement.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-fix-broken-packages-establish-quality-gates/08-VERIFICATION.md
@.planning/phases/08-fix-broken-packages-establish-quality-gates/08-02-SUMMARY.md

# Issue from VERIFICATION.md gap 2
- scripts/generate-coverage-report.sh uses `|| true` to continue past failures
- Compilation errors prevent coverage measurement for rpg, season, metrics packages
- Result: Baseline coverage is incomplete

# Decision from 08-02-SUMMARY deviation #4
- "Kept || true in coverage script" with rationale about baseline measurement
- However, this prevents complete measurement when compilation errors exist

# Current script state (line 11)
```bash
go test -coverprofile=coverage/coverage.out -covermode=atomic ./... || true
```

# After plans 08-06 and 08-07
- All compilation errors fixed
- Coverage should be measured for all packages
- `|| true` should be removed to enforce complete measurement
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove || true from coverage test command</name>
  <files>backend/scripts/generate-coverage-report.sh</files>
  <read_first>
    backend/scripts/generate-coverage-report.sh (lines 1-15 to see current script structure)
  </read_first>
  <action>
    Remove the `|| true` error suppression from the coverage test command:

    1. Read the current content of line 11: `go test -coverprofile=coverage/coverage.out -covermode=atomic ./... || true`
    2. Remove the `|| true` from the end of the line
    3. The corrected line should be: `go test -coverprofile=coverage/coverage.out -covermode=atomic ./...`
    4. Update or remove the comment on line 10 if needed:
       - Current: `# Note: Continue even if some tests fail - we want to measure baseline coverage`
       - Change to: `# Fail on compilation errors - all packages must compile for accurate measurement`
    5. Verify that `set -e` is present at line 2 (already exists) to ensure script fails on any command error

    The script already has `set -e` which causes exit on error, but `|| true` overrides this. Removing `|| true` ensures compilation errors cause the script to fail.
  </action>
  <verify>
    <automated>grep -n "|| true" /home/alex/armored-archer/backend/scripts/generate-coverage-report.sh || echo "No || true found in script"</automated>
  </verify>
  <done>
    coverage-report.sh no longer contains `|| true`, go test command fails immediately on compilation errors, `set -e` is present at line 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Generate complete baseline coverage measurement</name>
  <files>backend/coverage/coverage.out</files>
  <read_first>
    backend/scripts/generate-coverage-report.sh (run script to generate coverage)
  </read_first>
  <action>
    Generate a complete baseline coverage measurement after removing error suppression:

    1. Run `bash scripts/generate-coverage-report.sh` in backend directory
    2. Verify the script succeeds (exit code 0)
    3. Confirm coverage.out file is generated
    4. Verify overall coverage percentage is reported
    5. Confirm package-level coverage shows all 27 packages

    With all compilation errors fixed (from 08-06, 08-07), the script should now succeed and produce complete coverage data.
  </action>
  <verify>
    <automated>cd backend && bash scripts/generate-coverage-report.sh 2>&1 | grep "Overall coverage:" && head -1 coverage/coverage.out | grep "mode:"</automated>
  </verify>
  <done>
    Coverage script completes successfully with exit code 0, coverage.out file exists with valid "mode: atomic" header, overall coverage percentage reported.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify all packages included in coverage profile</name>
  <files>backend/coverage/coverage.out</files>
  <read_first>
    backend/coverage/coverage.out (analyze coverage profile for package coverage)
  </read_first>
  <action>
    Verify that the coverage profile includes all 27 packages:

    1. Extract all package paths from coverage.out: `go tool cover -func=coverage.out | grep -v "^total:" | awk '{print $2}' | sed 's|/.*||' | sort -u`
    2. Count the number of unique packages in coverage profile
    3. Verify count is at least 25 (allowing for packages with no code to cover)
    4. Check for critical packages: combat, rpg, matchmaking, season, notifications, metrics
    5. Confirm no packages are missing due to compilation errors

    This ensures the baseline measurement is complete and not missing data from previously broken packages.
  </action>
  <verify>
    <automated>cd backend && go tool cover -func=coverage/coverage.out | grep -v "^total:" | awk '{print $2}' | sed 's|/.*||' | sort -u | wc -l</automated>
  </verify>
  <done>
    Coverage profile includes data for all packages that have code (at least 25 unique packages), grep confirms critical packages (combat, rpg, matchmaking, season, notifications, metrics) are present.
  </done>
</task>

</tasks>

<verification>
After task completion:
- Run `bash backend/scripts/generate-coverage-report.sh` to verify script succeeds
- Check that coverage.out contains coverage data for all packages
- Verify no `|| true` in the script
- Confirm overall coverage percentage is reported accurately
</verification>

<success_criteria>
1. generate-coverage-report.sh no longer contains `|| true` on the go test line
2. Script has `set -e` at line 2 to enforce error propagation
3. Coverage script completes successfully with exit code 0
4. coverage.out file exists and contains "mode: atomic" header
5. Coverage profile includes at least 25 unique packages (all packages with code)
6. Critical packages (combat, rpg, matchmaking, season, notifications, metrics) are present in coverage profile
</success_criteria>

<output>
After completion, create `.planning/phases/08-fix-broken-packages-establish-quality-gates/08-08-SUMMARY.md`
</output>
