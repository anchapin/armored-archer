---
phase: 08-fix-broken-packages-establish-quality-gates
plan: 18
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/scripts/generate-coverage-report.sh
autonomous: true
requirements:
  - INF-01
  - INF-02
user_setup: []
must_haves:
  truths:
    - "Coverage report includes data for internal/season, internal/rpg, internal/gear, internal/notifications packages"
    - "All passing tests generate coverage for their corresponding internal packages"
  artifacts:
    - path: "backend/scripts/generate-coverage-report.sh"
      provides: "Coverage generation script with -coverpkg flag"
      contains: "-coverpkg"
    - path: "backend/coverage/coverage.out"
      provides: "Coverage including season, rpg, gear, notifications"
      contains: "internal/(season|rpg|gear|notifications)"
  key_links:
    - from: "backend/scripts/generate-coverage-report.sh"
      to: "go test command"
      via: "Uses -coverpkg flag to include internal packages in coverage"
      pattern: "go test.*-coverpkg.*-coverprofile"
---

<objective>
Fix coverage generation for season, rpg, gear, and notifications packages by updating the coverage script to use -coverpkg flag.

Purpose: Tests in tests/season, tests/rpg, tests/gear, tests/notifications pass successfully but don't generate coverage because they are in separate packages that import internal/season, internal/rpg, internal/gear, internal/notifications. Go coverage only profiles the package being tested, not imported dependencies. The fix is to use the -coverpkg flag to explicitly include internal packages in coverage profiling.

Output: Coverage.out includes coverage data for internal/season, internal/rpg, internal/gear, internal/notifications, enabling complete baseline coverage measurement.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-fix-broken-packages-establish-quality-gates/08-VERIFICATION.md

# Root cause from VERIFICATION.md gap analysis
- tests/season passes all tests but coverage reports "coverage: [no statements]"
- tests/rpg passes all tests but coverage reports "coverage: [no statements]"
- tests/gear passes all tests but coverage reports "coverage: [no statements]"
- tests/notifications (circuitbreaker test) passes all tests but coverage reports "coverage: [no statements]"
- Coverage.out does NOT contain any entries for internal/season, internal/rpg, internal/gear, internal/notifications

# Why coverage not generated
Tests in packages tests/season, tests/rpg, tests/gear, tests/notifications are external packages that import internal packages:
- package season_test imports internal/season
- package rpg_test imports internal/rpg
- package gear_test imports internal/gear
- package notifications_test imports internal/circuitbreaker (circuitbreaker_test.go)

When running `go test -coverprofile=coverage.out ./tests/season`, Go only profiles the package being tested (season_test), which has no statements - all code is in the imported internal/season package.

# What works (for comparison)
Internal packages with tests in the SAME package generate coverage:
- internal/config has config_test.go in same package - generates coverage
- internal/circuitbreaker has circuitbreaker_test.go in same package - generates coverage
- internal/rng has rng_property_test.go in same package - generates coverage

# Current coverage script (line 11)
```bash
go test -coverprofile=coverage/coverage.out -covermode=atomic ./...
```

# Fix required
Update the coverage script to use -coverpkg flag to include internal packages:
```bash
go test -coverprofile=coverage/coverage.out -covermode=atomic -coverpkg=./internal/... ./...
```

This tells Go to include coverage data for all packages under internal/, not just the test packages being run.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update coverage script to include -coverpkg flag</name>
  <files>backend/scripts/generate-coverage-report.sh</files>
  <read_first>
    backend/scripts/generate-coverage-report.sh (full file to see current coverage command)
  </read_first>
  <action>
    Update the coverage script on line 11 to include the -coverpkg flag:

    1. Read current line 11:
       ```bash
       go test -coverprofile=coverage/coverage.out -covermode=atomic ./...
       ```

    2. Add -coverpkg=./internal/... flag:
       ```bash
       go test -coverprofile=coverage/coverage.out -covermode=atomic -coverpkg=./internal/... ./...
       ```

    The -coverpkg flag tells Go to collect coverage for all packages under internal/, regardless of which test package is being run. This fixes the issue where tests/season, tests/rpg, tests/gear, tests/notifications pass but don't generate coverage for their corresponding internal packages.
  </action>
  <verify>
    <automated>cd backend && bash -n scripts/generate-coverage-report.sh 2>&1 && grep -n "go test.*-coverpkg" /home/alex/armored-archer/backend/scripts/generate-coverage-report.sh</automated>
  </verify>
  <done>
    Line 11 of generate-coverage-report.sh contains -coverpkg=./internal/... flag, and bash -n confirms script syntax is valid.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify coverage generation includes internal packages</name>
  <files>backend/coverage/coverage.out</files>
  <read_first>
    backend/scripts/generate-coverage-report.sh (to verify the script runs correctly)
  </read_first>
  <action>
    Run the updated coverage script and verify it includes internal packages:

    1. Run coverage generation:
       ```bash
       cd backend && bash scripts/generate-coverage-report.sh
       ```

    2. Verify coverage.out includes internal packages:
       ```bash
       go tool cover -func=coverage/coverage.out | grep -E "internal/(season|rpg|gear|notifications)"
       ```

    3. Confirm output includes entries like:
       - github.com/anchapin/armored-archer/backend/internal/season/season.go
       - github.com/anchapin/armored-archer/backend/internal/rpg/rpg.go
       - github.com/anchapin/armored-archer/backend/internal/gear/gear.go
       - github.com/anchapin/armored-archer/backend/internal/notifications/notifications.go

    The -coverpkg flag ensures coverage data is collected for all internal packages, not just those with tests in the same package.
  </action>
  <verify>
    <automated>cd backend && go tool cover -func=coverage/coverage.out | grep -E "internal/(season|rpg|gear|notifications)" | grep -v "^total:" | head -10</automated>
  </verify>
  <done>
    Coverage.out contains valid coverage entries for internal/season, internal/rpg, internal/gear, internal/notifications with actual coverage data (not just line count).
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify all tests still pass with new coverage flag</name>
  <files>backend/</files>
  <read_first>
    backend/scripts/generate-coverage-report.sh (full script)
  </read_first>
  <action>
    Verify all tests still pass when running with the -coverpkg flag:

    1. Run the full test suite with coverage:
       ```bash
       cd backend && go test -coverprofile=/tmp/verify_coverage.out -covermode=atomic -coverpkg=./internal/... ./...
       ```

    2. Check that all tests pass (no FAIL output)
    3. Verify the exit code is 0

    The -coverpkg flag should not affect test behavior - it only adds coverage profiling for internal packages.
  </action>
  <verify>
    <automated>cd backend && go test -coverprofile=/tmp/verify_coverage.out -covermode=atomic -coverpkg=./internal/... ./... 2>&1 | grep -E "^(ok|FAIL)" | head -20</automated>
  </verify>
  <done>
    All test packages show "ok" status, no "FAIL" output in test results.
  </done>
</task>

</tasks>

<verification>
After task completion:
- Run `cd backend && bash scripts/generate-coverage-report.sh` to verify coverage script runs successfully
- Verify coverage.out includes entries for internal/season, internal/rpg, internal/gear, internal/notifications
- Run `cd backend && go test -coverpkg=./internal/... ./...` to verify all tests pass with the new flag
- Check that overall coverage percentage has increased (more packages now included)
</verification>

<success_criteria>
1. generate-coverage-report.sh line 11 contains -coverpkg=./internal/... flag
2. Coverage.out contains at least 4 coverage entries for internal/season, internal/rpg, internal/gear, internal/notifications
3. All tests pass when run with -coverpkg flag (no FAIL output)
4. Overall coverage percentage increases due to additional packages being included
</success_criteria>

<output>
After completion, create `.planning/phases/08-fix-broken-packages-establish-quality-gates/08-18-SUMMARY.md`
</output>
