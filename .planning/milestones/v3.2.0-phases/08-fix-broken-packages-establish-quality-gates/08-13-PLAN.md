---
phase: 08-fix-broken-packages-establish-quality-gates
plan: 13
type: execute
wave: 7
depends_on: []
files_modified:
  - backend/tests/store/store_test.go
autonomous: true
requirements:
  - INF-01
  - INF-02
user_setup: []
must_haves:
  truths:
    - "All 27 Go backend packages compile successfully without errors"
    - "Store test package compiles without missing import errors"
    - "Baseline coverage measurement includes store test package"
  artifacts:
    - path: "backend/tests/store/store_test.go"
      provides: "Store test package"
      contains: "func TestGetProductAmount"
    - path: "coverage/coverage.out"
      provides: "Coverage report including store test package"
      contains: "tests/store"
  key_links:
    - from: "tests/store/store_test.go"
      to: "internal/store/store.go"
      via: "Import of store package and use of exported functions"
      pattern: "store\\.GetProductAmount"
---

<objective>
Fix missing import and syntax errors in tests/store/store_test.go to enable compilation and complete baseline coverage measurement.

Purpose: Line 220 of store_test.go has a syntax error with fmt.Sprintf call - the test uses fmt.Sprintf but doesn't import the "fmt" package. Additionally, there's likely a function signature error on line 217.

Output: Store test package compiles successfully, enabling complete coverage measurement for all 27 Go packages.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-fix-broken-packages-establish-quality-gates/08-VERIFICATION.md

# Root cause from VERIFICATION.md gap analysis
- "tests/store/store_test.go: Compilation error (package doesn't compile)"
- The package uses fmt.Sprintf but doesn't import "fmt"

# Current imports in store_test.go (lines 3-8)
```go
import (
	"testing"

	"github.com/anchapin/armored-archer/backend/internal/store"
	"github.com/anchapin/armored-archer/backend/tests/testhelpers"
)
```

# Line 217 with function signature issue
```go
result := store.GetProductAmount(tt.productID)
```
- GetProductAmount returns (int, string, error) but the test only captures one value

# Line 220 with fmt.Sprintf error
```go
testhelpers.AssertEqual(t, tt.expected, rank,
	tt.name(t, "Rank for ELO %d", tt.elo))
```
- The testhelpers.AssertEqual doesn't exist with this signature
- The "tt.name" is not a function - should be fmt.Sprintf

# Fix required
1. Add "fmt" to imports
2. Fix line 217 to capture all return values from GetProductAmount
3. Fix line 220 to use fmt.Sprintf correctly
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing fmt import and fix syntax errors</name>
  <files>backend/tests/store/store_test.go</files>
  <read_first>
    backend/tests/store/store_test.go (lines 1-8 to see current imports, and lines 199-227 to see TestGetRankFromElo function with the errors)
  </read_first>
  <action>
    Fix missing import and syntax errors in TestGetRankFromElo function:

    1. Add "fmt" to the import block (after line 5, before the github imports):
       ```go
       import (
           "fmt"
           "testing"
           ...
       )
       ```

    2. Fix line 217 to capture all return values from GetProductAmount:
       - Current: `result := store.GetProductAmount(tt.productID)`
       - Fix: `_, rank, _ := store.GetProductAmount(tt.productID)`

    3. Fix line 220 to use fmt.Sprintf correctly:
       - Current (wrong): `testhelpers.AssertEqual(t, tt.expected, rank, tt.name(t, "Rank for ELO %d", tt.elo))`
       - Fix: `testhelpers.AssertEqual(t, tt.expected, rank, fmt.Sprintf("Rank for ELO %d", tt.elo))`

    The corrected lines 217-220 should be:
    ```go
    _, rank, _ := store.GetProductAmount(tt.productID)
    ...
    testhelpers.AssertEqual(t, tt.expected, rank, fmt.Sprintf("Rank for ELO %d", tt.elo))
    ```

    Note: The testhelpers.AssertEqual signature is `func AssertEqual[T any](t TestingT, expected, actual T, message string)` - it takes 4 arguments, not 5 like the current code implies.
  </action>
  <verify>
    <automated>cd backend && go test -c ./tests/store 2>&1 | grep -i "undefined\|missing.*import\|cannot find" || echo "Store test compiles successfully"</automated>
  </verify>
  <done>
    fmt package imported in store_test.go, line 217 captures all return values from GetProductAmount, line 220 uses fmt.Sprintf correctly with correct argument count, go test -c ./tests/store compiles without errors.
  </done>
</task>

</tasks>

<verification>
After task completion:
- Run `cd backend && go test -c ./tests/store` to verify store test package compiles
- Run `cd backend && go build ./...` to verify all packages compile
- Verify no missing import or syntax errors remain
</verification>

<success_criteria>
1. "fmt" package imported in store_test.go import block
2. Line 217 captures all return values from GetProductAmount
3. Line 220 uses fmt.Sprintf with correct argument count
4. go test -c ./tests/store compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-fix-broken-packages-establish-quality-gates/08-13-SUMMARY.md`
</output>
