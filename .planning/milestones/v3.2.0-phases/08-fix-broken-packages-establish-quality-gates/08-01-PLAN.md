---
phase: 08-fix-broken-packages-establish-quality-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/notifications/notifications.go
  - internal/notifications/feedback_notifications.go
autonomous: true
requirements:
  - INF-01
user_setup: []
must_haves:
  truths:
    - "All Go backend packages compile without errors"
    - "Go test -cover works for all 27 packages"
    - "Coverage measurement includes previously broken packages"
  artifacts:
    - path: "internal/notifications/notifications.go"
      provides: "Notification preferences with working parseTime"
      contains: "func parseTime"
    - path: "internal/notifications/feedback_notifications.go"
      provides: "Feedback notification system without duplicate definitions"
      contains: "func parseTime"
  key_links:
    - from: "internal/notifications/notifications.go"
      to: "parseTime function"
      via: "Single definition of parseTime helper"
      pattern: "func parseTime.*string.*hour.*minute"
    - from: "internal/notifications/feedback_notifications.go"
      to: "parseTime function"
      via: "Single definition of parseTime helper"
      pattern: "func parseTime.*string.*int"
---

<objective>
Fix compilation errors in the notifications package to enable accurate baseline coverage measurement across all 27 Go backend packages.

Purpose: The notifications package has compilation errors that prevent `go test -cover` from measuring coverage for that package. Fixing these errors enables accurate baseline coverage measurement (INF-02) in subsequent plans.

Output: All 27 Go backend packages compile successfully, enabling full coverage measurement.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-fix-broken-packages-establish-quality-gates/08-RESEARCH.md
@.planning/phases/06-coverage-reporting-quality-gates/06-01-SUMMARY.md

# Existing infrastructure from v2.3.0
backend/scripts/generate-coverage-report.sh - Coverage generation script
.github/workflows/coverage.yml - CI workflow with threshold enforcement
scripts/track-coverage-history.sh - Coverage history tracking

# Known compilation errors in notifications package (from current codebase)
1. internal/notifications/notifications.go:295:25: parseTime returns 1 value, expecting 2
2. internal/notifications/feedback_notifications.go:350:6: parseTime redeclared (duplicate definition)
3. internal/notifications/feedback_notifications.go:139:13: nk.SendNotification undefined method
4. internal/notifications/feedback_notifications.go:393:2: unused variable 'data'
5. internal/notifications/feedback_notifications.go:468:7: undefined FeedbackNotification
6. internal/notifications/feedback_notifications.go:491:27: undefined FeedbackNotification
7. internal/notifications/feedback_notifications.go:493:13: undefined FeedbackNotification

# rpg, season, store packages already compile (verified during planning)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix parseTime function signature mismatch</name>
  <files>internal/notifications/notifications.go</files>
  <read_first>
    internal/notifications/notifications.go (lines 285-320 to see IsQuietHours function)
    internal/notifications/feedback_notifications.go (line 350 to see parseTime definition)
  </read_first>
  <action>
    Fix the parseTime function call in IsQuietHours method:

    1. Read internal/notifications/notifications.go lines 295-296 where parseTime is called with 2 return values
    2. Read internal/notifications/feedback_notifications.go line 350 where parseTime is defined returning 1 int
    3. In internal/notifications/notifications.go, modify line 295-296 to use the single-int parseTime signature:
       - Change: `startHour, startMin := parseTime(p.QuietHoursStart)`
       - Change: `endHour, endMin := parseTime(p.QuietHoursEnd)`
       - To: `startMin := parseTime(p.QuietHoursStart)` and `endMin := parseTime(p.QuietHoursEnd)`
    4. Update calculations that used startHour/endHour to extract hour/minute from total minutes:
       - `startHour = startMin / 60` and `startMin = startMin % 60`
       - Same pattern for endMin
    5. Remove the now-unused startHour/endMin variable declarations

    The parseTime function in feedback_notifications.go returns total minutes as int (e.g., "14:30" -> 870 minutes). The calling code in notifications.go expects (hour, minute) tuple. Convert to use total minutes directly.
  </action>
  <verify>
    <automated>cd backend && go build ./internal/notifications 2>&1 | grep -i "parseTime.*assignment" || echo "No parseTime assignment errors"</automated>
  </verify>
  <done>
    parseTime function calls in IsQuietHours use correct signature matching feedback_notifications.go definition, no compilation errors related to parseTime assignment.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove duplicate parseTime function definition</name>
  <files>internal/notifications/notifications.go</files>
  <read_first>
    internal/notifications/notifications.go (grep for "func parseTime" to find line 415)
    internal/notifications/feedback_notifications.go (line 350 for working parseTime)
  </read_first>
  <action>
    Remove the duplicate parseTime function definition:

    1. Find the duplicate parseTime function at line 415 in internal/notifications/notifications.go
    2. Delete the entire duplicate function definition (approximately 3-5 lines)
    3. Verify that all parseTime calls in notifications.go now reference the single definition in feedback_notifications.go

    The duplicate function at line 415 is a redeclaration of parseTime that's already defined at feedback_notifications.go:350. Removing it eliminates the redeclaration error.
  </action>
  <verify>
    <automated>cd backend && go build ./internal/notifications 2>&1 | grep -i "redeclared" || echo "No redeclaration errors"</automated>
  </verify>
  <done>
    Only one parseTime function exists in notifications package (in feedback_notifications.go), no redeclaration errors.
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix undefined FeedbackNotification type</name>
  <files>internal/notifications/feedback_notifications.go</files>
  <read_first>
    internal/notifications/feedback_notifications.go (lines 460-495 to see FeedbackNotification usage)
    internal/notifications/notifications.go (search for "type.*Notification struct" definitions)
  </read_first>
  <action>
    Fix undefined FeedbackNotification references:

    1. Read lines 468, 491, 493 in internal/notifications/feedback_notifications.go to see FeedbackNotification usage
    2. Search internal/notifications/notifications.go for type definitions to find the correct notification type name
    3. Replace undefined "FeedbackNotification" with the correct type name (likely "Notification" or "FeedbackNotification" if missing)
    4. If FeedbackNotification type doesn't exist, create it or use the existing Notification type

    The code references FeedbackNotification type which may not be defined. Either use the existing Notification type or add the missing FeedbackNotification struct definition with appropriate fields.
  </action>
  <verify>
    <automated>cd backend && go build ./internal/notifications 2>&1 | grep -i "undefined.*FeedbackNotification" || echo "No undefined FeedbackNotification errors"</automated>
  </verify>
  <done>
    All FeedbackNotification references use a defined type, no undefined type errors in notifications package.
  </done>
</task>

<task type="auto">
  <name>Task 4: Fix SendNotification method call and unused variable</name>
  <files>internal/notifications/feedback_notifications.go</files>
  <read_first>
    internal/notifications/feedback_notifications.go (line 139 for SendNotification call, line 393 for unused variable)
  </read_first>
  <action>
    Fix SendNotification method call and remove unused variable:

    1. At line 139, the code calls nk.SendNotification which doesn't exist on NakamaModule interface
    2. Check the actual NakamaModule interface for available notification methods (likely SendNotificationsAsync or similar)
    3. Replace nk.SendNotification with the correct method call from the Nakama API
    4. At line 393, remove or use the unused 'data' variable (e.g., prefix with _ if needed for future use, or delete if truly unused)

    The Nakama API changed and SendNotification is no longer available. Use the correct async notification method from the current NakamaModule interface.
  </action>
  <verify>
    <automated>cd backend && go build ./internal/notifications 2>&1 | grep -E "(SendNotification|unused|declared and not used)" || echo "No SendNotification or unused variable errors"</automated>
  </verify>
  <done>
    All SendNotification calls use correct NakamaModule methods, no unused variable errors in notifications package.
  </done>
</task>

<task type="auto">
  <name>Task 5: Verify all 27 packages compile successfully</name>
  <files></files>
  <read_first></read_first>
  <action>
    Verify compilation of all Go backend packages:

    1. Run `go build ./...` in backend directory
    2. Confirm no compilation errors across any packages
    3. List all packages that compile successfully to verify all 27 packages are working
    4. If any errors remain, fix them before proceeding

    This task validates that INF-01 is complete: all Go backend packages compile successfully, enabling accurate baseline coverage measurement.
  </action>
  <verify>
    <automated>cd backend && go build ./... 2>&1 | grep -i "error" || echo "All packages compile successfully"</automated>
  </verify>
  <done>
    All 27 Go backend packages compile without errors, verified by successful `go build ./...` execution with zero error output.
  </done>
</task>

</tasks>

<verification>
After task completion:
- Run `go build ./...` to verify all packages compile
- Run `go test ./... -cover` to verify tests can run across all packages
- Check that notifications package no longer has compilation errors
</verification>

<success_criteria>
1. All 27 Go backend packages compile without errors
2. `go build ./...` exits with code 0 and no error output
3. Notifications package compilation errors resolved:
   - parseTime function signature fixed
   - Duplicate parseTime definition removed
   - FeedbackNotification type defined or replaced
   - SendNotification method call fixed
   - Unused variable removed
</success_criteria>

<output>
After completion, create `.planning/phases/08-fix-broken-packages-establish-quality-gates/08-01-SUMMARY.md`
</output>
