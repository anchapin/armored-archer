---
phase: 08-fix-broken-packages-establish-quality-gates
plan: 07
type: execute
wave: 6
depends_on: []
files_modified:
  - backend/metrics/tracing.go
autonomous: true
requirements:
  - INF-01
user_setup: []
must_haves:
  truths:
    - "Metrics package compiles without trace.StatusCode errors"
    - "OpenTelemetry trace API uses correct codes.Code type"
    - "Span.SetStatus calls use codes.Error instead of trace.StatusCodeError"
  artifacts:
    - path: "backend/metrics/tracing.go"
      provides: "OpenTelemetry tracing implementation"
      contains: "import \"go.opentelemetry.io/otel/codes\""
  key_links:
    - from: "metrics/tracing.go"
      to: "go.opentelemetry.io/otel/codes"
      via: "Status code constants"
      pattern: "codes\\.Error"
    - from: "metrics/tracing.go"
      to: "trace.Span.SetStatus"
      via: "codes.Code parameter"
      pattern: "\\.SetStatus\\(codes\\.Error"
---

<objective>
Fix deprecated trace.StatusCode API usage in metrics/tracing.go to use the correct codes.Code type.

Purpose: The metrics package uses `trace.StatusCode` and `trace.StatusCodeError` which are undefined in the current OpenTelemetry API (v1.42.0). The status code constants moved to the `codes` package and the SetStatus method signature changed to accept `codes.Code` instead of `trace.StatusCode`.

Output: Metrics package compiles successfully with correct OpenTelemetry API usage.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-fix-broken-packages-establish-quality-gates/08-VERIFICATION.md
@.planning/phases/08-fix-broken-packages-establish-quality-gates/08-RESEARCH.md

# Compilation errors identified in VERIFICATION.md
1. metrics/tracing.go:239:43: undefined: trace.StatusCode
2. metrics/tracing.go:386:25: undefined: trace.StatusCodeError
3. metrics/tracing.go:405:25: undefined: trace.StatusCodeError
4. metrics/tracing.go:423:25: undefined: trace.StatusCodeError
5. metrics/tracing.go:443:25: undefined: trace.StatusCodeError
6. metrics/tracing.go:460:25: undefined: trace.StatusCodeError
7. metrics/tracing.go:478:25: undefined: trace.StatusCodeError
8. metrics/tracing.go:495:25: undefined: trace.StatusCodeError

# API change from OpenTelemetry v1.42.0
- Old API: trace.StatusCodeError (deprecated, removed)
- New API: codes.Error from go.opentelemetry.io/otel/codes package
- SetStatus signature: SetStatus(code codes.Code, description string)
- Existing imports include: "go.opentelemetry.io/otel/trace" but missing "go.opentelemetry.io/otel/codes"

# From code verification
- codes.Unset = 0 (no error)
- codes.Error = 1 (error state)
- Span.SetStatus expects codes.Code type, not trace.StatusCode
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add codes package import to tracing.go</name>
  <files>backend/metrics/tracing.go</files>
  <read_first>
    backend/metrics/tracing.go (lines 1-22 to see current imports section)
  </read_first>
  <action>
    Add the codes package import to enable use of codes.Error constant:

    1. Read the current imports section (lines 5-22)
    2. After line 19 (import "go.opentelemetry.io/otel/sdk/trace"), add a new import:
       - Add: `"go.opentelemetry.io/otel/codes"`
    3. The imports block should include both trace and codes packages:
       - trace package: `go.opentelemetry.io/otel/trace`
       - codes package: `go.opentelemetry.io/otel/codes`

    The codes package provides the Error constant needed for SetStatus calls.
  </action>
  <verify>
    <automated>grep "go.opentelemetry.io/otel/codes" /home/alex/armored-archer/backend/metrics/tracing.go || echo "codes package not imported yet"</automated>
  </verify>
  <done>
    tracing.go imports go.opentelemetry.io/otel/codes package, verified by grep finding the import statement in the imports section.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace trace.StatusCodeError with codes.Error</name>
  <files>backend/metrics/tracing.go</files>
  <read_first>
    backend/metrics/tracing.go (lines 235-245 for SpanTimer.SetStatus, and grep for all occurrences of trace.StatusCodeError)
  </read_first>
  <action>
    Replace all occurrences of trace.StatusCodeError with codes.Error:

    1. Find line 239 in SpanTimer.SetStatus method: `func (st *SpanTimer) SetStatus(code trace.StatusCode, description string)`
       - Change parameter type from `trace.StatusCode` to `codes.Code`
       - Corrected: `func (st *SpanTimer) SetStatus(code codes.Code, description string)`

    2. Replace trace.StatusCodeError with codes.Error in all SetStatus calls:
       - Line 386 in TraceRPC: `timer.SetStatus(trace.StatusCodeError, err.Error())`
         - Change to: `timer.SetStatus(codes.Error, err.Error())`
       - Line 405 in TraceDatabase: `timer.SetStatus(trace.StatusCodeError, err.Error())`
         - Change to: `timer.SetStatus(codes.Error, err.Error())`
       - Line 423 in TraceCache: `timer.SetStatus(trace.StatusCodeError, err.Error())`
         - Change to: `timer.SetStatus(codes.Error, err.Error())`
       - Line 443 in TraceMatch: `timer.SetStatus(trace.StatusCodeError, err.Error())`
         - Change to: `timer.SetStatus(codes.Error, err.Error())`
       - Line 460 in TraceCombat: `timer.SetStatus(trace.StatusCodeError, err.Error())`
         - Change to: `timer.SetStatus(codes.Error, err.Error())`
       - Line 478 in TraceGear: `timer.SetStatus(trace.StatusCodeError, err.Error())`
         - Change to: `timer.SetStatus(codes.Error, err.Error())`
       - Line 495 in TraceSeason: `timer.SetStatus(trace.StatusCodeError, err.Error())`
         - Change to: `timer.SetStatus(codes.Error, err.Error())`

    There are 7 SetStatus call sites that need updating (lines 386, 405, 423, 443, 460, 478, 495).
  </action>
  <verify>
    <automated>grep -n "trace.StatusCodeError\|trace.StatusCode" /home/alex/armored-archer/backend/metrics/tracing.go || echo "No trace.StatusCode references found (all replaced)"</automated>
  </verify>
  <done>
    All references to trace.StatusCodeError replaced with codes.Error, SpanTimer.SetStatus parameter type changed to codes.Code, grep confirms no trace.StatusCode references remain.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify metrics package compiles successfully</name>
  <files>backend/metrics/tracing.go</files>
  <read_first>
    backend/metrics/tracing.go (read to confirm changes applied)
  </read_first>
  <action>
    Verify that the metrics package now compiles successfully:

    1. Run `go build ./metrics` to compile the metrics package
    2. Verify no undefined trace.StatusCode or trace.StatusCodeError errors
    3. Verify the SetStatus method signature matches codes.Code parameter type
    4. Confirm exit code 0 with no compilation errors

    This validates that the OpenTelemetry API migration from deprecated trace.StatusCode to codes.Code is complete.
  </action>
  <verify>
    <automated>cd backend && go build ./metrics 2>&1 | grep -E "StatusCode|undefined.*trace" || echo "Metrics package compiles successfully"</automated>
  </verify>
  <done>
    metrics package compiles successfully with go build ./metrics exiting code 0, no undefined trace.StatusCode or trace.StatusCodeError errors.
  </done>
</task>

</tasks>

<verification>
After task completion:
- Run `go build ./metrics` to verify metrics package compiles
- Run `go build ./...` to verify overall backend compilation
- Check that no trace.StatusCode or trace.StatusCodeError references exist
- Verify codes package import is present
</verification>

<success_criteria>
1. tracing.go imports go.opentelemetry.io/otel/codes package
2. SpanTimer.SetStatus uses codes.Code parameter type
3. All 7 SetStatus calls use codes.Error instead of trace.StatusCodeError
4. go build ./metrics succeeds with exit code 0
5. No trace.StatusCode or trace.StatusCodeError references in metrics/tracing.go
</success_criteria>

<output>
After completion, create `.planning/phases/08-fix-broken-packages-establish-quality-gates/08-07-SUMMARY.md`
</output>
