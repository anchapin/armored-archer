---
phase: 07-enemy-sprites
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements:
  - ENEMY-01
  - ENEMY-02
  - ENEMY-03
  - ENEMY-04
  - ENEMY-05

must_haves:
  truths:
    - "Each basic enemy type has idle animation with 4-8 frames that loops"
    - "Each basic enemy type has walk animation with 4-8 frames for each of 4 directions"
    - "Each basic enemy type has attack animation with 6-8 frames"
    - "Each basic enemy type has hit animation with 2-4 frames for damage feedback"
    - "Each basic enemy type has death animation with 6-8 frames"
  artifacts:
    - path: "assets/sprites/enemies/enemy_sprites.tres"
      provides: "SpriteFrames with all enemy animations (8 enemy types × 5 states × 4 directions)"
      animations: ["goblin_idle_down", "goblin_idle_up", "goblin_idle_left", "goblin_idle_right", "goblin_walk_down", "goblin_walk_up", "goblin_walk_left", "goblin_walk_right", "goblin_attack_down", "goblin_attack_up", "goblin_attack_left", "goblin_attack_right", "goblin_hit_down", "goblin_hit_up", "goblin_hit_left", "goblin_hit_right", "goblin_death_down", "goblin_death_up", "goblin_death_left", "goblin_death_right", ... repeated for all 8 enemy types]
  key_links:
    - from: "enemy_sprites.tres"
      to: "scenes/enemies/base_enemy.tscn"
      via: "AnimatedSprite2D.sprite_frames property"
      pattern: "sprite_frames.*enemy_sprites"
---

<objective>
Create all enemy sprite placeholder assets and configure SpriteFrames with animations for all 8 basic enemy types.

Purpose: Provide animation foundation for enemies - placeholders use colored rectangles following pixel art convention from Phase 05. SpriteFrames resource enables AnimatedSprite2D to play enemy animations.

Output: SpriteFrames resource at assets/sprites/enemies/enemy_sprites.tres with all enemy animations configured
</objective>

<context>
@/home/alex/armored-archer/.planning/phases/07-enemy-sprites/spritespec/ENEMY-SPRITE-SPEC.md
@/home/alex/armored-archer/scenes/enemies/base_enemy.gd
@/home/alex/armored-archer/scenes/enemies/base_enemy.tscn
@/home/alex/armored-archer/assets/sprites/kenney/enemies/ (18 imported sprites)

# Enemy types from spec (8 basic enemies):
1. Goblin (melee) - green, 32x32
2. Skeleton Archer (ranged) - bone/blue, 32x32
3. Shadow Runner (speed) - dark/red, 32x32
4. Rat Swarm (swarmer) - brown, 24x24
5. Orc Warrior (brute) - green/large, 40x40
6. Elven Scout (scout) - green/brown, 32x32
7. Mushroom - red/white cap, 24x24
8. Slime - blue/green, 24x24

# Each enemy needs 5 animation states × 4 directions = 20 animations
# Total: 8 enemies × 20 = 160 animations

# SpriteFrames structure:
# - Loop: idle, walk
# - No loop: attack, hit, death
# - FPS: 8 for idle, 10-12 for walk, 8-10 for others
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create placeholder sprite texture files for all enemy types</name>
  <files>assets/sprites/enemies/goblin_idle_down.tres, etc. (160 files)</files>
  <action>
Create placeholder sprite textures for each enemy type using different colors for visual distinction:

For each of 8 enemy types (goblin, skeleton, shadow_runner, rat, orc, scout, mushroom, slime):
- idle: 6 frames, color unique to enemy (green, bone, dark, brown, etc.)
- walk: 6 frames, cycling through slight position/pose variations
- attack: 7 frames, show attack motion
- hit: 3 frames, flash white/red for damage feedback
- death: 7 frames, fall down and fade

Use different hues per enemy type for visual clarity:
- goblin: green (#4CAF50)
- skeleton: bone white (#E8E8D0)
- shadow_runner: dark purple (#2A2A35)
- rat: brown (#8B7355)
- orc: dark green (#5A7C4A)
- scout: forest green (#228B22)
- mushroom: red (#E53935)
- slime: blue (#2196F3)

Each direction uses same frames but rotated or mirrored as appropriate.
  </action>
  <verify>
ls assets/sprites/enemies/*.res | wc -l
# Should return 160 or more sprite files
  </verify>
  <done>160 placeholder sprite files exist in assets/sprites/enemies/ (8 enemy types × 20 animations)</done>
</task>

<task type="auto">
  <name>Task 2: Configure SpriteFrames resource with all enemy animations</name>
  <files>assets/sprites/enemies/enemy_sprites.tres</files>
  <action>
Create SpriteFrames resource at assets/sprites/enemies/enemy_sprites.tres. Configure all animations for each enemy type:

For each enemy type and each animation (use loop=true for idle/walk, loop=false for attack/hit/death):
1. Add animation: sprite_frames.add_animation("goblin_idle_down")
2. Set speed: sprite_frames.set_animation_speed("goblin_idle_down", 8.0)
3. Set loop: sprite_frames.set_animation_loop("goblin_idle_down", true)
4. Add frames: sprite_frames.add_frame("goblin_idle_down", load("res://assets/sprites/enemies/goblin_idle_down.tres"))

Enemy types and animations (8 enemies × 20 animations = 160 total):
- goblin: idle/walk/attack/hit/death × 4 directions
- skeleton: idle/walk/attack/hit/death × 4 directions
- shadow_runner: idle/walk/attack/hit/death × 4 directions
- rat: idle/walk/attack/hit/death × 4 directions
- orc: idle/walk/attack/hit/death × 4 directions
- scout: idle/walk/attack/hit/death × 4 directions
- mushroom: idle/walk/attack/hit/death × 4 directions
- slime: idle/walk/attack/hit/death × 4 directions
  </action>
  <verify>
# Verify SpriteFrames has all animations
cat assets/sprites/enemies/enemy_sprites.tres | grep -c "add_animation"
# Should return 160
  </verify>
  <done>SpriteFrames resource has all 160 animations configured with correct FPS and loop settings</done>
</task>

</tasks>

<verification>
- [ ] Enemy sprite folder exists at assets/sprites/enemies/
- [ ] 160 placeholder sprite textures created (8 enemy types × 5 states × 4 directions)
- [ ] SpriteFrames resource created with all animations
- [ ] Loop settings correct: idle/walk loop, attack/hit/death don't loop
- [ ] FPS settings appropriate per animation type
- [ ] Each enemy type has distinct color for visual identification
</verification>

<success_criteria>
SpriteFrames resource is ready for use by AnimatedSprite2D in enemy scenes. All 160 animations configured with correct timing. Each enemy type visually distinguishable by color.
</success_criteria>

<output>
SpriteFrames resource at assets/sprites/enemies/enemy_sprites.tres with:
- 8 enemy types: goblin, skeleton, shadow_runner, rat, orc, scout, mushroom, slime
- 20 animations per enemy: idle/walk/attack/hit/death × 4 directions
- Total: 160 animations
- Loop: idle, walk
- No loop: attack, hit, death
</output>