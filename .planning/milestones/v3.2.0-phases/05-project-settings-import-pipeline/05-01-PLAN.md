---
phase: 05-project-settings-import-pipeline
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - project.godot
autonomous: true
requirements:
  - PROJ-02
must_haves:
  truths:
    - "Viewport stretch mode is set to canvas_items"
    - "Scale mode is set to integer for whole-number scaling"
  artifacts:
    - path: "project.godot"
      provides: "Godot project configuration with stretch settings"
      contains: "window/stretch/mode=\"canvas_items\""
      contains_2: "window/stretch/scale_mode=\"integer\""
  key_links: []
---

<objective>
Configure viewport stretch settings in project.godot for pixel-perfect rendering

Purpose: Prevent pixel distortion when scaling the game window - ensures crisp, non-blurry pixels at any resolution

Output: Updated project.godot with stretch settings
</objective>

<execution_context>
@/home/alex/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@project.godot

# Current state:
# - Line 136: textures/default_texture_filter=1 (Nearest) - already correct (PROJ-01)
# - Missing: stretch mode and scale_mode settings
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add viewport stretch settings to project.godot</name>
  <files>project.godot</files>
  <action>
Add the following display stretch settings to project.godot under the [display] section:

```
[display]

window/size/viewport_width=640
window/size/viewport_height=360
window/stretch/mode="canvas_items"
window/stretch/scale_mode="integer"
window/stretch/aspect="expand"
```

If [display] section doesn't exist, add it. If viewport_width/height already exist, update them to 640x360. If stretch settings exist, update them. This ensures the game renders at low resolution (640x360) and scales up by whole numbers only.
  </action>
  <verify>
    <automated>grep -E "(window/stretch/mode|window/stretch/scale_mode)" project.godot</automated>
  </verify>
  <done>project.godot contains window/stretch/mode="canvas_items" and window/stretch/scale_mode="integer"</done>
</task>

</tasks>

<verification>
- Run Godot to validate project loads: `godot4 --headless --quit-after 3`
- Verify no errors in output
</verification>

<success_criteria>
User can verify viewport stretch mode is set to canvas_items with integer scaling
</success_criteria>

<output>
After completion, create `.planning/phases/05-project-settings-import-pipeline/05-01-SUMMARY.md`
</output>
