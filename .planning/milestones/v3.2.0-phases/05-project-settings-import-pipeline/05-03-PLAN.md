---
phase: 05-project-settings-import-pipeline
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/sprites/enemies/
  - assets/sprites/ui/
autonomous: true
requirements:
  - PROJ-04
must_haves:
  truths:
    - "User can navigate folder structure for characters/, enemies/, equipment/, ui/, backgrounds/"
  artifacts:
    - path: "assets/sprites/enemies/"
      provides: "Folder for enemy sprite assets"
    - path: "assets/sprites/ui/"
      provides: "Folder for UI sprite assets"
  key_links: []
---

<objective>
Create missing folder structure for sprite organization

Purpose: Ensure consistent folder structure for all sprite types (characters, enemies, equipment, ui, backgrounds)

Output: New folders created in assets/sprites/
</objective>

<execution_context>
@/home/alex/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
# Current folder structure:
assets/sprites/
├── characters/     (exists)
├── gear/          (exists)
└── [root .tres files]

# Missing:
- assets/sprites/enemies/
- assets/sprites/ui/

# Recommended structure per research:
assets/
├── sprites/
│   ├── characters/   (exists)
│   ├── enemies/      (create)
│   ├── equipment/    (create subfolder)
│   │   ├── bow/
│   │   ├── arrow/
│   │   ├── armor/
│   │   └── helm/
│   └── ui/          (create)
└── backgrounds/      (exists)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create missing sprite folders</name>
  <files>assets/sprites/enemies/, assets/sprites/ui/, assets/sprites/equipment/</files>
  <action>
Create the missing folder structure:

```
assets/sprites/enemies/   - for all enemy sprites (melee, ranged, boss, etc.)
assets/sprites/ui/        - for button icons, HUD elements, inventory slots
assets/sprites/equipment/ - for weapons and armor (will contain bow/, arrow/, armor/, helm/)
```

Run:
mkdir -p assets/sprites/enemies
mkdir -p assets/sprites/ui
mkdir -p assets/sprites/equipment/bow
mkdir -p assets/sprites/equipment/arrow
mkdir -p assets/sprites/equipment/armor
mkdir -p assets/sprites/equipment/helm
  </action>
  <verify>
    <automated>ls -la assets/sprites/ | grep -E "^d"</automated>
  </verify>
  <done>All required sprite folders exist: characters/, enemies/, equipment/, ui/, backgrounds/</done>
</task>

</tasks>

<verification>
- Run: `ls -d assets/sprites/*/`
- Verify output contains: characters, enemies, equipment, ui
- Also verify backgrounds exists at assets/ level
</verification>

<success_criteria>
User can navigate folder structure for characters/, enemies/, equipment/, ui/, backgrounds/
</success_criteria>

<output>
After completion, create `.planning/phases/05-project-settings-import-pipeline/05-03-SUMMARY.md`
</output>
