---
phase: 06-player-character-animation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - PLAY-01
  - PLAY-02
  - PLAY-03
  - PLAY-04
  - PLAY-05
  - PLAY-06

must_haves:
  truths:
    - "Player has idle animation with 4-8 frames that loops"
    - "Player has walk animation with 4-8 frames for each of 4 directions"
    - "Player has attack animation with 6-8 frames"
    - "Player has bow draw animation with 6-8 frames that loops during aim"
    - "Player has hit animation with 2-4 frames for damage feedback"
    - "Player has death animation with 6-8 frames"
  artifacts:
    - path: "assets/sprites/player/player_sprites.tres"
      provides: "SpriteFrames with all 24 animations (4 directions × 6 states)"
      animations: ["idle_down", "idle_up", "idle_left", "idle_right", "walk_down", "walk_up", "walk_left", "walk_right", "attack_down", "attack_up", "attack_left", "attack_right", "bow_draw_down", "bow_draw_up", "bow_draw_left", "bow_draw_right", "hit_down", "hit_up", "hit_left", "hit_right", "death_down", "death_up", "death_left", "death_right"]
  key_links:
    - from: "player_sprites.tres"
      to: "scenes/player.tscn"
      via: "AnimatedSprite2D.sprite_frames property"
      pattern: "sprite_frames.*player_sprites"
---

<objective>
Create all player sprite placeholder assets and configure SpriteFrames with 24 animations (4 directions × 6 states).

Purpose: Provide animation foundation - placeholders use colored rectangles following pixel art convention from Phase 05. SpriteFrames resource enables AnimatedSprite2D to play animations.

Output: SpriteFrames resource at assets/sprites/player/player_sprites.tres with all animations configured
</objective>

<context>
@/home/alex/armored-archer/.planning/phases/06-player-character-animation/06-RESEARCH.md
@/home/alex/armored-archer/.planning/phases/06-player-character-animation/CONTEXT.md
@/home/alex/armored-archer/assets/sprites/character_body.tres

# SpriteFrames structure from research:
# - 24 animations total: idle, walk, attack, bow_draw, hit, death × 4 directions (down/up/left/right)
# - Loop: idle, walk, bow_draw
# - No loop: attack, hit, death
# - FPS: 8 for idle, 10-12 for walk, 8-10 for others
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create placeholder sprite texture files</name>
  <files>assets/sprites/player/idle_down.tres, assets/sprites/player/idle_up.tres, etc. (24 files)</files>
  <action>
Create placeholder sprite textures using Godot's PlaceholderTexture2D or generated color rectangles. Use 32x32 pixel size (matching existing sprites). Create in folder assets/sprites/player/.

For each animation type:
- idle: 6 frames (4-8 range), color variations of blue/cyan
- walk: 6 frames, cycling through slight position/pose variations
- attack: 7 frames, show bow drawing and release motion
- bow_draw: 7 frames, draw bow from rest to full draw
- hit: 3 frames, flash white/red for damage feedback
- death: 7 frames, fall down and fade

Each direction uses same frames but rotated or mirrored as appropriate.

Use different hues per state for visual clarity:
- idle: cyan (#00BCD4)
- walk: green (#4CAF50)  
- attack: red (#F44336)
- bow_draw: yellow (#FFEB3B)
- hit: orange (#FF9800)
- death: purple (#9C27B0)
  </action>
  <verify>
ls assets/sprites/player/*.res | wc -l
# Should return 24 or more sprite files
  </verify>
  <done>24 placeholder sprite files exist in assets/sprites/player/</done>
</task>

<task type="auto">
  <name>Task 2: Configure SpriteFrames resource with all animations</name>
  <files>assets/sprites/player/player_sprites.tres</files>
  <action>
Create SpriteFrames resource at assets/sprites/player/player_sprites.tres. Configure all 24 animations:

For each animation (use loop=true for idle/walk/bow_draw, loop=false for attack/hit/death):
1. Add animation: sprite_frames.add_animation("idle_down")
2. Set speed: sprite_frames.set_animation_speed("idle_down", 8.0)
3. Set loop: sprite_frames.set_animation_loop("idle_down", true)
4. Add frames: sprite_frames.add_frame("idle_down", load("res://assets/sprites/player/idle_down.tres"))

Animations (24 total):
- idle: idle_down, idle_up, idle_left, idle_right (loop=true, 8 FPS)
- walk: walk_down, walk_up, walk_left, walk_right (loop=true, 12 FPS)
- attack: attack_down, attack_up, attack_left, attack_right (loop=false, 10 FPS)
- bow_draw: bow_draw_down, bow_draw_up, bow_draw_left, bow_draw_right (loop=true, 8 FPS)
- hit: hit_down, hit_up, hit_left, hit_right (loop=false, 10 FPS)
- death: death_down, death_up, death_left, death_right (loop=false, 8 FPS)
  </action>
  <verify>
# Verify SpriteFrames has all 24 animations
cat assets/sprites/player/player_sprites.tres | grep -c "add_animation"
# Should return 24
  </verify>
  <done>SpriteFrames resource has all 24 animations configured with correct FPS and loop settings</done>
</task>

</tasks>

<verification>
- [ ] Player sprite folder exists at assets/sprites/player/
- [ ] 24 placeholder sprite textures created (6 animation types × 4 directions)
- [ ] SpriteFrames resource created with all animations
- [ ] Loop settings correct: idle/walk/bow_draw loop, attack/hit/death don't loop
- [ ] FPS settings appropriate per animation type
</verification>

<success_criteria>
SpriteFrames resource is ready for use by AnimatedSprite2D in player scene. All 24 animations configured with correct timing.
</success_criteria>

<output>
After completion, create `.planning/phases/06-player-character-animation/06-01-SUMMARY.md`
</output>