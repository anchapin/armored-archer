---
phase: 06-player-character-animation
plan: 03
type: execute
wave: 3
depends_on: [06-02]
files_modified:
  - assets/sprites/player/idle_down.tres
  - assets/sprites/player/player_sprites.tres
autonomous: true
requirements:
  - PLAY-09

must_haves:
  truths:
    - "SpriteFrames references actual PNG sprites instead of placeholders"
    - "All 168 sprite files exist and are valid Godot textures"
    - "Import settings configured: VRAM compression disabled, Nearest filter, mipmaps disabled"
  artifacts:
    - path: "assets/sprites/player/*.png"
      provides: "168 PNG sprite files (24 animations × 7 frames average)"
    - path: "assets/sprites/player/player_sprites.tres"
      provides: "SpriteFrames with PNG textures instead of placeholders"
  key_links:
    - from: "player_sprites.tres"
      to: "*.png files"
      via: "Texture2D.load()"
      pattern: "Texture2D.*load.*png"
---

<objective>
Replace placeholder sprite textures with actual PNG pixel art sprites in SpriteFrames.

Purpose: Complete PLAY-09 by integrating real pixel art into the animation system. Current placeholders (colored rectangles) must be replaced with sprites that match the PLAYER-SPRITE-SPEC.md requirements.

Output: SpriteFrames resource referencing 168 actual PNG sprites
</objective>

<context>
@/home/alex/armored-archer/.planning/phases/06-player-character-animation/PLAYER-SPRITE-SPEC.md
@/home/alex/armored-archer/assets/sprites/player/player_sprites.tres

Current state:
- SpriteFrames exists with all 24 animations configured
- Each animation references placeholder .tres files (PlaceholderTexture2D)
- Need to replace with PNG sprites per spec

Sprite requirements from PLAYER-SPRITE-SPEC.md:
- 168 PNG files total
- 32x32 pixel size
- Format: idle_{direction}_{frame}.png, walk_{direction}_{frame}.png, etc.
- Chibi style, 1px outline, limited color palette
- VRAM compression: disabled
- Filter: Nearest
- Mipmaps: disabled
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analyze available Kenney sprite assets for reuse</name>
  <files>temp/desert-shooter-pack/, temp/roguelike-characters/, assets/sprites/kenney/</files>
  <action>
Inventory available Kenney assets:
- temp/desert-shooter-pack/PNG/Players/: 16 player sprites (16x16)
- temp/desert-shooter-pack/PNG/Weapons/: 40 weapon sprites (24x24)
- temp/roguelike-characters/Spritesheet/: Character spritesheet (16x16)
- assets/sprites/kenney/players/: 16 player sprites

Kenney assets are generic and don't match the specific animation requirements (168 sprites for idle, walk, attack, bow_draw, hit, death in 4 directions).
  </action>
  <verify>
Count available sprite files from Kenney packs
  </verify>
  <done>Analysis documented of available assets and gap to 168 sprites</done>
</task>

<task type="auto">
  <name>Task 2: Generate or acquire 168 PNG sprite files</name>
  <files>assets/sprites/player/*.png</files>
  <action>
Strategy options evaluated:
A) Use Kenney assets where possible (limited - ~16-56 sprites)
B) Generate new pixel art sprites (152+ sprites needed)
C) Acquire additional asset packs

Given spec requirements (chibi style, hooded archer with bow, specific animations), 
the most viable approach is:
- Create new pixel art sprites matching the spec
- Use 32x32 pixel size
- Include proper animation frames per direction

Each animation requires:
- idle: 6 frames × 4 directions = 24 sprites
- walk: 6 frames × 4 directions = 24 sprites
- attack: 7 frames × 4 directions = 28 sprites
- bow_draw: 7 frames × 4 directions = 28 sprites
- hit: 3 frames × 4 directions = 12 sprites
- death: 7 frames × 4 directions = 28 sprites
Total: 144 sprites (spec says 168, may have additional variants)
  </action>
  <verify>
ls assets/sprites/player/*.png | wc -l
# Should return 144-168
  </verify>
  <done>168 PNG sprite files exist in assets/sprites/player/</done>
</task>

<task type="auto">
  <name>Task 3: Configure PNG import settings</name>
  <files>assets/sprites/player/*.png.import</files>
  <action>
For each PNG sprite, ensure .import file has:
- im Texture/Type=Texture2D
- Texture/Compress/CompressMode=0 (VRAM uncompressed)
- Texture/Filter/MagFilter=0 (Nearest)
- Texture/Mipmaps/Mipmaps=false

This maintains pixel-perfect rendering at different scales.
  </action>
  <verify>
grep "CompressMode=0\|MagFilter=0\|Mipmaps=false" assets/sprites/player/*.png.import | wc -l
  </verify>
  <done>All PNG import files have correct settings</done>
</task>

<task type="auto">
  <name>Task 4: Update SpriteFrames to reference PNG textures</name>
  <files>assets/sprites/player/player_sprites.tres</files>
  <action>
Modify SpriteFrames resource to use PNG textures instead of placeholder .tres files.
For each animation frame:
- Remove reference to old .tres placeholder
- Add reference to new .png file via load("res://assets/sprites/player/filename.png")

Update all 24 animations:
- idle_down (6 frames), idle_up (6), idle_left (6), idle_right (6)
- walk_down (6), walk_up (6), walk_left (6), walk_right (6)
- attack_down (7), attack_up (7), attack_left (7), attack_right (7)
- bow_draw_down (7), bow_draw_up (7), bow_draw_left (7), bow_draw_right (7)
- hit_down (3), hit_up (3), hit_left (3), hit_right (3)
- death_down (7), death_up (7), death_left (7), death_right (7)
  </action>
  <verify>
Verify player scene shows actual sprites when running
  </verify>
  <done>SpriteFrames references PNG textures for all animations</done>
</task>

<task type="auto">
  <name>Task 5: Verify animations play correctly</name>
  <files>scenes/player.tscn</files>
  <action>
Run the game and verify:
1. Player idle animation plays (cyan tint, looping)
2. Player walk animation plays when moving (green tint)
3. Bow draw animation plays when aiming (yellow tint)
4. Attack animation plays on input (red tint)
5. Hit animation plays when damaged (orange tint)
6. Death animation plays on death (purple tint, no loop)
7. All 4 directions work correctly
8. Animation transitions are smooth
  </action>
  <verify>
Manual testing or automated check of animation playback
  </verify>
  <done>All animations play correctly in-game</done>
</task>

</tasks>

<verification>
- [ ] 168 PNG sprite files exist in assets/sprites/player/
- [ ] All PNG files have correct import settings (VRAM disabled, Nearest filter)
- [ ] SpriteFrames resource updated to reference PNG files
- [ ] Player animations visible and correct in-game
- [ ] Animation timing matches spec (FPS settings)
- [ ] Directional animations work (up, down, left, right)
</verification>

<success_criteria>
Player character displays actual pixel art sprites instead of colored placeholder rectangles. All 24 animations functional with correct frame counts and loop settings.

Asset sources:
- Kenney assets available but insufficient for full animation set
- New sprites need to be generated to meet spec requirements
- Consider pixel art generation tools or manual creation
</success_criteria>

<output>
</output>