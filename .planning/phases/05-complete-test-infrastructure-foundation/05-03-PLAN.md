---
phase: 05-complete-test-infrastructure-foundation
plan: 03
type: execute
wave: 1
depends_on: [05-00]
files_modified: [.github/workflows/ci.yml]
autonomous: true
requirements: [FND-05]

must_haves:
  truths:
    - "CI workflow runs Go tests with -race flag for data race detection"
    - "Race detector failures cause CI to fail with clear error messages"
    - "GOMAXPROCS=2 environment variable set for race detector performance"
    - "Race detector runs on every push and pull request"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "CI workflow with race detector enabled"
      contains: "-race"
      exports: ["backend-test-race"]
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "go test"
      via: "CI step runs Go tests with -race flag"
      pattern: "go test.*-race"
    - from: ".github/workflows/ci.yml"
      to: "GitHub Actions"
      via: "CI job triggers on push and pull_request"
      pattern: "on:\s*push:.*pull_request:"
---

<objective>
Enable Go race detector in CI workflow to catch data race conditions in concurrent code automatically.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-complete-test-infrastructure-foundation/05-00-PLAN.md
@.planning/phases/05-complete-test-infrastructure-foundation/05-04-PLAN.md

# From Wave 0 (Plan 05-00)

The Wave 0 stub added a new step to CI workflow but was incomplete. This plan completes the race detector integration.

# From Research (05-RESEARCH.md)

Race detector configuration:
- Use `-race` flag with `go test`
- Set `GOMAXPROCS=2` for better race detector performance
- Race detector slows tests 10x but is necessary for concurrent code

# Current CI State

From .github/workflows/ci.yml:
- backend-test job exists with PostgreSQL and Nakama services
- TypeScript tests run with `npm run test:coverage`
- No Go test execution with race detector yet
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Go race detector step to CI workflow</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>.github/workflows/ci.yml</read_first>
  <action>Add a dedicated Go test step with race detector to the CI workflow:

1. Find the backend-test job in .github/workflows/ci.yml (around line 120)
2. Find the "Run tests with coverage" step (around line 201)
3. ADD THE FOLLOWING NEW STEP AFTER the existing TypeScript test step:

```yaml
      - name: Run Go tests with race detector
        working-directory: ./backend
        env:
          TEST_DB_HOST: localhost
          TEST_DB_PORT: 5432
          TEST_DB_USER: postgres
          TEST_DB_PASSWORD: changeme
          TEST_DB_NAME: nakama
          GOMAXPROCS: 2
          TZ: UTC
        run: |
          echo "Running Go tests with race detector..."
          go test -v -race -timeout=60s ./...
```

4. Add a comment above the step:
   ```yaml
      # FND-05: Race detector for data race detection in concurrent code
   ```

IMPORTANT NOTES:
- Add this step AFTER the existing "Run tests with coverage" step
- Do NOT remove the existing TypeScript test step
- The backend has both TypeScript (legacy) and Go (current) tests
- Use 60s timeout for race detector (slower than normal tests)
- Set GOMAXPROCS=2 for race detector performance
- The step should fail if any data races are detected

EXACT text to use for the step (including indentation with 6 spaces):
```yaml
      # FND-05: Race detector for data race detection in concurrent code
      - name: Run Go tests with race detector
        working-directory: ./backend
        env:
          TEST_DB_HOST: localhost
          TEST_DB_PORT: 5432
          TEST_DB_USER: postgres
          TEST_DB_PASSWORD: changeme
          TEST_DB_NAME: nakama
          GOMAXPROCS: 2
          TZ: UTC
        run: |
          echo "Running Go tests with race detector..."
          go test -v -race -timeout=60s ./...
```</action>
  <verify>
    <automated>grep -q "Run Go tests with race detector" .github/workflows/ci.yml && grep -q "go test -v -race -timeout=60s" .github/workflows/ci.yml && grep -q "GOMAXPROCS: 2" .github/workflows/ci.yml</automated>
  </verify>
  <done>CI workflow has new step "Run Go tests with race detector" with -race flag, GOMAXPROCS=2, 60s timeout, proper environment variables, and FND-05 comment</done>
</task>

<task type="auto">
  <name>Task 2: Verify CI workflow syntax and race detector configuration</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>.github/workflows/ci.yml</read_first>
  <action>Verify the CI workflow changes are correct:

1. Check workflow YAML syntax: `grep -A 15 "Run Go tests with race detector" .github/workflows/ci.yml`
2. Verify -race flag is present: `grep "go test.*-race" .github/workflows/ci.yml`
3. Verify GOMAXPROCS is set: `grep "GOMAXPROCS: 2" .github/workflows/ci.yml`
4. Verify timeout is 60s: `grep "timeout=60s" .github/workflows/ci.yml`
5. Verify FND-05 comment exists: `grep "FND-05" .github/workflows/ci.yml`
6. Verify step is in backend-test job: `grep -B 30 "Run Go tests with race detector" .github/workflows/ci.yml | grep "backend-test:"`
7. Verify environment variables are set: `grep -A 8 "Run Go tests with race detector" .github/workflows/ci.yml | grep "TEST_DB_"`

Expected configuration:
- Step name: "Run Go tests with race detector"
- Command: `go test -v -race -timeout=60s ./...`
- Environment: GOMAXPROCS=2, TZ=UTC, TEST_DB_* variables
- Comment: "# FND-05: Race detector..."

If any issues found, fix them in this task.</action>
  <verify>
    <automated>grep -q "go test -v -race -timeout=60s" .github/workflows/ci.yml && grep -q "GOMAXPROCS: 2" .github/workflows/ci.yml && grep -q "FND-05.*Race detector" .github/workflows/ci.yml</automated>
  </verify>
  <done>CI workflow step has correct -race flag, timeout=60s, GOMAXPROCS=2, all required environment variables, FND-05 comment present, step properly positioned in backend-test job</done>
</task>

<task type="auto">
  <name>Task 3: Document race detector usage and expectations</name>
  <files>scripts/test-all.sh</files>
  <read_first>scripts/test-all.sh</read_first>
  <action>Add documentation comment to scripts/test-all.sh explaining race detector usage:

Add the following comment block at the TOP of the file (after shebang):

```bash
# Armored Archer Unified Test Runner
#
# This script runs both Go backend and Godot frontend tests with:
# - Race detector (-race flag) to detect data races in concurrent Go code
# - Shuffle (-shuffle=on flag) to verify test isolation
#
# Race Detector Notes:
# - Slows tests ~10x but catches critical concurrency bugs
# - GOMAXPROCS=2 recommended for better performance
# - Any data race will cause the test to fail with detailed report
#
# Usage:
#   ./scripts/test-all.sh              # Run all tests
#   make test-all                       # Run all tests via Makefile
#
# Exit codes:
#   0 - All tests passed
#   1 - One or more test suites failed
```

This provides context for developers about why tests run with -race flag and what to expect.

Use EXACT text above for the comment block.</action>
  <verify>
    <automated>grep -q "Race Detector Notes:" scripts/test-all.sh && grep -q "Slows tests ~10x" scripts/test-all.sh && grep -q "GOMAXPROCS=2" scripts/test-all.sh</automated>
  </verify>
  <done>scripts/test-all.sh has documentation comment explaining race detector usage, performance impact (10x slower), GOMAXPROCS recommendation, and exit codes</done>
</task>

</tasks>

<verification>
Overall verification:
- `grep "Run Go tests with race detector" .github/workflows/ci.yml` - step exists
- `grep "go test.*-race" .github/workflows/ci.yml` - -race flag present
- `grep "GOMAXPROCS: 2" .github/workflows/ci.yml` - GOMAXPROCS configured
- `grep "FND-05" .github/workflows/ci.yml` - requirement referenced
- `grep "Race Detector Notes:" scripts/test-all.sh` - documentation present
</verification>

<success_criteria>
1. CI workflow runs Go tests with -race flag on every push and PR
2. Race detector failures cause CI to fail with clear error output
3. GOMAXPROCS=2 set for race detector performance
4. 60s timeout configured for slower race detector execution
5. Documentation added to test-all.sh explaining race detector usage
</success_criteria>

<output>
After completion, create `.planning/phases/05-complete-test-infrastructure-foundation/05-03-SUMMARY.md`
</output>
