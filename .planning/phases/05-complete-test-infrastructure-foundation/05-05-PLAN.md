---
phase: 05-complete-test-infrastructure-foundation
plan: 05
type: execute
wave: 2
depends_on: [05-01, 05-02, 05-03, 05-04]
files_modified: [.github/workflows/ci.yml, scripts/test-all.sh, scripts/check-test-pyramid.sh]
autonomous: false
requirements: [FND-03, FND-04, FND-05, FND-06]

must_haves:
  truths:
    - "All test infrastructure components are integrated and working together"
    - "CI runs unified test suite with race detector and shuffle flags"
    - "Test pyramid validation runs in CI and fails on violations"
    - "Developer can run single command locally to replicate CI test execution"
    - "All four requirements (FND-03, FND-04, FND-05, FND-06) are satisfied"
  artifacts:
    - path: ".planning/phases/05-complete-test-infrastructure-foundation/05-05-SUMMARY.md"
      provides: "Final integration summary"
    - path: ".github/workflows/ci.yml"
      provides: "Complete CI workflow with all test infrastructure"
      contains: "race detector.*shuffle.*pyramid"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "scripts/test-all.sh"
      via: "CI uses same commands as local runner"
      pattern: "go test.*-race.*-shuffle"
    - from: ".github/workflows/ci.yml"
      to: "scripts/check-test-pyramid.sh"
      via: "CI validates test pyramid"
      pattern: "check-test-pyramid"
    - from: "scripts/test-all.sh"
      to: "backend/"
      via: "Unified runner executes all tests"
      pattern: "go test.*godot4"
---

<objective>
Integrate all test infrastructure components (unified runner, pyramid validation, race detector, shuffle flag) into a cohesive system with CI integration and final validation.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-complete-test-infrastructure-foundation/05-01-PLAN.md
@.planning/phases/05-complete-test-infrastructure-foundation/05-02-PLAN.md
@.planning/phases/05-complete-test-infrastructure-foundation/05-03-PLAN.md
@.planning/phases/05-complete-test-infrastructure-foundation/05-04-PLAN.md

# From Previous Plans

Plan 05-01: Unified test runner implemented (scripts/test-all.sh)
Plan 05-02: Test pyramid validation implemented (scripts/check-test-pyramid.sh)
Plan 05-03: Race detector enabled in CI (.github/workflows/ci.yml)
Plan 05-04: Shuffle flag enabled in CI and local runner

# Integration Task

This plan integrates all components and adds final CI workflow step for pyramid validation.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add test pyramid validation to CI workflow</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>.github/workflows/ci.yml</read_first>
  <action>Add test pyramid validation step to CI workflow after Go tests:

1. Find the backend-test job in .github/workflows/ci.yml
2. Find the "Run Go tests with race detector and shuffle" step (added in Plan 05-04)
3. ADD THE FOLLOWING NEW STEP AFTER the Go test step:

```yaml
      - name: Validate test pyramid ratios
        run: |
          echo "Validating test pyramid (70% unit, 20% integration, 10% E2E)..."
          ./scripts/check-test-pyramid.sh
```

4. Add a comment above the step:
   ```yaml
      # FND-04: Validate test pyramid ratios (70/20/10 with ±10% tolerance)
   ```

IMPORTANT NOTES:
- Add this step AFTER the Go test step
- The step should fail if pyramid ratios are outside tolerance
- This runs the same validation script as `make check-test-pyramid`

EXACT text to use for the step (including indentation with 6 spaces):
```yaml
      # FND-04: Validate test pyramid ratios (70/20/10 with ±10% tolerance)
      - name: Validate test pyramid ratios
        run: |
          echo "Validating test pyramid (70% unit, 20% integration, 10% E2E)..."
          ./scripts/check-test-pyramid.sh
```</action>
  <verify>
    <automated>grep -q "Validate test pyramid ratios" .github/workflows/ci.yml && grep -q "check-test-pyramid.sh" .github/workflows/ci.yml && grep -q "FND-04.*Validate test pyramid" .github/workflows/ci.yml</automated>
  </verify>
  <done>CI workflow has "Validate test pyramid ratios" step that runs check-test-pyramid.sh script, step positioned after Go tests, comment references FND-04 requirement</done>
</task>

<task type="auto">
  <name>Task 2: Add comprehensive test summary to CI workflow</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>.github/workflows/ci.yml</read_first>
  <action>Add a test summary step that shows overall test results:

1. Find the backend-test job in .github/workflows/ci.yml
2. ADD THE FOLLOWING NEW STEP AFTER the pyramid validation step:

```yaml
      - name: Test Summary
        if: always()
        run: |
          echo "======================================="
          echo "Test Infrastructure Summary"
          echo "======================================="
          echo ""
          echo "✅ Unified test runner: scripts/test-all.sh"
          echo "✅ Race detector enabled: -race flag"
          echo "✅ Test shuffle enabled: -shuffle=on flag"
          echo "✅ Pyramid validation: scripts/check-test-pyramid.sh"
          echo ""
          echo "Requirements satisfied:"
          echo "  FND-03: Unified test runner ✓"
          echo "  FND-04: Test pyramid enforcement ✓"
          echo "  FND-05: Race detector in CI ✓"
          echo "  FND-06: Test isolation via shuffle ✓"
          echo ""
          echo "======================================="
```

This provides clear feedback that all requirements are satisfied.

EXACT text to use for the step (including indentation with 6 spaces):
```yaml
      - name: Test Summary
        if: always()
        run: |
          echo "======================================="
          echo "Test Infrastructure Summary"
          echo "======================================="
          echo ""
          echo "✅ Unified test runner: scripts/test-all.sh"
          echo "✅ Race detector enabled: -race flag"
          echo "✅ Test shuffle enabled: -shuffle=on flag"
          echo "✅ Pyramid validation: scripts/check-test-pyramid.sh"
          echo ""
          echo "Requirements satisfied:"
          echo "  FND-03: Unified test runner ✓"
          echo "  FND-04: Test pyramid enforcement ✓"
          echo "  FND-05: Race detector in CI ✓"
          echo "  FND-06: Test isolation via shuffle ✓"
          echo ""
          echo "======================================="
```</action>
  <verify>
    <automated>grep -q "Test Infrastructure Summary" .github/workflows/ci.yml && grep -q "FND-03: Unified test runner" .github/workflows/ci.yml && grep -q "FND-06: Test isolation" .github/workflows/ci.yml</automated>
  </verify>
  <done>CI workflow has Test Summary step showing all requirements satisfied, step runs with `if: always()` to show summary even if tests fail, displays all four requirements with checkmarks</done>
</task>

<task type="auto">
  <name>Task 3: Create final integration verification script</name>
  <files>scripts/verify-test-infrastructure.sh</files>
  <read_first>scripts/test-all.sh, scripts/check-test-pyramid.sh</read_first>
  <action>Create a verification script that validates all test infrastructure components:

Create scripts/verify-test-infrastructure.sh with:

```bash
#!/bin/bash
set -e

echo "======================================="
echo "🔍 Test Infrastructure Verification"
echo "======================================="
echo ""

# Track overall success
ALL_VALID=1

# Check 1: Unified test runner exists and is executable
echo "Checking unified test runner..."
if [ -x "scripts/test-all.sh" ]; then
    echo "✅ scripts/test-all.sh exists and is executable"
else
    echo "❌ scripts/test-all.sh missing or not executable"
    ALL_VALID=0
fi

# Check 2: Unified runner has race detector flag
echo "Checking race detector flag..."
if grep -q "go test.*-race" scripts/test-all.sh; then
    echo "✅ Race detector flag (-race) present in test-all.sh"
else
    echo "❌ Race detector flag missing from test-all.sh"
    ALL_VALID=0
fi

# Check 3: Unified runner has shuffle flag
echo "Checking shuffle flag..."
if grep -q "go test.*-shuffle=on" scripts/test-all.sh; then
    echo "✅ Shuffle flag (-shuffle=on) present in test-all.sh"
else
    echo "❌ Shuffle flag missing from test-all.sh"
    ALL_VALID=0
fi

# Check 4: Pyramid validation script exists
echo "Checking pyramid validation script..."
if [ -x "scripts/check-test-pyramid.sh" ]; then
    echo "✅ scripts/check-test-pyramid.sh exists and is executable"
else
    echo "❌ scripts/check-test-pyramid.sh missing or not executable"
    ALL_VALID=0
fi

# Check 5: Pyramid validation has correct tolerances
echo "Checking pyramid tolerances..."
if grep -q "UNIT_MIN=60" scripts/check-test-pyramid.sh && \
   grep -q "UNIT_MAX=80" scripts/check-test-pyramid.sh && \
   grep -q "INTEGRATION_MIN=10" scripts/check-test-pyramid.sh && \
   grep -q "INTEGRATION_MAX=30" scripts/check-test-pyramid.sh; then
    echo "✅ Pyramid tolerances correct (70±10%, 20±10%, 10±10%)"
else
    echo "❌ Pyramid tolerances incorrect"
    ALL_VALID=0
fi

# Check 6: CI workflow has race detector
echo "Checking CI workflow for race detector..."
if grep -q "go test.*-race" .github/workflows/ci.yml; then
    echo "✅ CI workflow has race detector flag"
else
    echo "❌ CI workflow missing race detector flag"
    ALL_VALID=0
fi

# Check 7: CI workflow has shuffle flag
echo "Checking CI workflow for shuffle flag..."
if grep -q "go test.*-shuffle=on" .github/workflows/ci.yml; then
    echo "✅ CI workflow has shuffle flag"
else
    echo "❌ CI workflow missing shuffle flag"
    ALL_VALID=0
fi

# Check 8: CI workflow has pyramid validation
echo "Checking CI workflow for pyramid validation..."
if grep -q "check-test-pyramid.sh" .github/workflows/ci.yml; then
    echo "✅ CI workflow has pyramid validation step"
else
    echo "❌ CI workflow missing pyramid validation step"
    ALL_VALID=0
fi

# Check 9: Test classification directories exist
echo "Checking test classification directories..."
if [ -d "backend/tests/unit" ] && \
   [ -d "backend/tests/integration" ] && \
   [ -d "backend/tests/e2e" ] && \
   [ -d "test/suites/integration" ] && \
   [ -d "test/suites/e2e" ]; then
    echo "✅ All test classification directories exist"
else
    echo "❌ Some test classification directories missing"
    ALL_VALID=0
fi

# Summary
echo ""
echo "======================================="
if [ $ALL_VALID -eq 1 ]; then
    echo "✅ All checks passed!"
    echo ""
    echo "Requirements satisfied:"
    echo "  FND-03: Unified test runner ✓"
    echo "  FND-04: Test pyramid enforcement ✓"
    echo "  FND-05: Race detector in CI ✓"
    echo "  FND-06: Test isolation via shuffle ✓"
    echo "======================================="
    exit 0
else
    echo "❌ Some checks failed - see details above"
    echo "======================================="
    exit 1
fi
```

Make file executable with `chmod +x scripts/verify-test-infrastructure.sh`</action>
  <verify>
    <automated>test -f scripts/verify-test-infrastructure.sh && test -x scripts/verify-test-infrastructure.sh && grep -q "FND-03: Unified test runner" scripts/verify-test-infrastructure.sh</automated>
  </verify>
  <done>scripts/verify-test-infrastructure.sh exists, is executable, validates all 9 infrastructure components, shows requirements satisfied summary, exits 0 if all pass, 1 if any fail</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Manual verification of integrated test infrastructure</name>
  <what-built>Complete test infrastructure with unified runner, pyramid validation, race detector, and shuffle flag all integrated in CI and locally</what-built>
  <how-to-verify>
    1. Run verification script: `./scripts/verify-test-infrastructure.sh`
       Expected: All 9 checks pass with ✅ indicators

    2. Run unified test suite: `./scripts/test-all.sh`
       Expected: Go tests run with -race -shuffle flags, Godot tests run, unified report shown

    3. Run pyramid validation: `./scripts/check-test-pyramid.sh`
       Expected: Test counts and percentages shown, validation passes or fails with clear message

    4. Verify CI workflow: `grep -A 5 "Run Go tests" .github/workflows/ci.yml`
       Expected: Shows -race and -shuffle=on flags in test command

    5. Verify Makefile targets: `grep "^test" Makefile | head -5`
       Expected: test-all, check-test-pyramid targets present

    6. (Optional) Push to feature branch to trigger CI and verify workflow runs with all flags
  </how-to-verify>
  <resume-signal>Type "approved" if all verifications pass, or describe issues found</resume-signal>
</task>

</tasks>

<verification>
Overall verification:
- `./scripts/verify-test-infrastructure.sh` - all 9 checks pass
- `./scripts/test-all.sh` - runs complete test suite
- `./scripts/check-test-pyramid.sh` - validates pyramid ratios
- `grep "go test.*-race.*-shuffle" .github/workflows/ci.yml` - CI has both flags
- `make test-all` - works via Makefile target
</verification>

<success_criteria>
1. All four requirements (FND-03, FND-04, FND-05, FND-06) are satisfied
2. Unified test runner executes both Go and Godot tests with consolidated reporting
3. Race detector runs in CI and fails on data races
4. Shuffle flag runs in CI and verifies test isolation
5. Test pyramid validation enforces 70/20/10 ratio
6. Developer can run single command locally to replicate CI execution
7. Verification script confirms all components are properly configured
</success_criteria>

<output>
After completion, create `.planning/phases/05-complete-test-infrastructure-foundation/05-05-SUMMARY.md`
</output>
