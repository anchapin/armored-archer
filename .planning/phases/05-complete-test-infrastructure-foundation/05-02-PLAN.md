---
phase: 05-complete-test-infrastructure-foundation
plan: 02
type: execute
wave: 1
depends_on: [05-00]
files_modified: [scripts/check-test-pyramid.sh, Makefile]
autonomous: true
requirements: [FND-04]

must_haves:
  truths:
    - "Test pyramid validation script counts tests by type (unit/integration/e2e)"
    - "Script validates 70/20/10 ratio with ±10% tolerance"
    - "Script exits with error if pyramid is outside tolerance"
    - "Script displays current percentages and pass/fail status"
  artifacts:
    - path: "scripts/check-test-pyramid.sh"
      provides: "Test pyramid validation script"
      min_lines: 60
      contains: "UNIT_MIN=60.*UNIT_MAX=80"
  key_links:
    - from: "scripts/check-test-pyramid.sh"
      to: "backend/tests/unit/"
      via: "find backend/tests/unit -name *_test.go"
      pattern: "find.*unit.*_test.go"
    - from: "scripts/check-test-pyramid.sh"
      to: "test/suites/"
      via: "find test/suites -name test_*.gd"
      pattern: "find.*suites.*test_.*gd"
    - from: "Makefile"
      to: "scripts/check-test-pyramid.sh"
      via: "make check-test-pyramid target"
      pattern: "check-test-pyramid:"
---

<objective>
Implement test pyramid validation script that enforces 70% unit, 20% integration, 10% E2E ratio with automated checking and CI integration.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-complete-test-infrastructure-foundation/05-00-PLAN.md

# From Wave 0 Stub

From scripts/check-test-pyramid.sh (Plan 05-00):
- Stub structure created with tolerance constants
- Directory find patterns defined
- TODO comments for implementation

# From Research (05-RESEARCH.md)

Test pyramid targets:
- Unit tests: 70% ±10% (60-80%)
- Integration tests: 20% ±10% (10-30%)
- E2E tests: 10% ±10% (0-20%)

Directory structure for classification:
- backend/tests/unit/ - Unit tests (fast, no external deps)
- backend/tests/integration/ - Integration tests (database, network)
- backend/tests/e2e/ - E2E tests (full workflows)
- test/suites/{player,combat,gear}/ - Godot unit tests (by subsystem)
- test/suites/integration/ - Godot integration tests
- test/suites/e2e/ - Godot E2E tests
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement test pyramid validation script</name>
  <files>scripts/check-test-pyramid.sh</files>
  <read_first>scripts/check-test-pyramid.sh</read_first>
  <action>Implement the full test pyramid validation script by replacing the Wave 0 stub with working implementation:

```bash
#!/bin/bash
set -e

echo "======================================="
echo "📊 Test Pyramid Validation"
echo "======================================="
echo ""

# Tolerance for pyramid ratios (±10%)
UNIT_MIN=60
UNIT_MAX=80
INTEGRATION_MIN=10
INTEGRATION_MAX=30
E2E_MAX=20

# Count Go tests by directory
echo "Counting Go backend tests..."
GO_UNIT=$(find backend/tests/unit -name "*_test.go" 2>/dev/null | wc -l)
GO_INTEGRATION=$(find backend/tests/integration -name "*_test.go" 2>/dev/null | wc -l)
GO_E2E=$(find backend/tests/e2e -name "*_test.go" 2>/dev/null | wc -l)

# Count Godot tests by directory
echo "Counting Godot frontend tests..."
GODOT_UNIT=$(find test/suites/player test/suites/combat test/suites/gear test/suites/network test/suites/matchmaking test/suites/season test/suites/store test/suites/campaign test/suites/gem test/suites/transmog test/suites/analytics test/suites/object_pool -name "test_*.gd" 2>/dev/null | wc -l)
GODOT_INTEGRATION=$(find test/suites/integration -name "test_*.gd" 2>/dev/null | wc -l)
GODOT_E2E=$(find test/suites/e2e -name "test_*.gd" 2>/dev/null | wc -l)

# Calculate totals
TOTAL_UNIT=$((GO_UNIT + GODOT_UNIT))
TOTAL_INTEGRATION=$((GO_INTEGRATION + GODOT_INTEGRATION))
TOTAL_E2E=$((GO_E2E + GODOT_E2E))
TOTAL_TESTS=$((TOTAL_UNIT + TOTAL_INTEGRATION + TOTAL_E2E))

if [ $TOTAL_TESTS -eq 0 ]; then
    echo "❌ No tests found!"
    exit 1
fi

# Calculate percentages
UNIT_PCT=$((TOTAL_UNIT * 100 / TOTAL_TESTS))
INTEGRATION_PCT=$((TOTAL_INTEGRATION * 100 / TOTAL_TESTS))
E2E_PCT=$((TOTAL_E2E * 100 / TOTAL_TESTS))

# Display results
echo "Test Distribution:"
echo "  Unit tests:        $TOTAL_UNIT ($UNIT_PCT%)"
echo "  Integration tests: $TOTAL_INTEGRATION ($INTEGRATION_PCT%)"
echo "  E2E tests:         $TOTAL_E2E ($E2E_PCT%)"
echo "  Total:             $TOTAL_TESTS"
echo ""

# Validate ratios
PYRAMID_VALID=1

if [ $UNIT_PCT -lt $UNIT_MIN ] || [ $UNIT_PCT -gt $UNIT_MAX ]; then
    echo "❌ Test pyramid violation: Unit tests $UNIT_PCT% (target: 70% ±10%)"
    PYRAMID_VALID=0
fi

if [ $INTEGRATION_PCT -lt $INTEGRATION_MIN ] || [ $INTEGRATION_PCT -gt $INTEGRATION_MAX ]; then
    echo "❌ Test pyramid violation: Integration tests $INTEGRATION_PCT% (target: 20% ±10%)"
    PYRAMID_VALID=0
fi

if [ $E2E_PCT -gt $E2E_MAX ]; then
    echo "❌ Test pyramid violation: E2E tests $E2E_PCT% (target: 10% ±10%)"
    PYRAMID_VALID=0
fi

if [ $PYRAMID_VALID -eq 1 ]; then
    echo "✅ Test pyramid validated: Unit $UNIT_PCT%, Integration $INTEGRATION_PCT%, E2E $E2E_PCT%"
    echo "======================================="
    exit 0
else
    echo "======================================="
    exit 1
fi
```

Use EXACT code above. Replace the Wave 0 stub with this implementation.</action>
  <verify>
    <automated>test -x scripts/check-test-pyramid.sh && bash -n scripts/check-test-pyramid.sh && grep -q "UNIT_MIN=60" scripts/check-test-pyramid.sh</automated>
  </verify>
  <done>scripts/check-test-pyramid.sh is executable, syntactically valid, has correct tolerance values (60-80%, 10-30%, 0-20%), counts tests by directory, calculates percentages, validates ratios</done>
</task>

<task type="auto">
  <name>Task 2: Add Makefile target for pyramid validation</name>
  <files>Makefile</files>
  <read_first>Makefile</read_first>
  <action>Add check-test-pyramid target to Makefile for convenient execution:

1. Find the test section in Makefile (around line 30-40)
2. Add the following target AFTER the test-all target:

```makefile
check-test-pyramid:
	@echo "$(BLUE)Validating test pyramid ratios...$(RESET)"
	@./scripts/check-test-pyramid.sh
```

3. Add `check-test-pyramid` to the .PHONY line at the top (around line 13)

4. Update the help text to include the new target (around line 30):
   Add: `	@echo "  make check-test-pyramid Validate test pyramid (70/20/10 ratio)"`

The EXACT text for the target is:
```makefile
check-test-pyramid:
	@echo "$(BLUE)Validating test pyramid ratios...$(RESET)"
	@./scripts/check-test-pyramid.sh
```

Use TAB characters (not spaces) for indentation in Makefile.</action>
  <verify>
    <automated>grep -q "^check-test-pyramid:" Makefile && grep -q "check-test-pyramid" Makefile | grep ".PHONY"</automated>
  </verify>
  <done>Makefile has check-test-pyramid target that calls scripts/check-test-pyramid.sh, target listed in .PHONY, help text updated</done>
</task>

<task type="auto">
  <name>Task 3: Test pyramid validation execution</name>
  <files>scripts/check-test-pyramid.sh</files>
  <read_first>scripts/check-test-pyramid.sh</read_first>
  <action>Verify the pyramid validation script works correctly:

1. Test syntax: `bash -n scripts/check-test-pyramid.sh`
2. Verify tolerance constants: `grep "UNIT_MIN=60" scripts/check-test-pyramid.sh`
3. Verify directory find patterns:
   - `grep "find backend/tests/unit" scripts/check-test-pyramid.sh`
   - `grep "find test/suites/integration" scripts/check-test-pyramid.sh`
4. Verify percentage calculation: `grep "UNIT_PCT=" scripts/check-test-pyramid.sh`
5. Verify validation logic: `grep "if \[ \$UNIT_PCT -lt" scripts/check-test-pyramid.sh`
6. Make executable: `chmod +x scripts/check-test-pyramid.sh`
7. Run dry test: `./scripts/check-test-pyramid.sh 2>&1 | head -20` (should show counts and percentages)

If any issues found, fix them in this task.

Expected behavior:
- Script is executable (`chmod +x`)
- Syntax is valid (`bash -n` passes)
- Tolerance values are correct (60-80%, 10-30%, 0-20%)
- Find patterns match directory structure from Wave 0
- Percentages calculate correctly
- Exit code 0 if within tolerance, 1 if outside</action>
  <verify>
    <automated>test -x scripts/check-test-pyramid.sh && bash -n scripts/check-test-pyramid.sh 2>&1 && grep -q "UNIT_MIN=60.*UNIT_MAX=80" scripts/check-test-pyramid.sh</automated>
  </verify>
  <done>Pyramid validation script is executable, syntactically valid, has correct tolerance values, counts tests by directory type, calculates percentages correctly, validates against 70/20/10 ratio</done>
</task>

</tasks>

<verification>
Overall verification:
- `./scripts/check-test-pyramid.sh` executes and shows test distribution
- `make check-test-pyramid` calls the validation script
- Script shows counts and percentages for unit/integration/e2e
- Exit code 0 when within tolerance, 1 when outside
</verification>

<success_criteria>
1. Developer can run `./scripts/check-test-pyramid.sh` or `make check-test-pyramid` to validate pyramid
2. Script counts tests by type (unit/integration/e2e) for both Go and Godot
3. Script calculates and displays percentages
4. Script validates against 70±10% / 20±10% / 10±10% tolerance
5. Exit code 0 if valid, 1 if invalid
</success_criteria>

<output>
After completion, create `.planning/phases/05-complete-test-infrastructure-foundation/05-02-SUMMARY.md`
</output>
