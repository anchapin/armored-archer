---
phase: 05-complete-test-infrastructure-foundation
plan: 00
type: execute
wave: 0
depends_on: []
files_modified: []
autonomous: true
requirements: [FND-03, FND-04, FND-05, FND-06]

must_haves:
  truths:
    - "Developer can run single command that executes both Go and Godot tests"
    - "Test runner generates unified report with pass/fail status for both test suites"
    - "CI enforces test pyramid ratio (70% unit, 20% integration, 10% E2E)"
    - "Go race detector runs in CI with -race flag and fails build on data races"
    - "Tests run with -shuffle=on flag in CI to verify isolation"
  artifacts:
    - path: "scripts/test-all.sh"
      provides: "Unified test runner for Go and Godot tests"
      min_lines: 80
    - path: "scripts/check-test-pyramid.sh"
      provides: "Test pyramid validation script"
      min_lines: 60
    - path: ".github/workflows/ci.yml"
      provides: "CI workflow with race detector and shuffle flags"
      contains: "-race -shuffle=on"
    - path: "backend/tests/unit/"
      provides: "Directory for unit tests (70% target)"
    - path: "backend/tests/integration/"
      provides: "Directory for integration tests (20% target)"
    - path: "backend/tests/e2e/"
      provides: "Directory for E2E tests (10% target)"
    - path: "test/suites/integration/"
      provides: "Directory for Godot integration tests"
    - path: "test/suites/e2e/"
      provides: "Directory for Godot E2E tests"
  key_links:
    - from: "scripts/test-all.sh"
      to: "backend/"
      via: "go test -v -race -shuffle=on ./..."
      pattern: "go test.*-race.*-shuffle"
    - from: "scripts/test-all.sh"
      to: "test/"
      via: "godot4 --headless --script res://test/run_all_tests.gd"
      pattern: "godot4.*--headless"
    - from: ".github/workflows/ci.yml"
      to: "go test"
      via: "CI runs tests with -race and -shuffle flags"
      pattern: "-race.*-shuffle"
---

<objective>
Create Wave 0 test infrastructure stubs for unified test runner, test pyramid validation, and CI workflow updates. This establishes the discoverability and structure before implementation.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-complete-test-infrastructure-foundation/05-RESEARCH.md
@.planning/phases/05-complete-test-infrastructure-foundation/05-VALIDATION.md

# Existing Infrastructure

From .github/workflows/ci.yml:
- CI workflow exists with backend-test job running `npm test`
- No `-race` flag present
- No `-shuffle=on` flag present
- PostgreSQL and Nakama services configured

From scripts/local-godot-tests.sh:
- Local test runner pattern exists for Godot tests
- Can use as reference for unified runner

From Makefile:
- `make backend-test` runs TypeScript tests
- `make backend-test-go` runs Go tests with `go test ./...`
- No unified test-all target

From backend/tests/:
- Integration directory exists with Go tests
- No unit/ or e2e/ subdirectories for test classification

From test/suites/:
- Tests organized by subsystem (player, combat, gear, etc.)
- No integration/ or e2e/ subdirectories for Godot tests
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create unified test runner script stub</name>
  <files>scripts/test-all.sh</files>
  <read_first>scripts/local-godot-tests.sh</read_first>
  <action>Create scripts/test-all.sh with the following structure (stub for Wave 0):

1. Shebang: `#!/bin/bash` with `set -e -o pipefail`
2. Color constants: RED, GREEN, YELLOW, NC for output formatting
3. Header: "Armored Archer Unified Test Suite"
4. Create test-results directory: `mkdir -p test-results`
5. Backend tests section:
   - Change to backend directory
   - Run: `go test -v -race -shuffle=on -timeout=30s ./... 2>&1 | tee ../test-results/backend.txt`
   - Track exit code
6. Frontend tests section:
   - Run: `godot4 --headless --script res://test/run_all_tests.gd 2>&1 | tee test-results/frontend.txt`
   - Track exit code
7. Unified exit with appropriate code and message

Use EXACT commands above. Do not implement the full logic yet - create the stub with TODO comments pointing to Plan 05-01 for implementation.

Make file executable with chmod +x in a comment (not executed in Wave 0).</action>
  <verify>
    <automated>test -f scripts/test-all.sh && grep -q "Armored Archer Unified Test Suite" scripts/test-all.sh && grep -q "go test.*-race.*-shuffle" scripts/test-all.sh</automated>
  </verify>
  <done>scripts/test-all.sh exists with unified test runner structure, backend test command with -race -shuffle flags, frontend test command, and TODO comments for implementation</done>
</task>

<task type="auto">
  <name>Task 2: Create test pyramid validation script stub</name>
  <files>scripts/check-test-pyramid.sh</files>
  <read_first>scripts/test-all.sh</read_first>
  <action>Create scripts/check-test-pyramid.sh with the following structure (stub for Wave 0):

1. Shebang: `#!/bin/bash` with `set -e`
2. Header: "Test Pyramid Validation"
3. Tolerance constants:
   - UNIT_MIN=60, UNIT_MAX=80 (70% ±10%)
   - INTEGRATION_MIN=10, INTEGRATION_MAX=30 (20% ±10%)
   - E2E_MAX=20 (10% ±10%)
4. Count Go tests by directory (use find commands):
   - GO_UNIT: find backend/tests/unit -name "*_test.go"
   - GO_INTEGRATION: find backend/tests/integration -name "*_test.go"
   - GO_E2E: find backend/tests/e2e -name "*_test.go"
5. Count Godot tests by directory:
   - GODOT_UNIT: find test/suites/player test/suites/combat test/suites/gear -name "test_*.gd"
   - GODOT_INTEGRATION: find test/suites/integration -name "test_*.gd"
   - GODOT_E2E: find test/suites/e2e -name "test_*.gd"
6. Calculate totals and percentages
7. Validate ratios and exit with error if outside tolerance
8. Display results with ✅ or ❌ indicators

Use EXACT directory patterns and tolerance values above. Add TODO comments pointing to Plan 05-02 for implementation details.

Make file executable with chmod +x in a comment (not executed in Wave 0).</action>
  <verify>
    <automated>test -f scripts/check-test-pyramid.sh && grep -q "Test Pyramid Validation" scripts/check-test-pyramid.sh && grep -q "UNIT_MIN=60" scripts/check-test-pyramid.sh</automated>
  </verify>
  <done>scripts/check-test-pyramid.sh exists with pyramid validation structure, tolerance constants (70±10%, 20±10%, 10±10%), directory find patterns, and TODO comments for implementation</done>
</task>

<task type="auto">
  <name>Task 3: Create test classification directory structure</name>
  <files>backend/tests/unit/, backend/tests/integration/, backend/tests/e2e/, test/suites/integration/, test/suites/e2e/</files>
  <read_first>backend/tests/integration/db_suite_test.go, test/suites/player/test_player_stats_manager.gd</read_first>
  <action>Create the following directory structure for test classification:

1. Create backend/tests/unit/ directory with .gitkeep marker
2. Create backend/tests/e2e/ directory with .gitkeep marker (integration/ already exists)
3. Create test/suites/integration/ directory with .gitkeep marker
4. Create test/suites/e2e/ directory with .gitkeep marker
5. Add README.md in each directory explaining the classification criteria:
   - backend/tests/unit/README.md: "Unit tests test individual functions and types in isolation. No external dependencies (database, network, file system). Fast execution (<1ms per test)."
   - backend/tests/integration/README.md: "Integration tests test interactions between components. May use testcontainers for database, but test specific integration points. Medium execution time (<100ms per test)."
   - backend/tests/e2e/README.md: "E2E tests test complete workflows from start to finish. Use real services (database, Nakama). Slow execution (>100ms per test). Use sparingly."
   - test/suites/integration/README.md: "Integration tests for Godot that test interactions between autoloads and subsystems."
   - test/suites/e2e/README.md: "E2E tests for Godot that test complete game loops (e.g., full match flow)."

Use EXACT text for README.md content above. This is Wave 0 structure creation - no test migration yet.</action>
  <verify>
    <automated>test -d backend/tests/unit && test -d backend/tests/e2e && test -d test/suites/integration && test -d test/suites/e2e && test -f backend/tests/unit/README.md && test -f backend/tests/e2e/README.md</automated>
  </verify>
  <done>All test classification directories exist with README.md files explaining classification criteria. Directory structure ready for test migration in subsequent plans.</done>
</task>

<task type="auto">
  <name>Task 4: Update CI workflow with race detector and shuffle flags</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>.github/workflows/ci.yml</read_first>
  <action>Modify .github/workflows/ci.yml to add race detector and shuffle flags:

1. Find the backend-test job (around line 120)
2. Find the "Run tests with coverage" step (around line 201)
3. Add the following EXACT changes:

   BEFORE (existing TypeScript test command):
   ```yaml
   run: npm run test:coverage
   ```

   ADD NEW Go test step AFTER (preserve existing TypeScript test):
   ```yaml
   - name: Run Go tests with race detector and shuffle
     working-directory: ./backend
     env:
       NAKAMA_HOST: localhost
       NAKAMA_PORT: 7350
       NAKAMA_SERVER_KEY: defaultkey
       TEST_DB_HOST: localhost
       TEST_DB_PORT: 5432
       TEST_DB_USER: postgres
       TEST_DB_PASSWORD: changeme
       TEST_DB_NAME: nakama
       GOMAXPROCS: 2
       TZ: UTC
     run: |
       echo "Running Go tests with race detector and shuffle..."
       go test -v -race -shuffle=on -timeout=30s ./...
   ```

4. Add a comment above the step: "# FND-05, FND-06: Race detector and shuffle flags for test isolation"

IMPORTANT: Do NOT remove the existing TypeScript test step. The backend has both TypeScript (legacy) and Go (current) tests. We're adding Go test execution with race/shuffle flags.</action>
  <verify>
    <automated>grep -q "go test -v -race -shuffle=on" .github/workflows/ci.yml && grep -q "FND-05, FND-06" .github/workflows/ci.yml</automated>
  </verify>
  <done>CI workflow updated to run Go tests with -race and -shuffle=on flags. Race detector enabled for data race detection. Shuffle flag enabled for test isolation verification.</done>
</task>

</tasks>

<verification>
Wave 0 verification commands:
- `test -f scripts/test-all.sh && grep -q "go test.*-race.*-shuffle" scripts/test-all.sh`
- `test -f scripts/check-test-pyramid.sh && grep -q "UNIT_MIN=60" scripts/check-test-pyramid.sh`
- `test -d backend/tests/unit && test -d backend/tests/e2e && test -d test/suites/integration && test -d test/suites/e2e`
- `grep -q "go test -v -race -shuffle=on" .github/workflows/ci.yml`
</verification>

<success_criteria>
1. scripts/test-all.sh exists with unified test runner structure
2. scripts/check-test-pyramid.sh exists with pyramid validation structure
3. Test classification directories exist (unit/, integration/, e2e/) with README.md files
4. CI workflow updated with -race and -shuffle=on flags for Go tests
5. All stubs have TODO comments pointing to implementation plans
6. Wave 0 complete, enabling Plans 05-01 through 05-04 to implement against these stubs
</success_criteria>

<output>
After completion, create `.planning/phases/05-complete-test-infrastructure-foundation/05-00-SUMMARY.md`
</output>
