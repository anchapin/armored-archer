---
phase: 26-verify-cicd-enforcement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [.github/workflows/coverage.yml, backend/tests/quality/coverage_gates.sh]
autonomous: true
requirements: [COV-08]
must_haves:
  truths:
    - "coverage_gates.sh returns exit code 0 when coverage meets threshold"
    - "coverage_gates.sh returns exit code 1 when overall coverage is below threshold"
    - "coverage_gates.sh returns exit code 2 when critical package coverage is below 80%"
  artifacts:
    - path: ".github/workflows/coverage.yml"
      provides: "CI workflow with Stage 3 default"
    - path: "backend/tests/quality/coverage_gates.sh"
      provides: "Correct threshold enforcement logic"
  key_links:
    - from: ".github/workflows/coverage.yml"
      to: "backend/tests/quality/coverage_gates.sh"
      via: "GitHub Actions step"
---

<objective>
Verify that the Stage 3 coverage gate (60%) correctly enforces thresholds and returns appropriate exit codes for different failure scenarios.
</objective>

<execution_context>
@/home/alex/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/research/PHASE-26-RESEARCH.md
@backend/tests/quality/coverage_gates.sh
@.github/workflows/coverage.yml
@data/coverage-thresholds.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean up coverage workflow duplication</name>
  <files>.github/workflows/coverage.yml</files>
  <action>
    Remove the duplicate "Extract coverage metrics" step in the `backend-coverage` job.
    Ensure the "Enforce coverage gates" step uses the Stage 3 default when no input is provided (which is handled by the script itself).
  </action>
  <verify>
    grep -c "Extract coverage metrics" .github/workflows/coverage.yml | grep "^1$"
  </verify>
  <done>Workflow cleaned of duplication, single extraction step remains.</done>
</task>

<task type="auto">
  <name>Task 2: Verify Stage 3 Pass Case (73.3% > 60%)</name>
  <files>backend/tests/quality/coverage_gates.sh</files>
  <action>
    Run the coverage gate script with Stage 3 explicitly and implicitly.
    Current coverage is 73.3%, so it should pass.
  </action>
  <verify>
    cd backend && COVERAGE_GATE_STAGE=3 bash tests/quality/coverage_gates.sh && echo "PASS: Explicit Stage 3" && bash tests/quality/coverage_gates.sh && echo "PASS: Implicit Default (Stage 3)"
  </verify>
  <done>Script returns exit code 0 for current coverage against 60% threshold.</done>
</task>

<task type="auto">
  <name>Task 3: Verify Overall Coverage Failure (Exit Code 1)</name>
  <files>backend/tests/quality/coverage_gates.sh</files>
  <action>
    Temporarily mock the Stage 3 threshold in data/coverage-thresholds.json to 80.0% (higher than current 73.3%).
    Run the script and verify it returns exit code 1.
    Restore the original threshold (60.0%) afterwards.
  </action>
  <verify>
    sed -i 's/"3": 60.0/"3": 80.0/' data/coverage-thresholds.json && (cd backend && ! bash tests/quality/coverage_gates.sh); EXIT_CODE=$?; sed -i 's/"3": 80.0/"3": 60.0/' data/coverage-thresholds.json; [ $EXIT_CODE -eq 1 ] && echo "PASS: Exit Code 1 for overall failure"
  </verify>
  <done>Script returns exit code 1 when overall coverage is below the stage threshold.</done>
</task>

<task type="auto">
  <name>Task 4: Verify Critical Package Failure (Exit Code 2)</name>
  <files>backend/tests/quality/coverage_gates.sh</files>
  <action>
    Temporarily mock the critical threshold in data/coverage-thresholds.json to 99.0% (higher than current critical package coverage).
    Run the script and verify it returns exit code 2.
    Restore the original threshold (80.0%) afterwards.
  </action>
  <verify>
    sed -i 's/"critical": 80.0/"critical": 99.0/' data/coverage-thresholds.json && (cd backend && ! bash tests/quality/coverage_gates.sh); EXIT_CODE=$?; sed -i 's/"critical": 99.0/"critical": 80.0/' data/coverage-thresholds.json; [ $EXIT_CODE -eq 2 ] && echo "PASS: Exit Code 2 for critical package failure"
  </verify>
  <done>Script returns exit code 2 when a critical package is below the critical threshold.</done>
</task>

</tasks>

<success_criteria>
1. `coverage_gates.sh` passes (exit 0) with Stage 3 (60%) on current coverage.
2. `coverage_gates.sh` fails with exit 1 for overall coverage regression.
3. `coverage_gates.sh` fails with exit 2 for critical package regression.
4. `.github/workflows/coverage.yml` duplication removed.
</success_criteria>

<output>
After completion, create `.planning/phases/26-verify-cicd-enforcement/26-01-SUMMARY.md`
</output>
