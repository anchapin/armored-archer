---
phase: 26-verify-cicd-enforcement
plan: 02
type: execute
wave: 2
depends_on: [26-01]
files_modified: [BRANCH_PROTECTION.md]
autonomous: true
requirements: [COV-08]
must_haves:
  truths:
    - "coverage.yml workflow correctly collectors results and fails if coverage job fails"
    - "BRANCH_PROTECTION.md documents the required status check: coverage-gate"
  artifacts:
    - path: ".planning/phases/26-verify-cicd-enforcement/VERIFICATION.md"
      provides: "Phase 26 verification evidence"
  key_links:
    - from: "coverage.yml"
      to: "GitHub PR Settings"
      via: "required status check name"
---

<objective>
Verify that the CI workflow correctly collectors results and would block a PR if the coverage gate fails. Create Phase 26 VERIFICATION.md.
</objective>

<execution_context>
@/home/alex/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/26-verify-cicd-enforcement/26-01-SUMMARY.md
@.github/workflows/coverage.yml
@BRANCH_PROTECTION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify CI Collector Logic</name>
  <files>.github/workflows/coverage.yml</files>
  <action>
    Examine the `coverage-gate` job in `.github/workflows/coverage.yml`.
    Confirm it depends on `backend-coverage` and `godot-coverage`.
    Confirm it fails if either dependency fails.
    Since we cannot run a real PR, verify the logic via script analysis or mock simulation if possible.
  </action>
  <verify>
    grep -A 20 "coverage-gate:" .github/workflows/coverage.yml | grep "needs: \[backend-coverage, godot-coverage\]" && grep -A 10 "if: always()" .github/workflows/coverage.yml | grep -A 10 "BACKEND_RESULT="
  </verify>
  <done>Collector job logic verified to fail on dependency failure.</done>
</task>

<task type="auto">
  <name>Task 2: Verify Branch Protection Documentation</name>
  <files>BRANCH_PROTECTION.md</files>
  <action>
    Ensure `BRANCH_PROTECTION.md` explicitly mentions that the `coverage-gate` job is a required status check for merging to `main`.
    This provides clear guidance to users on what blocks their PR.
  </action>
  <verify>
    grep "coverage-gate" BRANCH_PROTECTION.md
  </verify>
  <done>Required status check explicitly documented.</done>
</task>

<task type="auto">
  <name>Task 3: Create VERIFICATION.md</name>
  <files>.planning/phases/26-verify-cicd-enforcement/VERIFICATION.md</files>
  <action>
    Create a comprehensive VERIFICATION.md for Phase 26 documenting:
    - Stage 3 gate pass/fail results (from Plan 01)
    - Exit codes for different failure modes
    - CI collector logic verification
    - Branch protection documentation verification
  </action>
  <verify>
    ls .planning/phases/26-verify-cicd-enforcement/VERIFICATION.md
  </verify>
  <done>VERIFICATION.md created and populated.</done>
</task>

</tasks>

<success_criteria>
1. `coverage-gate` collector job logic verified.
2. `BRANCH_PROTECTION.md` updated with specific required check.
3. `VERIFICATION.md` created with all test results.
</success_criteria>

<output>
After completion, create `.planning/phases/26-verify-cicd-enforcement/26-02-SUMMARY.md`
</output>
