# Phase 4: PvP Balance & Ranking - Plan

## Overview
This phase addresses PvP balance and ranking systems to ensure fair competitive gameplay with appropriate weapon power curves, skill-based matchmaking, and seasonal leaderboards with decay mechanisms.

## Requirements
- PVP-01: Weapon/Power Balancing
- PVP-02: Matchmaking Fairness
- PVP-03: Ranking System

## Success Criteria
1. Weapon damage curves are balanced across all tiers to prevent one-shot exploits and ensure no single weapon dominates meta
2. Matchmaking pairs players with similar skill levels, accounting for ranking differences to create balanced competitive matches
3. Seasonal leaderboards track player progress with decay mechanisms that prevent farming and encourage active participation

## Tasks

<task>
<id>01</id>
<name>Weapon Balance Manager</name>
<requirement>PVP-01</requirement>

<description>
Create a centralized weapon balance system to manage damage curves, power tiers, and balance adjustments.

Key Features:
- Weapon tier definitions with base damage multipliers
- Damage curve calculations with diminishing returns for high-power weapons
- Balance adjustment system for hotfixes without code deployment
- PvP-specific damage modifiers separate from PvE
- Weapon power rating calculation for matchmaking

Implementation:
- Create WeaponBalanceManager autoload
- Define weapon tiers (common, rare, epic, legendary)
- Implement damage curve formulas with power scaling
- Add PvP damage reduction coefficient
- Create admin RPC for balance adjustments
</description>

<files_to_create>
autoloads/WeaponBalanceManager.gd
backend/src/modules/weapon_balance.ts
</files_to_create>

<files_modified>
backend/src/modules/rpc_handlers.ts
backend/src/data/migrations/001_weapon_balance.sql
</files_modified>

<action_details>
1. Create WeaponBalanceManager.gd:
   - weapon_tiers enum with power multipliers
   - get_pvp_damage(base_damage, tier) returns PvP-optimized damage
   - get_weapon_power_rating(weapon_data) calculates matchmaker power
   - apply_balance_adjustment(weapon_id, multiplier) for hotfixes

2. Create backend weapon_balance.ts:
   - Validate weapon power on registration
   - Track weapon usage statistics for balance tuning
   - Expose admin RPC for balance adjustments
   - Log balance changes for audit trail

3. Update combat logic:
   - Use WeaponBalanceManager for PvP damage calculations
   - Apply PvP-specific modifiers
   - Report balance metrics for monitoring

4. Create database migration:
   - Add weapon_balance_adjustments table
   - Track balance change history
</action_details>

<verification_criteria>
- WeaponBalanceManager loaded as autoload
- PvP damage differs from PvE damage for same weapon
- No weapon exceeds 200% of tier's average damage
- Admin balance adjustment RPC functional
- Balance metrics logged to database
</verification_criteria>
</task>

<task>
<id>02</id>
<name>Skill Rating System</name>
<requirement>PVP-02</requirement>

<description>
Implement player skill rating (ELO-style) system to track and display player competitive performance.

Key Features:
- ELO-based rating calculation with win/loss adjustments
- Rating displayed in PvP menus
- Rating history tracking for progression
- Confidence interval for new players
- Separate ratings for 1v1 and 2v2 modes

Implementation:
- Create PlayerRatingManager autoload (Godot)
- Create backend player_rating module
- Implement ELO formula: R_new = R_old + K * (S - E)
- K-factor varies by player experience (new players have higher K)
- Store rating in player storage with history
- Display rating in PvP UI
</description>

<files_to_create>
autoloads/PlayerRatingManager.gd
backend/src/modules/player_rating.ts
backend/src/data/migrations/002_player_rating.sql
</files_to_create>

<files_modified>
backend/src/modules/rpc_handlers.ts
scenes/pvp/matchmaking_menu.gd
</files_modified>

<action_details>
1. Create PlayerRatingManager.gd:
   - get_current_rating() retrieves local rating
   - update_rating(opponent_rating, is_win, mode) calculates ELO change
   - get_rating_history() returns progression data
   - get_confidence_interval() for new player uncertainty

2. Create backend player_rating.ts:
   - validate_rating_change() prevents rating manipulation
   - apply_rating_adjustment() updates database
   - get_leaderboard_snapshot() for top players
   - track_match_outcome() for statistics

3. Create database migration:
   - Add player_ratings table
   - Add rating_history table
   - Index on player_id and timestamp

4. Update matchmaking menu:
   - Display player rating
   - Show rating change from previous session
   - Visual indication of rating bracket
</action_details>

<verification_criteria>
- PlayerRatingManager functional on client
- Rating updates after each PvP match
- ELO formula produces reasonable adjustments (±25 for close matches)
- New players gain/lose more points than established players
- Rating displayed in PvP UI
</verification_criteria>
</task>

<task>
<id>03</id>
<name>Matchmaking Pool Manager</name>
<requirement>PVP-02</requirement>

<description>
Create balanced matchmaking pool system that pairs players with similar skill ratings.

Key Features:
- Rating-based pool matching (±100 rating bracket)
- Wait time expansion (widen bracket over time)
- Separate pools for 1v1 and 2v2
- Player availability tracking
- Queue position display

Implementation:
- Create MatchmakingPoolManager autoload (Godot)
- Update backend matchmaking logic
- Implement pool matching algorithm
- Add wait time tracking
- Create queue status UI
</description>

<files_to_create>
autoloads/MatchmakingPoolManager.gd
backend/src/modules/matchmaking_pool.ts
</files_to_create>

<files_modified>
backend/src/modules/matchmaking.ts
scenes/pvp/matchmaking_queue.gd
</files_modified>

<action_details>
1. Create MatchmakingPoolManager.gd:
   - join_queue(mode, rating) enters player pool
   - leave_queue() removes from pool
   - get_queue_position() returns wait estimate
   - get_queue_status() for UI updates

2. Update backend matchmaking_pool.ts:
   - find_match(player) uses rating bracket matching
   - expand_search_over_time() widens bracket gradually
   - prioritize_close_rating() for better matches
   - validate_match_compatibility() checks conditions

3. Update queue UI:
   - Display queue position
   - Show estimated wait time
   - Cancel queue button

Matching Algorithm:
- Initial bracket: ±100 rating
- After 30s: ±200 rating
- After 60s: ±300 rating
- After 90s: Any rating available
</action_details>

<verification_criteria>
- Players matched within ±100 rating when available
- Wait time expansion widens bracket correctly
- Queue position accurate
- Separate pools for 1v1 and 2v2
- No matches exceed ±300 rating difference after 90s wait
</verification_criteria>
</task>

<task>
<id>04</id>
<name>Seasonal Ranking Leaderboards</name>
<requirement>PVP-03</requirement>

<description>
Implement seasonal leaderboards with rating decay to prevent farming and encourage active play.

Key Features:
- Season duration (30 days) with reset
- Rating decay for inactive players (1% per day inactive)
- Top 100 leaderboards displayed
- Season rewards for top performers
- Historical season records

Implementation:
- Create SeasonManager updates for PvP seasons
- Create backend season_leaderboard module
- Implement rating decay calculation
- Create leaderboard UI scene
- Add season transition system
</description>

<files_to_create>
backend/src/modules/season_leaderboard.ts
backend/src/data/migrations/003_season_tables.sql
scenes/pvp/leaderboard_ui.tscn
scenes/pvp/leaderboard_ui.gd
</files_to_create>

<files_modified>
autoloads/SeasonManager.gd
backend/src/modules/rpc_handlers.ts
scenes/pvp/pvp_menu.gd
</files_modified>

<action_details>
1. Update SeasonManager.gd:
   - get_current_season() returns active season
   - get_season_end_time() for countdown
   - apply_rating_decay() calculates decay based on inactivity

2. Create backend season_leaderboard.ts:
   - get_top_players(season_id, limit) for leaderboard
   - apply_daily_decay() runs nightly job
   - record_season_completion() archives season data
   - get_player_rank(season_id, player_id) for position

3. Create leaderboard UI:
   - Top 100 players list
   - Show rating, wins, losses
   - Display season countdown
   - Filter by mode (1v1/2v2)

4. Create database migration:
   - Add seasons table
   - Add season_rankings table
   - Add season_history table

Rating Decay Formula:
- Inactive > 7 days: 1% decay per day
- Inactive > 30 days: 2% decay per day
- Minimum rating: 1000 floor (cannot decay below)
</action_details>

<verification_criteria>
- Leaderboard displays top 100 players
- Rating decays for inactive players
- Season resets after 30 days
- Historical seasons accessible
- Top players receive season completion rewards
</verification_criteria>
</task>

<task>
<id>05</id>
<name>Matchmaking Statistics & Analytics</name>
<requirement>PVP-02</requirement>

<description>
Implement analytics for matchmaking quality monitoring and balance tuning.

Key Features:
- Match quality tracking (rating difference distribution)
- Weapon usage statistics for balance adjustments
- Win rate analysis by weapon tier
- Match completion rates
- Abandoned match tracking

Implementation:
- Create MatchmakingAnalyticsManager autoload
- Create backend analytics module
- Dashboard for monitoring
- Automated alerts for balance issues
</description>

<files_to_create>
autoloads/MatchmakingAnalyticsManager.gd
backend/src/modules/matchmaking_analytics.ts
</files_to_create>

<files_modified>
backend/src/modules/rpc_handlers.ts
</files_modified>

<action_details>
1. Create MatchmakingAnalyticsManager.gd:
   - log_match_data(rating_diff, weapons, duration) for analytics
   - get_match_quality_metrics() for dashboard
   - track_abandonment() for incomplete matches

2. Create backend matchmaking_analytics.ts:
   - aggregate_match_metrics() generates quality reports
   - detect_balance_issues() flags outliers
   - generate_weapon_stats() for balance tuning
   - export_analytics_report() for admin review

Metrics Tracked:
- Average rating difference per match (target: < 100)
- Match completion rate (target: > 90%)
- Weapon win rates by tier (target: 45-55% variance)
- Queue times by rating bracket (target: < 60s median)
- Abandonment rate (target: < 5%)
</action_details>

<verification_criteria>
- Match metrics collected and stored
- Analytics dashboard accessible
- Balance issues flagged automatically
- Weapon statistics accurate
- Reports exportable
</verification_criteria>
</task>

<task>
<id>06</id>
<name>PvP Balance Integration Tests</name>
<requirement>PVP-01, PVP-02, PVP-03</requirement>

<description>
Create comprehensive integration tests for PvP balance and ranking systems.

Test Coverage:
- Weapon balance calculations
- ELO rating updates
- Matchmaking pool matching
- Rating decay application
- Leaderboard accuracy
- Analytics collection

Implementation:
- Create test files for each manager
- Add PvP scenario tests
- Mock backend for rating tests
- Database migration tests
</description>

<files_to_create>
test/test_weapon_balance_manager.gd
test/test_player_rating_manager.gd
test/test_matchmaking_pool_manager.gd
test/test_season_leaderboard.gd
test/test_matchmaking_analytics.gd
backend/src/modules/__tests__/weapon_balance.test.ts
backend/src/modules/__tests__/player_rating.test.ts
backend/src/modules/__tests__/matchmaking_pool.test.ts
backend/src/modules/__tests__/season_leaderboard.test.ts
backend/src/modules/__tests__/matchmaking_analytics.test.ts
</files_to_create>

<files_modified>
test/run_all_tests.gd
</files_modified>

<action_details>
1. Create test_weapon_balance_manager.gd:
   - test_pvp_damage_different_from_pve()
   - test_damage_curve_diminishing_returns()
   - test_no_weapon_exceeds_tier_average()
   - test_balance_adjustment_applies()
   - test_weapon_power_rating_calculation()

2. Create test_player_rating_manager.gd:
   - test_elo_formula_correct()
   - test_rating_update_after_win()
   - test_rating_update_after_loss()
   - test_new_player_k_factor_higher()
   - test_confidence_interval_calculation()

3. Create test_matchmaking_pool_manager.gd:
   - test_initial_rating_bracket_100()
   - test_bracket_expansion_over_time()
   - test_separate_1v1_and_2v2_pools()
   - test_queue_position_accuracy()

4. Create test_season_leaderboard.gd:
   - test_leaderboard_top_100()
   - test_rating_decay_applied()
   - test_season_resets_after_30_days()
   - test_minimum_rating_floor()

5. Create backend tests:
   - Balance calculation tests
   - ELO formula validation
   - Pool matching algorithm tests
   - Decay calculation tests
   - Analytics aggregation tests
</action_details>

<verification_criteria>
- All tests pass with godot --headless --script
- Test coverage > 80% for all PvP modules
- ELO formula validated with test cases
- Matchmaking brackets tested with edge cases
- Rating decay tests include floor condition
- Analytics tests mock database correctly
</verification_criteria>
</task>
</tasks>
