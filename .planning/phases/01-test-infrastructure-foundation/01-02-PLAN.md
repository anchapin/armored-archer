---
phase: 01-test-infrastructure-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - addons/gut/
  - .gutconfig.json
  - test/run_all_tests.gd
  - test/suites/
autonomous: true
requirements:
  - FND-02
must_haves:
  truths:
    - "Developer can run Godot tests with GUT framework"
    - "GUT provides comprehensive assertions and test organization"
    - "Tests are organized into suites with proper structure"
  artifacts:
    - path: "addons/gut/"
      provides: "GUT testing framework for Godot 4"
      contains: "gut.gd, gut_plugin.gd"
    - path: ".gutconfig.json"
      provides: "GUT configuration for CI integration"
      exports: ["json format", "test directories"]
    - path: "test/suites/"
      provides: "Organized test suites structure"
      contains: "player/, combat/, gear/"
  key_links:
    - from: "test/suites/**/*.gd"
      to: "addons/gut/gut.gd"
      via: "extends GutTest"
      pattern: "extends GutTest"
---

<objective>
Install and configure GUT (Godot Unit Test) framework for enhanced Godot testing with comprehensive assertions, test doubles, and CI integration.

Purpose: The current custom test framework (test_framework.gd) is basic. GUT 9.5.0 provides mature testing capabilities including assertions, mocking, signal testing, and JUnit XML output for CI reporting.

Output: GUT framework installed, configured, and demonstrated with example test suites
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-test-infrastructure-foundation/01-RESEARCH.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

# Current Test Framework
From test/test_framework.gd (existing):
- Basic assertion helpers (assert_true, assert_eq, assert_null, etc.)
- Simple test runner with pass/fail tracking
- No test doubles, no CI integration, no signal testing

# GUT Framework (from Research)
```gdscript
# GUT test structure
extends GutTest

var _player: PlayerStatsManager

func before_each():
    _player = PlayerStatsManager.new()
    add_child_autofree(_player)

func after_each():
    _player = null

func test_level_starts_at_one():
    assert_eq(_player.level, 1, "New player should start at level 1")

func test_add_xp_increases_xp():
    _player.add_xp(100)
    assert_eq(_player.xp, 100, "XP should increase by amount added")
```

# Installation
Download GUT 9.5.0 for Godot 4.5+ from:
https://github.com/bitwes/Gut/releases
Extract addons/gut to project root
Enable GUT plugin in Project Settings > Plugins
</context>

<tasks>

<task type="auto">
  <name>Task 1: Download and install GUT framework</name>
  <files>addons/gut/, project.godot</files>
  <action>
Install GUT 9.5.0 for Godot 4.x:

1. Download GUT 9.5.0 from https://github.com/bitwes/Gut/releases
2. Extract to addons/gut/ directory in project root
3. Enable GUT plugin in project.godot:
   - Add [plugin] section for GUT
   - Set enabled=true
4. Verify installation: Check that GUT appears in Project Settings > Plugins
5. Add .gutconfig.json configuration file with:
   - Directory structure: test/suites/
   - Output format: JUnit XML for CI
   - Script timeout: 60 seconds
   - Double strategy: INCLUDE_INTERNAL

DO NOT:
- Don't use Godot 3.x version of GUT — must be 9.5.0 for Godot 4.x
- Don't modify GUT source code — use as-is
- Don't enable Godot 3.x compatibility mode
  </action>
  <verify>
<automated>godot4 --headless --script -c "print('GUT installed: ', ClassDB.class_exists('Gut'))"</automated>
  </verify>
  <done>GUT addon installed; plugin enabled in project.godot; .gutconfig.json created</done>
</task>

<task type="auto">
  <name>Task 2: Create test suite structure</name>
  <files>test/suites/, test/suites/player/, test/suites/combat/, test/suites/gear/</files>
  <action>
Create organized test suite directories:

1. Create test/suites/ directory structure:
   - test/suites/player/ — PlayerStatsManager tests
   - test/suites/combat/ — CombatManager tests
   - test/suites/gear/ — GearManager/GearRegistry tests
   - test/suites/network/ — NetworkManager tests
2. Move existing test files into appropriate suites:
   - test/test_player_stats_manager.gd → test/suites/player/test_player_stats_manager.gd
   - test/test_combat_manager.gd → test/suites/combat/test_combat_manager.gd
   - test/test_gear_manager.gd → test/suites/gear/test_gear_manager.gd
   - (Move others to appropriate directories)
3. Create README.md in test/suites/ explaining organization
4. Update .gutconfig.json to point to new directories

DO NOT:
- Don't delete old test files until migration is verified
- Don't modify test logic yet — just reorganize
- Don't create empty directories without tests
  </action>
  <verify>
<automated>ls -la test/suites/*/test_*.gd | wc -l</automated>
  </verify>
  <done>Suites organized by subsystem; old tests moved; README documentation created</done>
</task>

<task type="auto">
  <name>Task 3: Migrate sample tests to GUT pattern</name>
  <files>test/suites/player/test_player_stats_manager.gd, test/suites/combat/test_combat_manager.gd</files>
  <action>
Migrate 2-3 test files to GUT pattern:

1. Select player_stats_manager and combat_manager tests as samples
2. Refactor each test to extend GutTest instead of custom framework
3. Use GUT lifecycle methods:
   - before_each() — setup before each test
   - after_each() — cleanup after each test
4. Use GUT assertions:
   - assert_eq(actual, expected, message)
   - assert_ne(actual, expected, message)
   - assert_true(condition, message)
   - assert_null(value, message)
5. Use add_child_autofree() for automatic cleanup
6. Add inner classes for related test groups

DO NOT:
- Don't migrate all tests — just create examples
- Don't change test logic — only framework syntax
- Don't use GUT's advanced features (doubles, signals) yet — that's Phase 3
  </action>
  <verify>
<automated>godot4 --headless --script res://test/run_all_tests.gd 2>&1 | grep -E "(PASS|FAIL)"</automated>
  </verify>
  <done>Sample tests use GUT pattern; tests pass; patterns documented for migration</done>
</task>

<task type="auto">
  <name>Task 4: Update test runner for GUT</name>
  <files>test/run_all_tests.gd, .github/workflows/test.yml</files>
  <action>
Update test runner to execute GUT tests:

1. Modify test/run_all_tests.gd to use GUT's test runner:
   - Load GUT plugin
   - Configure test directories from .gutconfig.json
   - Run tests and capture results
   - Output JUnit XML for CI
2. Add command-line flag support:
   - `--help` — show usage
   - `--verbose` — detailed output
   - `--junit` — output JUnit XML
3. Update GitHub Actions workflow to use new runner:
   - Parse JUnit output for test results
   - Upload test artifacts
   - Fail build on test failures

DO NOT:
- Don't break existing test execution during migration
- Don't remove compatibility with custom framework tests until all migrated
- Don't hardcode test paths — use .gutconfig.json
  </action>
  <verify>
<automated>godot4 --headless --script res://test/run_all_tests.gd --help</automated>
  </verify>
  <done>Test runner uses GUT; JUnit output works; CI workflow updated</done>
</task>

</tasks>

<verification>
1. GUT addon is installed and enabled
2. Test suites are organized by subsystem
3. Sample tests demonstrate GUT patterns (before_each, assertions, autofree)
4. Test runner executes GUT tests with JUnit output
5. CI workflow parses GUT test results
</verification>

<success_criteria>
- Developer can run GUT tests with `godot4 --headless --script res://test/run_all_tests.gd`
- Tests are organized into suites by subsystem
- GUT provides comprehensive assertions and test lifecycle hooks
- JUnit XML output enables CI integration
</success_criteria>

<output>
After completion, create `.planning/phases/01-test-infrastructure-foundation/01-02-SUMMARY.md`
</output>
