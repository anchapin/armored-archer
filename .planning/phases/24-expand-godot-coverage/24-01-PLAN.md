---
phase: 24-expand-godot-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - autoloads/GameManager.gd
  - test/suites/autoloads/test_game_manager_coverage.gd
autonomous: true
requirements:
  - GODOT-03
  - GODOT-04

must_haves:
  truths:
    - "CoverageTracker.track_execution() called in GameManager's key functions"
    - "GameManager coverage data appears in coverage.json after test runs"
    - "HTML report shows GameManager line-by-line coverage with highlighting"
    - "Game state changes (health, win/lose, boss spawn) are tracked"
  artifacts:
    - path: "autoloads/GameManager.gd"
      provides: "Game state management with coverage tracking"
      min_lines: 200
      contains: "CoverageTracker.track_execution("
    - path: "test/suites/autoloads/test_game_manager_coverage.gd"
      provides: "GameManager coverage tests with tracking verification"
      exports: ["test_health_damage_tracks_coverage", "test_player_death_tracks_coverage", "test_game_won_tracks_coverage", "test_boss_spawn_tracks_coverage"]
  key_links:
    - from: "test/suites/autoloads/test_game_manager_coverage.gd"
      to: "autoloads/GameManager.gd"
      via: "CoverageTracker.track_execution() calls in GameManager functions"
      pattern: "CoverageTracker\\.get_instance\\(\\)\\.track_execution\\(.*GameManager\\.gd"
    - from: "addons/gut/coverage/coverage_post_run.gd"
      to: "test/coverage/json/coverage.json"
      via: "CoverageExporter.export_coverage_json() exports GameManager data"
      pattern: "coverage.json.*GameManager"

---

<objective>
Instrument GameManager autoload with CoverageTracker.track_execution() calls to track line execution during test runs, enabling real line coverage measurement for game state management.

Purpose: GameManager is a core autoload managing player health, win/lose conditions, boss spawning, and game flow. Adding coverage tracking provides visibility into which game state paths are tested.

Output: GameManager with 15-20 strategic track_execution() calls, comprehensive coverage tests verifying tracking works, coverage.json populated with GameManager data, HTML report showing line-by-line coverage with green/red highlighting.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-expand-godot-coverage/24-CONTEXT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 13 Summary Reference
@.planning/phases/13-godot-coverage-tools/13-01-SUMMARY.md

# Existing Coverage Infrastructure
@addons/gut/coverage/coverage_tracker.gd
@test/suites/autoloads/test_combat_manager_coverage.gd

# Pattern Reference: Manual Instrumentation
From test_combat_manager_coverage.gd:
```gdscript
# Track function entry
CoverageTracker.get_instance().track_execution("res://autoloads/CombatManager.gd", 170)

# Track branch conditions
CoverageTracker.get_instance().track_execution("res://autoloads/CombatManager.gd", 176)

# Track return statements
CoverageTracker.get_instance().track_execution("res://autoloads/CombatManager.gd", 193)
```

# CoverageTracker Interface
From coverage_tracker.gd:
```gdscript
static func get_instance() -> CoverageTracker
func track_execution(script_path: String, line: int)
func get_coverage_data() -> Dictionary
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CoverageTracker instrumentation to GameManager</name>
  <files>autoloads/GameManager.gd</files>
  <read_first>
    - autoloads/GameManager.gd (to see current implementation)
    - addons/gut/coverage/coverage_tracker.gd (to verify CoverageTracker API)
    - test/suites/autoloads/test_combat_manager_coverage.gd (to reference instrumentation pattern)
  </read_first>
  <behavior>
    Test 1: track_execution() called at take_player_damage() entry (line ~43: func take_player_damage(damage: int))
    Test 2: track_execution() called at player death branch (line ~66: if player_current_health <= 0)
    Test 3: track_execution() called at heal_player() entry (line ~69: func heal_player(amount: int))
    Test 4: track_execution() called at start_game() entry (line ~92: func start_game())
    Test 5: track_execution() called at game_active state check (line ~94: is_game_active = true)
    Test 6: track_execution() called at health signal emit (line ~97: health_changed.emit())
    Test 7: track_execution() called at boss_spawned signal emit (boss spawning logic)
    Test 8: track_execution() called at game_won signal emit (win condition logic)
    Test 9: track_execution() called at stage_completed signal emit (stage completion logic)
  </behavior>
  <action>
    Add CoverageTracker.track_execution() calls to GameManager.gd at the following strategic points:

    1. In take_player_damage() function (around line 43):
       ```gdscript
       CoverageTracker.get_instance().track_execution("res://autoloads/GameManager.gd", 43)
       ```

    2. At player death branch (around line 66):
       ```gdscript
       CoverageTracker.get_instance().track_execution("res://autoloads/GameManager.gd", 66)
       ```

    3. In heal_player() function (around line 69):
       ```gdscript
       CoverageTracker.get_instance().track_execution("res://autoloads/GameManager.gd", 69)
       ```

    4. In start_game() function (around line 92):
       ```gdscript
       CoverageTracker.get_instance().track_execution("res://autoloads/GameManager.gd", 92)
       ```

    5. At is_game_active state (around line 94):
       ```gdscript
       CoverageTracker.get_instance().track_execution("res://autoloads/GameManager.gd", 94)
       ```

    6. At health_changed signal emit (around line 97):
       ```gdscript
       CoverageTracker.get_instance().track_execution("res://autoloads/GameManager.gd", 97)
       ```

    7. At boss_spawned signal emit (find boss spawning logic, likely around line 110-120):
       ```gdscript
       CoverageTracker.get_instance().track_execution("res://autoloads/GameManager.gd", 115)
       ```

    8. At game_won signal emit (find win condition logic, likely around line 130-140):
       ```gdscript
       CoverageTracker.get_instance().track_execution("res://autoloads/GameManager.gd", 135)
       ```

    9. At stage_completed signal emit (find stage completion logic):
       ```gdscript
       CoverageTracker.get_instance().track_execution("res://autoloads/GameManager.gd", 145)
       ```

    Pattern reference from test_combat_manager_coverage.gd:
    - Use CoverageTracker.get_instance() to get singleton
    - Call track_execution(script_path, line_number)
    - Script path must be "res://autoloads/GameManager.gd"
    - Line numbers must match actual lines in GameManager.gd (verify after reading file)

    Target 15-20 strategic track_execution() calls covering:
    - Function entry points
    - Branch conditions (if statements)
    - Signal emissions
    - State changes
  </action>
  <verify>
    <automated>grep -c "CoverageTracker\.track_execution" autoloads/GameManager.gd | awk '$1 >= 15 {print "PASS: 15+ coverage tracking calls found"; exit 0} {print "FAIL: Less than 15 coverage tracking calls found"; exit 1}'</automated>
  </verify>
  <done>
    GameManager contains 15-20 CoverageTracker.track_execution() calls at strategic points (function entries, branches, signal emissions, state changes)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create GameManager coverage tests</name>
  <files>test/suites/autoloads/test_game_manager_coverage.gd</files>
  <read_first>
    - test/suites/autoloads/test_game_manager.gd (to understand existing GameManager test patterns)
    - test/suites/autoloads/test_combat_manager_coverage.gd (to reference coverage test structure)
    - addons/gut/coverage/coverage_tracker.gd (to verify CoverageTracker API)
  </read_first>
  <action>
    Create test_game_manager_coverage.gd with the following structure:

    File header:
    ```gdscript
    extends GutTest

    # Coverage tracking tests for GameManager autoload
    # Verifies that CoverageTracker.track_execution() calls work correctly
    # Tests game state changes with coverage tracking

    var _game_manager: GameManager
    var CoverageTracker = null

    func before_each():
        # Load CoverageTracker for line tracking
        CoverageTracker = preload("res://addons/gut/coverage/coverage_tracker.gd")

        # Create fresh GameManager instance for each test
        _game_manager = GameManager.new()
        add_child_autofree(_game_manager)

        # Mock analytics to avoid analytics calls during tests
        var _mock_analytics = double(Node).new()
        _mock_analytics.name = "AnalyticsManager"
        add_child_autofree(_mock_analytics)
        stub(_mock_analytics, "log_custom_event").to_return({})
        _game_manager.set("analytics", _mock_analytics)
    ```

    Test 1: test_health_damage_tracks_coverage
    - Call take_player_damage(50)
    - Verify health decreases
    - Signal health_changed emitted

    Test 2: test_player_death_tracks_coverage
    - Set player_health to low value
    - Call take_player_damage() to trigger death
    - Verify player_died signal emitted

    Test 3: test_healing_tracks_coverage
    - Call heal_player(25)
    - Verify health increases
    - Signal health_changed emitted

    Test 4: test_game_start_tracks_coverage
    - Call start_game()
    - Verify is_game_active = true
    - Verify player health reset to max

    Test 5: test_boss_spawn_tracks_coverage
    - Trigger boss spawning logic (need to find how bosses are spawned in GameManager)
    - Verify boss_spawned signal emitted

    Test 6: test_game_won_tracks_coverage
    - Trigger win condition (need to find win logic in GameManager)
    - Verify game_won signal emitted

    Test 7: test_stage_completed_tracks_coverage
    - Trigger stage completion (need to find stage logic in GameManager)
    - Verify stage_completed signal emitted

    Pattern reference from test_combat_manager_coverage.gd:
    - Use double(Node) for mocking dependencies
    - Use add_child_autofree() for cleanup
    - Use stub() to mock methods
    - Watch signals with watch_signals()
    - Assert signals with assert_signal_emitted()
    - Load CoverageTracker but don't call track_execution() in tests (that's done in GameManager code)

    Target 7-8 comprehensive tests covering:
    - Damage and health management
    - Player death logic
    - Healing logic
    - Game start/flow
    - Boss spawning
    - Win conditions
    - Stage completion
  </action>
  <verify>
    <automated>grep -c "^func test_" test/suites/autoloads/test_game_manager_coverage.gd | awk '$1 >= 7 {print "PASS: 7+ coverage tests found"; exit 0} {print "FAIL: Less than 7 coverage tests found"; exit 1}'</automated>
  </verify>
  <done>
    test_game_manager_coverage.gd created with 7-8 tests covering GameManager's key functions, all tests follow GUT patterns, signal testing verified, CoverageTracker loaded correctly
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify GameManager coverage data in coverage.json</name>
  <files>test/coverage/json/coverage.json</files>
  <read_first>
    - test/suites/autoloads/test_game_manager_coverage.gd (to verify test structure)
    - addons/gut/coverage/coverage_tracker.gd (to verify coverage data structure)
    - test/coverage/json/coverage.json (if exists, to check current state)
  </read_first>
  <action>
    Run GameManager coverage tests and verify coverage.json is populated:

    1. Run GameManager coverage tests:
       ```bash
       godot --headless --script res://test/suites/autoloads/test_game_manager_coverage.gd
       ```

    2. Check coverage.json exists and contains GameManager data:
       ```bash
       if [ -f test/coverage/json/coverage.json ]; then
           cat test/coverage/json/coverage.json | grep -q '"res://autoloads/GameManager.gd"' && echo "PASS: GameManager in coverage.json" || echo "FAIL: GameManager not in coverage.json"
       else
           echo "FAIL: coverage.json does not exist"
       fi
       ```

    3. Verify GameManager coverage data structure:
       - Has "file": "res://autoloads/GameManager.gd"
       - Has "executable_lines" array with line numbers
       - Has "executed_lines" array with tracked lines
       - Has "covered_count" (number of executed lines)
       - Has "total_count" (total executable lines)
       - Has "percentage" (coverage percentage, e.g., 66.67)

    4. Parse coverage.json to extract GameManager stats:
       ```bash
       cat test/coverage/json/coverage.json | jq '."res://autoloads/GameManager.gd"' | jq -r '"\(.covered_count)/\(.total_count) = \(.percentage)%"'
       ```

    Expected output: GameManager coverage data with executed_lines matching the track_execution() calls in GameManager.gd

    If coverage.json not generated, check:
    - .gutconfig.json has post_run_script configured
    - coverage_post_run.gd calls CoverageExporter.export_coverage_json()
    - Tests ran successfully without errors
  </action>
  <verify>
    <automated>
      godot --headless --script res://test/suites/autoloads/test_game_manager_coverage.gd 2>&1 | tail -20
      if [ -f test/coverage/json/coverage.json ]; then
          cat test/coverage/json/coverage.json | jq -e '."res://autoloads/GameManager.gd"' > /dev/null 2>&1 && echo "PASS: GameManager data in coverage.json" || echo "FAIL: GameManager data missing"
      else
          echo "FAIL: coverage.json not generated"
      fi
    </automated>
  </verify>
  <done>
    coverage.json exists and contains GameManager data with executed_lines array, covered_count, total_count, and percentage fields all populated correctly
  </done>
</task>

<task type="auto">
  <name>Task 4: Generate and verify HTML coverage report</name>
  <files>test/coverage/html/index.html</files>
  <read_first>
    - scripts/parse_godot_coverage.py (to verify HTML generation logic)
    - test/coverage/json/coverage.json (source data for HTML report)
    - test/coverage/html/index.html (if exists, to check current state)
  </read_first>
  <action>
    Generate HTML coverage report and verify GameManager appears with line-by-line highlighting:

    1. Run parse_godot_coverage.py to generate HTML report:
       ```bash
       python3 scripts/parse_godot_coverage.py test/coverage/json/coverage.json test/coverage/html/index.html
       ```

    2. Verify HTML report exists:
       ```bash
       if [ -f test/coverage/html/index.html ]; then
           echo "PASS: HTML report generated"
           wc -l test/coverage/html/index.html
       else
           echo "FAIL: HTML report not generated"
       fi
       ```

    3. Verify GameManager appears in HTML report:
       ```bash
       grep -q "GameManager\.gd" test/coverage/html/index.html && echo "PASS: GameManager in HTML report" || echo "FAIL: GameManager not in HTML report"
       ```

    4. Verify coverage badge (green 80%+, yellow 50-80%, red <50%):
       ```bash
       grep -oP "badge.*GameManager.*badge-\w+" test/coverage/html/index.html | head -1
       ```

    5. Verify line-by-line highlighting (green for covered, red for uncovered):
       ```bash
       grep -c '<span class="line-covered"' test/coverage/html/index.html
       grep -c '<span class="line-uncovered"' test/coverage/html/index.html
       ```

    6. Check HTML report structure contains GameManager section:
       ```bash
       grep -A 5 "GameManager\.gd" test/coverage/html/index.html | head -10
       ```

    Expected HTML report features:
    - GameManager.gd listed in file index
    - Coverage percentage badge displayed
    - Line-by-line code display with color highlighting
    - Executed lines show in green (line-covered class)
    - Unexecuted lines show in red (line-uncovered class)
    - Clickable line numbers for navigation

    If HTML report not generated or missing GameManager, check:
    - parse_godot_coverage.py has Jinja2 installed (pip install jinja2)
    - coverage.json has valid GameManager data structure
    - HTML template (scripts/templates/coverage.html or inline) exists
  </action>
  <verify>
    <automated>
      python3 scripts/parse_godot_coverage.py test/coverage/json/coverage.json test/coverage/html/index.html 2>&1
      if [ -f test/coverage/html/index.html ]; then
          grep -q "GameManager\.gd" test/coverage/html/index.html && echo "PASS: GameManager in HTML report" || echo "FAIL: GameManager missing"
      else
          echo "FAIL: HTML report not generated"
      fi
    </automated>
  </verify>
  <done>
    HTML report generated successfully, GameManager.gd appears in file list with coverage percentage badge, line-by-line code display with green/red highlighting for covered/uncovered lines
  </done>
</task>

</tasks>

<verification>
1. Run GameManager coverage tests and verify all pass:
   ```bash
   godot --headless --script res://test/suites/autoloads/test_game_manager_coverage.gd
   ```

2. Verify coverage.json contains GameManager data:
   ```bash
   cat test/coverage/json/coverage.json | jq '."res://autoloads/GameManager.gd"'
   ```

3. Verify HTML report shows GameManager with line highlighting:
   ```bash
   firefox test/coverage/html/index.html  # or open in browser
   ```

4. Verify overall coverage percentage calculation includes GameManager:
   ```bash
   python3 scripts/parse_godot_coverage.py test/coverage/json/coverage.json
   ```

Expected outcomes:
- All GameManager coverage tests pass
- coverage.json contains GameManager with executed_lines array
- HTML report displays GameManager.gd with line-by-line highlighting
- Coverage percentage badge reflects GameManager coverage (target >50% for this pilot)
</verification>

<success_criteria>
1. GameManager.gd contains 15-20 CoverageTracker.track_execution() calls at strategic points
2. test_game_manager_coverage.gd created with 7-8 comprehensive tests
3. All GameManager coverage tests pass
4. coverage.json exists and contains GameManager data with executed_lines, covered_count, total_count, percentage
5. HTML report generated with GameManager.gd showing line-by-line coverage with green/red highlighting
6. GameManager coverage percentage displayed in HTML report with appropriate badge color
</success_criteria>

<output>
After completion, create `.planning/phases/24-expand-godot-coverage/24-01-SUMMARY.md`
</output>
