---
phase: 24-expand-godot-coverage
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - autoloads/SeasonManager.gd
  - test/suites/autoloads/test_season_manager_coverage.gd
autonomous: true
requirements:
  - GODOT-03
  - GODOT-04

must_haves:
  truths:
    - "CoverageTracker.track_execution() called in SeasonManager's key functions"
    - "SeasonManager coverage data appears in coverage.json after test runs"
    - "HTML report shows SeasonManager line-by-line coverage with highlighting"
    - "Season info, leaderboard, rewards paths are tracked"
  artifacts:
    - path: "autoloads/SeasonManager.gd"
      provides: "Seasonal ranking and rewards with coverage tracking"
      min_lines: 200
      contains: "CoverageTracker.track_execution("
    - path: "test/suites/autoloads/test_season_manager_coverage.gd"
      provides: "SeasonManager coverage tests with tracking verification"
      exports: ["test_get_season_info_tracks_coverage", "test_get_leaderboard_tracks_coverage", "test_claim_rewards_tracks_coverage", "test_update_rank_tracks_coverage"]
  key_links:
    - from: "test/suites/autoloads/test_season_manager_coverage.gd"
      to: "autoloads/SeasonManager.gd"
      via: "CoverageTracker.track_execution() calls in SeasonManager functions"
      pattern: "CoverageTracker\\.get_instance\\(\\)\\.track_execution\\(.*SeasonManager\\.gd"
    - from: "addons/gut/coverage/coverage_post_run.gd"
      to: "test/coverage/json/coverage.json"
      via: "CoverageExporter.export_coverage_json() exports SeasonManager data"
      pattern: "coverage.json.*SeasonManager"

---

<objective>
Instrument SeasonManager autoload with CoverageTracker.track_execution() calls to track line execution during test runs, enabling real line coverage measurement for seasonal ranking, leaderboards, and rewards.

Purpose: SeasonManager handles seasonal information retrieval, leaderboards, rank updates, and season rewards. Adding coverage tracking provides visibility into which seasonal progression paths are tested.

Output: SeasonManager with 12-15 strategic track_execution() calls, comprehensive coverage tests verifying tracking works, coverage.json populated with SeasonManager data, HTML report showing line-by-line coverage with green/red highlighting.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-expand-godot-coverage/24-CONTEXT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 13 Summary Reference
@.planning/phases/13-godot-coverage-tools/13-01-SUMMARY.md

# Existing Coverage Infrastructure
@addons/gut/coverage/coverage_tracker.gd
@test/suites/autoloads/test_combat_manager_coverage.gd

# Pattern Reference: Manual Instrumentation
From test_combat_manager_coverage.gd:
```gdscript
# Track function entry
CoverageTracker.get_instance().track_execution("res://autoloads/CombatManager.gd", 170)

# Track branch conditions
CoverageTracker.get_instance().track_execution("res://autoloads/CombatManager.gd", 176)

# Track return statements
CoverageTracker.get_instance().track_execution("res://autoloads/CombatManager.gd", 193)
```

# CoverageTracker Interface
From coverage_tracker.gd:
```gdscript
static func get_instance() -> CoverageTracker
func track_execution(script_path: String, line: int)
func get_coverage_data() -> Dictionary
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CoverageTracker instrumentation to SeasonManager</name>
  <files>autoloads/SeasonManager.gd</files>
  <read_first>
    - autoloads/SeasonManager.gd (to see current implementation - read full file)
    - addons/gut/coverage/coverage_tracker.gd (to verify CoverageTracker API)
    - test/suites/autoloads/test_combat_manager_coverage.gd (to reference instrumentation pattern)
  </read_first>
  <behavior>
    Test 1: track_execution() called at get_season_info() entry (line ~44: func get_season_info())
    Test 2: track_execution() called at network check branch (line ~46: if not network_manager)
    Test 3: track_execution() called at season_info_loaded signal emit (line ~63: season_info_loaded.emit())
    Test 4: track_execution() called at get_leaderboard() entry (line ~78: func get_leaderboard())
    Test 5: track_execution() called at leaderboard_loaded signal emit (line ~99: leaderboard_loaded.emit())
    Test 6: track_execution() called at update_rank() function entry (need to find function)
    Test 7: track_execution() called at rank_updated signal emit (need to find update logic)
    Test 8: track_execution() called at get_season_rewards() entry (need to find function)
    Test 9: track_execution() called at rewards_loaded signal emit (need to find rewards logic)
    Test 10: track_execution() called at claim_season_rewards() entry (need to find function)
  </behavior>
  <action>
    Add CoverageTracker.track_execution() calls to SeasonManager.gd at the following strategic points:

    1. In get_season_info() function (around line 44):
       ```gdscript
       CoverageTracker.get_instance().track_execution("res://autoloads/SeasonManager.gd", 44)
       ```

    2. At network manager check (around line 46):
       ```gdscript
       CoverageTracker.get_instance().track_execution("res://autoloads/SeasonManager.gd", 46)
       ```

    3. At season info data assignment (around line 58):
       ```gdscript
       CoverageTracker.get_instance().track_execution("res://autoloads/SeasonManager.gd", 58)
       ```

    4. At season_info_loaded signal emit (around line 63):
       ```gdscript
       CoverageTracker.get_instance().track_execution("res://autoloads/SeasonManager.gd", 63)
       ```

    5. In get_leaderboard() function (around line 78):
       ```gdscript
       CoverageTracker.get_instance().track_execution("res://autoloads/SeasonManager.gd", 78)
       ```

    6. At leaderboard data assignment (around line 100):
       ```gdscript
       CoverageTracker.get_instance().track_execution("res://autoloads/SeasonManager.gd", 100)
       ```

    7. At leaderboard_loaded signal emit (around line 102):
       ```gdscript
       CoverageTracker.get_instance().track_execution("res://autoloads/SeasonManager.gd", 102)
       ```

    8. In update_rank() function (need to find function definition, likely around line 110+):
       ```gdscript
       CoverageTracker.get_instance().track_execution("res://autoloads/SeasonManager.gd", 115)
       ```

    9. At rank_updated signal emit (need to find update logic, likely in update_rank()):
       ```gdscript
       CoverageTracker.get_instance().track_execution("res://autoloads/SeasonManager.gd", 125)
       ```

    10. In get_season_rewards() function (need to find function, likely around line 140+):
       ```gdscript
       CoverageTracker.get_instance().track_execution("res://autoloads/SeasonManager.gd", 145)
       ```

    11. At rewards_loaded signal emit (need to find rewards logic, likely in get_season_rewards()):
       ```gdscript
       CoverageTracker.get_instance().track_execution("res://autoloads/SeasonManager.gd", 155)
       ```

    12. In claim_season_rewards() function (need to find function, likely around line 160+):
       ```gdscript
       CoverageTracker.get_instance().track_execution("res://autoloads/SeasonManager.gd", 165)
       ```

    13. At rewards_claimed signal emit (need to find claim logic, likely in claim_season_rewards()):
       ```gdscript
       CoverageTracker.get_instance().track_execution("res://autoloads/SeasonManager.gd", 175)
       ```

    14. At analytics tracking for season start (around line 71-75):
       ```gdscript
       CoverageTracker.get_instance().track_execution("res://autoloads/SeasonManager.gd", 73)
       ```

    Pattern reference from test_combat_manager_coverage.gd:
    - Use CoverageTracker.get_instance() to get singleton
    - Call track_execution(script_path, line_number)
    - Script path must be "res://autoloads/SeasonManager.gd"
    - Line numbers must match actual lines in SeasonManager.gd (verify after reading full file)

    Target 12-15 strategic track_execution() calls covering:
    - Function entry points (get_season_info, get_leaderboard, update_rank, get_season_rewards, claim_season_rewards)
    - Branch conditions (network checks, error handling)
    - Signal emissions (season_info_loaded, leaderboard_loaded, rank_updated, rewards_loaded, rewards_claimed)
    - Network operations (RPC calls)
    - Server response handling
    - Analytics tracking
  </action>
  <verify>
    <automated>grep -c "CoverageTracker\.track_execution" autoloads/SeasonManager.gd | awk '$1 >= 12 {print "PASS: 12+ coverage tracking calls found"; exit 0} {print "FAIL: Less than 12 coverage tracking calls found"; exit 1}'</automated>
  </verify>
  <done>
    SeasonManager contains 12-15 CoverageTracker.track_execution() calls at strategic points (function entries, branches, signal emissions, network operations, analytics tracking)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create SeasonManager coverage tests</name>
  <files>test/suites/autoloads/test_season_manager_coverage.gd</files>
  <read_first>
    - test/suites/autoloads/test_season_manager.gd (to understand existing SeasonManager test patterns)
    - test/suites/autoloads/test_combat_manager_coverage.gd (to reference coverage test structure)
    - addons/gut/coverage/coverage_tracker.gd (to verify CoverageTracker API)
  </read_first>
  <action>
    Create test_season_manager_coverage.gd with the following structure:

    File header:
    ```gdscript
    extends GutTest

    # Coverage tracking tests for SeasonManager autoload
    # Verifies that CoverageTracker.track_execution() calls work correctly
    # Tests seasonal ranking and rewards with coverage tracking

    var _season_manager: SeasonManager
    var CoverageTracker = null
    var _mock_network: Node
    var _mock_analytics: Node

    func before_each():
        # Load CoverageTracker for line tracking
        CoverageTracker = preload("res://addons/gut/coverage/coverage_tracker.gd")

        # Create fresh SeasonManager instance for each test
        _season_manager = SeasonManager.new()
        add_child_autofree(_season_manager)

        # Create mock NetworkManager for RPC isolation
        _mock_network = double(Node).new()
        _mock_network.name = "NetworkManager"
        add_child_autofree(_mock_network)
        stub(_mock_network, "is_connected").to_return(true)
        stub(_mock_network, "send_rpc").to_return({"success": true, "result": {}})
        _season_manager.set("network_manager", _mock_network)

        # Mock analytics to avoid analytics calls during tests
        _mock_analytics = double(Node).new()
        _mock_analytics.name = "AnalyticsManager"
        add_child_autofree(_mock_analytics)
        stub(_mock_analytics, "has_method").to_return(false)
        _season_manager.set("analytics", _mock_analytics)
    ```

    Test 1: test_get_season_info_tracks_coverage
    - Call get_season_info()
    - Verify season_info_loaded signal emitted
    - Verify current_season, player_rank, player_score populated

    Test 2: test_get_leaderboard_tracks_coverage
    - Call get_leaderboard(50)
    - Verify leaderboard_loaded signal emitted
    - Verify leaderboard array populated with entries

    Test 3: test_update_rank_tracks_coverage
    - Call update_rank() (need to verify how to call this - likely server RPC or internal update)
    - Verify rank_updated signal emitted (if exists)

    Test 4: test_get_season_rewards_tracks_coverage
    - Call get_season_rewards()
    - Verify rewards_loaded signal emitted
    - Verify season_rewards dictionary populated

    Test 5: test_claim_season_rewards_tracks_coverage
    - Call claim_season_rewards()
    - Verify rewards_claimed_signal emitted (if exists)
    - Verify has_claimed_rewards set to true

    Test 6: test_network_error_handling_tracks_coverage
    - Mock send_rpc to return error for get_season_info
    - Verify error handling paths executed
    - Verify push_error called

    Test 7: test_disconnected_state_tracks_coverage
    - Mock is_connected to return false
    - Call get_season_info() - should return early
    - Verify push_error called for disconnected state

    Test 8: test_analytics_tracking_tracks_coverage
    - Mock analytics with has_method returning true for log_season_start
    - Call get_season_info()
    - Verify analytics.log_season_start() called (via stub verification)

    Pattern reference from test_combat_manager_coverage.gd:
    - Use double(Node) for mocking dependencies
    - Use add_child_autofree() for cleanup
    - Use stub() to mock methods
    - Watch signals with watch_signals()
    - Assert signals with assert_signal_emitted()
    - Load CoverageTracker but don't call track_execution() in tests (that's done in SeasonManager code)

    Target 8-9 comprehensive tests covering:
    - Season info retrieval
    - Leaderboard fetching
    - Rank updates
    - Season rewards retrieval
    - Reward claiming
    - Network error handling
    - Disconnected state handling
    - Analytics tracking
  </action>
  <verify>
    <automated>grep -c "^func test_" test/suites/autoloads/test_season_manager_coverage.gd | awk '$1 >= 8 {print "PASS: 8+ coverage tests found"; exit 0} {print "FAIL: Less than 8 coverage tests found"; exit 1}'</automated>
  </verify>
  <done>
    test_season_manager_coverage.gd created with 8-9 tests covering SeasonManager's key functions, all tests follow GUT patterns, signal testing verified, CoverageTracker loaded correctly
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify SeasonManager coverage data in coverage.json</name>
  <files>test/coverage/json/coverage.json</files>
  <read_first>
    - test/suites/autoloads/test_season_manager_coverage.gd (to verify test structure)
    - addons/gut/coverage/coverage_tracker.gd (to verify coverage data structure)
    - test/coverage/json/coverage.json (if exists, to check current state)
  </read_first>
  <action>
    Run SeasonManager coverage tests and verify coverage.json is populated:

    1. Run SeasonManager coverage tests:
       ```bash
       godot --headless --script res://test/suites/autoloads/test_season_manager_coverage.gd
       ```

    2. Check coverage.json exists and contains SeasonManager data:
       ```bash
       if [ -f test/coverage/json/coverage.json ]; then
           cat test/coverage/json/coverage.json | grep -q '"res://autoloads/SeasonManager.gd"' && echo "PASS: SeasonManager in coverage.json" || echo "FAIL: SeasonManager not in coverage.json"
       else
           echo "FAIL: coverage.json does not exist"
       fi
       ```

    3. Verify SeasonManager coverage data structure:
       - Has "file": "res://autoloads/SeasonManager.gd"
       - Has "executable_lines" array with line numbers
       - Has "executed_lines" array with tracked lines
       - Has "covered_count" (number of executed lines)
       - Has "total_count" (total executable lines)
       - Has "percentage" (coverage percentage, e.g., 66.67)

    4. Parse coverage.json to extract SeasonManager stats:
       ```bash
       cat test/coverage/json/coverage.json | jq '."res://autoloads/SeasonManager.gd"' | jq -r '"\(.covered_count)/\(.total_count) = \(.percentage)%"'
       ```

    Expected output: SeasonManager coverage data with executed_lines matching the track_execution() calls in SeasonManager.gd

    If coverage.json not generated, check:
    - .gutconfig.json has post_run_script configured
    - coverage_post_run.gd calls CoverageExporter.export_coverage_json()
    - Tests ran successfully without errors
  </action>
  <verify>
    <automated>
      godot --headless --script res://test/suites/autoloads/test_season_manager_coverage.gd 2>&1 | tail -20
      if [ -f test/coverage/json/coverage.json ]; then
          cat test/coverage/json/coverage.json | jq -e '."res://autoloads/SeasonManager.gd"' > /dev/null 2>&1 && echo "PASS: SeasonManager data in coverage.json" || echo "FAIL: SeasonManager data missing"
      else
          echo "FAIL: coverage.json not generated"
      fi
    </automated>
  </verify>
  <done>
    coverage.json exists and contains SeasonManager data with executed_lines array, covered_count, total_count, and percentage fields all populated correctly
  </done>
</task>

<task type="auto">
  <name>Task 4: Generate and verify HTML coverage report</name>
  <files>test/coverage/html/index.html</files>
  <read_first>
    - scripts/parse_godot_coverage.py (to verify HTML generation logic)
    - test/coverage/json/coverage.json (source data for HTML report)
    - test/coverage/html/index.html (if exists, to check current state)
  </read_first>
  <action>
    Generate HTML coverage report and verify SeasonManager appears with line-by-line highlighting:

    1. Run parse_godot_coverage.py to generate HTML report:
       ```bash
       python3 scripts/parse_godot_coverage.py test/coverage/json/coverage.json test/coverage/html/index.html
       ```

    2. Verify HTML report exists:
       ```bash
       if [ -f test/coverage/html/index.html ]; then
           echo "PASS: HTML report generated"
           wc -l test/coverage/html/index.html
       else
           echo "FAIL: HTML report not generated"
       fi
       ```

    3. Verify SeasonManager appears in HTML report:
       ```bash
       grep -q "SeasonManager\.gd" test/coverage/html/index.html && echo "PASS: SeasonManager in HTML report" || echo "FAIL: SeasonManager not in HTML report"
       ```

    4. Verify coverage badge (green 80%+, yellow 50-80%, red <50%):
       ```bash
       grep -oP "badge.*SeasonManager.*badge-\w+" test/coverage/html/index.html | head -1
       ```

    5. Verify line-by-line highlighting (green for covered, red for uncovered):
       ```bash
       grep -c '<span class="line-covered"' test/coverage/html/index.html
       grep -c '<span class="line-uncovered"' test/coverage/html/index.html
       ```

    6. Check HTML report structure contains SeasonManager section:
       ```bash
       grep -A 5 "SeasonManager\.gd" test/coverage/html/index.html | head -10
       ```

    Expected HTML report features:
    - SeasonManager.gd listed in file index
    - Coverage percentage badge displayed
    - Line-by-line code display with color highlighting
    - Executed lines show in green (line-covered class)
    - Unexecuted lines show in red (line-uncovered class)
    - Clickable line numbers for navigation

    If HTML report not generated or missing SeasonManager, check:
    - parse_godot_coverage.py has Jinja2 installed (pip install jinja2)
    - coverage.json has valid SeasonManager data structure
    - HTML template (scripts/templates/coverage.html or inline) exists
  </action>
  <verify>
    <automated>
      python3 scripts/parse_godot_coverage.py test/coverage/json/coverage.json test/coverage/html/index.html 2>&1
      if [ -f test/coverage/html/index.html ]; then
          grep -q "SeasonManager\.gd" test/coverage/html/index.html && echo "PASS: SeasonManager in HTML report" || echo "FAIL: SeasonManager missing"
      else
          echo "FAIL: HTML report not generated"
      fi
    </automated>
  </verify>
  <done>
    HTML report generated successfully, SeasonManager.gd appears in file list with coverage percentage badge, line-by-line code display with green/red highlighting for covered/uncovered lines
  </done>
</task>

</tasks>

<verification>
1. Run SeasonManager coverage tests and verify all pass:
   ```bash
   godot --headless --script res://test/suites/autoloads/test_season_manager_coverage.gd
   ```

2. Verify coverage.json contains SeasonManager data:
   ```bash
   cat test/coverage/json/coverage.json | jq '."res://autoloads/SeasonManager.gd"'
   ```

3. Verify HTML report shows SeasonManager with line highlighting:
   ```bash
   firefox test/coverage/html/index.html  # or open in browser
   ```

4. Verify overall coverage percentage calculation includes SeasonManager:
   ```bash
   python3 scripts/parse_godot_coverage.py test/coverage/json/coverage.json
   ```

Expected outcomes:
- All SeasonManager coverage tests pass
- coverage.json contains SeasonManager with executed_lines array
- HTML report displays SeasonManager.gd with line-by-line highlighting
- Coverage percentage badge reflects SeasonManager coverage (target >50% for this pilot)
</verification>

<success_criteria>
1. SeasonManager.gd contains 12-15 CoverageTracker.track_execution() calls at strategic points
2. test_season_manager_coverage.gd created with 8-9 comprehensive tests
3. All SeasonManager coverage tests pass
4. coverage.json exists and contains SeasonManager data with executed_lines, covered_count, total_count, percentage
5. HTML report generated with SeasonManager.gd showing line-by-line coverage with green/red highlighting
6. SeasonManager coverage percentage displayed in HTML report with appropriate badge color
</success_criteria>

<output>
After completion, create `.planning/phases/24-expand-godot-coverage/24-03-SUMMARY.md`
</output>
