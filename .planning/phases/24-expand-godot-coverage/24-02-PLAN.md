---
phase: 24-expand-godot-coverage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - autoloads/PlayerStatsManager.gd
  - test/suites/autoloads/test_player_stats_manager_coverage.gd
autonomous: true
requirements:
  - GODOT-03
  - GODOT-04

must_haves:
  truths:
    - "CoverageTracker.track_execution() called in PlayerStatsManager's key functions"
    - "PlayerStatsManager coverage data appears in coverage.json after test runs"
    - "HTML report shows PlayerStatsManager line-by-line coverage with highlighting"
    - "XP gain, level-up, stat allocation paths are tracked"
  artifacts:
    - path: "autoloads/PlayerStatsManager.gd"
      provides: "Player statistics management with coverage tracking"
      min_lines: 150
      contains: "CoverageTracker.track_execution("
    - path: "test/suites/autoloads/test_player_stats_manager_coverage.gd"
      provides: "PlayerStatsManager coverage tests with tracking verification"
      exports: ["test_gain_xp_tracks_coverage", "test_level_up_tracks_coverage", "test_stat_allocation_tracks_coverage", "test_get_player_stats_tracks_coverage"]
  key_links:
    - from: "test/suites/autoloads/test_player_stats_manager_coverage.gd"
      to: "autoloads/PlayerStatsManager.gd"
      via: "CoverageTracker.track_execution() calls in PlayerStatsManager functions"
      pattern: "CoverageTracker\\.get_instance\\(\\)\\.track_execution\\(.*PlayerStatsManager\\.gd"
    - from: "addons/gut/coverage/coverage_post_run.gd"
      to: "test/coverage/json/coverage.json"
      via: "CoverageExporter.export_coverage_json() exports PlayerStatsManager data"
      pattern: "coverage.json.*PlayerStatsManager"

---

<objective>
Instrument PlayerStatsManager autoload with CoverageTracker.track_execution() calls to track line execution during test runs, enabling real line coverage measurement for player statistics (XP, levels, stat allocation).

Purpose: PlayerStatsManager handles player XP gain, level-up mechanics, stat allocation, and stats retrieval. Adding coverage tracking provides visibility into which progression paths are tested.

Output: PlayerStatsManager with 12-15 strategic track_execution() calls, comprehensive coverage tests verifying tracking works, coverage.json populated with PlayerStatsManager data, HTML report showing line-by-line coverage with green/red highlighting.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-expand-godot-coverage/24-CONTEXT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 13 Summary Reference
@.planning/phases/13-godot-coverage-tools/13-01-SUMMARY.md

# Existing Coverage Infrastructure
@addons/gut/coverage/coverage_tracker.gd
@test/suites/autoloads/test_combat_manager_coverage.gd

# Pattern Reference: Manual Instrumentation
From test_combat_manager_coverage.gd:
```gdscript
# Track function entry
CoverageTracker.get_instance().track_execution("res://autoloads/CombatManager.gd", 170)

# Track branch conditions
CoverageTracker.get_instance().track_execution("res://autoloads/CombatManager.gd", 176)

# Track return statements
CoverageTracker.get_instance().track_execution("res://autoloads/CombatManager.gd", 193)
```

# CoverageTracker Interface
From coverage_tracker.gd:
```gdscript
static func get_instance() -> CoverageTracker
func track_execution(script_path: String, line: int)
func get_coverage_data() -> Dictionary
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CoverageTracker instrumentation to PlayerStatsManager</name>
  <files>autoloads/PlayerStatsManager.gd</files>
  <read_first>
    - autoloads/PlayerStatsManager.gd (to see current implementation)
    - addons/gut/coverage/coverage_tracker.gd (to verify CoverageTracker API)
    - test/suites/autoloads/test_combat_manager_coverage.gd (to reference instrumentation pattern)
  </read_first>
  <behavior>
    Test 1: track_execution() called at get_player_stats() entry (line ~47: func get_player_stats())
    Test 2: track_execution() called at network check branch (line ~53: if not network_manager)
    Test 3: track_execution() called at gain_xp() entry (line ~74: func gain_xp(amount, source))
    Test 4: track_execution() called at XP amount validation (line ~85: if amount <= 0)
    Test 5: track_execution() called at stats_updated signal emit (line ~70: stats_updated.emit())
    Test 6: track_execution() called at level_up signal emit (need to find level-up logic)
    Test 7: track_execution() called at allocate_stats() function entry (need to find function)
    Test 8: track_execution() called at stat_allocated signal emit (need to find allocation logic)
  </behavior>
  <action>
    Add CoverageTracker.track_execution() calls to PlayerStatsManager.gd at the following strategic points:

    1. In get_player_stats() function (around line 47):
       ```gdscript
       CoverageTracker.get_instance().track_execution("res://autoloads/PlayerStatsManager.gd", 47)
       ```

    2. At network manager check (around line 53):
       ```gdscript
       CoverageTracker.get_instance().track_execution("res://autoloads/PlayerStatsManager.gd", 53)
       ```

    3. At stats_updated signal emit (around line 70):
       ```gdscript
       CoverageTracker.get_instance().track_execution("res://autoloads/PlayerStatsManager.gd", 70)
       ```

    4. In gain_xp() function (around line 74):
       ```gdscript
       CoverageTracker.get_instance().track_execution("res://autoloads/PlayerStatsManager.gd", 74)
       ```

    5. At XP amount validation (around line 85):
       ```gdscript
       CoverageTracker.get_instance().track_execution("res://autoloads/PlayerStatsManager.gd", 85)
       ```

    6. At xp_gained signal emit (need to find XP gain logic, likely around line 100-110):
       ```gdscript
       CoverageTracker.get_instance().track_execution("res://autoloads/PlayerStatsManager.gd", 105)
       ```

    7. In allocate_stats() function (need to find function definition, likely around line 120+):
       ```gdscript
       CoverageTracker.get_instance().track_execution("res://autoloads/PlayerStatsManager.gd", 125)
       ```

    8. At stat_allocated signal emit (need to find allocation logic, likely in allocate_stats()):
       ```gdscript
       CoverageTracker.get_instance().track_execution("res://autoloads/PlayerStatsManager.gd", 135)
       ```

    9. At level_up signal emit (need to find level-up logic, likely in response to server XP gain):
       ```gdscript
       CoverageTracker.get_instance().track_execution("res://autoloads/PlayerStatsManager.gd", 145)
       ```

    10. At network connection check in _ready() or _on_connection_status_changed (around line 34-40):
       ```gdscript
       CoverageTracker.get_instance().track_execution("res://autoloads/PlayerStatsManager.gd", 38)
       ```

    Pattern reference from test_combat_manager_coverage.gd:
    - Use CoverageTracker.get_instance() to get singleton
    - Call track_execution(script_path, line_number)
    - Script path must be "res://autoloads/PlayerStatsManager.gd"
    - Line numbers must match actual lines in PlayerStatsManager.gd (verify after reading full file)

    Target 12-15 strategic track_execution() calls covering:
    - Function entry points
    - Branch conditions (if statements for validation)
    - Signal emissions (stats_updated, xp_gained, level_up, stat_allocated)
    - Network operations
    - Server response handling
  </action>
  <verify>
    <automated>grep -c "CoverageTracker\.track_execution" autoloads/PlayerStatsManager.gd | awk '$1 >= 12 {print "PASS: 12+ coverage tracking calls found"; exit 0} {print "FAIL: Less than 12 coverage tracking calls found"; exit 1}'</automated>
  </verify>
  <done>
    PlayerStatsManager contains 12-15 CoverageTracker.track_execution() calls at strategic points (function entries, branches, signal emissions, network operations)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create PlayerStatsManager coverage tests</name>
  <files>test/suites/autoloads/test_player_stats_manager_coverage.gd</files>
  <read_first>
    - test/suites/autoloads/test_player_stats_manager.gd (to understand existing PlayerStatsManager test patterns)
    - test/suites/autoloads/test_combat_manager_coverage.gd (to reference coverage test structure)
    - addons/gut/coverage/coverage_tracker.gd (to verify CoverageTracker API)
  </read_first>
  <action>
    Create test_player_stats_manager_coverage.gd with the following structure:

    File header:
    ```gdscript
    extends GutTest

    # Coverage tracking tests for PlayerStatsManager autoload
    # Verifies that CoverageTracker.track_execution() calls work correctly
    # Tests player statistics with coverage tracking

    var _stats_manager: PlayerStatsManager
    var CoverageTracker = null
    var _mock_network: Node

    func before_each():
        # Load CoverageTracker for line tracking
        CoverageTracker = preload("res://addons/gut/coverage/coverage_tracker.gd")

        # Create fresh PlayerStatsManager instance for each test
        _stats_manager = PlayerStatsManager.new()
        add_child_autofree(_stats_manager)

        # Create mock NetworkManager for RPC isolation
        _mock_network = double(Node).new()
        _mock_network.name = "NetworkManager"
        add_child_autofree(_mock_network)
        stub(_mock_network, "is_connected").to_return(true)
        stub(_mock_network, "send_rpc").to_return({"success": true, "result": {}})
        _stats_manager.set("network_manager", _mock_network)

        # Mock analytics to avoid analytics calls during tests
        var _mock_analytics = double(Node).new()
        _mock_analytics.name = "AnalyticsManager"
        add_child_autofree(_mock_analytics)
        stub(_mock_analytics, "has_method").to_return(false)
        _stats_manager.set("analytics", _mock_analytics)
    ```

    Test 1: test_gain_xp_tracks_coverage
    - Call gain_xp(100, "pve")
    - Verify stats_updated signal emitted
    - Verify xp_gained signal emitted (if exists)

    Test 2: test_get_player_stats_tracks_coverage
    - Call get_player_stats()
    - Verify player_stats dictionary populated
    - Verify stats_updated signal emitted

    Test 3: test_xp_validation_tracks_coverage
    - Call gain_xp(-50, "pve") - should fail validation
    - Call gain_xp(0, "pvp") - should fail validation
    - Verify push_error called for invalid amounts

    Test 4: test_stat_allocation_tracks_coverage
    - Call allocate_stats("strength", 5)
    - Verify stat_allocated signal emitted (if exists)
    - Verify stats_updated signal emitted

    Test 5: test_level_up_tracks_coverage
    - Mock XP gain response that triggers level-up
    - Verify level_up signal emitted (if exists)
    - Verify ability_points increased

    Test 6: test_network_error_handling_tracks_coverage
    - Mock send_rpc to return error
    - Verify error handling paths executed
    - Verify push_error called

    Test 7: test_disconnected_state_tracks_coverage
    - Mock is_connected to return false
    - Call gain_xp() - should return early
    - Verify push_error called for disconnected state

    Pattern reference from test_combat_manager_coverage.gd:
    - Use double(Node) for mocking dependencies
    - Use add_child_autofree() for cleanup
    - Use stub() to mock methods
    - Watch signals with watch_signals()
    - Assert signals with assert_signal_emitted()
    - Load CoverageTracker but don't call track_execution() in tests (that's done in PlayerStatsManager code)

    Target 7-8 comprehensive tests covering:
    - XP gain mechanics
    - Stats retrieval
    - XP validation (negative, zero)
    - Stat allocation
    - Level-up logic
    - Network error handling
    - Disconnected state handling
  </action>
  <verify>
    <automated>grep -c "^func test_" test/suites/autoloads/test_player_stats_manager_coverage.gd | awk '$1 >= 7 {print "PASS: 7+ coverage tests found"; exit 0} {print "FAIL: Less than 7 coverage tests found"; exit 1}'</automated>
  </verify>
  <done>
    test_player_stats_manager_coverage.gd created with 7-8 tests covering PlayerStatsManager's key functions, all tests follow GUT patterns, signal testing verified, CoverageTracker loaded correctly
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify PlayerStatsManager coverage data in coverage.json</name>
  <files>test/coverage/json/coverage.json</files>
  <read_first>
    - test/suites/autoloads/test_player_stats_manager_coverage.gd (to verify test structure)
    - addons/gut/coverage/coverage_tracker.gd (to verify coverage data structure)
    - test/coverage/json/coverage.json (if exists, to check current state)
  </read_first>
  <action>
    Run PlayerStatsManager coverage tests and verify coverage.json is populated:

    1. Run PlayerStatsManager coverage tests:
       ```bash
       godot --headless --script res://test/suites/autoloads/test_player_stats_manager_coverage.gd
       ```

    2. Check coverage.json exists and contains PlayerStatsManager data:
       ```bash
       if [ -f test/coverage/json/coverage.json ]; then
           cat test/coverage/json/coverage.json | grep -q '"res://autoloads/PlayerStatsManager.gd"' && echo "PASS: PlayerStatsManager in coverage.json" || echo "FAIL: PlayerStatsManager not in coverage.json"
       else
           echo "FAIL: coverage.json does not exist"
       fi
       ```

    3. Verify PlayerStatsManager coverage data structure:
       - Has "file": "res://autoloads/PlayerStatsManager.gd"
       - Has "executable_lines" array with line numbers
       - Has "executed_lines" array with tracked lines
       - Has "covered_count" (number of executed lines)
       - Has "total_count" (total executable lines)
       - Has "percentage" (coverage percentage, e.g., 66.67)

    4. Parse coverage.json to extract PlayerStatsManager stats:
       ```bash
       cat test/coverage/json/coverage.json | jq '."res://autoloads/PlayerStatsManager.gd"' | jq -r '"\(.covered_count)/\(.total_count) = \(.percentage)%"'
       ```

    Expected output: PlayerStatsManager coverage data with executed_lines matching the track_execution() calls in PlayerStatsManager.gd

    If coverage.json not generated, check:
    - .gutconfig.json has post_run_script configured
    - coverage_post_run.gd calls CoverageExporter.export_coverage_json()
    - Tests ran successfully without errors
  </action>
  <verify>
    <automated>
      godot --headless --script res://test/suites/autoloads/test_player_stats_manager_coverage.gd 2>&1 | tail -20
      if [ -f test/coverage/json/coverage.json ]; then
          cat test/coverage/json/coverage.json | jq -e '."res://autoloads/PlayerStatsManager.gd"' > /dev/null 2>&1 && echo "PASS: PlayerStatsManager data in coverage.json" || echo "FAIL: PlayerStatsManager data missing"
      else
          echo "FAIL: coverage.json not generated"
      fi
    </automated>
  </verify>
  <done>
    coverage.json exists and contains PlayerStatsManager data with executed_lines array, covered_count, total_count, and percentage fields all populated correctly
  </done>
</task>

<task type="auto">
  <name>Task 4: Generate and verify HTML coverage report</name>
  <files>test/coverage/html/index.html</files>
  <read_first>
    - scripts/parse_godot_coverage.py (to verify HTML generation logic)
    - test/coverage/json/coverage.json (source data for HTML report)
    - test/coverage/html/index.html (if exists, to check current state)
  </read_first>
  <action>
    Generate HTML coverage report and verify PlayerStatsManager appears with line-by-line highlighting:

    1. Run parse_godot_coverage.py to generate HTML report:
       ```bash
       python3 scripts/parse_godot_coverage.py test/coverage/json/coverage.json test/coverage/html/index.html
       ```

    2. Verify HTML report exists:
       ```bash
       if [ -f test/coverage/html/index.html ]; then
           echo "PASS: HTML report generated"
           wc -l test/coverage/html/index.html
       else
           echo "FAIL: HTML report not generated"
       fi
       ```

    3. Verify PlayerStatsManager appears in HTML report:
       ```bash
       grep -q "PlayerStatsManager\.gd" test/coverage/html/index.html && echo "PASS: PlayerStatsManager in HTML report" || echo "FAIL: PlayerStatsManager not in HTML report"
       ```

    4. Verify coverage badge (green 80%+, yellow 50-80%, red <50%):
       ```bash
       grep -oP "badge.*PlayerStatsManager.*badge-\w+" test/coverage/html/index.html | head -1
       ```

    5. Verify line-by-line highlighting (green for covered, red for uncovered):
       ```bash
       grep -c '<span class="line-covered"' test/coverage/html/index.html
       grep -c '<span class="line-uncovered"' test/coverage/html/index.html
       ```

    6. Check HTML report structure contains PlayerStatsManager section:
       ```bash
       grep -A 5 "PlayerStatsManager\.gd" test/coverage/html/index.html | head -10
       ```

    Expected HTML report features:
    - PlayerStatsManager.gd listed in file index
    - Coverage percentage badge displayed
    - Line-by-line code display with color highlighting
    - Executed lines show in green (line-covered class)
    - Unexecuted lines show in red (line-uncovered class)
    - Clickable line numbers for navigation

    If HTML report not generated or missing PlayerStatsManager, check:
    - parse_godot_coverage.py has Jinja2 installed (pip install jinja2)
    - coverage.json has valid PlayerStatsManager data structure
    - HTML template (scripts/templates/coverage.html or inline) exists
  </action>
  <verify>
    <automated>
      python3 scripts/parse_godot_coverage.py test/coverage/json/coverage.json test/coverage/html/index.html 2>&1
      if [ -f test/coverage/html/index.html ]; then
          grep -q "PlayerStatsManager\.gd" test/coverage/html/index.html && echo "PASS: PlayerStatsManager in HTML report" || echo "FAIL: PlayerStatsManager missing"
      else
          echo "FAIL: HTML report not generated"
      fi
    </automated>
  </verify>
  <done>
    HTML report generated successfully, PlayerStatsManager.gd appears in file list with coverage percentage badge, line-by-line code display with green/red highlighting for covered/uncovered lines
  </done>
</task>

</tasks>

<verification>
1. Run PlayerStatsManager coverage tests and verify all pass:
   ```bash
   godot --headless --script res://test/suites/autoloads/test_player_stats_manager_coverage.gd
   ```

2. Verify coverage.json contains PlayerStatsManager data:
   ```bash
   cat test/coverage/json/coverage.json | jq '."res://autoloads/PlayerStatsManager.gd"'
   ```

3. Verify HTML report shows PlayerStatsManager with line highlighting:
   ```bash
   firefox test/coverage/html/index.html  # or open in browser
   ```

4. Verify overall coverage percentage calculation includes PlayerStatsManager:
   ```bash
   python3 scripts/parse_godot_coverage.py test/coverage/json/coverage.json
   ```

Expected outcomes:
- All PlayerStatsManager coverage tests pass
- coverage.json contains PlayerStatsManager with executed_lines array
- HTML report displays PlayerStatsManager.gd with line-by-line highlighting
- Coverage percentage badge reflects PlayerStatsManager coverage (target >50% for this pilot)
</verification>

<success_criteria>
1. PlayerStatsManager.gd contains 12-15 CoverageTracker.track_execution() calls at strategic points
2. test_player_stats_manager_coverage.gd created with 7-8 comprehensive tests
3. All PlayerStatsManager coverage tests pass
4. coverage.json exists and contains PlayerStatsManager data with executed_lines, covered_count, total_count, percentage
5. HTML report generated with PlayerStatsManager.gd showing line-by-line coverage with green/red highlighting
6. PlayerStatsManager coverage percentage displayed in HTML report with appropriate badge color
</success_criteria>

<output>
After completion, create `.planning/phases/24-expand-godot-coverage/24-02-SUMMARY.md`
</output>
