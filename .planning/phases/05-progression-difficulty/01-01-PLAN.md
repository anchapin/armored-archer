# Phase 5: Progression & Difficulty - Plan

## Overview
This phase addresses progression and difficulty systems to ensure rewarding advancement with tuned XP curves, appropriate level scaling, meaningful stat choices, balanced gear, and dynamic difficulty adjustment based on player performance.

## Requirements
- PROG-01: XP Curve Tuning
- PROG-02: Level Scaling
- PROG-03: Stat Allocation System
- PROG-04: Gear Stat Balance
- DIFFICULTY-01: Dynamic Difficulty Adjustment
- DIFFICULTY-02: Pacing Variety
- DIFFICULTY-03: Clear Progression Indicators

## Success Criteria
1. Level progression uses satisfying growth curves where early levels feel fast and rewarding while later levels provide meaningful, grindy advancement
2. Enemy difficulty scales appropriately with player level through both damage increases and more sophisticated AI behaviors
3. Players can respec stats with a reasonable cost that encourages diverse builds without punishing experimentation
4. Equipment stats have diminishing returns that prevent power stacking exploits while still making upgrades feel valuable
5. Game difficulty adjusts dynamically based on player performance (win streaks increase challenge, losing streaks reduce it) to maintain engagement
6. Encounters provide varied pacing with intense combat segments, exploration/puzzle moments, and narrative downtime that prevents fatigue
7. Players always know what to do next through clear map markers, quest objectives, and level requirements that prevent getting stuck

## Tasks

<task>
<id>01</id>
<name>XP Curve Tuning</name>
<requirement>PROG-01</requirement>

<description>
Implement satisfying XP growth curves where early levels feel fast and rewarding while later levels provide meaningful advancement.

Key Features:
- Early levels (1-10): Fast progression (30-60 min per level)
- Mid levels (11-30): Moderate progression (1-2 hours per level)
- Late levels (31-50): Slower progression (2-4 hours per level)
- Milestone XP requirements with bonus rewards
- Level-up celebration effects

Implementation:
- Update PlayerStatsManager XP curves
- Create XP milestones with rewards
- Add level-up effects and celebrations
- Update UI to show XP progress bars
</description>

<files_to_create>
autoloads/XPManager.gd
scenes/ui/level_up_effect.tscn
scenes/ui/level_up_effect.gd
</files_to_create>

<files_modified>
autoloads/PlayerStatsManager.gd
scenes/player/player_character.gd
scenes/ui/hud.gd
</files_modified>

<action_details>
1. Create XPManager.gd:
   - get_xp_for_level(level) returns required XP with curve
   - get_level_curve_type(level) returns early/mid/late progression
   - calculate_xp_gain(base_xp, level) applies modifiers
   - add_xp(amount) with level-up detection
   - get_progress_percentage() for UI display

2. Update PlayerStatsManager.gd:
   - Add XP milestones tracking
   - Update XP calculation to use XPManager curves
   - Add level-up celebration triggers

3. Create level-up effects:
   - Visual celebration (particles, sound, animation)
   - Stat improvement popup
   - Screen flash effect

4. Update HUD:
   - Add XP progress bar
   - Show level progress
   - Display next level XP requirement

XP Curve Formula:
- Levels 1-10: 1000-5000 XP per level (early game)
- Levels 11-30: 5000-10000 XP per level (mid game)
- Levels 31-50: 10000-20000 XP per level (late game)
- Multiplier: 1.0x base, quest completion bonus +50%
</action_details>

<verification_criteria>
- Early levels complete in <30 minutes
- Mid levels complete in 1-2 hours
- Late levels complete in 2-4 hours
- XP progress bar accurate in HUD
- Level-up effects trigger correctly
</verification_criteria>
</task>

<task>
<id>02</id>
<name>Level Scaling</name>
<requirement>PROG-02</requirement>

<description>
Implement enemy difficulty scaling based on player level through damage increases and AI behavior sophistication.

Key Features:
- Enemy damage scales with player level
- AI difficulty increases (aggression, tactics) with level
- Boss scaling with phase progression
- Encounter difficulty indicators in UI

Implementation:
- Create DifficultyScalingManager autoload
- Update BaseEnemy with level-aware damage
- Update BossManager with scaling
- Create encounter difficulty indicators
</description>

<files_to_create>
autoloads/DifficultyScalingManager.gd
scenes/ui/difficulty_indicator.tscn
scenes/ui/difficulty_indicator.gd
</files_to_create>

<files_modified>
scenes/enemies/base_enemy.gd
autoloads/BossManager.gd
autoloads/EnemyAIManager.gd
backend/src/modules/encounter_data.ts
</files_modified>

<action_details>
1. Create DifficultyScalingManager.gd:
   - get_enemy_damage_mult(player_level) returns scaling factor
   - get_ai_difficulty_tier(player_level) returns AI behavior modifications
   - get_boss_phase_progression(player_level) returns boss progression

2. Update BaseEnemy.gd:
   - Add level parameter for damage scaling
   - Modify take_damage() to apply player level multiplier
   - Add AI behavior modifiers based on difficulty

3. Update BossManager.gd:
   - Add scaling for boss phases
   - Phase progression based on player level
   - Multiple boss difficulties

4. Create difficulty indicators:
   - Display encounter difficulty (Easy/Medium/Hard)
   - Color-coded indicators
   - Warning for high-level content

Scaling Formula:
- Level 1-10: 0.8x-1.0x damage
- Level 11-20: 1.0x-1.2x damage
- Level 21-30: 1.2x-1.5x damage
- Level 31-50: 1.5x-2.0x damage
</action_details>

<verification_criteria>
- Enemy damage scales with player level
- AI behavior varies by difficulty tier
- Bosses have phase progression
- Difficulty indicators display correctly
</verification_criteria>
</task>

<task>
<id>03</id>
<name>Stat Allocation System</name>
<requirement>PROG-03</requirement>

<description>
Implement stat respec system with reasonable cost to encourage diverse builds without punishing experimentation.

Key Features:
- Respec cost (5-10% of player's current gem total)
- Stat redistribution UI
- Build save/load system
- Stat point budget per level

Implementation:
- Create StatAllocationManager autoload
- Update PlayerStatsManager with respec support
- Create stat allocation UI scene
- Add build save slots
</description>

<files_to_create>
autoloads/StatAllocationManager.gd
scenes/ui/stat_allocation_ui.tscn
scenes/ui/stat_allocation_ui.gd
</files_to_create>

<files_modified>
autoloads/PlayerStatsManager.gd
scenes/player/gear_data.gd
backend/src/modules/rpc_handlers.ts
</files_modified>

<action_details>
1. Create StatAllocationManager.gd:
   - get_respec_cost() calculates cost (5-10% of gems)
   - redistribute_stats(new_allocation) applies stat changes
   - validate_allocation(allocation) ensures total points spent = available
   - save_build(build_data) stores build configuration

2. Update PlayerStatsManager.gd:
   - Add build save/load methods
   - Track total stat points available
   - Add respec cooldown timer

3. Create stat allocation UI:
   - Stat point display and controls
   - Confirmation dialog for changes
   - Respec cost display
   - Build save slots

4. Update backend:
   - Add RPC for build save/load
   - Validate builds on server
   - Prevent stat manipulation

Respec Cost:
- Cost = 5% of player's current gem total (minimum 100, maximum 1000 gems)
- Free respec: One free respec per season
- Cooldown: 24 hours between respecs
</action_details>

<verification_criteria>
- Players can respec stats with gem cost
- Stat points validate correctly
- Build save/load functional
- Multiple build slots available
- Respec cost is reasonable (5-10% of gems)
</verification_criteria>
</task>

<task>
<id>04</id>
<name>Gear Stat Balance</name>
<requirement>PROG-04</requirement>

<description>
Implement diminishing returns on equipment stats to prevent power stacking exploits while keeping upgrades valuable.

Key Features:
- Diminishing returns curve (70% of max at soft cap)
- Stat synergy calculations
- Gear power rating display
- Balance adjustments per slot

Implementation:
- Update GearManager with diminishing returns
- Create GearBalanceCalculator autoload
- Update gear UI to show power rating
- Add synergy indicators
</description>

<files_to_create>
autoloads/GearBalanceCalculator.gd
backend/src/modules/gear_balance.ts
backend/src/data/migrations/004_gear_balance.sql
</files_to_create>

<files_modified>
autoloads/GearManager.gd
autoloads/GearRegistry.gd
scenes/player/gear_data.gd
scenes/ui/loadout.gd
</files_modified>

<action_details>
1. Create GearBalanceCalculator.gd:
   - calculate_effective_stat(base_stat, synergy_bonus, soft_cap) returns diminishing returns
   - get_gear_power_rating(gear_data) calculates overall power
   - validate_gear_power(gear_data) prevents exploits
   - get_synergy_bonus(gear_set) calculates set bonuses

2. Update GearManager.gd:
   - Apply diminishing returns to gear stats
   - Track synergy bonuses
   - Add power rating display

3. Update GearRegistry.gd:
   - Add soft cap values to gear data
   - Add synergy groups
   - Validate gear on registration

4. Create backend gear_balance.ts:
   - validate_gear_stats() prevents overpowered items
   - calculate_gear_power() returns power rating
   - track_gear_usage() for balance tuning

5. Create database migration:
   - Add gear_balance_adjustments table
   - Track balance change history

Diminishing Returns Formula:
- Effective = Base × (1 - (Stat / SoftCap) × 0.5)
- SoftCap = 70% of maximum stat value
- SynergyBonus = +5-15% for matching gear sets
</action_details>

<verification_criteria>
- Gear stats apply diminishing returns
- Soft cap prevents 70% stat stacking
- Synergy bonuses calculated correctly
- Power rating displayed in UI
- No gear exceeds power rating threshold
</verification_criteria>
</task>

<task>
<id>05</id>
<name>Dynamic Difficulty Adjustment</name>
<requirement>DIFFICULTY-01</requirement>

<description>
Implement dynamic difficulty adjustment based on player performance (win streaks increase challenge, losing streaks reduce it).

Key Features:
- Win streak tracking (3+ wins increases difficulty)
- Losing streak tracking (3+ losses decreases difficulty)
- Difficulty modifiers (+/- 20% from baseline)
- Performance metrics dashboard

Implementation:
- Create DynamicDifficultyManager autoload
- Update CombatManager with difficulty modifiers
- Create performance metrics UI
- Add streak indicators
</description>

<files_to_create>
autoloads/DynamicDifficultyManager.gd
scenes/ui/difficulty_adjustment.tscn
scenes/ui/difficulty_adjustment.gd
backend/src/modules/dynamic_difficulty.ts
</files_to_create>

<files_modified>
autoloads/CombatManager.gd
autoloads/CampaignManager.gd
backend/src/modules/rpc_handlers.ts
backend/src/data/migrations/005_difficulty_metrics.sql
</files_modified>

<action_details>
1. Create DynamicDifficultyManager.gd:
   - track_win_streak() increments/decrements on match outcomes
   - get_difficulty_modifier() returns +/-20% modifier
   - calculate_target_difficulty(base_difficulty, modifier) for encounters
   - get_performance_rating() for dashboard

2. Update CombatManager.gd:
   - Apply difficulty modifiers to damage calculations
   - Adjust enemy AI difficulty
   - Scale encounter rewards

3. Update CampaignManager.gd:
   - Use difficulty modifier for encounter generation
   - Display current difficulty in campaign map
   - Save difficulty setting

4. Create difficulty UI:
   - Display current difficulty level
   - Show active streaks
   - Visual feedback for difficulty changes

5. Create backend module:
   - track_match_outcome() for performance metrics
   - get_player_performance() returns win rate and streaks
   - adjust_difficulty() applies server-side changes

Difficulty Adjustment Rules:
- 3+ wins in a row: Increase difficulty by +10%
- 3+ losses in a row: Decrease difficulty by -10%
- Max modifier: +/- 20% from baseline
- Difficulty levels: Easy (-20%), Normal (0%), Hard (+10%), Extreme (+20%)
</action_details>

<verification_criteria>
- Difficulty increases after 3+ wins
- Difficulty decreases after 3+ losses
- Modifier limited to +/- 20%
- Difficulty UI shows current level
- Performance metrics tracked
</verification_criteria>
</task>

<task>
<id>06</id>
<name>Pacing & Variety System</name>
<requirement>DIFFICULTY-02</requirement>

<description>
Implement varied encounter pacing (60% combat, 20% exploration, 20% narrative) to prevent fatigue.

Key Features:
- Encounter pacing tracking
- Content type classification (combat/puzzle/exploration/narrative)
- Fatigue monitoring and breaks
- Pacing optimization

Implementation:
- Create PacingManager autoload
- Update CampaignManager with pacing data
- Create encounter type markers
- Add fatigue indicators
</description>

<files_to_create>
autoloads/PacingManager.gd
backend/src/modules/encounter_pacing.ts
scenes/ui/fatigue_indicator.tscn
scenes/ui/fatigue_indicator.gd
backend/src/data/migrations/006_encounter_pacing.sql
</files_to_create>

<files_modified>
autoloads/CampaignManager.gd
autoloads/EncounterData.gd
scenes/player/player_character.gd
</files_modified>

<action_details>
1. Create PacingManager.gd:
   - classify_encounter(encounter_data) returns content type
   - track_pacing_state(combat_time, type) monitors intensity
   - get_fatigue_level(intensity, duration) returns fatigue score
   - suggest_break() recommends pacing adjustments

2. Update CampaignManager.gd:
   - Add encounter type markers to stages
   - Track recent pacing (last 10 encounters)
   - Display pacing warnings

3. Create fatigue indicators:
   - Visual fatigue meter (0-100%)
   - Break recommendation popup
   - Auto-pause option for fatigue

4. Create backend module:
   - log_encounter_pacing() stores pacing data
   - get_pacing_report() generates pacing analytics
   - optimize_pacing() recommends adjustments

Pacing Targets:
- Combat encounters: 60% of total
- Exploration encounters: 20% of total
- Narrative encounters: 20% of total
- Max combat streak: 5 encounters before non-combat content
- Min exploration streak: 3 encounters before combat content
</action_details>

<verification_criteria>
- Encounter types classified correctly
- Pacing distribution follows 60/20/20 ratio
- Fatigue indicators display accurately
- Pacing warnings trigger appropriately
</verification_criteria>
</task>

<task>
<id>07</id>
<name>Clear Progression Indicators</name>
<requirement>DIFFICULTY-03</requirement>

<description>
Implement clear progression indicators (map markers, quest objectives, level requirements) so players always know what to do next.

Key Features:
- Dynamic map markers for active quests
- Quest objective tracking
- Level requirement indicators
- Progress path visualization

Implementation:
- Create ProgressionIndicatorManager autoload
- Update CampaignManager with marker system
- Create quest tracking UI
- Add level requirement gates
</description>

<files_to_create>
autoloads/ProgressionIndicatorManager.gd
scenes/ui/quest_tracker.tscn
scenes/ui/quest_tracker.gd
scenes/ui/level_requirement_gate.tscn
scenes/ui/level_requirement_gate.gd
backend/src/modules/progression_tracking.ts
backend/src/data/migrations/007_progression_tracking.sql
</files_to_create>

<files_modified>
autoloads/CampaignManager.gd
autoloads/PlayerStatsManager.gd
scenes/player/player_character.gd
scenes/ui/hud.gd
scenes/ui/campaign_map.tscn
</files_modified>

<action_details>
1. Create ProgressionIndicatorManager.gd:
   - get_active_quests() returns current quest list
   - get_quest_objectives(quest_id) returns objectives
   - get_level_requirements(level) returns conditions
   - update_map_markers() refreshes campaign map

2. Update CampaignManager.gd:
   - Add quest markers to stages
   - Display active objectives
   - Show next available content
   - Track quest completion progress

3. Create quest tracking UI:
   - Quest list with objectives
   - Progress indicators
   - Auto-navigation to quest location

4. Create level requirement gates:
   - Display level requirement
   - Block access to content
   - Show progress to requirement
   - Reward for completing requirement

5. Update player character:
   - Add quest tracking integration
   - Add level requirement checks

6. Update HUD:
   - Add quest objective display
   - Show next objective

7. Create backend module:
   - track_quest_progress() stores quest data
   - get_player_progression() returns overall progress
   - update_map_markers() syncs player position

Progression Features:
- Quest markers show on campaign map
- Quest objectives update dynamically
- Level requirements display with progress
- Auto-navigation to quest targets
</action_details>

<verification_criteria>
- Map markers show active quests
- Quest objectives display correctly
- Level requirements block appropriately
- Progress path visualizes clearly
- Auto-navigation functions
</verification_criteria>
</task>

<task>
<id>08</id>
<name>Progression & Difficulty Integration Tests</name>
<requirement>PROG-01, PROG-02, PROG-03, PROG-04, DIFFICULTY-01, DIFFICULTY-02, DIFFICULTY-03</requirement>

<description>
Create comprehensive integration tests for all progression and difficulty systems.

Test Coverage:
- XP curve calculations and level progression
- Difficulty scaling with player level
- Stat respec and allocation
- Gear diminishing returns
- Dynamic difficulty adjustment
- Pacing and encounter variety
- Progression indicators and quest tracking

Implementation:
- Create test files for each manager
- Add progression scenario tests
- Mock backend for progression tests
- Database migration tests
</description>

<files_to_create>
test/test_xp_manager.gd
test/test_difficulty_scaling_manager.gd
test/test_stat_allocation_manager.gd
test/test_gear_balance_calculator.gd
test/test_dynamic_difficulty_manager.gd
test/test_pacing_manager.gd
test/test_progression_indicator_manager.gd
test/test_progression_scenarios.gd
backend/src/modules/__tests__/xp_manager.test.ts
backend/src/modules/__tests__/difficulty_scaling.test.ts
backend/src/modules/__tests__/stat_allocation.test.ts
backend/src/modules/__tests__/gear_balance.test.ts
backend/src/modules/__tests__/dynamic_difficulty.test.ts
backend/src/modules/__tests__/pacing.test.ts
backend/src/modules/__tests__/progression_tracking.test.ts
</files_to_create>

<files_modified>
test/run_all_tests.gd
</files_modified>

<action_details>
1. Create test_xp_manager.gd:
   - test_xp_curve_early_levels()
   - test_xp_curve_mid_levels()
   - test_xp_curve_late_levels()
   - test_level_up_detection()
   - test_xp_bonus_multiplier()
   - test_progress_percentage_calculation()

2. Create test_difficulty_scaling_manager.gd:
   - test_enemy_damage_scaling()
   - test_ai_difficulty_tiers()
   - test_boss_phase_progression()
   - test_difficulty_indicators()
   - test_scaling_formula_valid()

3. Create test_stat_allocation_manager.gd:
   - test_respec_cost_calculation()
   - test_stat_point_validation()
   - test_build_save_load()
   - test_multiple_build_slots()
   - test_respec_cooldown()

4. Create test_gear_balance_calculator.gd:
   - test_diminishing_returns_formula()
   - test_soft_cap_application()
   - test_synergy_bonus_calculation()
   - test_power_rating_calculation()
   - test_gear_power_validation()

5. Create test_dynamic_difficulty_manager.gd:
   - test_win_streak_tracking()
   - test_lose_streak_tracking()
   - test_difficulty_modifier_calculation()
   - test_modifier_limits()
   - test_performance_rating_calculation()

6. Create test_pacing_manager.gd:
   - test_encounter_classification()
   - test_pacing_distribution()
   - test_fatigue_level_calculation()
   - test_pacing_targets_met()
   - test_break_recommendations()

7. Create test_progression_indicator_manager.gd:
   - test_map_marker_updates()
   - test_quest_objective_tracking()
   - test_level_requirement_gates()
   - test_progress_visualization()
   - test_auto_navigation()

8. Create test_progression_scenarios.gd:
   - test_full_progression_flow()
   - test_quest_completion_flow()
   - test_level_requirement_flow()
   - test_dynamic_adjustment_integration()

9. Create backend tests:
   - XP curve validation tests
   - Difficulty scaling algorithm tests
   - Stat allocation safety tests
   - Gear balance validation tests
   - Dynamic difficulty adjustment tests
   - Pacing optimization tests
   - Progression tracking accuracy tests
</action_details>

<verification_criteria>
- All tests pass with godot --headless --script
- Test coverage > 80% for all progression modules
- XP curves validated with test cases
- Difficulty scaling formulas tested with edge cases
- Stat allocation validated with point budget checks
- Gear diminishing returns tested with overflow protection
- Dynamic difficulty tested with streak scenarios
- Pacing tests verify 60/20/20 distribution
- Progression indicators tested for all UI components
- Backend tests mock database correctly
</verification_criteria>
</task>

</tasks>
