---
phase: 22-verify-phase-15
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [PBT-01, PBT-02, PBT-03, PBT-04, PBT-05, PBT-06]
user_setup: []

must_haves:
  truths:
    - "All 31 property tests pass successfully"
    - "Property test count verified: 31 tests across 5 packages"
    - "Progression system invariants verified (8 tests in rpg)"
    - "Matchmaking system invariants verified (7 tests in modules)"
    - "Inventory system invariants verified (8 tests in rpc)"
    - "Combat system invariants verified (6 tests in combat)"
    - "RNG system invariants verified (7 tests in rng)"
  artifacts:
    - path: "backend/internal/rpg/rpg_property_test.go"
      provides: "Progression invariant tests"
      min_lines: 200
    - path: "backend/internal/modules/matchmaking_property_test.go"
      provides: "Matchmaking invariant tests"
      min_lines: 180
    - path: "backend/internal/rpc/rpc_property_test.go"
      provides: "Inventory invariant tests"
      min_lines: 200
    - path: "backend/internal/combat/combat_property_test.go"
      provides: "Combat invariant tests"
      min_lines: 150
    - path: "backend/internal/rng/rng_property_test.go"
      provides: "RNG invariant tests"
      min_lines: 150
  key_links:
    - from: "go test -run Property"
      to: "31 property test functions"
      via: "Test runner"
      pattern: "TestXPProperty|TestLevelProperty|TestStatProperty|TestMatchmakingProperty|TestInventoryProperty|TestDamageProperty|TestHitChanceProperty|TestCritProperty|TestRollIntProperty|TestRollFloatProperty|TestRollChoiceProperty|TestShuffleProperty|TestSeedProperty"
---

<objective>
Verify all 31 property-based tests from Phase 15 pass successfully, confirming invariants for progression, matchmaking, inventory, combat, and RNG systems.

Purpose: Ensure Phase 15's property test implementation is correct and all invariants hold before generating VERIFICATION.md. This unblocks milestone completion by confirming PBT-01 through PBT-06 requirements are satisfied.

Output: Confirmation that 31 property tests pass with 100% success rate.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md (Phase 22 section)
@.planning/REQUIREMENTS.md (Property-Based Testing section)
@.planning/phases/22-verify-phase-15/22-RESEARCH.md
@.planning/phases/22-verify-phase-15/22-VALIDATION.md

# Phase 15 Context
@.planning/phases/15-property-based-testing-expansion/15-CONTEXT.md
@.planning/phases/15-property-based-testing-expansion/15-01-SUMMARY.md
@.planning/phases/15-property-based-testing-expansion/15-02-SUMMARY.md
@.planning/phases/15-property-based-testing-expansion/15-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run progression property tests</name>
  <files>backend/internal/rpg/rpg_property_test.go</files>
  <read_first>backend/internal/rpg/rpg_property_test.go</read_first>
  <action>
    Run all progression property tests to verify XP consistency, level-up mechanics, and stat allocation invariants.

    Execute: go test -v ./internal/rpg/ -run "Property" -timeout 30s

    Expected output: 8 PASS results for:
    - TestXPProperty_NonNegative
    - TestXPProperty_XPSourceConsistency
    - TestLevelProperty_Monotonic
    - TestLevelProperty_XPScalingQuadratic
    - TestLevelProperty_LevelUpGrantsAbilityPoints
    - TestLevelProperty_MaxLevelCap
    - TestStatProperty_Conservation
    - TestStatProperty_ValidStatsOnly

    This verifies PBT-01 (progression invariants) and contributes to PBT-06 (20+ total tests).
  </action>
  <verify>
    <automated>cd /home/alex/armored-archer/backend && go test -v ./internal/rpg/ -run "Property" -timeout 30s 2>&1 | grep -c "PASS: Test.*Property"</automated>
  </verify>
  <done>
    8 progression property tests pass with 100% success rate. File rpg_property_test.go exists with 205+ lines.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run matchmaking property tests</name>
  <files>backend/internal/modules/matchmaking_property_test.go</files>
  <read_first>backend/internal/modules/matchmaking_property_test.go</read_first>
  <action>
    Run all matchmaking property tests to verify skill fairness, algorithm properties, and distribution invariants.

    Execute: go test -v ./internal/modules/ -run "Property" -timeout 30s

    Expected output: 7 PASS results for:
    - TestMatchmakingProperty_SkillFairness
    - TestMatchmakingProperty_SkillSymmetry
    - TestMatchmakingProperty_OrderIndependence
    - TestMatchmakingProperty_Identity
    - TestMatchmakingProperty_DistributionFairness
    - TestMatchmakingProperty_OutlierHandling
    - TestMatchmakingProperty_EdgeCases

    This verifies PBT-02 (matchmaking invariants) and PBT-05 (±15% tolerance for skill matching).
  </action>
  <verify>
    <automated>cd /home/alex/armored-archer/backend && go test -v ./internal/modules/ -run "Property" -timeout 30s 2>&1 | grep -c "PASS: Test.*Property"</automated>
  </verify>
  <done>
    7 matchmaking property tests pass with 100% success rate. File matchmaking_property_test.go exists with 295+ lines.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run inventory property tests</name>
  <files>backend/internal/rpc/rpc_property_test.go</files>
  <read_first>backend/internal/rpc/rpc_property_test.go</read_first>
  <action>
    Run all inventory property tests to verify slot uniqueness, rarity hierarchy, and item constraint invariants.

    Execute: go test -v ./internal/rpc/ -run "Property" -timeout 30s

    Expected output: 8 PASS results for:
    - TestInventoryProperty_SlotUniqueness
    - TestInventoryProperty_SlotTypeConstraint
    - TestInventoryProperty_OverwriteSameSlot
    - TestInventoryProperty_RarityHierarchy
    - TestInventoryProperty_RarityMonotonic
    - TestInventoryProperty_EmptyInventory
    - TestInventoryProperty_FullInventory
    - TestInventoryProperty_DuplicateGear

    This verifies PBT-03 (inventory invariants) and contributes to PBT-06 (20+ total tests).
  </action>
  <verify>
    <automated>cd /home/alex/armored-archer/backend && go test -v ./internal/rpc/ -run "Property" -timeout 30s 2>&1 | grep -c "PASS: Test.*Property"</automated>
  </verify>
  <done>
    8 inventory property tests pass with 100% success rate. File rpc_property_test.go exists with 435+ lines.
  </done>
</task>

<task type="auto">
  <name>Task 4: Run combat property tests</name>
  <files>backend/internal/combat/combat_property_test.go</files>
  <read_first>backend/internal/combat/combat_property_test.go</read_first>
  <action>
    Run all combat property tests to verify damage calculations, hit chance, and crit rate invariants.

    Execute: go test -v ./internal/combat/ -run "Property" -timeout 30s

    Expected output: 6 PASS results for:
    - TestDamageProperty_NonNegative
    - TestDamageProperty_Deterministic
    - TestDamageProperty_DefenseReducesDamage
    - TestDamageProperty_CritIncreasesDamage
    - TestHitChanceProperty_Bounded
    - TestCritProperty_Statistical

    This verifies combat invariants and contributes to PBT-06 (20+ total tests).
  </action>
  <verify>
    <automated>cd /home/alex/armored-archer/backend && go test -v ./internal/combat/ -run "Property" -timeout 30s 2>&1 | grep -c "PASS: Test.*Property"</automated>
  </verify>
  <done>
    6 combat property tests pass with 100% success rate. File combat_property_test.go exists.
  </done>
</task>

<task type="auto">
  <name>Task 5: Run RNG property tests</name>
  <files>backend/internal/rng/rng_property_test.go</files>
  <read_first>backend/internal/rng/rng_property_test.go</read_first>
  <action>
    Run all RNG property tests to verify random number generation invariants.

    Execute: go test -v ./internal/rng/ -run "Property" -timeout 30s

    Expected output: 7 PASS results for:
    - TestRollIntProperty_WithinBounds
    - TestRollIntProperty_UniformDistribution
    - TestRollFloatProperty_WithinBounds
    - TestRollChoiceProperty_FromSlice
    - TestShuffleProperty_Permutation
    - TestSeedProperty_DifferentSequences
    - TestSeedProperty_SameSequence

    This verifies RNG invariants and contributes to PBT-06 (20+ total tests).
  </action>
  <verify>
    <automated>cd /home/alex/armored-archer/backend && go test -v ./internal/rng/ -run "Property" -timeout 30s 2>&1 | grep -c "PASS: Test.*Property"</automated>
  </verify>
  <done>
    7 RNG property tests pass with 100% success rate. File rng_property_test.go exists.
  </done>
</task>

<task type="auto">
  <name>Task 6: Verify total property test count and PBT-04</name>
  <files></files>
  <read_first></read_first>
  <action>
    Count all property test functions across all packages and verify PBT-04 (testing/quick usage).

    Count property tests:
    grep "^func Test.*Property" backend/internal/**/*property_test.go | wc -l

    Expected result: 31 total property tests (8 + 7 + 8 + 6 + 7)

    Verify testing/quick usage:
    grep -c "testing/quick" backend/internal/combat/combat_property_test.go

    Expected result: combat_property_test.go imports testing/quick (used for TestDamageProperty_CritIncreasesDamage)

    Note: Other property tests use explicit loops with fixed seeds per 15-CONTEXT.md decision, which is acceptable for PBT-04 requirement.
  </action>
  <verify>
    <automated>grep -h "^func Test.*Property" /home/alex/armored-archer/backend/internal/**/*property_test.go | wc -l</automated>
  </verify>
  <done>
    31 total property tests verified. testing/quick used in combat tests. PBT-06 (20+ total) and PBT-04 (testing/quick for simple invariants) satisfied.
  </done>
</task>

</tasks>

<verification>
All 31 property tests pass:
- Progression (8 tests): XP non-negativity, source consistency, level monotonicity, quadratic scaling, ability points grant, max level cap, stat conservation, valid stats only
- Matchmaking (7 tests): Skill fairness, symmetry, order independence, identity, distribution fairness, outlier handling, edge cases
- Inventory (8 tests): Slot uniqueness, type constraints, overwrite behavior, rarity hierarchy, rarity monotonic, empty/full inventory, duplicate gear
- Combat (6 tests): Damage non-negative, deterministic, defense reduction, crit increase, hit chance bounded, crit statistical
- RNG (7 tests): RollInt bounds/uniform, RollFloat bounds, RollChoice from slice, Shuffle permutation, Seed different sequences, Seed same sequence
</verification>

<success_criteria>
- All 31 property tests execute and pass with 100% success rate
- Property test count verified: 31 across 5 packages
- PBT-01 satisfied: 8 progression invariant tests exist and pass
- PBT-02 satisfied: 7 matchmaking invariant tests exist and pass with ±15% tolerance
- PBT-03 satisfied: 8 inventory invariant tests exist and pass
- PBT-04 satisfied: testing/quick used for simple invariants (combat tests), explicit loops used elsewhere per CONTEXT decision
- PBT-05 satisfied: rapid library evaluation deferred per 15-CONTEXT.md decision (sufficient for current scope)
- PBT-06 satisfied: 31 total property tests (exceeds 20+ target)
</success_criteria>

<output>
After completion, create `.planning/phases/22-verify-phase-15/22-01-SUMMARY.md` documenting test results and verification.
</output>
