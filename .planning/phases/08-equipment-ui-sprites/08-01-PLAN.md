---
phase: 08-equipment-ui-sprites
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements:
  - EQUIP-01
  - EQUIP-02
  - EQUIP-03
  - EQUIP-04
  - EQUIP-05

must_haves:
  truths:
    - "Equipment sprites exist for all gear types (bow, arrow, armor, helm, amulet)"
    - "Equipment sprites have different rarities (common, rare, epic, legendary)"
    - "UI icons exist for inventory display"
    - "Each equipment type has multiple variants"
  artifacts:
    - path: "assets/sprites/equipment/bow/bow_sprites.tres"
      provides: "SpriteFrames with bow sprites (4 types × 3 states)"
    - path: "assets/sprites/equipment/arrow/arrow_sprites.tres"
      provides: "SpriteFrames with arrow sprites (multiple types)"
    - path: "assets/sprites/equipment/armor/armor_sprites.tres"
      provides: "SpriteFrames with armor sprites (multiple types)"
    - path: "assets/sprites/equipment/helm/helm_sprites.tres"
      provides: "SpriteFrames with helm sprites (multiple types)"
    - path: "assets/sprites/ui/icons.tres"
      provides: "SpriteFrames with UI icons"

---

<objective>
Create equipment and UI sprite placeholders for all gear types.

Purpose: Provide sprite foundation for equipment system - placeholders use colored rectangles. Equipment sprites are typically static (no complex animation states like characters).

Output: SpriteFrames resources for each equipment type in assets/sprites/equipment/
</objective>

<context>
@/home/alex/armored-archer/.planning/phases/08-equipment-ui-sprites/spritespec/EQUIPMENT-UI-SPRITE-SPEC.md
@/home/alex/armored-archer/assets/sprites/equipment/
@/home/alex/armored-archer/assets/sprites/kenney/interface/ (206 interface elements)
@/home/alex/armored-archer/assets/sprites/kenney/weapons/ (40 weapon sprites)

# Equipment types from spec:
# Bows: Wooden (common), Composite (rare), Elven (epic), Dragon (legendary)
# Arrows: Wooden, Iron, Steel, Silver, Enchanted
# Armor: Leather, Chain, Plate, Dragon
# Helms: Leather, Chain, Plate, Dragon
# Amulets: Health, Mana, Speed, Strength

# Each equipment item typically has one sprite (static icon)
# No complex animation states needed like characters
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create bow sprite placeholders</name>
  <files>assets/sprites/equipment/bow/*.tres</files>
  <action>
Create placeholder sprites for bow types:

For each bow type (4 types × 1 sprite each = 4 files):
- wooden_bow.tres (common) - brown
- composite_bow.tres (rare) - brown with red
- elven_bow.tres (epic) - green with gold
- dragon_bow.tres (legendary) - red with orange

Use PlaceholderTexture2D with 48x48 size.
  </action>
  <verify>
ls assets/sprites/equipment/bow/*.res | wc -l
# Should return 4 or more
  </verify>
  <done>4 bow sprites created</done>
</task>

<task type="auto">
  <name>Task 2: Create arrow sprite placeholders</name>
  <files>assets/sprites/equipment/arrow/*.tres</files>
  <action>
Create placeholder sprites for arrow types:

For each arrow type (5 types × 1 sprite each = 5 files):
- wooden_arrow.tres (common) - wood brown
- iron_arrow.tres (common) - gray
- steel_arrow.tres (rare) - silver
- silver_arrow.tres (rare) - silver with blue
- enchanted_arrow.tres (epic) - purple glow

Use PlaceholderTexture2D with 16x16 or 32x32 size.
  </action>
  <verify>
ls assets/sprites/equipment/arrow/*.res | wc -l
# Should return 5 or more
  </verify>
  <done>5 arrow sprites created</done>
</task>

<task type="auto">
  <name>Task 3: Create armor sprite placeholders</name>
  <files>assets/sprites/equipment/armor/*.tres</files>
  <action>
Create placeholder sprites for armor types:

For each armor type (4 types × 1 sprite each = 4 files):
- leather_armor.tres (common) - brown
- chain_armor.tres (rare) - silver/gray
- plate_armor.tres (epic) - silver with blue
- dragon_armor.tres (legendary) - red with gold

Use PlaceholderTexture2D with 48x48 size.
  </action>
  <verify>
ls assets/sprites/equipment/armor/*.res | wc -l
# Should return 4 or more
  </verify>
  <done>4 armor sprites created</done>
</task>

<task type="auto">
  <name>Task 4: Create helm sprite placeholders</name>
  <files>assets/sprites/equipment/helm/*.tres</files>
  <action>
Create placeholder sprites for helm types:

For each helm type (4 types × 1 sprite each = 4 files):
- leather_helm.tres (common) - brown
- chain_helm.tres (rare) - silver/gray
- plate_helm.tres (epic) - silver with blue
- dragon_helm.tres (legendary) - red with gold

Use PlaceholderTexture2D with 32x32 size.
  </action>
  <verify>
ls assets/sprites/equipment/helm/*.res | wc -l
# Should return 4 or more
  </verify>
  <done>4 helm sprites created</done>
</task>

<task type="auto">
  <name>Task 5: Create UI icon placeholders</name>
  <files>assets/sprites/ui/*.tres</files>
  <action>
Create placeholder sprites for UI icons:

Common UI icons (10+ icons):
- health_icon.tres - red heart
- mana_icon.tres - blue orb
- speed_icon.tres - yellow lightning
- strength_icon.tres - red fist
- inventory_icon.tres - brown bag
- equipment_icon.tres - armor symbol
- quest_icon.tres - scroll
- map_icon.tres - map symbol
- settings_icon.tres - gear
- close_icon.tres - X mark

Use PlaceholderTexture2D with 32x32 size.
  </action>
  <verify>
ls assets/sprites/ui/*.res | wc -l
# Should return 10 or more
  </verify>
  <done>10+ UI icon sprites created</done>
</task>

<task type="auto">
  <name>Task 6: Create SpriteFrames for equipment</name>
  <files>assets/sprites/equipment/*/equipment_sprites.tres</files>
  <action>
Create SpriteFrames resources for each equipment type:

1. assets/sprites/equipment/bow/bow_sprites.tres - all bow sprites
2. assets/sprites/equipment/arrow/arrow_sprites.tres - all arrow sprites
3. assets/sprites/equipment/armor/armor_sprites.tres - all armor sprites
4. assets/sprites/equipment/helm/helm_sprites.tres - all helm sprites

Each SpriteFrames contains the sprites as single-frame animations.
  </action>
  <verify>
ls assets/sprites/equipment/*/*sprites.tres | wc -l
# Should return 4
  </verify>
  <done>SpriteFrames resources created for all equipment types</done>
</task>

</tasks>

<verification>
- [ ] Bow sprites exist (4 types)
- [ ] Arrow sprites exist (5 types)
- [ ] Armor sprites exist (4 types)
- [ ] Helm sprites exist (4 types)
- [ ] UI icons exist (10+ icons)
- [ ] SpriteFrames created for equipment types
- [ ] Each equipment type has different colors for rarity distinction
</verification>

<success_criteria>
Equipment and UI sprite placeholders are ready for use. All equipment types have placeholder sprites with visual distinction by rarity.
</success_criteria>

<output>
Created sprites:
- 4 bow types (common, rare, epic, legendary)
- 5 arrow types
- 4 armor types
- 4 helm types
- 10+ UI icons
- SpriteFrames resources for equipment types
</output>