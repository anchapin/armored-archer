---
phase: 18-rpc-handler-coverage
plan: 02
type: test
wave: 2
depends_on: [18-01]
files_modified: [backend/tests/rpc/rpc_handler_test.go, backend/tests/rpc/feedback_handler_test.go]
---

# Phase 18-02 Plan: Core RPC Handler Unit Tests

## Objective
Implement unit tests for the primary RPC handlers in `rpc.go` and `feedback.go` to achieve initial coverage targets.

## Tasks

### 1. Test Player Stats and Inventory
- Create `backend/tests/rpc/rpc_handler_test.go`.
- Implement `TestGetPlayerStats`:
  - Success case (existing stats).
  - Success case (new player, creates default stats).
  - Error case (unauthenticated).
- Implement `TestGetInventory`:
  - Success case (existing items).
  - Success case (empty inventory).

### 2. Test Feedback RPCs
- Create `backend/tests/rpc/feedback_handler_test.go`.
- Implement `TestSubmitFeedback`:
  - Valid submission.
  - Validation errors (missing category, title).
- Implement `TestListFeedback`:
  - Retrieval with filters.
  - Sorting and pagination logic.
- Implement `TestVoteFeedback`:
  - Successful vote.
  - Duplicate vote prevention.

## Success Criteria
- [ ] `internal/rpc` package coverage reaches **50%+**.
- [ ] At least 10 unique test cases covering various RPC flows.
- [ ] All tests pass against `testcontainers-go` database.
