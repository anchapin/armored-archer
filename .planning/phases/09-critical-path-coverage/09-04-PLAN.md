---
phase: 09-critical-path-coverage
plan: 04
type: execute
wave: 2
depends_on: ["01", "02", "03"]
files_modified: [backend/tests/quality/coverage_gates.sh]
autonomous: false
requirements: [CRIT-04]
user_setup: []

must_haves:
  truths:
    - "Critical path coverage threshold (80%) is enforced before overall 60% target"
    - "Coverage gates script checks combat, matchmaking, and rpg packages individually"
    - "CI/CD prevents merging when any critical path is below 80%"
    - "Quality gates enforce at least one assertion per test"
  artifacts:
    - path: "backend/tests/quality/coverage_gates.sh"
      provides: "Coverage threshold enforcement for critical paths"
    - path: "backend/coverage/coverage.out"
      provides: "Coverage report for measurement"
  key_links:
    - from: "backend/tests/quality/coverage_gates.sh"
      to: "coverage/coverage.out"
      via: "go tool cover -func parsing"
      pattern: "go tool cover"
---

<objective>
Verify and enforce 80% critical path coverage threshold across combat, matchmaking, and RPG systems

Purpose: Before allowing overall 60% coverage target, ensure the highest-risk systems (critical path) are thoroughly tested. This gate prevents partial coverage that misses high-risk bugs while rewarding complete testing of critical systems.

Output: Updated CI/CD coverage gate that enforces 80% threshold for critical path packages before overall 60% enforcement
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-critical-path-coverage/09-RESEARCH.md
@backend/tests/quality/coverage_gates.sh
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify critical path coverage gate enforcement</name>
  <action>Verify the coverage gate script properly enforces 80% threshold for combat, matchmaking, and rpg packages. This is a manual verification task to confirm the automated coverage system works correctly and provides clear output for debugging when thresholds are not met.</action>
  <what-built>Critical path coverage verification system from Phase 8</what-built>
  <how-to-verify>
    1. Generate coverage report for all three critical path packages:
       cd backend && go test -coverprofile=coverage/coverage.out ./internal/combat ./internal/matchmaking ./internal/rpg ./tests/combat ./tests/matchmaking ./tests/rpg

    2. Run coverage gates script with critical flag:
       bash backend/tests/quality/coverage_gates.sh critical

    3. Review output to verify:
       - Each package (combat, matchmaking, rpg) shows coverage percentage
       - 80% threshold is enforced for each package
       - Overall coverage is calculated and displayed
       - Gates pass only when all critical packages meet 80% threshold
       - Error message clearly indicates which package(s) failed threshold

    4. If any critical package is below 80%:
       - Note which package(s) need more tests
       - This is expected during execution (plans 01-03 adding tests)
       - After plans 01-03 complete, re-run verification

  </how-to-verify>
  <verify>
    <automated>cd backend && go test -v ./internal/combat ./internal/matchmaking ./internal/rpg ./tests/combat ./tests/matchmaking ./tests/rpg && bash backend/tests/quality/coverage_gates.sh critical</automated>
  </verify>
  <done>Human confirms coverage gate system correctly enforces 80% threshold for critical path packages with clear error messaging when thresholds are not met</done>
  <resume-signal>Type "approved" to confirm coverage gate system works correctly, or describe issues found</resume-signal>
</task>

</tasks>

<verification>
After plan completion:

1. Verify coverage gate script is functional:
   - Script exists at backend/tests/quality/coverage_gates.sh
   - Script accepts "critical" argument
   - Script generates per-package coverage

2. Verify critical path threshold enforcement:
   - 80% threshold is defined in script
   - Combat package checked individually
   - Matchmaking package checked individually
   - RPG package checked individually
   - Gate fails if any package < 80%
   - Overall coverage displayed when all pass

3. Confirm CRIT-04 requirement met:
   - Critical path threshold enforced before overall 60% target
   - Quality gates prevent partial coverage
   - CI/CD can use script for enforcement

Success criteria:
- Coverage gate enforces 80% for combat, matchmaking, rpg
- Script provides clear output for each package
- Gate behavior documented and verified
- CRIT-04 marked complete in REQUIREMENTS.md
</verification>

<success_criteria>
CRIT-04 achieved when:
1. Coverage gate script checks critical path packages individually
2. 80% threshold is enforced for each package
3. Overall 60% target is NOT enforced until critical paths pass
4. Verification confirms gate behavior works as expected
5. Quality gate system prevents partial coverage gaming
</success_criteria>

<output>
After completion, create `.planning/phases/09-critical-path-coverage/09-04-SUMMARY.md`
</output>
