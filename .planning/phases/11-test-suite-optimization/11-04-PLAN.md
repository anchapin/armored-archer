---
phase: 11-test-suite-optimization
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/benchmarks/combat_benchmark_test.go
  - backend/tests/benchmarks/matchmaking_benchmark_test.go
  - backend/tests/benchmarks/rpg_benchmark_test.go
autonomous: true
requirements: ["OPT-04"]
user_setup: []

must_haves:
  truths:
    - "Go benchmarks created for critical paths (combat, matchmaking, RPG)"
    - "Benchmarks track execution time and memory allocations"
  artifacts:
    - path: "backend/tests/benchmarks/combat_benchmark_test.go"
      provides: "Combat system performance benchmarks"
      contains: "BenchmarkCalculateDamage"
    - path: "backend/tests/benchmarks/matchmaking_benchmark_test.go"
      provides: "Matchmaking system performance benchmarks"
      contains: "BenchmarkFindMatch"
    - path: "backend/tests/benchmarks/rpg_benchmark_test.go"
      provides: "RPG/Progression system performance benchmarks"
      contains: "BenchmarkCalculateXP"
  key_links:
    - from: "backend/tests/benchmarks/combat_benchmark_test.go"
      to: "backend/internal/combat"
      via: "Import combat module for benchmarking"
      pattern: "armored-archer/internal/combat"
    - from: "backend/tests/benchmarks/matchmaking_benchmark_test.go"
      to: "backend/internal/matchmaking"
      via: "Import matchmaking module for benchmarking"
      pattern: "armored-archer/internal/matchmaking"
    - from: "backend/tests/benchmarks/rpg_benchmark_test.go"
      to: "backend/internal/rpg"
      via: "Import RPG module for benchmarking"
      pattern: "armored-archer/internal/rpg"
---

<objective>
Create performance benchmarks for critical paths (combat, matchmaking, RPG)

Purpose: Establish performance baseline for critical systems by creating Go benchmark files that measure execution time and memory allocations for key functions
Output: Three benchmark files (combat, matchmaking, RPG) with standard Go benchmark conventions
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-test-suite-optimization/11-RESEARCH.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Existing benchmark infrastructure (from Makefile)
```makefile
benchmark:
	@echo "$(BLUE)Running Go benchmarks...$(RESET)"
	cd $(BACKEND_DIR) && go test -bench=. -benchmem -run=^$$ ./internal/rpc/...
```

# Existing benchmark directory (from backend/tests/benchmarks/)
# - baseline.txt
# - compare.sh
# - new.txt
# - comparison.txt

# Go benchmark pattern (from RESEARCH.md)
```go
func BenchmarkCalculateDamage(b *testing.B) {
    attacker := NewTestPlayer(attackerStats)
    defender := NewTestPlayer(defenderStats)

    b.ResetTimer()
    b.ReportAllocs()

    for i := 0; i < b.N; i++ {
        CalculateDamage(attacker, defender)
    }
}
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create combat system benchmarks</name>
  <files>backend/tests/benchmarks/combat_benchmark_test.go</files>
  <read_first>
    - backend/tests/combat/combat_test.go
    - backend/internal/combat/combat.go
  </read_first>
  <action>
    Create backend/tests/benchmarks/combat_benchmark_test.go with performance benchmarks for combat system:

    ```go
    package benchmarks

    import (
        "testing"
        "armored-archer/internal/combat"
        "armored-archer/tests/testhelpers"
    )

    // BenchmarkCalculateDamage benchmarks the damage calculation function
    func BenchmarkCalculateDamage(b *testing.B) {
        attacker := testhelpers.NewTestPlayerWithStats(100, 50, 20)
        defender := testhelpers.NewTestPlayerWithStats(100, 50, 20)

        b.ResetTimer()
        b.ReportAllocs()

        for i := 0; i < b.N; i++ {
            _ = combat.CalculateDamage(attacker, defender)
        }
    }

    // BenchmarkCalculateCombat benchmarks full combat simulation
    func BenchmarkCalculateCombat(b *testing.B) {
        attacker := testhelpers.NewTestPlayerWithStats(100, 50, 20)
        defender := testhelpers.NewTestPlayerWithStats(100, 50, 20)

        b.ResetTimer()
        b.ReportAllocs()

        for i := 0; i < b.N; i++ {
            _ = combat.SimulateCombatRound(attacker, defender)
        }
    }

    // BenchmarkApplyEffect benchmarks effect application
    func BenchmarkApplyEffect(b *testing.B) {
        player := testhelpers.NewTestPlayer()
        effect := combat.NewEffect("damage", 10, 1)

        b.ResetTimer()
        b.ReportAllocs()

        for i := 0; i < b.N; i++ {
            _ = combat.ApplyEffect(player, effect)
        }
    }

    // BenchmarkValidateCombat benchmarks combat validation
    func BenchmarkValidateCombat(b *testing.B) {
        state := testhelpers.NewTestCombatState()

        b.ResetTimer()
        b.ReportAllocs()

        for i := 0; i < b.N; i++ {
            _ = combat.ValidateState(state)
        }
    }
    ```

    Ensure benchmark file follows Go conventions:
    - Package: benchmarks
    - Functions named Benchmark* with *testing.B parameter
    - b.ResetTimer() called after setup
    - b.ReportAllocs() for memory tracking
    - Use testhelpers for test data
  </action>
  <verify>
    <automated>test -f backend/tests/benchmarks/combat_benchmark_test.go && grep -q "BenchmarkCalculateDamage" backend/tests/benchmarks/combat_benchmark_test.go</automated>
  </verify>
  <done>
    - combat_benchmark_test.go created in backend/tests/benchmarks/
    - Benchmarks for: CalculateDamage, CalculateCombat, ApplyEffect, ValidateCombat
    - All benchmarks use b.ResetTimer() and b.ReportAllocs()
    - Uses testhelpers for test data
  </done>
</task>

<task type="auto">
  <name>Task 2: Create matchmaking benchmarks</name>
  <files>backend/tests/benchmarks/matchmaking_benchmark_test.go</files>
  <read_first>
    - backend/tests/matchmaking/matchmaking_test.go
    - backend/internal/matchmaking/matchmaking.go
  </read_first>
  <action>
    Create backend/tests/benchmarks/matchmaking_benchmark_test.go with performance benchmarks for matchmaking system:

    ```go
    package benchmarks

    import (
        "testing"
        "armored-archer/internal/matchmaking"
        "armored-archer/tests/testhelpers"
    )

    // BenchmarkFindMatch benchmarks match finding with small pool
    func BenchmarkFindMatch(b *testing.B) {
        players := testhelpers.NewTestPlayers(100)
        criteria := testhelpers.NewTestMatchCriteria()

        b.ResetTimer()
        b.ReportAllocs()

        for i := 0; i < b.N; i++ {
            _ = matchmaking.FindMatch(players, criteria)
        }
    }

    // BenchmarkFindMatchLargePool benchmarks match finding with large pool
    func BenchmarkFindMatchLargePool(b *testing.B) {
        players := testhelpers.NewTestPlayers(1000)
        criteria := testhelpers.NewTestMatchCriteria()

        b.ResetTimer()
        b.ReportAllocs()

        for i := 0; i < b.N; i++ {
            _ = matchmaking.FindMatch(players, criteria)
        }
    }

    // BenchmarkFilterMatches benchmarks match filtering
    func BenchmarkFilterMatches(b *testing.B) {
        players := testhelpers.NewTestPlayers(500)
        criteria := testhelpers.NewTestMatchCriteria()

        b.ResetTimer()
        b.ReportAllocs()

        for i := 0; i < b.N; i++ {
            _ = matchmaking.FilterMatches(players, criteria)
        }
    }

    // BenchmarkCalculateElo benchmarks Elo rating calculation
    func BenchmarkCalculateElo(b *testing.B) {
        playerElo := 1500.0
        opponentElo := 1600.0

        b.ResetTimer()
        b.ReportAllocs()

        for i := 0; i < b.N; i++ {
            _ = matchmaking.CalculateEloChange(playerElo, opponentElo, true)
        }
    }

    // BenchmarkUpdateRank benchmarks rank update operations
    func BenchmarkUpdateRank(b *testing.B) {
        rankings := testhelpers.NewTestRankings(100)

        b.ResetTimer()
        b.ReportAllocs()

        for i := 0; i < b.N; i++ {
            rankings.UpdatePlayer("player_1", 1500)
        }
    }
    ```

    Ensure benchmark file follows Go conventions:
    - Package: benchmarks
    - Functions named Benchmark* with *testing.B parameter
    - Tests both small and large player pools
    - Tests filtering and rating calculations
  </action>
  <verify>
    <automated>test -f backend/tests/benchmarks/matchmaking_benchmark_test.go && grep -q "BenchmarkFindMatch" backend/tests/benchmarks/matchmaking_benchmark_test.go</automated>
  </verify>
  <done>
    - matchmaking_benchmark_test.go created in backend/tests/benchmarks/
    - Benchmarks for: FindMatch (small/large pool), FilterMatches, CalculateElo, UpdateRank
    - All benchmarks use b.ResetTimer() and b.ReportAllocs()
    - Tests various pool sizes (100, 500, 1000)
  </done>
</task>

<task type="auto">
  <name>Task 3: Create RPG/progression benchmarks</name>
  <files>backend/tests/benchmarks/rpg_benchmark_test.go</files>
  <read_first>
    - backend/tests/rpg/rpg_test.go
    - backend/internal/rpg/rpg.go
  </read_first>
  <action>
    Create backend/tests/benchmarks/rpg_benchmark_test.go with performance benchmarks for RPG progression system:

    ```go
    package benchmarks

    import (
        "testing"
        "armored-archer/internal/rpg"
        "armored-archer/tests/testhelpers"
    )

    // BenchmarkCalculateXP benchmarks XP calculation
    func BenchmarkCalculateXP(b *testing.B) {
        player := testhelpers.NewTestPlayer()
        baseXP := 100

        b.ResetTimer()
        b.ReportAllocs()

        for i := 0; i < b.N; i++ {
            _ = rpg.CalculateXP(player, baseXP)
        }
    }

    // BenchmarkCalculateLevel benchmarks level up calculation
    func BenchmarkCalculateLevel(b *testing.B) {
        totalXP := 5000

        b.ResetTimer()
        b.ReportAllocs()

        for i := 0; i < b.N; i++ {
            _ = rpg.CalculateLevel(totalXP)
        }
    }

    // BenchmarkAllocatePoints benchmarks stat point allocation
    func BenchmarkAllocatePoints(b *testing.B) {
        player := testhelpers.NewTestPlayerWithPoints(5)

        b.ResetTimer()
        b.ReportAllocs()

        for i := 0; i < b.N; i++ {
            _ = rpg.AllocateStatPoints(player, "strength", 1)
        }
    }

    // BenchmarkCheckLevel benchmarks level check operation
    func BenchmarkCheckLevel(b *testing.B) {
        player := testhelpers.NewTestPlayer()
        requiredLevel := 10

        b.ResetTimer()
        b.ReportAllocs()

        for i := 0; i < b.N; i++ {
            _ = rpg.CheckLevelRequirement(player, requiredLevel)
        }
    }

    // BenchmarkGetProgress benchmarks progress calculation
    func BenchmarkGetProgress(b *testing.B) {
        player := testhelpers.NewTestPlayer()

        b.ResetTimer()
        b.ReportAllocs()

        for i := 0; i < b.N; i++ {
            _ = rpg.GetProgress(player)
        }
    }
    ```

    Ensure benchmark file follows Go conventions:
    - Package: benchmarks
    - Functions named Benchmark* with *testing.B parameter
    - Tests common RPG operations: XP, leveling, stat allocation
    - Uses testhelpers for test data
  </action>
  <verify>
    <automated>test -f backend/tests/benchmarks/rpg_benchmark_test.go && grep -q "BenchmarkCalculateXP" backend/tests/benchmarks/rpg_benchmark_test.go</automated>
  </verify>
  <done>
    - rpg_benchmark_test.go created in backend/tests/benchmarks/
    - Benchmarks for: CalculateXP, CalculateLevel, AllocatePoints, CheckLevel, GetProgress
    - All benchmarks use b.ResetTimer() and b.ReportAllocs()
    - Tests common RPG progression operations
  </done>
</task>

</tasks>

<verification>
1. Verify combat benchmarks: test -f backend/tests/benchmarks/combat_benchmark_test.go
2. Verify matchmaking benchmarks: test -f backend/tests/benchmarks/matchmaking_benchmark_test.go
3. Verify RPG benchmarks: test -f backend/tests/benchmarks/rpg_benchmark_test.go
4. Run benchmarks: cd backend && go test -bench=. -benchmem benchmarks/
</verification>

<success_criteria>
1. Benchmark files created for combat, matchmaking, and RPG systems
2. All benchmarks measure execution time and memory allocations
3. All benchmarks follow Go naming conventions (Benchmark*)
4. All benchmarks use b.ResetTimer() and b.ReportAllocs()
5. Benchmarks can be run with standard go test -bench command
</success_criteria>

<output>
After completion, create `.planning/phases/11-test-suite-optimization/11-04-SUMMARY.md`
</output>
