---
phase: 16-go-coverage-to-60
plan: 05
type: execute
wave: 1
depends_on: []
files_modified: [backend/tests/cache/provider_test.go]
autonomous: true
gap_closure: true
requirements: [COV-01]
user_setup: []
must_haves:
  truths:
    - "Cache provider package functions are tested with mocked CacheManager"
    - "Global cache initialization and retrieval are tested"
    - "Test cache setter for unit tests works correctly"
    - "Test cache reset for unit tests works correctly"
    - "Thread-safe global cache operations are verified"
  artifacts:
    - path: "backend/tests/cache/provider_test.go"
      provides: "Cache provider package test coverage"
      min_lines: 150
  key_links:
    - from: "backend/tests/cache/provider_test.go"
      to: "backend/internal/cache/provider.go"
      via: "utils.CacheManager interface mocking"
      pattern: "cache\\."
    - from: "backend/tests/cache/provider_test.go"
      to: "backend/internal/utils/cache.go"
      via: "mock or real CacheManager for testing"
      pattern: "utils\\.NewCacheManager"
---

<objective>
Increase cache provider package coverage from 0% to 90%+ using utils.CacheManager (or mock), testing global cache initialization, retrieval, test utility functions, and thread-safe operations.

Purpose: The cache provider package has 0% coverage and manages global cache state used throughout the application. Testing requires using utils.CacheManager or creating a mock interface.

Output: backend/tests/cache/provider_test.go with comprehensive coverage of cache provider functions
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/16-go-coverage-to-60/16-VERIFICATION.md
@.planning/phases/16-go-coverage-to-60/16-CONTEXT.md
@.planning/phases/16-go-coverage-to-60/16-RESEARCH.md

# Key dependencies
@backend/internal/cache/provider.go - Global cache management with 4 untested functions
@backend/internal/utils/cache.go - CacheManager implementation (88.5% coverage)
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From backend/internal/cache/provider.go:
```go
var globalCache *utils.CacheManager
var globalCacheLock sync.RWMutex

func InitGlobalCache(logger runtime.Logger)
func GetGlobalCache() *utils.CacheManager
func SetTestCache(cache *utils.CacheManager)
func ResetTestCache()
```

From backend/internal/utils/cache.go (88.5% coverage):
```go
type CacheManager struct {
    caches map[string]*Cache
    lock   sync.RWMutex
    logger runtime.Logger
}

func NewCacheManager(logger runtime.Logger) *CacheManager
func (cm *CacheManager) Get(cacheName, key string) (interface{}, bool)
func (cm *CacheManager) Set(cacheName, key string, value interface{}, ttl int64)
func (cm *CacheManager) Delete(cacheName, key string)
func (cm *CacheManager) Clear(cacheName string)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create cache provider test file with setup</name>
  <files>backend/tests/cache/provider_test.go</files>
  <read_first>
    - backend/internal/cache/provider.go - To understand global cache functions
    - backend/internal/utils/cache.go - To understand CacheManager usage
  </read_first>
  <action>
    Create backend/tests/cache/provider_test.go with:

    1. Test setup and teardown:
       ```go
       func setupTest(t *testing.T) *utils.CacheManager {
           cache.SetTestCache(nil) // Reset global cache
           mockLogger := &testhelpers.MockLogger{}
           return utils.NewCacheManager(mockLogger)
       }

       func TestMain(m *testing.M) {
           cache.SetTestCache(nil)
           os.Exit(m.Run())
       }
       ```

    2. Test InitGlobalCache:
       - Test initial call initializes globalCache
       - Test subsequent calls do not reinitialize (check globalCache != nil)
       - Test globalCacheLock is used for thread safety
       - Verify logger parameter is passed to utils.NewCacheManager

    3. Test GetGlobalCache:
       - Test returns nil when globalCache not initialized
       - Test returns globalCache when initialized
       - Test globalCacheLock.RLock/RLock is used for read access
  </action>
  <verify>
    <automated>cd /home/alex/armored-archer/backend && go test -v ./tests/cache/... -run "TestInitGlobalCache|TestGetGlobalCache"</automated>
  </verify>
  <done>InitGlobalCache creates cache singleton, GetGlobalCache returns cache or nil, thread-safe with locks</done>
</task>

<task type="auto">
  <name>Task 2: Test SetTestCache and ResetTestCache utilities</name>
  <files>backend/tests/cache/provider_test.go</files>
  <read_first>
    - backend/tests/cache/provider_test.go - To build on existing tests
    - backend/internal/cache/provider.go - To understand test utility functions
  </read_first>
  <action>
    Add tests to backend/tests/cache/provider_test.go for test utilities:

    1. Test SetTestCache:
       - Test sets globalCache to provided cache manager
       - Test globalCacheLock.Lock is used for write access
       - Test overwrites existing globalCache
       - Test can set nil cache

    2. Test ResetTestCache:
       - Test clears globalCache (sets to nil)
       - Test globalCacheLock.Lock is used for write access
       - Test GetGlobalCache returns nil after reset
       - Test can call ResetTestCache multiple times safely

    3. Test test cache isolation:
       - Test SetTestCache does not affect InitGlobalCache behavior
       - Test ResetTestCache clears SetTestCache value
       - Test multiple test runs don't interfere with each other
  </action>
  <verify>
    <automated>cd /home/alex/armored-archer/backend && go test -v ./tests/cache/... -run "TestSetTestCache|TestResetTestCache"</automated>
  </verify>
  <done>SetTestCache sets global cache for testing, ResetTestCache clears it, isolation verified</done>
</task>

<task type="auto">
  <name>Task 3: Test thread-safe global cache operations</name>
  <files>backend/tests/cache/provider_test.go</files>
  <read_first>
    - backend/tests/cache/provider_test.go - To build on existing tests
    - backend/internal/cache/provider.go - To understand thread-safety
  </read_first>
  <action>
    Add tests to backend/tests/cache/provider_test.go for thread-safety:

    1. Test concurrent InitGlobalCache calls:
       - Launch multiple goroutines calling InitGlobalCache
       - Verify only one initialization occurs
       - Verify globalCache is not nil after all goroutines complete

    2. Test concurrent GetGlobalCache calls:
       - Initialize global cache
       - Launch multiple goroutines calling GetGlobalCache
       - Verify all goroutines get the same cache instance
       - Verify no race conditions (use go test -race)

    3. Test concurrent SetTestCache calls:
       - Launch multiple goroutines calling SetTestCache with different caches
       - Verify final globalCache is one of the set values
       - Verify no race conditions (use go test -race)

    4. Test concurrent mix of operations:
       - Launch goroutines calling InitGlobalCache, GetGlobalCache, SetTestCache, ResetTestCache
       - Verify operations complete without deadlock
       - Verify no race conditions (use go test -race)
  </action>
  <verify>
    <automated>cd /home/alex/armored-archer/backend && go test -race -v ./tests/cache/... -run "TestConcurrent"</automated>
  </verify>
  <done>All global cache operations are thread-safe, no race conditions detected under concurrent access</done>
</task>

<task type="auto">
  <name>Task 4: Test cache provider integration with CacheManager</name>
  <files>backend/tests/cache/provider_test.go</files>
  <read_first>
    - backend/tests/cache/provider_test.go - To build on existing tests
    - backend/internal/cache/provider.go - To understand integration points
    - backend/internal/utils/cache.go - To understand CacheManager API
  </read_first>
  <action>
    Add tests to backend/tests/cache/provider_test.go for integration:

    1. Test InitGlobalCache creates functional CacheManager:
       - Call InitGlobalCache with mock logger
       - GetGlobalCache should return non-nil CacheManager
       - Use CacheManager.Set/Get to verify it works
       - Test multiple cache operations work

    2. Test SetTestCache integration:
       - Create a mock CacheManager
       - SetTestCache(mockCache)
       - GetGlobalCache should return mockCache
       - Verify CacheManager methods work on mock

    3. Test ResetTestCache integration:
       - InitGlobalCache
       - ResetTestCache
       - GetGlobalCache should return nil
       - Verify subsequent InitGlobalCache works
  </action>
  <verify>
    <automated>cd /home/alex/armored-archer/backend && go test -v ./tests/cache/... -run "TestIntegration"</automated>
  </verify>
  <done>InitGlobalCache creates working CacheManager, SetTestCache replaces it, ResetTestCache clears it</done>
</task>

<task type="auto">
  <name>Task 5: Verify cache provider package coverage meets 90% target</name>
  <files>backend/tests/cache/provider_test.go</files>
  <read_first>
    - backend/tests/cache/provider_test.go - To verify all tests pass
    - backend/internal/cache/provider.go - To ensure all functions are tested
  </read_first>
  <action>
    Run coverage analysis on cache provider package:

    1. Execute: cd /home/alex/armored-archer/backend && go test -coverprofile=/tmp/cache_provider_coverage.out -covermode=atomic -coverpkg=./internal/cache/... ./tests/cache/...

    2. Check coverage percentage: go tool cover -func=/tmp/cache_provider_coverage.out | grep "^total:"

    3. Verify coverage is at least 90% (target: address gap from 0% to 90%)

    4. If coverage < 90%, identify uncovered lines and add tests

    Expected coverage from tests:
    - InitGlobalCache: 100% (initialization, idempotency, thread-safety)
    - GetGlobalCache: 100% (nil case, initialized case, thread-safety)
    - SetTestCache: 100% (setting, overwriting, nil value)
    - ResetTestCache: 100% (clearing, idempotency)
    - All locking paths tested (Lock/RLock in various scenarios)
  </action>
  <verify>
    <automated>cd /home/alex/armored-archer/backend && go test -coverprofile=/tmp/cache_provider_coverage.out -covermode=atomic -coverpkg=./internal/cache/... ./tests/cache/... && go tool cover -func=/tmp/cache_provider_coverage.out | grep "^total:" | grep -E "total:\s+\(9[0-9]\.0%|100\.0%\)"</automated>
  </verify>
  <done>Cache provider package coverage at 90%+ (from 0%), all 4 functions tested with thread-safety verification</done>
</task>

</tasks>

<verification>
1. All cache provider tests pass: go test -v ./tests/cache/...
2. Cache provider package coverage reaches 90%+: go tool cover -func shows 90%+ overall
3. No race conditions: go test -race passes for concurrent tests
4. Global cache isolation works between tests
</verification>

<success_criteria>
1. Cache provider package coverage increases from 0% to 90%+
2. All 4 functions in provider.go have test coverage
3. Thread-safety verified with concurrent access tests
4. Integration with CacheManager tested and working
5. Test utilities (SetTestCache, ResetTestCache) verified
</success_criteria>

<output>
After completion, create `.planning/phases/16-go-coverage-to-60/16-05-SUMMARY.md`
</output>
