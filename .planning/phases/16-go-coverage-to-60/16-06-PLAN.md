---
phase: 16-go-coverage-to-60
plan: 06
type: execute
wave: 2
depends_on: [16-04, 16-05]
files_modified: [backend/coverage.out, .planning/phases/16-go-coverage-to-60/16-VERIFICATION.md]
autonomous: true
gap_closure: true
requirements: [COV-01, COV-08]
user_setup: []
must_haves:
  truths:
    - "Overall Go coverage reaches 60% target after gap closure"
    - "60% threshold enforcement in CI/CD passes with improved coverage"
    - "Gap analysis shows reduced zero-coverage functions"
    - "All critical packages maintain high coverage (RPG 90%+, Matchmaking 90%+, etc.)"
    - "VERIFICATION.md updated to show gaps closed"
  artifacts:
    - path: "backend/coverage.out"
      provides: "Updated coverage report showing 60%+ overall"
    - path: ".planning/phases/16-go-coverage-to-60/16-VERIFICATION.md"
      provides: "Updated verification report showing gaps closed"
  key_links:
    - from: "backend/tests/logger/logger_test.go"
      to: "backend/coverage.out"
      via: "coverage generation"
      pattern: "go test -coverprofile"
    - from: "backend/tests/cache/provider_test.go"
      to: "backend/coverage.out"
      via: "coverage generation"
      pattern: "go test -coverprofile"
    - from: ".planning/phases/16-go-coverage-to-60/16-VERIFICATION.md"
      to: ".planning/ROADMAP.md"
      via: "phase completion status"
      pattern: "Phase 16.*COMPLETE"
---

<objective>
Verify overall Go coverage reaches 60% target after gap closure plans 16-04 (logger) and 16-05 (cache), update VERIFICATION.md to reflect closed gaps, and validate that 60% CI/CD threshold enforcement will pass.

Purpose: Gap closure plans address utility package coverage gaps. This plan validates the overall impact and updates verification status.

Output: Updated coverage report showing 60%+ overall, updated VERIFICATION.md showing gaps closed
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/16-go-coverage-to-60/16-VERIFICATION.md
@.planning/phases/16-go-coverage-to-60/16-CONTEXT.md
@.planning/phases/16-go-coverage-to-60/16-RESEARCH.md
@.planning/phases/16-go-coverage-to-60/16-01-SUMMARY.md
@.planning/phases/16-go-coverage-to-60/16-02-SUMMARY.md
@.planning/phases/16-go-coverage-to-60/16-03-SUMMARY.md
@.planning/phases/16-go-coverage-to-60/16-04-PLAN.md
@.planning/phases/16-go-coverage-to-60/16-05-PLAN.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From backend/tests/quality/coverage_gates.sh (stage-based enforcement):
```bash
# Usage: COVERAGE_GATE_STAGE=1|2|3 ./coverage_gates.sh
# Stage 1: 45% threshold
# Stage 2: 52.5% threshold
# Stage 3: 60% threshold
# Exits with code 0: pass, 1: overall threshold not met, 2: critical package threshold not met
```

Expected coverage impact from gap closure:
- Logger: 0% -> 80%+ (adds ~1-2% to overall, logger.go is ~265 lines)
- Cache provider: 0% -> 90%+ (adds ~0.5% to overall, provider.go is ~49 lines)
- Expected overall: 39.2% + ~1.5% = ~40.7% (still below 60%)

Note: Overall 60% target may not be reached with just logger + cache coverage. Additional tests or reconsideration of RPC handler testing may be needed. This plan will verify actual impact.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Generate new overall coverage report after gap closure</name>
  <files>backend/coverage.out</files>
  <read_first>
    - backend/coverage.out - Existing coverage profile (if exists)
    - .planning/phases/16-go-coverage-to-60/16-VERIFICATION.md - To verify gap closure status
  </read_first>
  <action>
    Generate fresh coverage report after plans 16-04 and 16-05:

    1. Execute full coverage run:
       ```bash
       cd /home/alex/armored-archer/backend
       go test -coverprofile=coverage.out -covermode=atomic -coverpkg=./internal/... ./... 2>&1 | tail -20
       ```

    2. Extract overall coverage percentage:
       ```bash
       go tool cover -func=coverage.out | grep "^total:"
       ```

    3. Extract package-level coverage for previously low packages:
       ```bash
       go tool cover -func=coverage.out | grep -E "^github.com/anchapin/armored-archer/backend/internal/(logger|cache|config|utils|rpc)/"
       ```

    4. Document coverage results:
       - Overall coverage percentage
       - Logger package coverage
       - Cache provider package coverage
       - Gap to 60% target (if any)
       - Comparison with pre-gap-closure (39.2%)
  </action>
  <verify>
    <automated>cd /home/alex/armored-archer/backend && go test -coverprofile=coverage.out -covermode=atomic -coverpkg=./internal/... ./... && go tool cover -func=coverage.out | grep "^total:"</automated>
  </verify>
  <done>New coverage report generated, overall percentage documented, low-coverage packages analyzed</done>
</task>

<task type="auto">
  <name>Task 2: Verify 60% CI/CD threshold enforcement</name>
  <files>backend/tests/quality/coverage_gates.sh</files>
  <read_first>
    - backend/tests/quality/coverage_gates.sh - To understand gate enforcement
    - data/coverage-thresholds.json - To verify stage 3 threshold is 60%
  </read_first>
  <action>
    Run Stage 3 gate enforcement to verify threshold behavior:

    1. Execute gate script with Stage 3 (60% threshold):
       ```bash
       cd /home/alex/armored-archer/backend
       COVERAGE_GATE_STAGE=3 bash tests/quality/coverage_gates.sh 2>&1
       ```

    2. Verify gate behavior:
       - If coverage >= 60%: Gate should pass (exit code 0)
       - If coverage < 60%: Gate should fail (exit code 1) with clear message
       - Message should indicate gap percentage and next steps

    3. Test Stage 1 (45%) and Stage 2 (52.5%) gates:
       ```bash
       COVERAGE_GATE_STAGE=1 bash tests/quality/coverage_gates.sh 2>&1
       COVERAGE_GATE_STAGE=2 bash tests/quality/coverage_gates.sh 2>&1
       ```
       - Verify lower stages pass if coverage >= stage threshold
       - Verify clear progression messaging

    4. Document gate results:
       - Stage 1 result (pass/fail with coverage %)
       - Stage 2 result (pass/fail with coverage %)
       - Stage 3 result (pass/fail with coverage %)
       - Recommendation for closing remaining gap (if any)
  </action>
  <verify>
    <automated>cd /home/alex/armored-archer/backend && COVERAGE_GATE_STAGE=3 bash tests/quality/coverage_gates.sh 2>&1; echo "Exit code: $?"</automated>
  </verify>
  <done>Stage 3 gate executed, result documented (pass/fail with coverage %), lower stages verified</done>
</task>

<task type="auto">
  <name>Task 3: Analyze remaining gaps and determine next steps</name>
  <files>.planning/phases/16-go-coverage-to-60/16-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/16-go-coverage-to-60/16-VERIFICATION.md - Current gap status
    - backend/coverage.out - Coverage results from Task 1
    - backend/scripts/gap-analysis.sh - To get updated gap analysis
  </read_first>
  <action>
    Analyze remaining coverage gaps and determine path forward:

    1. Run gap analysis script:
       ```bash
       cd /home/alex/armored-archer/backend
       bash scripts/gap-analysis.sh 2>&1 | head -50
       ```

    2. Extract key metrics:
       - Total gaps count
       - High/Medium/Low priority gaps
       - Packages with most gaps
       - Complexity estimation of remaining gaps

    3. Identify largest gap contributors:
       - RPC handlers (internal/rpc/rpc.go, rpc/feedback.go)
       - Any other utility packages with low coverage
       - Estimate coverage impact of addressing each gap

    4. Determine if 60% target is achievable:
       - Calculate coverage needed from remaining gaps
       - Estimate effort required for RPC handler tests
       - Consider tradeoffs: accept lower overall target vs. invest in RPC testing

    5. Document recommendations:
       - If 60% achievable: specific plans for remaining gaps
       - If 60% not achievable: lower target or expand Phase 16 scope
       - Estimated effort for each option
  </action>
  <verify>
    <automated>cd /home/alex/armored-archer/backend && bash scripts/gap-analysis.sh 2>&1 | grep -E "Total gaps|priority gaps|Package summary"</automated>
  </verify>
  <done>Gap analysis completed, remaining gaps quantified, path to 60% target documented with options</done>
</task>

<task type="auto">
  <name>Task 4: Update VERIFICATION.md with gap closure status</name>
  <files>.planning/phases/16-go-coverage-to-60/16-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/16-go-coverage-to-60/16-VERIFICATION.md - Current verification report
    - backend/coverage.out - New coverage results
    - .planning/phases/16-go-coverage-to-60/16-04-SUMMARY.md - Logger gap closure results
    - .planning/phases/16-go-coverage-to-60/16-05-SUMMARY.md - Cache gap closure results
  </read_first>
  <action>
    Update 16-VERIFICATION.md to reflect gap closure:

    1. Update re_verification section:
       ```yaml
       re_verification:
         previous_status: gaps_found
         previous_score: 6/8
         gaps_closed:
           - "Logger package coverage (0% -> 80%+) via plan 16-04"
           - "Cache provider coverage (0% -> 90%+) via plan 16-05"
         gaps_remaining: [list any remaining gaps]
         regressions: []
       status: [status_based_on_results]
       score: [new_score]/8
       verified: [timestamp]
       ```

    2. Update gap 1 (Overall coverage reaches 60%):
       - Set status to "verified", "partial", or "failed" based on actual results
       - Update reason with new coverage percentage
       - Remove closed items from missing list
       - Add any newly identified gaps

    3. Update gap 2 (60% threshold enforced):
       - Set status to "verified" if coverage >= 60%
       - Set status to "partial" if gate works but coverage < 60%
       - Update artifacts section with current state

    4. Update verification table:
       - Mark closed gaps as VERIFIED
       - Update coverage values
       - Update evidence with new results

    5. Update gap summary section:
       - Document logger and cache coverage improvements
       - Note any remaining gaps
       - Provide recommendation for next steps
  </action>
  <verify>
    <automated>grep -E "gaps_closed|gaps_remaining|status:|score:" /home/alex/armored-archer/.planning/phases/16-go-coverage-to-60/16-VERIFICATION.md</automated>
  </verify>
  <done>VERIFICATION.md updated with gap closure status, all closed gaps marked, remaining gaps documented</done>
</task>

<task type="auto">
  <name>Task 5: Create recommendation summary for Phase 17</name>
  <files>.planning/phases/16-go-coverage-to-60/16-06-GAP_RECOMMENDATIONS.md</files>
  <read_first>
    - .planning/phases/16-go-coverage-to-60/16-VERIFICATION.md - Updated gap status
    - backend/scripts/gap-analysis.sh output - Remaining gaps
    - .planning/REQUIREMENTS.md - To map gaps to requirements
  </read_first>
  <action>
    Create 16-06-GAP_RECOMMENDATIONS.md with forward path:

    1. Document current state:
       - Overall coverage: [actual %]
       - Target: 60%
       - Gap: [remaining %]
       - Critical packages status: [RPG, Matchmaking, etc.]

    2. Map remaining gaps to requirements:
       - COV-01 (60% overall): [closed/partial/open]
       - COV-08 (60% enforcement): [closed/partial/open]

    3. Provide options for reaching 60%:
       Option A: Test RPC handlers with Nakama runtime mocking
         - Effort: High (requires mock setup or testcontainers)
         - Coverage impact: ~15-20% (many RPC functions)
         - Risk: Complex, may not be worth the effort

       Option B: Accept 50% overall target as good enough
         - Rationale: Critical packages at 90%+, coverage quality high
         - Effort: None (adjust targets)
         - Risk: May compromise on quality gates

       Option C: Increase coverage in other packages
         - Effort: Medium (identify and test remaining functions)
         - Coverage impact: ~5-10% (depends on available functions)
         - Risk: May not reach 60% target

       Option D: Extend Phase 16 with additional gap closure plans
         - Effort: High (2-3 more plans)
         - Coverage impact: ~10-15% (if successful)
         - Risk: Delays Phase 17 start

    4. Provide recommendation:
       - Choose best option based on business value vs. effort
       - Document rationale for recommendation
       - Suggest Phase 17 scope based on recommendation
  </action>
  <verify>
    <automated>grep -E "Option A|Option B|Option C|Option D|Recommendation" /home/alex/armored-archer/.planning/phases/16-go-coverage-to-60/16-06-GAP_RECOMMENDATIONS.md</automated>
  </verify>
  <done>Gap recommendations documented, options for reaching 60% analyzed, recommendation provided with rationale</done>
</task>

<task type="auto">
  <name>Task 6: Final verification and requirements mapping</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md - To update requirement status
    - .planning/phases/16-go-coverage-to-60/16-VERIFICATION.md - Verification results
    - .planning/ROADMAP.md - To update phase status
  </read_first>
  <action>
    Final verification and requirements update:

    1. Update REQUIREMENTS.md:
       - Update COV-01 status based on actual coverage result
       - Update COV-08 status based on gate enforcement result
       - Ensure traceability matrix is accurate

    2. Verify all phase requirements addressed:
       - COV-01: 60% overall coverage [status]
       - COV-02: Incremental gates (verified in 16-01, remains valid)
       - COV-03: Progression 75% (verified in 16-02, remains valid)
       - COV-04: Matchmaking 80% (verified in 16-02, remains valid)
       - COV-05: Store/Season/Notifications 50-55% (verified in 16-02, remains valid)
       - COV-06: Gap analysis (verified in 16-01, remains valid)
       - COV-07: Factory fixtures/testcontainers (verified in 16-02, remains valid)
       - COV-08: 60% enforcement [status]

    3. Document final phase status:
       - Plans completed: 16-01, 16-02, 16-03, 16-04, 16-05, 16-06
       - Coverage achieved: [actual %]
       - Requirements satisfied: [count]/8
       - Phase ready for: [completion or continuation]
  </action>
  <verify>
    <automated>grep -E "COV-01|COV-02|COV-03|COV-04|COV-05|COV-06|COV-07|COV-08" /home/alex/armored-archer/.planning/REQUIREMENTS.md | grep -E "Pending|Complete"</automated>
  </verify>
  <done>All requirements status verified and updated, final phase status documented, ready for handoff</done>
</task>

</tasks>

<verification>
1. Overall coverage measured and documented: go tool cover -func coverage.out | grep total
2. Stage 3 gate executed with clear result: COVERAGE_GATE_STAGE=3 ./coverage_gates.sh
3. Gap analysis run to identify remaining gaps: bash scripts/gap-analysis.sh
4. VERIFICATION.md updated with closure status
5. REQUIREMENTS.md updated with final status
</verification>

<success_criteria>
1. Coverage report generated showing overall coverage after gap closure
2. Stage 3 gate result documented (pass or fail with clear reason)
3. Remaining gaps identified and quantified
4. VERIFICATION.md updated with gap closure status
5. Gap recommendations created with clear options for Phase 17
6. REQUIREMENTS.md updated with final requirement status
</success_criteria>

<output>
After completion, create `.planning/phases/16-go-coverage-to-60/16-06-SUMMARY.md`
</output>
