---
phase: 23-execute-mutation-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/mutation-exec-handler.sh
  - scripts/run-mutation-tests.sh
autonomous: true
requirements:
  - MUT-05
  - MUT-06

must_haves:
  truths:
    - "Local mutation testing executes successfully without errors"
    - "go-mutesting tool is correctly installed and accessible"
    - "testcontainers database is available during mutation testing"
    - "flaky test quarantine is properly respected during mutation runs"
  artifacts:
    - path: "scripts/mutation-exec-handler.sh"
      provides: "Custom execution handler for go-mutesting"
      exports: ["handles MUTATE_ORIGINAL/MUTATE_CHANGED environment variables"]
    - path: "scripts/run-mutation-tests.sh"
      provides: "Mutation testing orchestration script"
      exports: ["runs go-mutesting on all packages", "enforces package-specific thresholds"]
    - path: "backend/tests/quality/mutation_config.yaml"
      provides: "Package-specific mutation score thresholds"
      contains: "threshold: 85 for combat, 80 for matchmaking, 75 for others"
  key_links:
    - from: "scripts/run-mutation-tests.sh"
      to: "scripts/mutation-exec-handler.sh"
      via: "--exec flag"
      pattern: "--exec.*mutation-exec-handler"
    - from: "scripts/mutation-exec-handler.sh"
      to: "testcontainers postgres"
      via: "export TESTDB_HOST=localhost TESTDB_PORT=5432"
      pattern: "export TESTDB"
    - from: "scripts/mutation-exec-handler.sh"
      to: "data/flaky-test-quarantine.json"
      via: "jq parsing of quarantine file"
      pattern: "QUARANTINE_FILE.*flaky-test-quarantine"
---

<objective>
Verify local mutation testing infrastructure is functional before executing full workflow

Purpose: Ensure go-mutesting, execution scripts, testcontainers, and quarantine handling work correctly before running full mutation testing workflow
Output: Verified local execution with successful test run on at least one package
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-execute-mutation-testing/23-RESEARCH.md

@scripts/mutation-exec-handler.sh
@scripts/run-mutation-tests.sh
@backend/tests/quality/mutation_config.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify go-mutesting installation and mutator availability</name>
  <files>~/go/bin/go-mutesting</files>
  <read_first>
    - ~/go/bin/go-mutesting (run --help to verify)
  </read_first>
  <action>
    Run go-mutesting --help to verify installation is working
    Run go-mutesting --list-mutators to confirm available mutation operators (expected: branch/case, branch/else, branch/if, expression/comparison, expression/remove, statement/remove)

    Verify go-mutesting binary exists and is executable:
    - File path: ~/go/bin/go-mutesting (8MB+ size indicates proper installation)
    - Should output help text when run with --help flag
    - Should list mutators when run with --list-mutators flag

    If go-mutesting is not found or fails:
    - Install with: go install github.com/zimmski/go-mutesting/cmd/go-mutesting@latest
    - Verify ~/go/bin is in PATH
  </action>
  <verify>
    <automated>~/go/bin/go-mutesting --help | grep -q "Usage:" && ~/go/bin/go-mutesting --list-mutators | grep -q "branch/if"</automated>
  </verify>
  <done>
    go-mutesting is installed and outputs help text; --list-mutators shows at least 6 mutators including branch/if
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify testcontainers database availability for mutation testing</name>
  <files>backend/docker-compose.yml</files>
  <read_first>
    - backend/docker-compose.yml (check postgres service configuration)
  </read_first>
  <action>
    Start testcontainers environment for mutation testing:
    1. Navigate to backend directory
    2. Run: docker-compose up -d postgres
    3. Wait for postgres to be healthy (up to 60 seconds)
    4. Verify: docker-compose ps should show postgres as "Up"

    Export environment variables expected by mutation-exec-handler.sh:
    - TESTDB_HOST=localhost
    - TESTDB_PORT=5432

    Verify database connectivity:
    - Run: docker exec armored_archer_postgres pg_isready -U postgres
    - Expected output: "postgres is ready" or similar success message

    If postgres container doesn't exist:
    - Check backend/docker-compose.yml for postgres service definition
    - Service name should be "postgres" or similar
    - Default user is typically "postgres"
  </action>
  <verify>
    <automated>cd backend && docker-compose ps | grep -q "postgres.*Up" && docker exec armored_archer_postgres pg_isready -U postgres</automated>
  </verify>
  <done>
    postgres container is running and responds to pg_isready command
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify mutation-exec-handler.sh handles MUTATE_ORIGINAL/MUTATE_CHANGED correctly</name>
  <files>scripts/mutation-exec-handler.sh</files>
  <read_first>
    - scripts/mutation-exec-handler.sh (verify environment variable validation and file replacement logic)
  </read_first>
  <action>
    Verify mutation-exec-handler.sh script is executable and has correct logic:

    Check script permissions:
    - Run: ls -la scripts/mutation-exec-handler.sh
    - Should show executable permissions (rwxr-xr-x or similar)

    Verify environment variable validation (lines 8-14):
    - Script checks for MUTATE_ORIGINAL and MUTATE_CHANGED environment variables
    - Exits with error code 1 if either is missing
    - Error message: "Error: Required environment variables not set"

    Verify file replacement logic (line 17):
    - Script copies MUTATE_CHANGED to MUTATE_ORIGINAL: cp "$MUTATE_CHANGED" "$MUTATE_ORIGINAL"
    - This allows go-mutesting to test each mutation

    Verify testcontainers integration (lines 24-25):
    - Exports TESTDB_HOST=localhost
    - Exports TESTDB_PORT=5432
    - Required for tests to connect to postgres container

    Verify quarantine handling (lines 27-43):
    - Reads from QUARANTINE_FILE="../../../data/flaky-test-quarantine.json"
    - Parses quarantine file with jq to get count
    - Builds skip list for quarantined tests using -run flag
    - Skip format: -run='^((?!(Quarantined|test1|test2).)*$)'

    Verify timeout handling (lines 47-51):
    - Default timeout is 30s
    - Appends 's' to numeric-only values
    - Uses MUTATE_TIMEOUT environment variable if set

    Verify test result interpretation (lines 64-76):
    - Exit code 0: mutation killed (tests passed) -> return 0
    - Exit code non-zero: mutation survived (tests failed) -> return 1
  </action>
  <verify>
    <automated>test -x scripts/mutation-exec-handler.sh && grep -q "MUTATE_ORIGINAL\|MUTATE_CHANGED" scripts/mutation-exec-handler.sh && grep -q "TESTDB_HOST=localhost" scripts/mutation-exec-handler.sh</automated>
  </verify>
  <done>
    mutation-exec-handler.sh is executable and contains required environment variable checks, testcontainers exports, quarantine handling, and timeout logic
  </done>
</task>

<task type="auto">
  <name>Task 4: Verify run-mutation-tests.sh threshold configuration</name>
  <files>scripts/run-mutation-tests.sh</files>
  <read_first>
    - scripts/run-mutation-tests.sh (verify package list and threshold logic)
  </read_first>
  <action>
    Verify run-mutation-tests.sh script configuration:

    Check default packages list (lines 21-28):
    - internal/combat (threshold: 85%)
    - internal/matchmaking (threshold: 80%)
    - internal/rpg (threshold: 75%)
    - internal/store (threshold: 75%)
    - internal/season (threshold: 75%)
    - internal/notifications (threshold: 75%)

    Verify get_threshold function (lines 42-46):
    - Reads from backend/tests/quality/mutation_config.yaml
    - Extracts threshold value with grep -A 5 and awk
    - Returns 75% as default if not found

    Verify go-mutesting command construction (lines 57-66):
    - Uses GOMUTESTING_BIN="${HOME}/go/bin/go-mutesting"
    - Adds --exec ../scripts/mutation-exec-handler.sh
    - Adds --blacklist if blacklist file exists
    - Appends package path as final argument

    Verify score extraction (lines 75-80):
    - Extracts score with: grep -i "mutation score" | grep -oP '\d+\.?\d*'
    - Uses 0 as default if extraction fails
    - go-mutesting output format: "The mutation score is 0.850000 (17 passed, 3 failed, 0 skipped, total is 20)"

    Verify threshold comparison (lines 83-92):
    - Compares integer values of score and threshold
    - Returns 1 (fail) if score < threshold
    - Returns 0 (pass) if score >= threshold

    Verify JSON report generation (lines 101-137):
    - Creates temporary JSON file at /tmp/mutation-report.json
    - Builds packages array with name and status for each package
    - Outputs JSON to stdout
  </action>
  <verify>
    <automated>grep -q "internal/combat" scripts/run-mutation-tests.sh && grep -q "threshold: 85" backend/tests/quality/mutation_config.yaml && grep -q "mutation score" scripts/run-mutation-tests.sh</automated>
  </verify>
  <done>
    run-mutation-tests.sh contains all 6 packages, correctly references mutation_config.yaml, extracts mutation scores, and compares against thresholds
  </done>
</task>

<task type="auto">
  <name>Task 5: Run quick mutation test on single package to verify full pipeline</name>
  <files>
    - internal/combat/
    - backend/tests/integration/
  </files>
  <read_first>
    - internal/combat/ (identify test files to run mutation testing)
  </read_first>
  <action>
    Execute mutation testing on internal/combat package to verify full pipeline:

    1. Ensure postgres container is running (from Task 2)
    2. Navigate to project root
    3. Run quick mutation test: ~/go/bin/go-mutesting --exec scripts/mutation-exec-handler.sh backend/internal/combat/ -v

    Expected behavior:
    - go-mutesting should analyze Go files in internal/combat/
    - Should generate mutations based on available mutators
    - For each mutation: copy mutated file, run go test with mutation-exec-handler.sh
    - Testcontainers database should be used (TESTDB_HOST/TESTDB_PORT set)
    - Quarantined tests should be skipped if flaky-test-quarantine.json has entries
    - Output should show mutation score summary at end

    Expected output format:
    - Individual mutation results (killed/survived)
    - Summary line: "The mutation score is X.XXXXXX (Y passed, Z failed, S skipped, total is T)"
    - Score should be between 0.0 and 1.0 (multiply by 100 for percentage)

    If tests fail due to database connection:
    - Verify TESTDB_HOST=localhost and TESTDB_PORT=5432 are exported
    - Check postgres container is running: docker-compose ps

    If tests fail due to test file not found:
    - Verify internal/combat has test files ( *_test.go )
    - Check test imports are correct

    If all mutations survive (score 0.0):
    - Tests may be missing assertions
    - This is expected for initial run - indicates need for test improvement

    Record the mutation score from the output for use in Task 02.
  </action>
  <verify>
    <automated>~/go/bin/go-mutesting --exec scripts/mutation-exec-handler.sh backend/internal/combat/ -v 2>&1 | grep -q "mutation score"</automated>
  </verify>
  <done>
    go-mutesting executes successfully on internal/combat package and outputs a mutation score value (format: "The mutation score is X.XXXXXX")
  </done>
</task>

</tasks>

<verification>
After completing all tasks:
1. go-mutesting is installed and lists mutators
2. postgres container is running and accepting connections
3. mutation-exec-handler.sh has executable permissions and correct logic
4. run-mutation-tests.sh has correct package list and threshold references
5. Quick mutation test on internal/combat completes and outputs mutation score
</verification>

<success_criteria>
1. All 5 tasks complete without errors
2. Quick mutation test on internal/combat outputs mutation score
3. No database connection errors in mutation test output
4. quarantine handling is verified (either no quarantined tests or they are skipped)
5. Ready to execute full mutation testing workflow in Plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/23-execute-mutation-testing/23-01-SUMMARY.md`
</output>
