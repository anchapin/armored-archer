---
phase: 23-execute-mutation-testing
plan: 03
type: execute
wave: 3
depends_on:
  - 23-02
files_modified:
  - docs/coverage-dashboard.html
  - .github/workflows/mutation-testing.yml
autonomous: false
requirements:
  - MUT-05
  - MUT-06

must_haves:
  truths:
    - "Mutation scores are visible on coverage dashboard"
    - "Dashboard mutation score cards show actual values (not 'N/A')"
    - "GitHub Actions workflow configuration is verified for nightly execution"
    - "Workflow has workflow_dispatch trigger for manual execution"
  artifacts:
    - path: "docs/coverage-dashboard.html"
      provides: "Coverage dashboard with mutation score display"
      contains: "JavaScript that loads mutation_score from coverage-history.json"
    - path: ".github/workflows/mutation-testing.yml"
      provides: "GitHub Actions workflow for nightly mutation testing"
      exports: ["schedule trigger (cron)", "workflow_dispatch trigger"]
  key_links:
    - from: "docs/coverage-dashboard.html"
      to: "data/coverage-history.json"
      via: "fetch() API call"
      pattern: "fetch.*coverage-history.json"
    - from: ".github/workflows/mutation-testing.yml"
      to: "scripts/run-mutation-tests.sh"
      via: "bash scripts/run-mutation-tests.sh"
      pattern: "run-mutation-tests.sh"
    - from: ".github/workflows/mutation-testing.yml"
      to: "scripts/track-mutation-history.sh"
      via: "bash scripts/track-mutation-history.sh"
      pattern: "track-mutation-history.sh"
---

<objective>
Verify dashboard integration and prepare GitHub Actions workflow for deployment

Purpose: Confirm mutation scores appear on coverage dashboard and verify GitHub Actions workflow is ready for nightly execution
Output: Dashboard displaying mutation scores, workflow file verified for cron and workflow_dispatch triggers
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
@.planning/phases/23-execute-mutation-testing/23-02-SUMMARY.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-execute-mutation-testing/23-RESEARCH.md

@docs/coverage-dashboard.html
@.github/workflows/mutation-testing.yml
@data/coverage-history.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify coverage dashboard JavaScript loads mutation scores correctly</name>
  <files>docs/coverage-dashboard.html</files>
  <read_first>
    - docs/coverage-dashboard.html (check JavaScript mutation score loading logic)
    - data/coverage-history.json (verify mutation_score data exists)
  </read_first>
  <action>
    Verify dashboard JavaScript correctly loads and displays mutation scores:

    1. Check historyData loading (line 355-416 in coverage-dashboard.html):
       - historyData contains coverage-history.json content
       - Verify latest entry has mutation_score field
       - Run: jq '.history[-1].mutation_score' data/coverage-history.json
       - Should return object with package scores

    2. Verify mutation thresholds configuration (lines 433-441):
       ```javascript
       const mutationThresholds = {
           "combat": 85,
           "matchmaking": 80,
           "rpg": 75,
           "store": 75,
           "season": 75,
           "notifications": 75,
           "overall": 78
       };
       ```
       - These thresholds must match backend/tests/quality/mutation_config.yaml
       - Overall threshold 78% is weighted average of package thresholds

    3. Verify mutation status helper functions (lines 480-490):
       - getMutationStatusClass(score, threshold):
         - Returns 'good' if score >= threshold
         - Returns 'warning' if score >= threshold * 0.8
         - Returns 'bad' otherwise
       - getMutationStatusText(score, threshold):
         - Returns 'PASS', 'WARN', 'FAIL' based on same logic

    4. Verify summary card updates (lines 698-729):
       - Updates mutationOverall element if mutationScores.overall exists
       - Updates mutationCombat element if mutationScores.combat exists
       - Updates mutationMatchmaking element if mutationScores.matchmaking exists
       - Each update includes status class (good/warning/bad) and score + '%'

    5. Verify mutation progress bar update (lines 722-729):
       - Sets progressFill.style.width to mutationScores.overall + '%'
       - Sets progressFill.className with status class
       - Sets progressFill.textContent to score + '%'

    6. Verify package grid mutation display (lines 493-528):
       - For each package in latestData.packages:
         - Gets mutationScore from latestData.mutation_score
         - Gets mutationThreshold from mutationThresholds
         - Displays mutation badge if score !== 'N/A'
         - Badge format: "Mutation: <span class="mutation-badge {status}">{score}%</span>"

    7. Test JavaScript loading locally:
       - Open docs/coverage-dashboard.html in browser
       - Check console for JavaScript errors
       - Verify mutation score cards show actual values (not 'N/A')
       - Verify progress bar shows percentage
       - Hover over package cards to see mutation badges

    If mutation scores show 'N/A':
       - Check data/coverage-history.json has mutation_score field
       - Verify latest entry has mutation_score object
       - Check browser console for fetch errors

    If JavaScript errors occur:
       - Check console for specific error messages
       - Verify historyData object structure matches coverage-history.json
       - Check for null/undefined access in JavaScript
  </action>
  <verify>
    <automated>grep -q "mutation_score.overall" docs/coverage-dashboard.html && grep -q "mutationScore.combat" docs/coverage-dashboard.html && grep -q "mutationScores.overall !== undefined" docs/coverage-dashboard.html</automated>
  </verify>
  <done>
    Dashboard JavaScript contains mutation score loading logic, threshold comparison, and display code for summary cards, progress bar, and package grid
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify GitHub Actions workflow configuration for nightly execution</name>
  <files>.github/workflows/mutation-testing.yml</files>
  <read_first>
    - .github/workflows/mutation-testing.yml (verify triggers and steps)
  </read_first>
  <action>
    Verify GitHub Actions workflow is configured for nightly execution:

    1. Check workflow triggers (lines 5-14):
       - schedule trigger:
         ```yaml
         schedule:
           - cron: '0 2 * * *'  # Daily at 2 AM UTC
         ```
         - Cron format: minute hour day month weekday
         - '0 2 * * *' = daily at 2:00 AM UTC
         - Avoids overlap with flaky tests at 3 AM

       - workflow_dispatch trigger:
         ```yaml
         workflow_dispatch:
           inputs:
             packages:
               description: 'Optional package filter (comma-separated, e.g., "internal/combat,internal/matchmaking")'
               required: false
               default: ''
               type: string
         ```
         - Allows manual trigger via GitHub Actions UI
         - Optional packages input for filtering
         - Empty default means all packages

    2. Verify workflow job configuration (lines 16-21):
       - Job name: "Mutation Testing (Go)"
       - Runs on: ubuntu-latest
       - Timeout: 180 minutes (3 hours for full mutation testing)

    3. Verify go-mutesting installation (lines 35-48):
       - Installs from: github.com/zimmski/go-mutesting/cmd/go-mutesting@latest
       - Adds GOPATH/bin to PATH: echo "$HOME/go/bin" >> $GITHUB_PATH
       - Installs jq for JSON parsing: sudo apt-get install -y jq
       - Verifies installation: go-mutesting --version

    4. Verify testcontainers startup (lines 50-57):
       - Starts postgres: docker-compose up -d postgres
       - Waits for health: timeout 60 bash -c 'until docker-compose ps | grep -q "Up"; do sleep 2; done'
       - Checks status: docker-compose ps

    5. Verify mutation testing execution (lines 59-81):
       - Gets packages input or uses default (empty = all packages)
       - Runs: bash scripts/run-mutation-tests.sh $PACKAGES
       - Sets output status: echo "status=pass/fail" >> $GITHUB_OUTPUT
       - Exits with error code if any package fails threshold

    6. Verify mutation history tracking (lines 83-87):
       - Runs even if mutation tests fail: if: always()
       - Executes: bash scripts/track-mutation-history.sh
       - This ensures history is updated even with failed runs

    7. Verify commit step (lines 89-99):
       - Only on push to main: if: github.event_name == 'push' && github.ref == 'refs/heads/main'
       - Configures git user: github-actions[bot]
       - Adds data/coverage-history.json to git
       - Commits with message: "chore: update mutation score history"
       - Pushes to repository

    8. Verify artifact upload (lines 101-107):
       - Uploads: data/coverage-history.json
       - Artifact name: mutation-results
       - Retention: 30 days
       - Runs even on failure: if: always()

    9. Verify PR comment (lines 164-237):
       - Only on pull_request events
       - Uses actions/github-script@v7
       - Comments with:
         - Overall mutation score
         - Package scores with thresholds
         - Pass/fail indicators for each package
         - Action required message for failed packages

    If workflow file has syntax errors:
       - YAML indentation must be 2 spaces
       - Check for missing colons or quotes
       - Use online YAML validator if needed

    If cron schedule won't execute:
       - Verify format: '0 2 * * *' (5 fields)
       - Check GitHub Actions workflow permissions
       - Repository must have recent activity for cron to run

    Expected workflow execution flow:
       1. Cron triggers at 2 AM UTC OR manual trigger via workflow_dispatch
       2. Checkout code
       3. Setup Go 1.25
       4. Install go-mutesting and jq
       5. Start postgres container
       6. Run mutation tests
       7. Track mutation history
       8. Commit and push coverage-history.json (if on main)
       9. Upload artifact
       10. Generate report
       11. Comment on PR (if applicable)
       12. Stop containers
  </action>
  <verify>
    <automated>grep -q "cron: '0 2 \* \* \*'" .github/workflows/mutation-testing.yml && grep -q "workflow_dispatch:" .github/workflows/mutation-testing.yml && grep -q "run-mutation-tests.sh" .github/workflows/mutation-testing.yml && grep -q "track-mutation-history.sh" .github/workflows/mutation-testing.yml</automated>
  </verify>
  <done>
    GitHub Actions workflow has both schedule and workflow_dispatch triggers, installs go-mutesting, runs mutation testing, tracks history, and uploads results as artifacts
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 23 execution complete:
    1. Local mutation testing infrastructure verified (Plan 01)
    2. Full mutation testing executed on 6 packages (Plan 02)
    3. Mutation scores recorded in coverage-history.json
    4. Dashboard JavaScript verified for mutation score display
    5. GitHub Actions workflow verified for nightly execution
  </what-built>
  <how-to-verify>
    1. Verify mutation scores in coverage-history.json:
       jq '.history[-1].mutation_score' data/coverage-history.json

    2. Verify dashboard displays mutation scores:
       - Open docs/coverage-dashboard.html in browser
       - Check mutation score cards (Overall, Combat, Matchmaking)
       - Should show actual percentages, not 'N/A'

    3. Verify GitHub Actions workflow exists:
       - Check: .github/workflows/mutation-testing.yml
       - Verify triggers: schedule and workflow_dispatch

    4. (Optional) Trigger workflow manually to verify execution:
       - Navigate to GitHub repository > Actions tab
       - Select "Mutation Testing" workflow
       - Click "Run workflow"
       - Monitor logs for execution

    Expected state:
       - coverage-history.json has mutation_score object with 6 packages + overall
       - Dashboard shows mutation score cards with actual values
       - GitHub Actions workflow file is valid YAML
       - Workflow has schedule (0 2 * * *) and workflow_dispatch triggers

    Requirements satisfied:
       - MUT-05: Nightly GitHub Actions workflow created (cron schedule configured)
       - MUT-06: Mutation score displayed on dashboard (JavaScript verified)
  </how-to-verify>
  <resume-signal>
    Type "approved" if mutation scores are visible on dashboard and workflow is configured, or describe issues found
  </resume-signal>
</task>

</tasks>

<verification>
After completing all tasks:
1. Dashboard JavaScript loads mutation scores from coverage-history.json
2. GitHub Actions workflow has schedule and workflow_dispatch triggers
3. Workflow configuration includes go-mutesting installation, mutation test execution, and history tracking
4. Mutation score cards on dashboard show actual values (not 'N/A')
</verification>

<success_criteria>
1. Tasks 01 and 02 complete without errors
2. Dashboard loads and displays mutation scores
3. GitHub Actions workflow is valid YAML with both triggers
4. Human verification confirms dashboard shows actual mutation scores
5. MUT-05 and MUT-06 requirements are satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/23-execute-mutation-testing/23-03-SUMMARY.md`
</output>
