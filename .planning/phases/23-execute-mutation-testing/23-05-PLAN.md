---
phase: 23-execute-mutation-testing
plan: 05
type: execute
wave: 2
depends_on:
  - 23-04
files_modified:
  - data/coverage-history.json
  - /tmp/mutation-test-output.log
autonomous: true
gap_closure: true
requirements:
  - MUT-05
  - MUT-06
user_setup: []

must_haves:
  truths:
    - All 6 packages tested with mutation testing
    - Mutation scores generated for all 6 packages
    - Overall weighted mutation score calculated
    - coverage-history.json contains complete mutation score data
  artifacts:
    - path: data/coverage-history.json
      provides: Historical mutation scores with complete package data
      contains: "mutation_score"
    - path: /tmp/mutation-test-output.log
      provides: Full mutation testing output
      min_lines: 100
  key_links:
    - from: scripts/run-mutation-tests.sh
      to: data/coverage-history.json
      via: track-mutation-history.sh execution
      pattern: "track-mutation-history"
</key_links>

---

<objective>
Re-run full mutation testing on all 6 packages with fixed package structure

Purpose: Now that all 6 packages have mutation test placeholders, re-run the full mutation testing workflow to generate baseline scores for all packages. This will enable calculation of the overall weighted mutation score and populate coverage-history.json with complete data.

Output: Complete mutation scores for all 6 packages in coverage-history.json
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-execute-mutation-testing/PHASE23-VERIFICATION.md
@.planning/phases/23-execute-mutation-testing/23-02-SUMMARY.md
@scripts/run-mutation-tests.sh
@scripts/track-mutation-history.sh
@backend/tests/quality/mutation_config.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full mutation testing on all 6 packages</name>
  <files>
    scripts/run-mutation-tests.sh
    /tmp/mutation-test-output.log
  </files>
  <read_first>
    scripts/run-mutation-tests.sh
    backend/tests/quality/mutation_config.yaml
  </read_first>
  <action>
    Execute full mutation testing on all 6 packages:

    ```bash
    cd backend && bash ../scripts/run-mutation-tests.sh 2>&1 | tee /tmp/mutation-test-output.log
    ```

    This will run mutation testing on:
    - internal/combat (threshold: 85%)
    - internal/matchmaking (threshold: 80%)
    - internal/rpg (threshold: 75%)
    - internal/store (threshold: 75%)
    - internal/season (threshold: 75%)
    - internal/notifications (threshold: 75%)

    The script should now successfully test all 6 packages because each has a mutation test placeholder file in its internal/ directory.

    Expected behavior:
    - Each package will generate a mutation score (e.g., "The mutation score is 0.XXXXXX")
    - Script will compare each score against its threshold
    - Packages below threshold will exit with error, above threshold will pass
    - Full execution should complete in 30-60 minutes depending on package size
  </action>
  <verify>
    <automated>grep -q "mutation score" /tmp/mutation-test-output.log && grep -c "PASS:\|FAIL:" /tmp/mutation-test-output.log | grep -q "6"</automated>
  </verify>
  <done>
    All 6 packages tested with mutation scores generated, no build errors, script completes with exit code indicating overall pass/fail status
  </done>
</task>

<task type="auto">
  <name>Task 2: Execute track-mutation-history.sh to update coverage-history.json</name>
  <files>
    data/coverage-history.json
    scripts/track-mutation-history.sh
  </files>
  <read_first>
    scripts/track-mutation-history.sh
    data/coverage-history.json
  </read_first>
  <action>
    Execute track-mutation-history.sh to record mutation scores in coverage-history.json:

    ```bash
    cd backend && bash ../scripts/track-mutation-history.sh
    ```

    This script will:
    1. Read the current git commit hash
    2. Extract mutation scores from the latest run-mutation-tests.sh output
    3. Calculate overall weighted mutation score using weights:
       - combat: 30
       - matchmaking: 30
       - rpg: 20
       - store: 6.67
       - season: 6.67
       - notifications: 6.67
    4. Append new entry to data/coverage-history.json with structure:
       ```json
       {
         "commit": "abc1234",
         "branch": "main",
         "date": "2026-03-23T12:34:56Z",
         "mutation_score": {
           "combat": 85.0,
           "matchmaking": 80.0,
           "rpg": 75.0,
           "store": 75.0,
           "season": 75.0,
           "notifications": 75.0,
           "overall": 78.5
         }
       }
       ```

    5. Maintain maximum 30 entries in history array (remove oldest if needed)
  </action>
  <verify>
    <automated>test -f data/coverage-history.json && grep -q '"mutation_score"' data/coverage-history.json && grep -q '"overall"' data/coverage-history.json</automated>
  </verify>
  <done>
    coverage-history.json updated with new entry containing complete mutation_score object with all 6 package scores plus overall weighted score
  </done>
</task>

</tasks>

<verification>
- Full mutation testing completed on all 6 packages without build errors
- Mutation scores generated for combat, matchmaking, rpg, store, season, notifications
- Overall weighted mutation score calculated
- coverage-history.json contains new entry with complete mutation_score data
- No packages show "N/A" for mutation scores
</verification>

<success_criteria>
Complete baseline established with mutation scores for all 6 packages, overall weighted mutation score calculated, and coverage-history.json populated with complete data for trend tracking.
</success_criteria>

<output>
After completion, create `.planning/phases/23-execute-mutation-testing/23-05-SUMMARY.md`
</output>
