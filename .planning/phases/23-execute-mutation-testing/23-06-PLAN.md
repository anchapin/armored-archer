---
phase: 23-execute-mutation-testing
plan: 06
type: checkpoint:human-verify
wave: 3
depends_on:
  - 23-05
files_modified: []
autonomous: false
gap_closure: true
requirements:
  - MUT-05
  - MUT-06
user_setup: []

must_haves:
  truths:
    - Coverage dashboard displays mutation scores for all 6 packages
    - Mutation scores are color-coded based on thresholds (good/warning/bad)
    - Overall mutation score shows weighted calculation
    - Dashboard shows actual percentage values (not "N/A")
  artifacts:
    - path: docs/coverage-dashboard.html
      provides: Mutation score visualization
      exports: ["loadMutationScores", "getMutationStatusClass"]
  key_links:
    - from: data/coverage-history.json
      to: docs/coverage-dashboard.html
      via: JavaScript fetch and parse
      pattern: "fetch.*coverage-history.json"
</key_links>

---

<objective>
Verify coverage dashboard displays mutation scores from complete baseline

Purpose: Confirm that the coverage dashboard correctly loads and displays the complete mutation score data from coverage-history.json, showing actual percentage values for all 6 packages with proper color coding based on thresholds.

Output: Human-verified dashboard display showing mutation scores
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-execute-mutation-testing/PHASE23-VERIFICATION.md
@.planning/phases/23-execute-mutation-testing/23-03-SUMMARY.md
@docs/coverage-dashboard.html
@data/coverage-history.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task: Verify mutation score display on coverage dashboard</name>
  <files>docs/coverage-dashboard.html</files>
  <action>Checkpoint for human verification - no automated action required. Mutation testing baseline completed in plan 23-05 with scores for all 6 packages recorded in coverage-history.json. This task validates that the dashboard correctly displays those scores.</action>
  <verify>Human verification required - see <how-to-verify> section below</verify>
  <done>Human confirms dashboard displays mutation scores for all 6 packages with proper color coding</done>
  <what-built>
    Complete mutation testing baseline with scores for all 6 packages (combat, matchmaking, rpg, store, season, notifications), overall weighted score calculated, and coverage-history.json populated with complete mutation_score data
  </what-built>
  <how-to-verify>
    1. Open coverage dashboard in browser:
       ```bash
       open docs/coverage-dashboard.html
       ```
       or navigate to docs/coverage-dashboard.html in your file browser and open with browser

    2. Verify mutation score display:
       - Look at "Mutation Score" summary card at top of dashboard
       - Should show overall percentage (e.g., "78%") with color coding
       - Should not show "N/A"

    3. Check package-specific mutation scores:
       - Find the package cards or grid in the dashboard
       - Each package (combat, matchmaking, rpg, store, season, notifications) should show:
         - Mutation percentage (e.g., "94%", "80%", "75%")
         - Color-coded badge (green/good, yellow/warning, red/bad)
       - No package should show "N/A" for mutation score

    4. Verify threshold compliance:
       - Combat: Should be >= 85% (green/good status)
       - Matchmaking: Should be >= 80% (green/good status)
       - RPG: Should be >= 75% (green/good status)
       - Store: Should be >= 75% (green/good status)
       - Season: Should be >= 75% (green/good status)
       - Notifications: Should be >= 75% (green/good status)

    5. Check mutation progress bar (if present):
       - Should show overall weighted score as percentage
       - Should be color-coded based on overall threshold (78%)
       - Should not show empty or "N/A" state

    6. Verify data source (optional - for debugging):
       - Check browser console for any errors loading coverage-history.json
       - Check browser Network tab to confirm data/coverage-history.json was fetched successfully
       - Verify JSON contains mutation_score object with all package scores
  </how-to-verify>
  <resume-signal>Type "approved" if dashboard shows mutation scores for all 6 packages, or describe any issues observed</resume-signal>
</task>

</tasks>

<verification>
- Dashboard successfully loads mutation scores from coverage-history.json
- All 6 packages show actual percentage values (not "N/A")
- Color coding correctly reflects threshold compliance
- Overall weighted mutation score displayed
- No console errors related to data loading
</verification>

<success_criteria>
Coverage dashboard displays complete mutation score data with actual percentage values for all 6 packages, proper color coding based on thresholds, and overall weighted score visible.
</success_criteria>

<output>
After completion, create `.planning/phases/23-execute-mutation-testing/23-06-SUMMARY.md`
</output>
