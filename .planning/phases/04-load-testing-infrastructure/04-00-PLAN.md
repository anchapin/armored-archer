---
phase: 04-load-testing-infrastructure
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/internal/rpc/rpc_bench_test.go
  - backend/internal/rpc/feedback_bench_test.go
  - test/suites/performance/test_60fps_gameplay_loops.gd
  - .github/workflows/benchmark-regression.yml
autonomous: true
requirements:
  - PERF-01
  - PERF-02
  - PERF-05
user_setup: []
must_haves:
  truths:
    - Test stub files exist for all benchmark and performance tests
    - Test stubs compile and can be discovered by test runners
    - Test stubs contain placeholder tests with descriptive names
    - CI workflow file exists with placeholder structure
    - No implementation exists yet (stub files only)
  artifacts:
    - path: backend/internal/rpc/rpc_bench_test.go
      provides: Go benchmark stub file for core RPC handlers
      min_lines: 20
      contains:
        - package rpc
        - "BenchmarkGetPlayerStats"
        - "BenchmarkGetLeaderboard"
        - "BenchmarkGetInventory"
        - placeholder comments with "// TODO: implement"
    - path: backend/internal/rpc/feedback_bench_test.go
      provides: Go benchmark stub file for feedback RPC handlers
      min_lines: 15
      contains:
        - package rpc
        - "BenchmarkSubmitFeedback"
        - "BenchmarkGetFeedbackStatistics"
        - placeholder comments with "// TODO: implement"
    - path: test/suites/performance/test_60fps_gameplay_loops.gd
      provides: Godot 60 FPS test stub file
      min_lines: 20
      contains:
        - "extends GutTest"
        - "test_core_gameplay_60_fps"
        - "test_combat_calculations_performance"
        - placeholder comments with "# TODO: implement"
    - path: .github/workflows/benchmark-regression.yml
      provides: CI workflow stub file for benchmark regression detection
      min_lines: 15
      contains:
        - "name: Benchmark Regression"
        - "on:"
        - "jobs:"
        - placeholder comments with "# TODO: implement"
  key_links:
    - from: backend/internal/rpc/rpc_bench_test.go
      to: testing/benchmark package
      via: Go testing framework import
      pattern: import.*testing
    - from: test/suites/performance/test_60fps_gameplay_loops.gd
      to: res://addons/gut/gut.gd
      via: GUT test framework extension
      pattern: extends GutTest
    - from: .github/workflows/benchmark-regression.yml
      to: GitHub Actions
      via: YAML workflow configuration
      pattern: name:.*Benchmark
---

# Plan 04-00: Wave 0 Test Stubs for Performance Infrastructure

## Objective

Create minimal test stub files for all performance, benchmark, and CI regression tests. These stubs provide the test structure and discoverability without implementation, enabling fast verification after subsequent implementation plans.

Purpose: Follow Nyquist validation pattern by establishing test infrastructure first, then implementing. Stubs ensure tests can be discovered and run (even if they fail) before implementation begins.

Output: Stub files for Go benchmarks, Godot performance tests, and CI workflow.

## Execution Context

@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-load-testing-infrastructure/04-VALIDATION.md
@backend/internal/rpc/rpc.go
@backend/tests/testhelpers/db_testcontainers.go

## Interfaces

From backend/internal/rpc/rpc.go:
```go
func GetPlayerStats(ctx context.Context, logger runtime.Logger, db *sql.DB, nk runtime.NakamaModule, payload string) (string, error)
func GetLeaderboard(ctx context.Context, logger runtime.Logger, db *sql.DB, nk runtime.NakamaModule, payload string) (string, error)
func GetInventory(ctx context.Context, logger runtime.Logger, db *sql.DB, nk runtime.NakamaModule, payload string) (string, error)
```

From backend/tests/testhelpers/db_testcontainers.go:
```go
type TestDB struct {
    Container *postgres.PostgresContainer
    DB        *sql.DB
    ConnStr   string
}

func SetupTestDB(ctx context.Context) (*TestDB, error)
```

## Tasks

### Task 1: Create Go benchmark stub file for core RPC handlers

**Files:** `backend/internal/rpc/rpc_bench_test.go` (NEW)

**Action:**
Create `rpc_bench_test.go` with placeholder benchmark functions. Do NOT implement the actual benchmark logic — only create stubs that compile and can be discovered by `go test -bench=.`.

Implement stub structure:

```go
package rpc

import (
	"context"
	"testing"
)

// BenchmarkGetPlayerStats measures performance of GetPlayerStats RPC handler.
// TODO: Implement with testcontainers setup, realistic data, and b.ResetTimer()
func BenchmarkGetPlayerStats(b *testing.B) {
	b.Skip("placeholder - to be implemented in Plan 04-01")
}

// BenchmarkGetLeaderboard measures performance of GetLeaderboard RPC handler.
// TODO: Implement with 100 test players, JOIN query simulation
func BenchmarkGetLeaderboard(b *testing.B) {
	b.Skip("placeholder - to be implemented in Plan 04-01")
}

// BenchmarkGetInventory measures performance of GetInventory RPC handler.
// TODO: Implement with 20 gear items, catalog JOIN simulation
func BenchmarkGetInventory(b *testing.B) {
	b.Skip("placeholder - to be implemented in Plan 04-01")
}

// BenchmarkGetPlayerStatsParallel measures concurrent performance.
// TODO: Implement with b.RunParallel() for concurrent access simulation
func BenchmarkGetPlayerStatsParallel(b *testing.B) {
	b.Skip("placeholder - to be implemented in Plan 04-01")
}
```

**Verify:**
```bash
cd backend && go test -bench=. -run=^$ ./internal/rpc/... 2>&1 | grep -E "(Benchmark|SKIP)"
```

Expected: All 4 benchmark functions are discovered and show as SKIP.

**Done:**
`rpc_bench_test.go` exists with 4 benchmark stubs, all skip with descriptive messages.

---

### Task 2: Create Go benchmark stub file for feedback RPC handlers

**Files:** `backend/internal/rpc/feedback_bench_test.go` (NEW)

**Action:**
Create `feedback_bench_test.go` with placeholder benchmark functions for feedback RPCs.

Implement stub structure:

```go
package rpc

import (
	"context"
	"testing"
)

// BenchmarkSubmitFeedback measures performance of feedback submission.
// TODO: Implement with testcontainers, INSERT simulation, cache invalidation
func BenchmarkSubmitFeedback(b *testing.B) {
	b.Skip("placeholder - to be implemented in Plan 04-01")
}

// BenchmarkGetFeedbackStatistics measures performance of statistics query.
// TODO: Implement with 50 feedback submissions, aggregate query simulation
func BenchmarkGetFeedbackStatistics(b *testing.B) {
	b.Skip("placeholder - to be implemented in Plan 04-01")
}

// BenchmarkGetFeedbackStatisticsCached measures cache hit performance.
// TODO: Implement cache pre-warming, measure cached vs uncached difference
func BenchmarkGetFeedbackStatisticsCached(b *testing.B) {
	b.Skip("placeholder - to be implemented in Plan 04-01")
}
```

**Verify:**
```bash
cd backend && go test -bench=. -run=^$ ./internal/rpc/... 2>&1 | grep -E "(Benchmark.*Feedback|SKIP)"
```

Expected: All 3 feedback benchmark functions are discovered and show as SKIP.

**Done:**
`feedback_bench_test.go` exists with 3 benchmark stubs, all skip with descriptive messages.

---

### Task 3: Create Godot 60 FPS test stub file

**Files:** `test/suites/performance/test_60fps_gameplay_loops.gd` (NEW)

**Action:**
Create `test_60fps_gameplay_loops.gd` with placeholder test functions. Do NOT implement actual test logic — only create stubs that can be discovered by GUT.

Implement stub structure:

```gdscript
extends GutTest

# Test core gameplay maintains 60 FPS target
# TODO: Implement with 60-second gameplay simulation, FPS sampling, assertions
func test_core_gameplay_60_fps() -> void:
	gut.skip("placeholder - to be implemented in Plan 04-02")

# Test combat calculations are fast enough for 60 FPS
# TODO: Implement with 1000 iterations, <1ms target per calculation
func test_combat_calculations_performance() -> void:
	gut.skip("placeholder - to be implemented in Plan 04-02")

# Test UI rendering maintains 60 FPS
# TODO: Implement with 10 buttons, 5 labels, 3 progress bars
func test_ui_rendering_performance() -> void:
	gut.skip("placeholder - to be implemented in Plan 04-02")

# Test performance with multiple enemies
# TODO: Implement with 10 enemy instances, 600-frame simulation
func test_multiple_enemies_performance() -> void:
	gut.skip("placeholder - to be implemented in Plan 04-02")
```

**Verify:**
```bash
godot4 --headless --script test/suites/performance/test_60fps_gameplay_loops.gd 2>&1 | grep -E "(test_.*_performance|SKIP|skipped)"
```

Expected: All 4 test functions are discovered and show as skipped.

**Done:**
`test_60fps_gameplay_loops.gd` exists with 4 test stubs, all skip with descriptive messages.

---

### Task 4: Create CI workflow stub file for benchmark regression

**Files:** `.github/workflows/benchmark-regression.yml` (NEW)

**Action:**
Create `benchmark-regression.yml` with placeholder workflow structure. Do NOT implement the full workflow — only create a discoverable skeleton.

Implement stub structure:

```yaml
# Benchmark Regression Detection CI Workflow
# TODO: Implement full workflow in Plan 04-04
# - Run benchmarks with go test -bench
# - Fetch baseline from git history
# - Compare with benchstat
# - Fail on >10% regression
# - Update baseline on main branch

name: Benchmark Regression

on:
  pull_request:
    branches: [ main, develop ]
  push:
    branches: [ main ]

# TODO: Add permissions for contents:write

jobs:
  # TODO: Implement benchmark job with steps:
  # - Checkout code
  # - Setup Go
  # - Run benchmarks
  # - Fetch baseline
  # - Compare results
  # - Fail on regression
  # - Update baseline on main
  benchmark:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "Benchmark regression workflow to be implemented in Plan 04-04"
```

**Verify:**
```bash
cat .github/workflows/benchmark-regression.yml | grep -E "(name:|on:|jobs:|benchmark:)"
```

Expected: YAML is valid, contains workflow name, triggers, and placeholder job.

**Done:**
`benchmark-regression.yml` exists with workflow structure and placeholder implementation.

---

## Verification

**Per-Task Verification:**
- After Task 1: `cd backend && go test -bench=. -run=^$ ./internal/rpc/...` shows 4 SKIP benchmarks
- After Task 2: Same command shows 7 total SKIP benchmarks (4 core + 3 feedback)
- After Task 3: `godot4 --headless --script test/suites/performance/test_60fps_gameplay_loops.gd` shows 4 skipped tests
- After Task 4: `cat .github/workflows/benchmark-regression.yml` is valid YAML

**Wave Verification:**
```bash
# Verify all stubs exist and are discoverable
cd backend && go test -bench=. -run=^$ ./internal/rpc/... 2>&1 | grep SKIP
godot4 --headless --script test/suites/performance/test_60fps_gameplay_loops.gd 2>&1 | grep -i skip
ls -la .github/workflows/benchmark-regression.yml
```

Expected: All 7 Go benchmarks skip, all 4 Godot tests skip, CI workflow file exists.

## Success Criteria

1. All 7 Go benchmark stubs exist and are discoverable by `go test -bench=.`
2. All 4 Godot test stubs exist and are discoverable by GUT
3. CI workflow stub exists with valid YAML structure
4. All stubs use `b.Skip()` or `gut.skip()` with descriptive messages pointing to implementation plan
5. No implementation logic exists — only stub structure
6. Wave 0 is complete, enabling Plans 04-01 through 04-04 to implement against these stubs

## Output

After completion, create `.planning/phases/04-load-testing-infrastructure/04-00-SUMMARY.md` with:
- List of stub files created
- Verification that all stubs are discoverable
- Confirmation that Wave 0 is complete (all "File Exists" columns in VALIDATION.md can be checked)
- Notes on stub structure and TODO comments for implementation reference
