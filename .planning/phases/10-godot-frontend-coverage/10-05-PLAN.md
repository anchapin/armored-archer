---
phase: 10
plan: 05
type: execute
wave: 2
depends_on: ["10-04"]
files_modified: [scripts/calculate_godot_coverage.py, data/autoload-to-test-mapping.json]
autonomous: true
requirements: [GODOT-05]
user_setup: []

must_haves:
  truths:
    - "Coverage command reports overall pass rate and per-autoload pass rates"
    - "Mapping file links autoloads to their test files for visibility"
    - "Coverage report shows per-autoload test status (pass/fail, test count)"
    - "Users can see which autoloads are tested and which are untested"
  artifacts:
    - path: "scripts/calculate_godot_coverage.py"
      provides: "Enhanced coverage proxy with autoload mapping support"
      min_lines: 120
    - path: "data/autoload-to-test-mapping.json"
      provides: "Autoload to test file mapping configuration"
      min_lines: 30
  key_links:
    - from: "scripts/calculate_godot_coverage.py"
      to: "data/autoload-to-test-mapping.json"
      via: "JSON file loading"
      pattern: "json\\.load\\(.*autoload-to-test-mapping\\.json"
---

<objective>
Enhance the Godot coverage proxy script with autoload-to-test mapping to provide visibility into which autoloads have tests and their respective coverage metrics.

Purpose: The aggregate pass rate can mask gaps where critical autoloads are untested while easy tests pass. Mapping enables autoload-specific coverage tracking.

Output: Updated calculate_godot_coverage.py with autoload mapping support and a new autoload-to-test-mapping.json configuration file.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-godot-frontend-coverage/10-RESEARCH.md
@.planning/phases/10-godot-frontend-coverage/10-VALIDATION.md

@scripts/calculate_godot_coverage.py
@data/coverage-history.json
</context>

<interfaces>
<!-- Key functions from coverage proxy script -->

From scripts/calculate_godot_coverage.py:
- parse_junit_xml(xml_path) - Main function, parses JUnit XML
- Returns dict: {pass_rate, total_tests, failures, errors, subsystems}
- subsystems dict groups tests by directory (player, combat, network, etc.)
- main() function handles CLI arguments and JSON output

Current output format:
```json
{
    "pass_rate": 95.5,
    "total_tests": 150,
    "failures": 5,
    "errors": 2,
    "subsystems": {"player": 20, "combat": 15, "network": 30, ...}
}
```

Desired enhanced output:
```json
{
    "pass_rate": 95.5,
    "total_tests": 150,
    "failures": 5,
    "errors": 2,
    "subsystems": {"player": 20, "combat": 15, "network": 30, "autoloads": 50},
    "autoloads": {
        "NetworkManager": {"tests": 20, "pass_rate": 100.0, "test_file": "test/suites/autoloads/test_network_manager.gd"},
        "CombatManager": {"tests": 15, "pass_rate": 100.0, "test_file": "test/suites/autoloads/test_combat_manager.gd"},
        "GameManager": {"tests": 15, "pass_rate": 100.0, "test_file": "test/suites/autoloads/test_game_manager.gd"}
    }
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create autoload-to-test mapping configuration file</name>
  <files>data/autoload-to-test-mapping.json</files>
  <read_first>
    - autoloads/
    - test/suites/autoloads/
  </read_first>
  <action>
    Create data/autoload-to-test-mapping.json with the following structure:

    ```json
    {
        "autoloads": {
            "NetworkManager": {
                "test_file": "test/suites/autoloads/test_network_manager.gd",
                "critical": true,
                "description": "Manages Nakama server connection and RPC calls"
            },
            "CombatManager": {
                "test_file": "test/suites/autoloads/test_combat_manager.gd",
                "critical": true,
                "description": "Handles combat calculations and match state"
            },
            "GameManager": {
                "test_file": "test/suites/autoloads/test_game_manager.gd",
                "critical": true,
                "description": "Manages game state, health, and progression"
            },
            "ThemeManager": {
                "test_file": "test/suites/autoloads/test_theme_manager.gd",
                "critical": false,
                "description": "Manages UI theme (light/dark mode)"
            },
            "AccessibilityManager": {
                "test_file": "test/suites/autoloads/test_accessibility_manager.gd",
                "critical": false,
                "description": "Manages accessibility settings (font scale, screen reader)"
            },
            "PlayerStatsManager": {
                "test_file": "test/suites/player/test_player_stats_manager.gd",
                "critical": true,
                "description": "Manages player level, XP, and stat allocation"
            },
            "MatchmakerManager": {
                "test_file": "test/suites/network/test_matchmaker_manager.gd",
                "critical": true,
                "description": "Handles asynchronous PvP matchmaking"
            },
            "GearManager": {
                "test_file": "test/suites/gear/test_gear_manager.gd",
                "critical": true,
                "description": "Manages player inventory and equipment"
            },
            "StoreManager": {
                "test_file": "test/suites/store/test_store_manager.gd",
                "critical": true,
                "description": "Handles in-app purchases via RevenueCat"
            },
            "SeasonManager": {
                "test_file": "test/suites/season/test_season_manager.gd",
                "critical": true,
                "description": "Manages seasonal content and leaderboards"
            },
            "CampaignManager": {
                "test_file": "test/suites/campaign/test_campaign_manager.gd",
                "critical": true,
                "description": "Manages campaign stage progression"
            },
            "GemManager": {
                "test_file": "test/suites/gem/test_gem_manager.gd",
                "critical": false,
                "description": "Manages gem socketing system"
            },
            "TransmogManager": {
                "test_file": "test/suites/transmog/test_transmog_manager.gd",
                "critical": false,
                "description": "Manages cosmetic skin system"
            },
            "AutoAimManager": {
                "test_file": "test/suites/auto_aim/test_auto_aim_manager.gd",
                "critical": false,
                "description": "Provides auto-aim assistance"
            },
            "ObjectPool": {
                "test_file": "test/suites/object_pool/test_object_pool.gd",
                "critical": false,
                "description": "Manages object pooling for performance"
            }
        },
        "version": "1.0.0",
        "last_updated": "2026-03-21T12:00:00Z"
    }
    ```

    The mapping file provides:
    - autoload name -> test file path
    - critical flag for prioritization
    - description for documentation
    - version tracking for updates
  </action>
  <verify>
    <automated>jq -e '.autoloads.NetworkManager.test_file == "test/suites/autoloads/test_network_manager.gd" and .autoloads | length == 15' data/autoload-to-test-mapping.json</automated>
  </verify>
  <done>Autoload-to-test mapping JSON created with 15 autoloads</done>
</task>

<task type="auto">
  <name>Task 2: Enhance calculate_godot_coverage.py with autoload mapping support</name>
  <files>scripts/calculate_godot_coverage.py</files>
  <read_first>
    - scripts/calculate_godot_coverage.py
    - data/autoload-to-test-mapping.json
  </read_first>
  <action>
    Modify scripts/calculate_godot_coverage.py to add autoload mapping functionality:

    1. Add new function load_autoload_mapping():
       ```python
       def load_autoload_mapping(mapping_path="data/autoload-to-test-mapping.json"):
           """Load autoload-to-test mapping configuration."""
           try:
               with open(mapping_path, 'r') as f:
                   return json.load(f)
           except FileNotFoundError:
               return {"autoloads": {}, "version": "1.0.0"}
       ```

    2. Add new function calculate_autoload_coverage(junit_tests, autoload_mapping):
       ```python
       def calculate_autoload_coverage(junit_tests, autoload_mapping):
           """Calculate per-autoload coverage from JUnit test results."""
           autoloads = autoload_mapping.get("autoloads", {})
           autoload_results = {}

           for autoload_name, autoload_info in autoloads.items():
               test_file = autoload_info.get("test_file", "")
               # Find tests for this autoload
               autoload_tests = [
                   t for t in junit_tests
                   if test_file in t.get("classname", "")
               ]

               total = len(autoload_tests)
               failures = len([t for t in autoload_tests if t.get("failure")])
               errors = len([t for t in autoload_tests if t.get("error")])
               pass_rate = ((total - failures - errors) / total * 100) if total > 0 else 0.0

               autoload_results[autoload_name] = {
                   "tests": total,
                   "failures": failures,
                   "errors": errors,
                   "pass_rate": round(pass_rate, 2),
                   "test_file": test_file,
                   "critical": autoload_info.get("critical", False)
               }

           return autoload_results
       ```

    3. Modify parse_junit_xml() to return individual test objects:
       ```python
       # Modify to return test list alongside summary
       def parse_junit_xml(xml_path):
           tree = ET.parse(xml_path)
           root = tree.getroot()

           tests = root.findall('.//testcase')
           test_objects = [
               {
                   "classname": test.get('classname', ''),
                   "name": test.get('name', ''),
                   "failure": test.find('failure') is not None,
                   "error": test.find('error') is not None
               }
               for test in tests
           ]

           # ... existing summary calculation ...

           return {
               'pass_rate': round(pass_rate, 2),
               'total_tests': total,
               'failures': failures,
               'errors': errors,
               'subsystems': subsystems,
               'test_objects': test_objects  # New field
           }
       ```

    4. Modify main() to include autoload coverage:
       ```python
       def main():
           if len(sys.argv) < 2:
               print(json.dumps({ 'error': 'Usage: ...', 'pass_rate': 0.0, ... }))
               sys.exit(1)

           xml_path = sys.argv[1]
           result = parse_junit_xml(xml_path)

           # Load autoload mapping and calculate per-autoload coverage
           autoload_mapping = load_autoload_mapping()
           autoload_results = calculate_autoload_coverage(result.get('test_objects', []), autoload_mapping)
           result['autoloads'] = autoload_results

           print(json.dumps(result, indent=2))
       ```

    Add these imports at the top:
    - os (for file path handling)
  </action>
  <verify>
    <automated>grep -q "load_autoload_mapping" scripts/calculate_godot_coverage.py && grep -q "calculate_autoload_coverage" scripts/calculate_godot_coverage.py && grep -q "autoload_results" scripts/calculate_godot_coverage.py</automated>
  </verify>
  <done>Coverage proxy script enhanced with autoload mapping support</done>
</task>

<task type="auto">
  <name>Task 3: Test enhanced coverage proxy with autoload mapping</name>
  <files>test/results/gut-results.xml</files>
  <read_first>
    - scripts/calculate_godot_coverage.py
    - data/autoload-to-test-mapping.json
  </read_first>
  <action>
    Run the enhanced coverage proxy script to verify autoload mapping works:

    ```bash
    # Run tests to generate JUnit XML
    godot4 --headless --script res://test/run_all_tests.gd --select suites/autoloads

    # Run enhanced coverage proxy
    python3 scripts/calculate_godot_coverage.py test/results/gut-results.xml | jq .

    # Verify autoloads field exists and has expected entries
    python3 scripts/calculate_godot_coverage.py test/results/gut-results.xml | jq '.autoloads | keys'
    ```

    Verify output includes:
    - "autoloads" top-level key
    - Per-autoload entries: NetworkManager, CombatManager, GameManager
    - Each autoload entry has: tests, failures, errors, pass_rate, test_file, critical
    - pass_rate for each autoload is calculated correctly

    Example expected output:
    ```json
    {
        "autoloads": {
            "NetworkManager": {"tests": 29, "failures": 0, "errors": 0, "pass_rate": 100.0, ...},
            "CombatManager": {"tests": 29, "failures": 0, "errors": 0, "pass_rate": 100.0, ...},
            "GameManager": {"tests": 29, "failures": 0, "errors": 0, "pass_rate": 100.0, ...}
        },
        "pass_rate": 95.5,
        ...
    }
    ```
  </action>
  <verify>
    <automated>python3 scripts/calculate_godot_coverage.py test/results/gut-results.xml | jq -e '.autoloads | has("NetworkManager") and has("CombatManager") and has("GameManager")'</automated>
  </verify>
  <done>Enhanced coverage proxy verified with autoload-specific coverage data</done>
</task>

</tasks>

<verification>
After all tasks complete, verify:

```bash
# Check mapping file exists and is valid JSON
jq '.' data/autoload-to-test-mapping.json

# Run enhanced coverage proxy
python3 scripts/calculate_godot_coverage.py test/results/gut-results.xml | jq '.autoloads'

# Verify autoloads field has expected entries
python3 scripts/calculate_godot_coverage.py test/results/gut-results.xml | jq '.autoloads | keys'
# Should include: NetworkManager, CombatManager, GameManager, ThemeManager, etc.

# Verify each autoload entry has required fields
python3 scripts/calculate_godot_coverage.py test/results/gut-results.xml | jq '.autoloads.NetworkManager | keys'
# Should include: tests, failures, errors, pass_rate, test_file, critical
```

Verify:
- Autoload-to-test mapping JSON created with 15 autoload entries
- Coverage proxy script outputs autoload-specific coverage data
- Each autoload entry includes tests, pass_rate, test_file, critical fields
- Script successfully loads and processes the mapping file
</verification>

<success_criteria>
1. Autoload-to-test mapping configuration file created with 15 autoloads
2. Coverage proxy script enhanced to calculate per-autoload coverage
3. Output includes "autoloads" field with per-autoload metrics
4. Each autoload entry contains: tests, failures, errors, pass_rate, test_file, critical
5. Script successfully parses mapping file and calculates coverage correctly
</success_criteria>

<output>
After completion, create `.planning/phases/10-godot-frontend-coverage/10-05-SUMMARY.md`
</output>
