---
phase: 10
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [test/suites/autoloads/test_network_manager.gd]
autonomous: true
requirements: [GODOT-01]
user_setup: []

must_haves:
  truths:
    - "NetworkManager autoload has comprehensive tests covering all RPC interactions"
    - "Tests verify session management, authentication, and reconnection logic"
    - "Tests use GUT framework patterns (GutTest, before_each, after_each)"
    - "Mock HTTPRequest prevents real network calls during tests"
  artifacts:
    - path: "test/suites/autoloads/test_network_manager.gd"
      provides: "GUT-based tests for NetworkManager autoload"
      min_lines: 300
  key_links:
    - from: "test/suites/autoloads/test_network_manager.gd"
      to: "autoloads/NetworkManager.gd"
      via: "load() and new() instantiation pattern"
      pattern: "load\\(.*NetworkManager\\.gd.*\\)\\.new\\(\\)"
---

<objective>
Create comprehensive GUT-based tests for NetworkManager autoload covering all RPC interactions, session management, authentication, and reconnection logic.

Purpose: NetworkManager is critical for all network communication and must be thoroughly tested to prevent connection issues and session handling bugs.

Output: New GUT-based test file in test/suites/autoloads/ with 20+ tests covering all major NetworkManager functionality.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-godot-frontend-coverage/10-RESEARCH.md
@.planning/phases/10-godot-frontend-coverage/10-VALIDATION.md

@autoloads/NetworkManager.gd
@test/suites/autoloads/test_theme_manager.gd
@test/suites/autoloads/test_accessibility_manager.gd
</context>

<interfaces>
<!-- Key patterns from existing autoload tests for NetworkManager test implementation -->

From test/suites/autoloads/test_theme_manager.gd and test_accessibility_manager.gd:
- Test file extends GutTest
- before_each() creates fresh instances: var test_manager = ThemeManager.new(ConfigFile.new())
- after_each() cleans up: test_manager.queue_free()
- watch_signals() for signal emission testing
- assert_signal_emitted() and assert_signal_emitted_with_parameters() for signal verification

From autoloads/NetworkManager.gd:
- NetworkManager extends Node with EnvironmentType enum
- Key methods: authenticate_device(), _refresh_session(), send_rpc(), send_rpc_async()
- Key signals: session_created, session_refreshed, connection_status_changed, reconnection_attempted, connection_lost
- Session state: session_token, refresh_token, user_id, username, device_id, is_connected
- RPC state: _rpc_request_id, _pending_rpc_callbacks, http_request
- Reconnection state: _reconnect_timer, _retry_attempts, _is_reconnecting
- Environment types: DEVELOPMENT (0), STAGING (1), PRODUCTION (2)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create GUT-based NetworkManager test file structure</name>
  <files>test/suites/autoloads/test_network_manager.gd</files>
  <read_first>
    - autoloads/NetworkManager.gd
    - test/suites/autoloads/test_theme_manager.gd
    - test/suites/autoloads/test_accessibility_manager.gd
  </read_first>
  <action>
    Create test/suites/autoloads/test_network_manager.gd with the following structure:

    ```gdscript
    extends GutTest

    var _network: NetworkManager
    var _mock_http: Node  # Mock HTTPRequest for network isolation

    func before_each():
        # Create fresh NetworkManager instance for each test (ISO-04 pattern)
        _network = NetworkManager.new()
        add_child_autofree(_network)

        # Create mock HTTPRequest using GUT's double() functionality
        # This prevents real network calls during testing
        _mock_http = double(HTTPRequest).new()
        _mock_http.request_completed = Signal()
        add_child_autofree(_mock_http)

        # Stub HTTPRequest.request() to return OK and prevent actual network calls
        stub(_mock_http, "request").to_return(OK)

        # Replace the http_request node in NetworkManager
        _network.http_request = _mock_http

    func after_each():
        # Cleanup is handled by add_child_autofree, but clear references
        _network = null
        _mock_http = null
    ```

    Ensure the file has:
    - Proper GutTest extension
    - before_each() hook for test isolation
    - after_each() hook for cleanup
    - add_child_autofree() for automatic resource cleanup
    - Comment references to ISO-04 (test isolation pattern)
    - Uses GUT's double() to mock HTTPRequest (no separate mock file needed)
  </action>
  <verify>
    <automated>grep -q "extends GutTest" test/suites/autoloads/test_network_manager.gd && grep -q "func before_each" test/suites/autoloads/test_network_manager.gd && grep -q "add_child_autofree" test/suites/autoloads/test_network_manager.gd && grep -q "double(HTTPRequest)" test/suites/autoloads/test_network_manager.gd</automated>
  </verify>
  <done>Test file created with proper GUT structure and test isolation hooks using double() for HTTPRequest mocking</done>
</task>

<task type="auto">
  <name>Task 2: Add session management tests to NetworkManager test file</name>
  <files>test/suites/autoloads/test_network_manager.gd</files>
  <read_first>
    - autoloads/NetworkManager.gd
    - test/suites/autoloads/test_network_manager.gd
  </read_first>
  <action>
    Add the following test methods to test/suites/autoloads/test_network_manager.gd:

    1. test_initial_state() - Verify empty session state on creation
    2. test_session_token_storage() - Test storing session_token, refresh_token, user_id, username
    3. test_logout_clears_session() - Test logout() clears all session data
    4. test_get_auth_headers_empty_token() - Test empty headers when no token
    5. test_get_auth_headers_with_token() - Test Bearer token format
    6. test_is_session_valid() - Test session validation logic (token + connected)
    7. test_device_id_generation() - Test device ID format (32 hex characters)
    8. test_session_file_operations() - Test _save_session_to_file and _load_session_from_file

    Each test should:
    - Use assert_eq() for value comparisons
    - Use assert_true() / assert_false() for boolean checks
    - Follow existing test naming pattern (test_<method>_<scenario>)
    - Have descriptive failure messages as second parameter
  </action>
  <verify>
    <automated>grep -c "func test_" test/suites/autoloads/test_network_manager.gd | grep -q "^[89]$"</automated>
  </verify>
  <done>8 session management tests added with proper GUT assertions</done>
</task>

<task type="auto">
  <name>Task 3: Add authentication and signal tests to NetworkManager test file</name>
  <files>test/suites/autoloads/test_network_manager.gd</files>
  <read_first>
    - autoloads/NetworkManager.gd
    - test/suites/autoloads/test_network_manager.gd
  </read_first>
  <action>
    Add the following test methods to test/suites/autoloads/test_network_manager.gd:

    1. test_authenticate_device_offline() - Test offline mode prevents authentication
    2. test_signal_emission() - Test all signals can be connected and emitted
    3. test_session_created_signal() - Test session_created emits on successful auth
    4. test_session_refreshed_signal() - Test session_refreshed emits on token refresh
    5. test_connection_status_signals() - Test connection_status_changed emits online/offline

    For signal tests:
    - Use watch_signals(_network) before triggering
    - Use assert_signal_emitted() to verify emission
    - Use assert_signal_emitted_with_parameters() for signals with parameters

    Example signal test pattern:
    ```gdscript
    func test_session_created_signal():
        watch_signals(_network)
        _network.session_created.emit(true, "")
        assert_signal_emitted(_network, "session_created", "session_created should be emitted")
    ```
  </action>
  <verify>
    <automated>grep -c "func test_" test/suites/autoloads/test_network_manager.gd | grep -q "^[13]$"</automated>
  </verify>
  <done>5 authentication and signal tests added with watch_signals() usage</done>
</task>

<task type="auto">
  <name>Task 4: Add RPC and reconnection tests to NetworkManager test file</name>
  <files>test/suites/autoloads/test_network_manager.gd</files>
  <read_first>
    - autoloads/NetworkManager.gd
    - test/suites/autoloads/test_network_manager.gd
  </read_first>
  <action>
    Add the following test methods to test/suites/autoloads/test_network_manager.gd:

    1. test_send_rpc_not_authenticated() - Test unauthenticated RPC returns error
    2. test_send_rpc_timeout() - Test RPC timeout handling
    3. test_send_rpc_auth_error() - Test 401/403 response handling
    4. test_send_rpc_async_fire_and_forget() - Test async RPC doesn't wait for response
    5. test_reconnection_attempts() - Test attempt_reconnection() retry logic
    6. test_reconnection_max_attempts() - Test MAX_RETRY_ATTEMPTS limit
    7. test_handle_connection_lost() - Test connection_lost signal and offline mode
    8. test_reconnection_exponential_backoff() - Verify backoff delay increases with attempts

    For reconnection tests:
    - Verify _retry_attempts increments
    - Verify _is_reconnecting state changes
    - Verify reconnection_attempted signal emits

    For RPC tests:
    - Use mock HTTPRequest to simulate responses
    - Test success (200-299), auth error (401/403), timeout scenarios
    - Verify error handling and response parsing
  </action>
  <verify>
    <automated>grep -c "func test_" test/suites/autoloads/test_network_manager.gd | grep -q "^[21]$"</automated>
  </verify>
  <done>8 RPC and reconnection tests added covering error scenarios</done>
</task>

<task type="auto">
  <name>Task 5: Add environment and utility tests to NetworkManager test file</name>
  <files>test/suites/autoloads/test_network_manager.gd</files>
  <read_first>
    - autoloads/NetworkManager.gd
    - test/suites/autoloads/test_network_manager.gd
  </read_first>
  <action>
    Add the following test methods to test/suites/autoloads/test_network_manager.gd:

    1. test_detect_environment_development() - Test environment detection returns valid type (0-2)
    2. test_environment_variable_loading() - Test _load_environment_variables() sets defaults
    3. test_get_environment() - Test get_environment() returns current_environment
    4. test_get_environment_name() - Test get_environment_name() returns string
    5. test_is_production() - Test production detection
    6. test_is_development() - Test development detection
    7. test_is_staging() - Test staging detection
    8. test_base_url_construction() - Test URL format: http://host:port
    9. test_validate_required_config() - Test config validation for missing vars

    For environment tests:
    - Verify _detect_environment() returns values in range 0-2
    - Test EnvironmentType enum values: DEVELOPMENT (0), STAGING (1), PRODUCTION (2)
    - Test get_environment_name() returns "DEVELOPMENT", "STAGING", or "PRODUCTION"

    For utility tests:
    - Verify base_url format matches "http://%s:%d" pattern
    - Test config validation warnings emit for production
  </action>
  <verify>
    <automated>grep -c "func test_" test/suites/autoloads/test_network_manager.gd | grep -q "^29$"</automated>
  </verify>
  <done>9 environment and utility tests added completing NetworkManager test suite</done>
</task>

</tasks>

<verification>
After all tasks complete, run the GUT test suite:
```bash
godot4 --headless --script res://test/run_all_tests.gd --select suites/autoloads --unit_test test_network_manager
```

Verify:
- All 29 tests pass
- No test failures or errors
- Test execution time < 30 seconds
- JUnit XML output generated at test/results/gut-results.xml
</verification>

<success_criteria>
1. NetworkManager autoload has comprehensive GUT-based tests covering all major functionality
2. 29+ tests covering: session management (8), auth/signals (5), RPC/reconnection (8), environment/utility (9)
3. All tests use proper GUT patterns (GutTest, before_each/after_each, watch_signals)
4. Mock HTTPRequest (using double()) prevents real network calls during test execution
5. Test execution completes in < 30 seconds with 100% pass rate
</success_criteria>

<output>
After completion, create `.planning/phases/10-godot-frontend-coverage/10-01-SUMMARY.md`
</output>
