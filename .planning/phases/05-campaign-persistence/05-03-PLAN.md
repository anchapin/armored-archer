---
phase: 05-campaign-persistence
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified: []
autonomous: false
requirements:
  - PERSIST-01
  - PERSIST-02
  - PERSIST-03
  - PERSIST-04

must_haves:
  truths:
    - "Campaign progress persists after closing and reopening the game"
    - "Campaign progress syncs correctly between local and server state"
    - "Stage completion sends correct difficulty to backend (no validation errors)"
    - "A new player starts with stage 1_1 unlocked and no completions"
    - "An existing player sees their previously completed stages restored from server"
  artifacts: []
  key_links:
    - from: "CampaignManager.gd"
      to: "stage_tracking.ts"
      via: "get_campaign_progress RPC"
      pattern: "armored_archer/get_campaign_progress"
    - from: "CampaignManager.gd"
      to: "gear_system.ts"
      via: "stage_complete RPC with correct difficulty"
      pattern: "armored_archer/stage_complete"
---

<objective>
Human verification checkpoint to confirm campaign persistence works end-to-end across sessions.

Purpose: Automated tests can verify code structure but cannot confirm the full gameplay loop persists correctly. This checkpoint requires a human to complete a PvE stage, close the game, reopen, and verify progress is restored.

Output: Human-confirmed working persistence or documented issues to fix.
</objective>

<execution_context>
@/home/alex/.agents/get-shit-done/workflows/execute-plan.md
@/home/alex/.agents/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/milestones/v3.4.0-ROADMAP.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify campaign persistence across sessions</name>
  <files>autoloads/CampaignManager.gd</files>
  <action>Human verification checkpoint — no code changes. Verify campaign persistence works end-to-end.</action>
  <what-built>Campaign state persistence across sessions — server sync, difficulty fix, merge logic</what-built>
  <how-to-verify>
    1. Open Godot editor, run the project (F5)
    2. Navigate to the Campaign Map screen
    3. Complete a PvE stage (select an unlocked stage, fight, win)
    4. Verify the stage shows as completed on the campaign map (checkmark)
    5. Close the Godot game entirely
    6. Reopen and run the project again
    7. Navigate to Campaign Map
    8. **Expected:** Previously completed stages still show as completed, next stage is unlocked
    9. Check the Godot output console for "sync_campaign_progress" related messages
    10. Verify no validation errors appear in the Nakama server logs (check localhost:7351 console)
  </how-to-verify>
  <verify>Campaign progress persists after game restart, no server validation errors, campaign map shows correct state</verify>
  <done>Human confirms persistence works or documents specific issues to fix</done>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- Stage completion persists across game restarts
- No server-side validation errors for difficulty values
- Campaign map correctly shows completed/unlocked state after restart
- Server logs show successful `get_campaign_progress` calls
</verification>

<success_criteria>
- Human confirms: completed stages persist after game restart
- Human confirms: no validation errors in server logs
- Human confirms: campaign map displays correct state from server data
- If issues found: documented with specific error messages and steps to reproduce
</success_criteria>

<output>
After completion, create `.planning/phases/05-campaign-persistence/05-03-SUMMARY.md`
</output>
