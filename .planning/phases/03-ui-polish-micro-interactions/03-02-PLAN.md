---
phase: 03-ui-polish-micro-interactions
plan: 02
type: execute
wave: 1
depends_on: ["01"]
files_modified:
  - autoloads/AnimationUtils.gd
  - autoloads/UITransitionOptimizer.gd
  - scenes/ui/main_menu.gd
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Screen transitions use device-tier settings from UITransitionOptimizer"
    - "Transitions are smooth (no frame drops on mobile)"
    - "All menu screens use consistent transition patterns"
  artifacts:
    - path: autoloads/UITransitionOptimizer.gd
      provides: "Enhanced with tween-based scene transitions"
      contains: "create_tween"
    - path: scenes/ui/main_menu.gd
      provides: "Uses smooth scene transitions"
      contains: "AnimationUtils"
  key_links:
    - from: UITransitionOptimizer.gd
      to: AnimationUtils.gd
      via: "Use AnimationUtils.slide_in/fade_in for transitions"
      pattern: "AnimationUtils\\."
---

<objective>
Implement smooth screen transitions between menus

Purpose: Create polished navigation experience with consistent fade/slide transitions that respect device capabilities.

Output: Enhanced UITransitionOptimizer with tween transitions, main_menu.gd with transition calls
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-ui-polish-micro-interactions/03-01-PLAN.md
@autoloads/UITransitionOptimizer.gd
@autoloads/AnimationUtils.gd
@scenes/ui/main_menu.gd
@scenes/ui/login_screen.gd
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add tween-based transition methods to UITransitionOptimizer</name>
  <files>autoloads/UITransitionOptimizer.gd</files>
  <behavior>
    - Test 1: transition_to() performs fade out -> scene change -> fade in
    - Test 2: transition respects device tier duration settings
    - Test 3: Multiple rapid transitions don't cause visual glitches
    - Test 4: Works with any scene change (menu to game, menu to store, etc.)
  </behavior>
  <action>
    Enhance UITransitionOptimizer with tween-based scene transitions:

    1. Add import for AnimationUtils (if needed)
    2. Add transition methods:

    ```gdscript
    ## Perform scene transition with fade
    func transition_to_scene(scene_path: String, use_slide: bool = false) -> void:
        var duration = get_transition_duration()
        var current_root = get_tree().current_scene
        
        if current_root and is_instance_valid(current_root):
            # Fade out current scene
            var fade_out_tween = create_tween()
            fade_out_tween.tween_property(current_root, "modulate:a", 0.0, duration)
            await fade_out_tween.finished
        
        # Change scene
        get_tree().change_scene_to_file(scene_path)
        await get_tree().process_frame
        
        # Fade in new scene
        var new_root = get_tree().current_scene
        if new_root and is_instance_valid(new_root):
            new_root.modulate.a = 0.0
            var fade_in_tween = create_tween()
            fade_in_tween.tween_property(new_root, "modulate:a", 1.0, duration)
    ```

    3. Add method for menu-to-menu slide transitions:
    ```gdscript
    func slide_transition_to(direction: String, new_scene_path: String) -> void:
        # Uses AnimationUtils.slide_in after scene change
        get_tree().change_scene_to_file(new_scene_path)
    ```

    Preserve all existing device-tier optimization logic.
  </action>
  <verify>
    <manual>Check UITransitionOptimizer.gd contains transition_to_scene with create_tween</manual>
  </verify>
  <done>
    UITransitionOptimizer has tween-based scene transitions, uses device-tier duration, maintains optimization logic
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add transition calls to main_menu.gd navigation</name>
  <files>scenes/ui/main_menu.gd</files>
  <behavior>
    - Test 1: Navigate to other screens with transition effect
    - Test 2: Back button uses reverse transition
    - Test 3: Transition is visible but doesn't slow down navigation
  </behavior>
  <action>
    Update main_menu.gd to use screen transitions:

    1. Read main_menu.gd to find navigation button handlers
    2. Replace direct scene changes with transition calls:

    ```gdscript
    # Example: Play button handler
    func _on_play_pressed() -> void:
        if has_node("/root/UITransitionOptimizer"):
            $"/root/UITransitionOptimizer".transition_to_scene("res://scenes/ui/gameplay.tscn")
        else:
            get_tree().change_scene_to_file("res://scenes/ui/gameplay.tscn")
    ```

    3. Apply to all navigation: play, store, loadout, settings, leaderboard
    
    Preserve existing button functionality, add transition as enhancement.
  </action>
  <verify>
    <manual>Check main_menu.gd uses UITransitionOptimizer for scene changes</manual>
  </verify>
  <done>
    main_menu.gd uses smooth transitions for all navigation, transitions respect device tier
  </done>
</task>

</tasks>

<verification>
## Phase 03-02 Verification

### Automated Checks
- UITransitionOptimizer has transition method: `grep -q "transition_to_scene" autoloads/UITransitionOptimizer.gd`
- main_menu.gd uses transitions: `grep -q "UITransitionOptimizer" scenes/ui/main_menu.gd`

### Manual Verification
- Navigate between menus: Should see fade/slide transition
- Check device tier affects duration (budget = faster)

### Success Criteria
- [ ] UITransitionOptimizer performs tween-based scene transitions
- [ ] Transition duration respects device tier
- [ ] main_menu.gd uses transitions for navigation
- [ ] No visual glitches during transitions
- [ ] Transitions feel smooth on mobile (60 FPS)
</verification>

<success_criteria>
1. Screen transitions use Tweens (not instant scene changes)
2. Duration adapts to device tier (0.15s budget, 0.3s flagship)
3. main_menu.gd uses transitions for all navigation
4. Back navigation has appropriate reverse transition
5. 60 FPS maintained during transitions on mobile
</success_criteria>

<output>
After completion, create `.planning/phases/03-ui-polish-micro-interactions/03-02-SUMMARY.md` with:
- Transition implementation summary
- Navigation points updated
- Performance notes
</output>