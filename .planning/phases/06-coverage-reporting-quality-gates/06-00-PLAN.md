---
phase: 06-coverage-reporting-quality-gates
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/coverage/.gitkeep
  - backend/scripts/generate-coverage-report.sh
  - scripts/calculate-godot-coverage.py
  - scripts/track-coverage-history.sh
  - scripts/detect-go-flaky-tests.sh
  - scripts/detect-godot-flaky-tests.py
  - scripts/quarantine-flaky-tests.sh
  - scripts/generate-flaky-dashboard.py
  - scripts/compare-screenshots.py
  - backend/tests/testhelpers/quarantine.go
  - backend/internal/rng/rng.go
  - backend/internal/combat/combat_property_test.go
  - backend/internal/rng/rng_property_test.go
  - test/suites/visual/test_visual_regression.gd
  - test/suites/visual/test_theme_consistency.gd
  - test/screenshots/baseline/.gitkeep
  - .github/workflows/coverage.yml
  - .github/workflows/flaky-tests.yml
  - .github/workflows/property-tests.yml
  - .github/workflows/visual-regression.yml
autonomous: true
requirements:
  - COV-01
  - COV-02
  - COV-03
  - COV-04
  - COV-05
  - FLK-01
  - FLK-02
  - FLK-03
  - FLK-04
  - VIS-01
  - VIS-02
  - VIS-03
  - PBT-01
  - PBT-02
  - PBT-03
user_setup: []
must_haves:
  truths:
    - "All test stub files exist before Wave 1 execution begins"
    - "Test stubs provide minimal valid structure that can be executed"
    - "Wave 1 plans can implement logic in existing stub files"
  artifacts:
    - path: backend/coverage/.gitkeep
      provides: "Coverage directory placeholder"
    - path: backend/scripts/generate-coverage-report.sh
      provides: "Go coverage generation stub"
    - path: scripts/calculate_godot_coverage.py
      provides: "Godot coverage calculation stub"
    - path: scripts/track-coverage-history.sh
      provides: "Coverage history tracking stub"
    - path: scripts/detect-go-flaky-tests.sh
      provides: "Go flaky test detection stub"
    - path: scripts/detect-godot-flaky-tests.py
      provides: "Godot flaky test detection stub"
    - path: scripts/quarantine-flaky-tests.sh
      provides: "Flaky test quarantine stub"
    - path: scripts/generate-flaky-dashboard.py
      provides: "Flaky test dashboard generation stub"
    - path: scripts/compare-screenshots.py
      provides: "Screenshot comparison stub"
    - path: backend/tests/testhelpers/quarantine.go
      provides: "Quarantine registry stub"
    - path: backend/internal/rng/rng.go
      provides: "RNG module stub"
    - path: backend/internal/combat/combat_property_test.go
      provides: "Combat property test stub"
    - path: backend/internal/rng/rng_property_test.go
      provides: "RNG property test stub"
    - path: test/suites/visual/test_visual_regression.gd
      provides: "Visual regression test stub"
    - path: test/suites/visual/test_theme_consistency.gd
      provides: "Theme consistency test stub"
    - path: test/screenshots/baseline/.gitkeep
      provides: "Baseline screenshot directory placeholder"
    - path: .github/workflows/coverage.yml
      provides: "Coverage CI workflow stub"
    - path: .github/workflows/flaky-tests.yml
      provides: "Flaky test CI workflow stub"
    - path: .github/workflows/property-tests.yml
      provides: "Property test CI workflow stub"
    - path: .github/workflows/visual-regression.yml
      provides: "Visual regression CI workflow stub"
  key_links: []
---

<objective>
Create all test stub files, directories, and CI workflow skeletons before Wave 1 execution begins. This ensures Wave 1-4 plans can implement logic in existing files rather than creating them, enabling test files to exist before being tested.

Purpose: Satisfy nyquist compliance — test files must exist before they are executed. Wave 0 creates minimal valid structure.

Output: Directory structure, stub files with minimal content, CI workflow skeletons.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-coverage-reporting-quality-gates/06-RESEARCH.md
@.planning/phases/05-complete-test-infrastructure-foundation/05-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Coverage Test Stubs</name>
  <files>
    backend/coverage/.gitkeep
    backend/scripts/generate-coverage-report.sh
    scripts/calculate_godot_coverage.py
    scripts/track-coverage-history.sh
  </files>
  <read_first>
    backend/go.mod
    .gutconfig.json
  </read_first>
  <action>
    Create minimal stub files:

    1. backend/coverage/.gitkeep:
       ```bash
       #!/bin/bash
       mkdir -p backend/coverage
       echo "# Coverage reports directory" > backend/coverage/.gitkeep
       echo "# HTML reports generated in CI" >> backend/coverage/.gitkeep
       ```

    2. backend/scripts/generate-coverage-report.sh:
       ```bash
       #!/bin/bash
       # Stub: Generate Go coverage report
       # Implemented in: 06-01-PLAN.md Task 1
       set -e
       mkdir -p backend/coverage/html
       echo "Coverage stub: generates backend/coverage/html/index.html"
       ```

    3. scripts/calculate_godot_coverage.py:
       ```python
       #!/usr/bin/env python3
       # Stub: Calculate Godot coverage from GUT JUnit XML
       # Implemented in: 06-01-PLAN.md Task 2
       import sys
       print('{"pass_rate": 100.0, "total_tests": 0, "failures": 0, "errors": 0}')
       ```

    4. scripts/track-coverage-history.sh:
       ```bash
       #!/bin/bash
       # Stub: Track coverage history in git
       # Implemented in: 06-01-PLAN.md Task 6
       mkdir -p data
       echo '{"history": []}' > data/coverage-history.json
       ```

    Make all scripts executable:
    ```bash
    chmod +x backend/scripts/generate-coverage-report.sh
    chmod +x scripts/calculate_godot_coverage.py
    chmod +x scripts/track-coverage-history.sh
    ```
  </action>
  <verify>
    <automated>test -f backend/coverage/.gitkeep && test -x backend/scripts/generate-coverage-report.sh && test -x scripts/calculate_godot_coverage.py && test -x scripts/track-coverage-history.sh</automated>
  </verify>
  <done>
    Coverage stub files exist: backend/coverage/.gitkeep, generate-coverage-report.sh, calculate_godot_coverage.py, track-coverage-history.sh.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Flaky Test Detection Stubs</name>
  <files>
    scripts/detect-go-flaky-tests.sh
    scripts/detect-godot-flaky-tests.py
    scripts/quarantine-flaky-tests.sh
    scripts/generate-flaky-dashboard.py
    backend/tests/testhelpers/quarantine.go
  </files>
  <read_first>
    backend/tests/combat/combat_test.go
  </read_first>
  <action>
    Create minimal stub files:

    1. scripts/detect-go-flaky-tests.sh:
       ```bash
       #!/bin/bash
       # Stub: Detect flaky Go tests via 3x retry
       # Implemented in: 06-02-PLAN.md Task 1
       mkdir -p data
       echo '{"flaky_tests": [], "timestamp": "'$(date -u +%Y-%m-%dT%H:%M:%SZ)'"}' > data/go-flaky-tests.json
       ```

    2. scripts/detect-godot-flaky-tests.py:
       ```python
       #!/usr/bin/env python3
       # Stub: Detect flaky Godot tests via 3x retry
       # Implemented in: 06-02-PLAN.md Task 2
       import json, datetime
       data = {"flaky_tests": {}, "timestamp": datetime.datetime.now().isoformat()}
       with open('data/godot-flaky-tests.json', 'w') as f:
           json.dump(data, f)
       ```

    3. scripts/quarantine-flaky-tests.sh:
       ```bash
       #!/bin/bash
       # Stub: Apply build tags to quarantined Go tests
       # Implemented in: 06-02-PLAN.md Task 3
       echo "Quarantine stub: prepends //go:build !flaky to test files"
       ```

    4. scripts/generate-flaky-dashboard.py:
       ```python
       #!/usr/bin/env python3
       # Stub: Generate flaky test dashboard
       # Implemented in: 06-02-PLAN.md Task 5
       print("# Flaky Test Dashboard\n\nNo flaky tests detected.")
       ```

    5. backend/tests/testhelpers/quarantine.go:
       ```go
       //go:build !flaky
       // +build !flaky

       package testhelpers

       // QuarantinedTests lists flaky tests to skip in normal CI.
       // Implemented in: 06-02-PLAN.md Task 3
       var QuarantinedTests = map[string]string{}

       // IsQuarantined returns true and the reason if the test is quarantined.
       func IsQuarantined(testName string) (bool, string) {
           reason, exists := QuarantinedTests[testName]
           return exists, reason
       }
       ```

    Make scripts executable:
    ```bash
    chmod +x scripts/detect-go-flaky-tests.sh
    chmod +x scripts/detect-godot-flaky-tests.py
    chmod +x scripts/quarantine-flaky-tests.sh
    chmod +x scripts/generate-flaky-dashboard.py
    ```
  </action>
  <verify>
    <automated>test -x scripts/detect-go-flaky-tests.sh && test -x scripts/detect-godot-flaky-tests.py && test -x scripts/quarantine-flaky-tests.sh && test -x scripts/generate-flaky-dashboard.py && test -f backend/tests/testhelpers/quarantine.go</automated>
  </verify>
  <done>
    Flaky test stub files exist: detect-go-flaky-tests.sh, detect-godot-flaky-tests.py, quarantine-flaky-tests.sh, generate-flaky-dashboard.py, quarantine.go.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Visual Regression Test Stubs</name>
  <files>
    scripts/compare-screenshots.py
    test/suites/visual/test_visual_regression.gd
    test/suites/visual/test_theme_consistency.gd
    test/screenshots/baseline/.gitkeep
  </files>
  <read_first>
    autoloads/design_tokens.gd
    autoloads/ThemeManager.gd
  </read_first>
  <action>
    Create minimal stub files:

    1. scripts/compare-screenshots.py:
       ```python
       #!/usr/bin/env python3
       # Stub: Compare screenshots using ImageMagick
       # Implemented in: 06-03-PLAN.md Task 1
       import sys, json
       print(json.dumps({"passed": True, "difference": 0.0, "threshold": 0.01}))
       ```

    2. test/suites/visual/test_visual_regression.gd:
       ```gdscript
       # Stub: Visual regression tests for base UI components
       # Implemented in: 06-03-PLAN.md Task 2
       extends GutTest

       func test_base_button_visual_regression():
           # Placeholder: captures and compares button screenshot
           assert_true(true, "Button visual regression test stub")

       func test_base_panel_visual_regression():
           # Placeholder: captures and compares panel screenshot
           assert_true(true, "Panel visual regression test stub")

       func test_base_progress_bar_visual_regression():
           # Placeholder: captures and compares progress bar screenshot
           assert_true(true, "Progress bar visual regression test stub")

       func test_theme_toggle_visual_regression():
           # Placeholder: captures and compares theme toggle screenshot
           assert_true(true, "Theme toggle visual regression test stub")

       func test_base_label_visual_regression():
           # Placeholder: captures and compares label screenshot (nice-to-have)
           assert_true(true, "Label visual regression test stub")

       func test_base_icon_visual_regression():
           # Placeholder: captures and compares icon screenshot (nice-to-have)
           assert_true(true, "Icon visual regression test stub")

       func test_base_container_visual_regression():
           # Placeholder: captures and compares container screenshot (nice-to-have)
           assert_true(true, "Container visual regression test stub")

       func test_loading_indicator_visual_regression():
           # Placeholder: captures and compares loading indicator screenshot (nice-to-have)
           assert_true(true, "Loading indicator visual regression test stub")
       ```

    3. test/suites/visual/test_theme_consistency.gd:
       ```gdscript
       # Stub: Theme consistency validation tests
       # Implemented in: 06-03-PLAN.md Task 3
       extends GutTest

       func test_theme_switching_layout_invariance():
           # Placeholder: verifies theme switch doesn't change layout
           assert_true(true, "Theme switching layout invariance stub")

       func test_color_contrast_accessibility():
           # Placeholder: verifies WCAG AA contrast compliance
           assert_true(true, "Color contrast accessibility stub")

       func test_design_token_usage():
           # Placeholder: verifies design tokens are used
           assert_true(true, "Design token usage stub")
       ```

    4. test/screenshots/baseline/.gitkeep:
       ```bash
       mkdir -p test/screenshots/baseline
       echo "# Baseline screenshots for visual regression testing" > test/screenshots/baseline/.gitkeep
       echo "# Naming convention: {component}_{theme}_{viewport}.png" >> test/screenshots/baseline/.gitkeep
       ```

    Make script executable:
    ```bash
    chmod +x scripts/compare-screenshots.py
    ```
  </action>
  <verify>
    <automated>test -x scripts/compare-screenshots.py && test -f test/suites/visual/test_visual_regression.gd && test -f test/suites/visual/test_theme_consistency.gd && test -f test/screenshots/baseline/.gitkeep</automated>
  </verify>
  <done>
    Visual regression stub files exist: compare-screenshots.py, test_visual_regression.gd, test_theme_consistency.gd, baseline/.gitkeep.
  </done>
</task>

<task type="auto">
  <name>Task 4: Create Property-Based Test Stubs</name>
  <files>
    backend/internal/rng/rng.go
    backend/internal/combat/combat_property_test.go
    backend/internal/rng/rng_property_test.go
  </files>
  <read_first>
    backend/internal/combat/combat.go
  </read_first>
  <action>
    Create minimal stub files:

    1. backend/internal/rng/rng.go:
       ```go
       // Package rng provides random number generation utilities.
       // Implemented in: 06-04-PLAN.md Task 1
       package rng

       import "math/rand"

       var (
           source = rand.NewSource(0)
           rng    = rand.New(source)
       )

       // RNG returns the random generator.
       func RNG() *rand.Rand {
           return rng
       }

       // Seed resets the RNG seed.
       func Seed(seed int64) {
           source = rand.NewSource(seed)
           rng = rand.New(source)
       }

       // RollInt generates a random int in range [min, max).
       func RollInt(min, max int) int {
           if min >= max {
               return min
           }
           return rng.Intn(max-min) + min
       }

       // RollFloat generates a random float64 in range [min, max).
       func RollFloat(min, max float64) float64 {
           if min >= max {
               return min
           }
           return rng.Float64()*(max-min) + min
       }
       ```

    2. backend/internal/combat/combat_property_test.go:
       ```go
       // Package combat property-based tests.
       // Implemented in: 06-04-PLAN.md Task 2
       package combat

       import "testing"

       func TestDamageProperty_NonNegative(t *testing.T) {
           // Stub: Damage should always be non-negative
           t.Skip("Property test stub: implemented in 06-04-PLAN.md Task 2")
       }

       func TestDamageProperty_Deterministic(t *testing.T) {
           // Stub: Damage should be deterministic
           t.Skip("Property test stub: implemented in 06-04-PLAN.md Task 2")
       }

       func TestDamageProperty_DefenseReducesDamage(t *testing.T) {
           // Stub: Higher defense should never increase damage
           t.Skip("Property test stub: implemented in 06-04-PLAN.md Task 2")
       }

       func TestDamageProperty_CritIncreasesDamage(t *testing.T) {
           // Stub: Critical hits should always deal more damage
           t.Skip("Property test stub: implemented in 06-04-PLAN.md Task 2")
       }

       func TestHitChanceProperty_Bounded(t *testing.T) {
           // Stub: Hit chance should be bounded [0.1, 0.95]
           t.Skip("Property test stub: implemented in 06-04-PLAN.md Task 2")
       }

       func TestCritProperty_Statistical(t *testing.T) {
           // Stub: Crit rate should produce expected distribution
           t.Skip("Property test stub: implemented in 06-04-PLAN.md Task 2")
       }
       ```

    3. backend/internal/rng/rng_property_test.go:
       ```go
       // Package rng property-based tests.
       // Implemented in: 06-04-PLAN.md Task 3
       package rng

       import "testing"

       func TestRollIntProperty_WithinBounds(t *testing.T) {
           // Stub: RollInt should return value within bounds
           t.Skip("Property test stub: implemented in 06-04-PLAN.md Task 3")
       }

       func TestRollIntProperty_UniformDistribution(t *testing.T) {
           // Stub: RollInt should produce uniform distribution
           t.Skip("Property test stub: implemented in 06-04-PLAN.md Task 3")
       }

       func TestRollFloatProperty_WithinBounds(t *testing.T) {
           // Stub: RollFloat should return value within bounds
           t.Skip("Property test stub: implemented in 06-04-PLAN.md Task 3")
       }

       func TestRollChoiceProperty_FromSlice(t *testing.T) {
           // Stub: RollChoice should return element from slice
           t.Skip("Property test stub: implemented in 06-04-PLAN.md Task 3")
       }

       func TestShuffleProperty_Permutation(t *testing.T) {
           // Stub: Shuffle should preserve all elements
           t.Skip("Property test stub: implemented in 06-04-PLAN.md Task 3")
       }

       func TestSeedProperty_DifferentSequences(t *testing.T) {
           // Stub: Different seeds should produce different sequences
           t.Skip("Property test stub: implemented in 06-04-PLAN.md Task 3")
       }

       func TestSeedProperty_SameSequence(t *testing.T) {
           // Stub: Same seed should produce same sequence
           t.Skip("Property test stub: implemented in 06-04-PLAN.md Task 3")
       }
       ```
  </action>
  <verify>
    <automated>test -f backend/internal/rng/rng.go && test -f backend/internal/combat/combat_property_test.go && test -f backend/internal/rng/rng_property_test.go</automated>
  </verify>
  <done>
    Property-based test stub files exist: rng.go, combat_property_test.go, rng_property_test.go.
  </done>
</task>

<task type="auto">
  <name>Task 5: Create CI Workflow Skeletons</name>
  <files>
    .github/workflows/coverage.yml
    .github/workflows/flaky-tests.yml
    .github/workflows/property-tests.yml
    .github/workflows/visual-regression.yml
  </files>
  <read_first>
    .github/workflows/test.yml
  </read_first>
  <action>
    Create minimal CI workflow skeletons:

    1. .github/workflows/coverage.yml:
       ```yaml
       # Coverage & Quality Gates
       # Implemented in: 06-01-PLAN.md Task 3
       name: Coverage & Quality Gates
       on:
         pull_request:
           branches: [main, develop]
         push:
           branches: [main, develop]
       jobs:
         backend-coverage:
           runs-on: ubuntu-latest
           steps:
             - uses: actions/checkout@v4
             - name: Coverage stub
               run: echo "Coverage workflow stub: implemented in 06-01-PLAN.md"
         godot-coverage:
           runs-on: ubuntu-latest
           steps:
             - uses: actions/checkout@v4
             - name: Coverage stub
               run: echo "Godot coverage workflow stub: implemented in 06-01-PLAN.md"
       ```

    2. .github/workflows/flaky-tests.yml:
       ```yaml
       # Flaky Test Detection
       # Implemented in: 06-02-PLAN.md Task 4
       name: Flaky Test Detection
       on:
         schedule:
           - cron: '0 3 * * 0'  # Sunday 3 AM
         workflow_dispatch:
       jobs:
         flaky-tests-backend:
           runs-on: ubuntu-latest
           steps:
             - uses: actions/checkout@v4
             - name: Flaky test stub
               run: echo "Flaky test workflow stub: implemented in 06-02-PLAN.md"
         flaky-tests-godot:
           runs-on: ubuntu-latest
           steps:
             - uses: actions/checkout@v4
             - name: Flaky test stub
               run: echo "Godot flaky test workflow stub: implemented in 06-02-PLAN.md"
       ```

    3. .github/workflows/property-tests.yml:
       ```yaml
       # Property-Based Tests
       # Implemented in: 06-04-PLAN.md Task 4
       name: Property-Based Tests
       on:
         pull_request:
           branches: [main, develop]
         push:
           branches: [main, develop]
       jobs:
         property-tests:
           runs-on: ubuntu-latest
           steps:
             - uses: actions/checkout@v4
             - name: Property test stub
               run: echo "Property test workflow stub: implemented in 06-04-PLAN.md"
       ```

    4. .github/workflows/visual-regression.yml:
       ```yaml
       # Visual Regression Tests
       # Implemented in: 06-03-PLAN.md Task 4
       name: Visual Regression Tests
       on:
         pull_request:
           paths:
             - 'scenes/ui/**'
             - 'autoloads/design_tokens.gd'
         push:
           branches: [main, develop]
       jobs:
         visual-regression:
           runs-on: ubuntu-latest
           steps:
             - uses: actions/checkout@v4
             - name: Visual regression stub
               run: echo "Visual regression workflow stub: implemented in 06-03-PLAN.md"
         theme-consistency:
           runs-on: ubuntu-latest
           steps:
             - uses: actions/checkout@v4
             - name: Theme consistency stub
               run: echo "Theme consistency workflow stub: implemented in 06-03-PLAN.md"
       ```
  </action>
  <verify>
    <automated>test -f .github/workflows/coverage.yml && test -f .github/workflows/flaky-tests.yml && test -f .github/workflows/property-tests.yml && test -f .github/workflows/visual-regression.yml</automated>
  </verify>
  <done>
    CI workflow skeleton files exist: coverage.yml, flaky-tests.yml, property-tests.yml, visual-regression.yml.
  </done>
</task>

</tasks>

<verification>
Overall phase verification:
1. Verify all stub files exist: `find backend scripts test .github/workflows -name "*.sh" -o -name "*.py" -o -name "*.go" -o -name "*.gd" -o -name "*.yml" | grep -E "(coverage|flaky|property|visual|quarantine|rng)" | wc -l` should be 20+
2. Verify all scripts are executable: `find backend/scripts scripts -name "*.sh" -exec test -x {} \; | wc -l` should be 6+
3. Verify Python scripts are executable: `find scripts -name "*.py" -exec test -x {} \; | wc -l` should be 4+
4. Verify Go test stubs compile: `cd backend && go test -run TestDamageProperty ./internal/combat/ 2>&1 | grep -q "SKIP"` or similar
5. Verify Godot test stubs exist: `test -f test/suites/visual/test_visual_regression.gd && test -f test/suites/visual/test_theme_consistency.gd`
</verification>

<success_criteria>
1. All 20+ stub files exist across backend, scripts, test, and .github/workflows
2. All shell scripts have executable permissions
3. All Python scripts have executable permissions
4. Go test stubs compile (even if skipped)
5. Godot test stubs exist and are syntactically valid
6. CI workflow skeletons are valid YAML
7. Wave 1-4 plans can implement logic in existing stub files
</success_criteria>

<output>
After completion, create `.planning/phases/06-coverage-reporting-quality-gates/06-00-SUMMARY.md`
</output>
