---
phase: 27-reach-60-percent-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [.planning/phases/27-reach-60-percent-coverage/27-01-SUMMARY.md]
autonomous: true
requirements: [COV-01]
must_haves:
  truths:
    - "Overall Go coverage exceeds 60% threshold"
    - "Critical packages (combat, matchmaking, rpg) exceed 80% threshold"
    - "Coverage gate script passes at Stage 3"
  artifacts:
    - path: ".planning/phases/27-reach-60-percent-coverage/27-01-SUMMARY.md"
      provides: "Coverage verification results"
  key_links:
    - from: "backend/coverage/coverage.out"
      to: "backend/tests/quality/coverage_gates.sh"
      via: "Coverage measurement and gate enforcement"
---

<objective>
Verify that the 60% coverage target has been achieved and document the results.
</objective>

<execution_context>
@/home/alex/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/coverage/coverage.out
@backend/data/coverage-thresholds.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify overall coverage exceeds 60%</name>
  <files>backend/coverage/coverage.out</files>
  <action>
    Run the coverage gate script to verify overall coverage exceeds 60% threshold.
    The cached coverage.out file should show ~73.3% overall coverage.
  </action>
  <verify>
    <automated>cd backend && bash tests/quality/coverage_gates.sh</automated>
  </verify>
  <done>Overall coverage 73.3% exceeds 60% threshold.</done>
</task>

<task type="auto">
  <name>Task 2: Verify critical packages exceed 80%</name>
  <files>backend/coverage/coverage.out</files>
  <action>
    Verify that combat, matchmaking, and rpg packages all exceed 80% coverage.
    Extract individual package coverage from the coverage report.
  </action>
  <verify>
    <automated>cd backend && go tool cover -func=coverage/coverage.out | grep -E "^total:|internal/combat/|internal/matchmaking/|internal/rpg/"</automated>
  </verify>
  <done>Critical packages combat, matchmaking, rpg all exceed 80% coverage.</done>
</task>

<task type="auto">
  <name>Task 3: Create Plan 01 Summary</name>
  <files>.planning/phases/27-reach-60-percent-coverage/27-01-SUMMARY.md</files>
  <action>
    Create a summary document capturing:
    - Overall coverage: 73.3% (exceeds 60% target)
    - Critical package coverage
    - Success criteria verification
  </action>
  <verify>
    <automated>ls .planning/phases/27-reach-60-percent-coverage/27-01-SUMMARY.md</automated>
  </verify>
  <done>Summary created documenting coverage verification.</done>
</task>

</tasks>

<success_criteria>
1. Overall coverage: 73.3% > 60% (PASS)
2. Combat coverage > 80% (PASS)
3. Matchmaking coverage > 80% (PASS)
4. RPG coverage > 80% (PASS)
5. Coverage gate script returns exit 0 (PASS)
</success_criteria>

<output>
After completion, create `.planning/phases/27-reach-60-percent-coverage/27-01-SUMMARY.md`
</output>
