---
phase: 15
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/rpg/rpg_property_test.go
autonomous: true
requirements:
  - PBT-01
  - PBT-04
  - PBT-06
must_haves:
  truths:
    - XP is never negative or decreases
    - Level is monotonic function of XP (higher XP never decreases level)
    - Level-up grants correct number of ability points (3 per level)
    - Stat allocation conserves total points (allocated points = ability points spent)
    - XP formula consistency across different XP sources (PvE, PvP, quests)
    - XP requirements follow quadratic scaling (baseXP * level^2)
  artifacts:
    - path: backend/internal/rpg/rpg_property_test.go
      provides: Progression system invariant tests
      min_lines: 150
  key_links:
    - from: backend/internal/rpg/rpg_property_test.go
      to: backend/internal/rpg/rpg.go
      via: Direct imports and function calls
      pattern: "rpg\\.|CalculateLevel|AddXP|AllocateStat"
---

<objective>
Add property-based tests for progression system to verify XP consistency, level-up mechanics, and stat allocation invariants.

Purpose: Systematically discover edge cases in progression formulas that unit tests might miss, ensuring XP gains, level calculations, and stat allocation behave correctly across all possible inputs.

Output: 6-8 property tests covering XP formulas, level-up mechanics, and stat allocation invariants in `backend/internal/rpg/rpg_property_test.go`
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-property-based-testing-expansion/15-CONTEXT.md
@.planning/phases/15-property-based-testing-expansion/15-RESEARCH.md
@.planning/phases/15-property-based-testing-expansion/15-VALIDATION.md
@.planning/REQUIREMENTS.md

# Reference patterns from existing property tests
@backend/internal/rng/rng_property_test.go
@backend/internal/combat/combat_property_test.go

# Progression system implementation
@backend/internal/rpg/rpg.go

# Test fixtures
@backend/tests/testhelpers/fixtures_builder.go
</context>

<interfaces>
<!-- Key types and contracts from progression system that tests need -->

From backend/internal/rpg/rpg.go:
```go
const (
    MaxLevel = 100
    BaseXP = 100
    XPScalingFactor = 1.5
    PointsPerLevel = 3
    MaxStatPointsPerLevel = 5
)

type PlayerStats struct {
    UserID        string
    Level         int
    XP            int
    AbilityPoints int
    Stats         struct {
        Attack   int
        Defense  int
        Dodge    int
        CritRate int
    }
}

func CalculateLevel(totalXP int) int
func XPRequiredForLevel(level int) int
func XPRequiredForNextLevel(currentLevel int) int
func (p *PlayerStats) AddXP(xpAmount int, source string) (levelsGained int, newLevel int)
func (p *PlayerStats) AllocateStat(statName string, points int) error
func (p *PlayerStats) GetTotalStats() int
```

Valid stats: attack, defense, dodge, crit_rate
Valid XP sources: pve, pvp, quest (per XPGainRequest.Source field)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create progression property test file with XP invariants</name>
  <files>backend/internal/rpg/rpg_property_test.go</files>
  <read_first>
    - backend/internal/rpg/rpg.go (to understand XP formulas and constants)
    - backend/internal/rng/rng_property_test.go (to follow proven patterns: fixed seeds, explicit loops, tolerance-based validation)
    - backend/internal/combat/combat_property_test.go (to understand skip logic pattern for invalid inputs)
  </read_first>
  <action>
Create backend/internal/rpg/rpg_property_test.go with the following property tests using testing/quick:

1. **TestXPProperty_NonNegative** - XP never becomes negative after AddXP
   - Use quick.Check with property function accepting (xp int, amount int)
   - Skip negative amounts (return true if amount < 0)
   - Verify final XP >= 0 after AddXP
   - Config: MaxCount=1000, fixed seed (rand.New(rand.NewSource(42)))

2. **TestXPProperty_XPSourceConsistency** - XP gains consistent across sources (pve, pvp, quest)
   - Use explicit loop with 1000 iterations (not quick.Check - need to iterate over sources)
   - For each iteration: generate random XP amount, test AddXP with each source
   - Verify levelsGained and newLevel are identical across all sources
   - Fixed seed: rand.Seed(42)

3. **TestLevelProperty_Monotonic** - Level never decreases when XP increases
   - Use explicit loop with 1000 iterations
   - For each iteration: generate random XP, add random additional XP
   - Verify: if xp2 > xp1 then level2 >= level1
   - Fixed seed: rand.Seed(42)

Import testing/quick and math/rand. Follow the exact structure from rng_property_test.go:
- Fixed seed at test start (Seed(42) or rand.New(rand.NewSource(42)))
- Explicit loop with 1000 iterations for mathematical invariants
- t.Errorf for failures with context
  </action>
  <verify>
    <automated>go test -v ./backend/internal/rpg/ -run "XPProperty" -timeout 30s</automated>
  </verify>
  <done>
    - File backend/internal/rpg/rpg_property_test.go exists with 3 XP property tests
    - All XP property tests pass (exit code 0)
    - Tests follow rng_property_test.go patterns (fixed seeds, explicit loops, t.Errorf)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add level-up mechanics property tests</name>
  <files>backend/internal/rpg/rpg_property_test.go</files>
  <read_first>
    - backend/internal/rpg/rpg.go (to understand XPRequiredForLevel formula)
    - backend/internal/rpg/rpg_property_test.go (to see existing XP tests and continue patterns)
    - backend/internal/rng/rng_property_test.go (for tolerance-based statistical validation pattern)
  </read_first>
  <action>
Add the following property tests to backend/internal/rpg/rpg_property_test.go:

4. **TestLevelProperty_XPScalingQuadratic** - XP requirements follow quadratic formula (baseXP * level^2)
   - Use explicit loop with 1000 iterations (levels 1-100)
   - For each level: calculate expectedXP = BaseXP * level^2
   - Verify XPRequiredForLevel(level) == expectedXP
   - Fixed seed: rand.Seed(42)

5. **TestLevelProperty_LevelUpGrantsAbilityPoints** - Each level grants exactly PointsPerLevel (3) ability points
   - Use quick.Check with property function accepting (xp int)
   - Create NewPlayerStats, record initial ability points
   - Call AddXP to trigger level-up
   - Verify: abilityPoints increased by levelsGained * PointsPerLevel
   - Config: MaxCount=1000, fixed seed

6. **TestLevelProperty_MaxLevelCap** - Level never exceeds MaxLevel (100)
   - Use explicit loop with 100 iterations
   - For each iteration: generate huge XP (10000000+)
   - Verify CalculateLevel(xp) <= MaxLevel
   - Fixed seed: rand.Seed(42)

Add imports if needed. Continue following rng_property_test.go patterns.
  </action>
  <verify>
    <automated>go test -v ./backend/internal/rpg/ -run "LevelProperty" -timeout 30s</automated>
  </verify>
  <done>
    - 3 level-up property tests added to rpg_property_test.go
    - All level property tests pass (exit code 0)
    - Tests verify quadratic scaling, ability points grant, and max level cap
  </done>
</task>

<task type="auto">
  <name>Task 3: Add stat allocation property tests</name>
  <files>backend/internal/rpg/rpg_property_test.go</files>
  <read_first>
    - backend/internal/rpg/rpg.go (to understand AllocateStat and ValidStats)
    - backend/internal/rpg/rpg_property_test.go (to see existing tests and continue patterns)
    - backend/internal/combat/combat_property_test.go (for skip logic pattern with invalid inputs)
  </read_first>
  <action>
Add the following property tests to backend/internal/rpg/rpg_property_test.go:

7. **TestStatProperty_Conservation** - Total stats increase equals allocated points
   - Use explicit loop with 1000 iterations
   - For each iteration:
     - Create NewPlayerStats, record initial GetTotalStats()
     - Generate random valid stat name (from ValidStats array)
     - Generate random points (1 to MaxStatPointsPerLevel)
     - Set player.AbilityPoints to cover allocation
     - Call AllocateStat(statName, points)
     - Verify: GetTotalStats() - initialStats == points
   - Fixed seed: rand.Seed(42)

8. **TestStatProperty_ValidStatsOnly** - AllocateStat rejects invalid stat names
   - Use quick.Check with property function accepting (statName string, points int)
   - Check if statName is in ValidStats array
   - If not valid: call AllocateStat, verify error is returned
   - If valid: skip (return true)
   - Config: MaxCount=1000, fixed seed

Add imports if needed. Follow combat_property_test.go skip logic pattern for invalid inputs.
  </action>
  <verify>
    <automated>go test -v ./backend/internal/rpg/ -run "StatProperty" -timeout 30s</automated>
  </verify>
  <done>
    - 2 stat allocation property tests added to rpg_property_test.go
    - All stat property tests pass (exit code 0)
    - Tests verify stat conservation and validation of invalid stat names
  </done>
</task>

</tasks>

<verification>
Run all progression property tests:
```bash
go test -v ./backend/internal/rpg/ -run "Property" -timeout 30s
```

Verify test count:
```bash
go test -v ./backend/internal/rpg/ -run "Property" 2>&1 | grep "^--- PASS" | wc -l
```
Expected: 6-8 tests passing

Check code follows patterns:
```bash
grep -c "rand.Seed\|rand.New(rand.NewSource" backend/internal/rpg/rpg_property_test.go
grep -c "for i := 0; i < 1000" backend/internal/rpg/rpg_property_test.go
```
Both counts should be > 0 (fixed seeds and explicit loops present)
</verification>

<success_criteria>
1. All 6-8 progression property tests pass (XP, level-up, stat allocation)
2. Tests follow proven patterns from rng_property_test.go and combat_property_test.go
3. XP consistency verified across all sources (pve, pvp, quest)
4. Level monotonicity and XP quadratic scaling confirmed
5. Stat allocation conservation and validation working correctly
6. File rpg_property_test.go exists with min 150 lines of test code
</success_criteria>

<output>
After completion, create `.planning/phases/15-property-based-testing-expansion/15-01-SUMMARY.md`
</output>
