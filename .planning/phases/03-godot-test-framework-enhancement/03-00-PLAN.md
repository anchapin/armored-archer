---
phase: 03-godot-test-framework-enhancement
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - test/suites/autoloads/test_accessibility_manager.gd
  - test/suites/autoloads/test_theme_manager.gd
  - test/suites/signals/test_signal_patterns.gd
  - test/gut_config.gd
autonomous: true
requirements:
  - ISO-04
  - MOCK-03
user_setup: []

must_haves:
  truths:
    - "GUT 9.5.x framework is installed and accessible via addons/gut/gut.gd"
    - "Test stub files exist and can be executed without errors"
    - "Test directory structure follows GUT conventions (suites/autoloads/, suites/signals/)"
    - "Stub tests extend GutTest and have empty test methods that pass"
    - "CI test runner script can execute stubs with godot4 --headless"
  artifacts:
    - path: addons/gut/gut.gd
      provides: "GUT 9.5.x test framework"
      contains: "class GutTest"
    - path: test/suites/autoloads/test_accessibility_manager.gd
      provides: "Autoload test stub for AccessibilityManager"
      min_lines: 20
    - path: test/suites/autoloads/test_theme_manager.gd
      provides: "Autoload test stub for ThemeManager"
      min_lines: 20
    - path: test/suites/signals/test_signal_patterns.gd
      provides: "Signal testing pattern stubs"
      min_lines: 20
    - path: test/gut_config.gd
      provides: "GUT configuration for test execution"
      contains: "var gut_config"
  key_links:
    - from: test/suites/autoloads/test_accessibility_manager.gd
      to: "res://addons/gut/gut.gd"
      via: "extends GutTest"
      pattern: "extends GutTest"
    - from: test/suites/autoloads/test_theme_manager.gd
      to: "res://addons/gut/gut.gd"
      via: "extends GutTest"
      pattern: "extends GutTest"
    - from: test/run_all_tests.gd
      to: "res://addons/gut/gut.gd"
      via: "GUT test runner execution"
      pattern: "Gut.*new\\(\\)"
---

<objective>
Create Wave 0 test stubs for autoload testing infrastructure

Purpose: Establish the Nyquist feedback loop by creating minimal test stub files that can be executed before implementation begins. This ensures automated verify commands in subsequent plans have valid targets and enables RED→GREEN→REFACTOR TDD workflow.

Output: Working test stub files that extend GutTest, pass when executed, and verify GUT framework installation.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-godot-test-framework-enhancement/03-RESEARCH.md
@addons/gut/gut.gd
@test/run_all_tests.gd
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify GUT framework installation and version</name>
  <files>addons/gut/gut.gd</files>
  <action>
    Verify GUT (Godot Unit Test) framework is installed and accessible:

    1. Check that addons/gut/gut.gd exists
    2. Read the file header to confirm version 9.5.x
    3. Verify that GutTest class is defined (search for "class_name GutTest" or "class GutTest")
    4. Confirm the plugin.cfg exists in addons/gut/ (indicates proper installation)

    If GUT is not installed:
    - This is a human setup task (requires downloading from GitHub)
    - Create checkpoint with installation instructions
    - Direct to: https://github.com/bitwes/Gut/releases/tag/v9.5.0
    - Instructions: Download, extract to addons/gut/, enable plugin in Project Settings

    If GUT is installed but wrong version:
    - Note version mismatch in task output
    - Continue if compatible (9.x.x)
    - Create checkpoint if major version differs

    DO NOT install GUT automatically — it requires manual plugin activation in Godot Editor.
  </action>
  <verify>
    <automated>test -f addons/gut/gut.gd && grep -q "class_name GutTest\|class GutTest" addons/gut/gut.gd</automated>
  </verify>
  <done>
    GUT 9.5.x framework confirmed installed, GutTest class available for extension, plugin enabled in project settings
  </done>
</task>

<task type="auto">
  <name>Task 2: Create test directory structure</name>
  <files>test/suites/autoloads/, test/suites/signals/</files>
  <action>
    Create directory structure for autoload and signal tests following GUT conventions:

    1. Create test/suites/autoloads/ directory if it doesn't exist
    2. Create test/suites/signals/ directory if it doesn't exist

    Use bash commands:
    ```bash
    mkdir -p test/suites/autoloads
    mkdir -p test/suites/signals
    ```

    Verify directories were created successfully.

    DO NOT create any test files yet — that happens in the next task.
  </action>
  <verify>
    <automated>test -d test/suites/autoloads && test -d test/suites/signals</automated>
  </verify>
  <done>
    test/suites/autoloads/ and test/suites/signals/ directories exist and are empty
  </done>
</task>

<task type="auto">
  <name>Task 3: create autoload test stub files</name>
  <files>test/suites/autoloads/test_accessibility_manager.gd, test/suites/autoloads/test_theme_manager.gd</files>
  <action>
    Create minimal test stub files for autoload testing. These stubs establish the file structure and extend GutTest but contain only empty test methods.

    **File: test/suites/autoloads/test_accessibility_manager.gd**
    ```gdscript
    extends GutTest

    # Wave 0 test stub for AccessibilityManager autoload testing
    # This file will be populated with actual tests in Plan 03-01

    func test_fresh_instance_per_test():
        # Stub: Will verify fresh instance creation in before_each()
        pass

    func test_config_file_injection():
        # Stub: Will verify ConfigFile dependency injection
        pass

    func test_set_font_scale_clamps_to_max():
        # Stub: Will verify font scale clamping behavior
        pass

    func test_set_font_scale_clamps_to_min():
        # Stub: Will verify font scale clamping behavior
        pass

    func test_set_font_scale_emits_signal():
        # Stub: Will verify settings_changed signal emission
        pass
    ```

    **File: test/suites/autoloads/test_theme_manager.gd**
    ```gdscript
    extends GutTest

    # Wave 0 test stub for ThemeManager autoload testing
    # This file will be populated with actual tests in Plan 03-01

    func test_fresh_instance_per_test():
        # Stub: Will verify fresh instance creation in before_each()
        pass

    func test_config_file_injection():
        # Stub: Will verify ConfigFile dependency injection
        pass

    func test_set_theme_emits_signal_with_parameters():
        # Stub: Will verify theme_changed signal emission with parameters
        pass

    func test_async_theme_change():
        # Stub: Will verify async signal handling with wait_for_signal
        pass

    func test_set_theme_with_invalid_name_defaults_to_dark():
        # Stub: Will verify theme validation behavior
        pass

    func test_toggle_theme_switches_between_dark_and_light():
        # Stub: Will verify toggle_theme behavior
        pass
    ```

    These stub files:
    - Extend GutTest (verify GUT integration works)
    - Have descriptive test method names (document intended coverage)
    - Use pass statements (tests pass when executed)
    - Include comments explaining they're Wave 0 stubs
  </action>
  <verify>
    <automated>godot4 --headless --script res://test/run_all_tests.gd --select suites/autoloads</automated>
  </verify>
  <done>
    test/suites/autoloads/test_accessibility_manager.gd and test_theme_manager.gd created, both extend GutTest, all stub tests pass (11 tests total)
  </done>
</task>

<task type="auto">
  <name>Task 4: Create signal testing pattern stub file</name>
  <files>test/suites/signals/test_signal_patterns.gd</files>
  <action>
    Create minimal test stub file for signal testing patterns. This demonstrates GUT's watch_signals() and wait_for_signal() capabilities.

    **File: test/suites/signals/test_signal_patterns.gd**
    ```gdscript
    extends GutTest

    # Wave 0 test stub for signal testing patterns
    # This file demonstrates GUT signal testing capabilities

    var test_object

    func before_each():
        # Stub: Will create test objects that emit signals
        test_object = Node.new()
        add_child_autoqfree(test_object)

    func test_watch_signals_basic():
        # Stub: Will demonstrate watch_signals() usage
        watch_signals(test_object)
        pass

    func test_wait_for_signal_async():
        # Stub: Will demonstrate wait_for_signal() for async signals
        pass

    func test_signal_emission_with_parameters():
        # Stub: Will demonstrate assert_signal_emitted_with_parameters()
        pass
    ```

    This stub file:
    - Demonstrates signal testing patterns referenced in research
    - Will be used as documentation for Plan 03-01 signal tests
    - Passes when executed (empty test methods)
  </action>
  <verify>
    <automated>godot4 --headless --script res://test/run_all_tests.gd --select suites/signals</automated>
  </verify>
  <done>
    test/suites/signals/test_signal_patterns.gd created, extends GutTest, demonstrates signal testing patterns, stub tests pass
  </done>
</task>

<task type="auto">
  <name>Task 5: Verify GUT configuration and CI test runner</name>
  <files>test/gut_config.gd, test/run_all_tests.gd</files>
  <action>
    Verify GUT configuration exists and test runner script can execute stubs:

    1. Check if test/gut_config.gd exists:
       - If yes: Read it to verify basic configuration (log_level, directories)
       - If no: Create minimal GUT config:
         ```gdscript
         extends GutUtils

         var gut_config = {
             "log_level": 1,  # Log only failures
             "directories": ["res://test/suites/"],
             "double_strategy": "SCRIPT_ONLY"
         }
         ```

    2. Verify test/run_all_tests.gd exists and references GUT:
       - Read file to confirm it instantiates Gut and runs tests
       - Look for pattern: `var gut = Gut.new()` or `gut = load(...)`

    3. Test that stubs can be executed via CI command:
       - Run: `godot4 --headless --script res://test/run_all_tests.gd`
       - Verify command completes without errors
       - Confirm stub tests pass (14 tests: 11 autoload + 3 signal)

    DO NOT modify test/run_all_tests.gd unless it's missing or broken. If modifications are needed, preserve existing test runner logic.
  </action>
  <verify>
    <automated>godot4 --headless --script res://test/run_all_tests.gd</automated>
  </verify>
  <done>
    test/gut_config.gd exists with valid configuration, test/run_all_tests.gd can execute stubs, all 14 stub tests pass without errors, CI command verified
  </done>
</task>

</tasks>

<verification>
## Wave 0 Verification

### Automated Checks
- GUT 9.5.x framework installed: `test -f addons/gut/gut.gd && grep -q "GutTest" addons/gut/gut.gd`
- Test directories created: `test -d test/suites/autoloads && test -d test/suites/signals`
- Autoload stubs pass: `godot4 --headless --script res://test/run_all_tests.gd --select suites/autoloads`
- Signal stubs pass: `godot4 --headless --script res://test/run_all_tests.gd --select suites/signals`
- Full stub suite passes: `godot4 --headless --script res://test/run_all_tests.gd`

### Manual Verification
- Open Godot Editor and verify GUT plugin is enabled in Project Settings > Plugins
- Run stub tests from Editor (GUT button) to confirm GUI test runner works
- Check that stub test files appear in GUT's test list

### Success Criteria
- [ ] GUT 9.5.x framework installed and accessible
- [ ] Test directory structure follows GUT conventions
- [ ] All stub tests pass (14 tests total)
- [ ] CI test runner can execute stubs without errors
- [ ] Nyquist feedback loop established for Plan 03-01
</verification>

<success_criteria>
1. Developer can run `godot4 --headless --script res://test/run_all_tests.gd` and see all 14 stub tests pass
2. Stub files exist at test/suites/autoloads/test_accessibility_manager.gd, test_theme_manager.gd, and test/suites/signals/test_signal_patterns.gd
3. All stub tests extend GutTest and have descriptive method names documenting intended coverage
4. GUT configuration exists at test/gut_config.gd
5. Plan 03-01's automated verify commands now have valid targets (no more MISSING status)
</success_criteria>

<output>
After completion, create `.planning/phases/03-godot-test-framework-enhancement/03-00-SUMMARY.md` with:
- GUT framework version confirmation
- List of created stub files (3 files, 14 test methods)
- Verification that all stubs pass
- Confirmation that Nyquist loop is established for Plan 03-01
- Any deviations from expected GUT installation or configuration
</output>
